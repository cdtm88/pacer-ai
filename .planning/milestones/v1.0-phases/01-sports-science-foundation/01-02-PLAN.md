---
phase: 01-sports-science-foundation
plan: 02
type: tdd
wave: 1
depends_on: [01-01]
files_modified:
  - sports_science/zones.py
  - tests/sports_science/test_zones.py
requirements: [TOOL-01, TOOL-02, TOOL-09, TRUST-01]

autonomous: true

must_haves:
  truths:
    - "calculate_power_zones(200) returns 7 Coggan/Allen zones with watt boundaries and a methodology string"
    - "calculate_hr_zones(lthr) returns HR zones with boundaries and a methodology string"
    - "A power value at an exact zone boundary belongs to exactly one zone (upper bound exclusive)"
    - "Both zone functions return a ToolResult with value, unit, methodology, inputs"
    - "sports_science/zones.py contains zero anthropic imports"
  artifacts:
    - path: "sports_science/zones.py"
      provides: "calculate_power_zones, calculate_hr_zones"
      exports: ["calculate_power_zones", "calculate_hr_zones"]
  key_links:
    - from: "sports_science/zones.py"
      to: "sports_science/constants.py"
      via: "imports POWER_ZONE_BOUNDARIES (no hardcoded zone multipliers)"
      pattern: "POWER_ZONE_BOUNDARIES"
    - from: "sports_science/zones.py"
      to: "sports_science/types.py"
      via: "returns ToolResult"
      pattern: "ToolResult"
---

<objective>
Implement `sports_science/zones.py` with `calculate_power_zones(ftp)` (TOOL-01) and `calculate_hr_zones(max_hr_or_lthr)` (TOOL-02). Both return a `ToolResult` (TOOL-09) and source all boundary multipliers from `constants.py` (no magic numbers). The critical correctness property is exclusive upper bounds — no power value may be assigned to two zones (Pitfall 4).

This is a TDD plan: write the boundary tests first (they encode the off-by-one trap), watch them fail, then implement until green.

Purpose: Zones are the simplest tool functions and establish the ToolResult-returning pattern under test. Getting the boundary semantics exactly right here prevents a class of bug that recurs in every zone-coloured UI downstream.
Output: Tested `calculate_power_zones` and `calculate_hr_zones`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-sports-science-foundation/01-CONTEXT.md
@.planning/phases/01-sports-science-foundation/01-PATTERNS.md
@sports_science/types.py
@sports_science/constants.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Power and HR training zones (RED then GREEN)</name>
  <files>sports_science/zones.py, tests/sports_science/test_zones.py</files>
  <read_first>
    - sports_science/constants.py (POWER_ZONE_BOUNDARIES — the canonical boundary table; do not redefine multipliers)
    - sports_science/types.py (ToolResult contract)
    - .planning/phases/01-sports-science-foundation/01-PATTERNS.md (zones.py core pattern, zone membership rule, HR zone note, parametrize test pattern)
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Pitfall 4 — Coggan zone boundary dual membership; Anti-Patterns)
  </read_first>
  <behavior>
    RED first — write these tests in tests/sports_science/test_zones.py and confirm they fail before implementing:
    - test_power_zones_ftp200: calculate_power_zones(200) returns value of length 7; each entry has zone, name, lower_watts, upper_watts; Z1 lower_watts=0; Z7 upper_watts is None; Z4 (Threshold) lower_watts=180 and upper_watts=210 (0.90*200, 1.05*200)
    - test_zone_boundary_no_overlap (parametrized): classifying a watt value into a zone using >= lower AND < upper (Z7 = >= lower only) assigns 110W (55% of 200) to Z2 not Z1, and 150W (75% of 200) to Z2 not Z3 — exact boundary belongs to the higher zone's lower edge, never both
    - test_hr_zones_lthr155: calculate_hr_zones(155) returns HR zones with boundary values and a methodology string naming the HR model
    - test_returns_tool_result: both functions return a ToolResult instance whose model_dump() has keys value, unit, methodology, inputs
  </behavior>
  <action>
    GREEN — after tests fail, implement `sports_science/zones.py`:

    `calculate_power_zones(ftp: float) -> ToolResult`: iterate POWER_ZONE_BOUNDARIES from constants.py; for each zone compute lower_watts = round(lower * ftp) and upper_watts = round(upper * ftp) if upper is not None else None; build a list of {zone, name, lower_watts, upper_watts}; return ToolResult(value=zones, unit="watts", methodology="Coggan/Allen 7-zone power model" (the methodology string is a human-readable source name per D-02), inputs={"ftp": ftp}). The zone-membership rule (>= lower AND < upper, Z7 >= lower only) is the property the boundary test enforces; encode the boundary table so that exact-boundary values resolve to exactly one zone.

    `calculate_hr_zones(max_hr_or_lthr: float) -> ToolResult`: produce HR zones following the same structural shape (list of {zone, name, lower_bpm, upper_bpm} with exclusive upper bounds, top zone upper=None). Use an LTHR-based 5-zone model; methodology string "Coggan/Allen HR zones from LTHR". Return ToolResult(value=zones, unit="bpm", methodology=..., inputs={"lthr": max_hr_or_lthr}). Define any HR multipliers as named constants in constants.py if not already present — do not inline magic numbers.

    Do not import anything from `anthropic` (TRUST-01).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sports_science/test_zones.py -x -q && .venv/bin/pytest tests/sports_science/test_import_boundary.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `sports_science/zones.py` defines `calculate_power_zones(ftp) -> ToolResult` and `calculate_hr_zones(max_hr_or_lthr) -> ToolResult`
    - `pytest tests/sports_science/test_zones.py -x -q` exits 0 with test_power_zones_ftp200, test_hr_zones_lthr155, test_zone_boundary_no_overlap, test_returns_tool_result passing
    - calculate_power_zones(200).value has length 7; Z4 lower_watts=180 upper_watts=210; Z7 upper_watts is None
    - No watt value resolves to two zones in the parametrized boundary test (exclusive upper bound)
    - zones.py imports POWER_ZONE_BOUNDARIES from constants (no inline zone multipliers)
    - `pytest tests/sports_science/test_import_boundary.py -x` exits 0 (TRUST-01)
  </acceptance_criteria>
  <done>calculate_power_zones and calculate_hr_zones implemented test-first; both return ToolResult; exclusive upper bounds prevent dual membership; constants sourced from constants.py; import boundary clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sports_science/ -> anthropic SDK | No import may cross from zones.py to the LLM SDK (TRUST-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | sports_science/zones.py import boundary | mitigate | test_import_boundary.py greps for anthropic across sports_science/; runs in this plan's verify (TRUST-01) |
| T-01-05 | Tampering | Zone boundary off-by-one (dual membership) | mitigate | test_zone_boundary_no_overlap parametrizes exact-boundary watt values; exclusive upper bound enforced (Pitfall 4) |
</threat_model>

<verification>
- `pytest tests/sports_science/test_zones.py -x -q` exits 0 with all behaviors passing
- `pytest tests/sports_science/test_import_boundary.py -x -q` exits 0 (TRUST-01 holds)
- No watt value resolves to two zones in the parametrized boundary test
- Both functions return ToolResult instances
</verification>

<success_criteria>
- TOOL-01: calculate_power_zones returns 7 zones with correct watt boundaries and methodology
- TOOL-02: calculate_hr_zones returns HR zones with boundaries and methodology
- TOOL-09: both functions return ToolResult
- TRUST-01: zero anthropic imports confirmed by the boundary test
- Exclusive upper bound prevents dual zone membership
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- New file: `sports_science/zones.py`
- Symbols: `calculate_power_zones(ftp) -> ToolResult`, `calculate_hr_zones(max_hr_or_lthr) -> ToolResult`
- Possibly new constants in `sports_science/constants.py`: HR zone boundary multipliers (if not already defined)
- Tests: test_power_zones_ftp200, test_hr_zones_lthr155, test_zone_boundary_no_overlap, test_returns_tool_result (in test_zones.py)
</artifacts_produced>

<output>
Create `.planning/phases/01-sports-science-foundation/01-02-SUMMARY.md` when done
</output>
