---
phase: 01-sports-science-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - pytest.ini
  - ruff.toml
  - .env.example
  - .gitignore
  - sports_science/__init__.py
  - sports_science/types.py
  - sports_science/constants.py
  - tests/__init__.py
  - tests/sports_science/__init__.py
  - tests/sports_science/conftest.py
  - tests/sports_science/test_types.py
  - tests/sports_science/test_zones.py
  - tests/sports_science/test_metrics.py
  - tests/sports_science/test_pmc.py
  - tests/sports_science/test_ftp.py
  - tests/sports_science/test_load.py
  - tests/sports_science/test_compliance.py
  - tests/sports_science/test_capability_gap.py
  - tests/sports_science/test_import_boundary.py
autonomous: false
requirements: [TOOL-09]
user_setup:
  - service: supabase
    why: "Cloud-hosted Postgres project must exist before migrations can be applied (D-07). Wave 2 (plan 05) applies the schema; this plan only records the env var contract."
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Data API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API Keys -> anon public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API Keys -> service_role secret (NEVER expose to frontend)"
    dashboard_config:
      - task: "Create a new Supabase project (free tier is sufficient for MVP)"
        location: "https://supabase.com/dashboard -> New project"

must_haves:
  truths:
    - "Running `pytest tests/ -q` discovers all test modules and reports collected tests (failing is expected pre-implementation; collection errors are not)"
    - "`ToolResult` is importable from `sports_science.types` and round-trips through `.model_dump()`"
    - "`sports_science/constants.py` is the single source of CTL_TC, ATL_TC, PMC_MIN_DAYS, zone boundaries, NP spike thresholds, and quality-effort thresholds"
    - "A Python 3.12 virtual environment exists with all pinned dependencies installed"
    - "The Supabase CLI is installed and on PATH"
  artifacts:
    - path: "sports_science/types.py"
      provides: "ToolResult pydantic BaseModel (value, unit, methodology, inputs)"
      contains: "class ToolResult"
    - path: "sports_science/constants.py"
      provides: "All physiological constants — no magic numbers elsewhere"
      contains: "CTL_TC"
    - path: "pytest.ini"
      provides: "pytest config with asyncio_mode = auto"
      contains: "asyncio_mode"
    - path: "tests/sports_science/conftest.py"
      provides: "Shared fixtures: sample_ftp, flat_power_array, variable_power_array, sample_quality_efforts"
      contains: "def sample_ftp"
    - path: "requirements.txt"
      provides: "Pinned dependency versions"
      contains: "numpy==2.4.6"
  key_links:
    - from: "tests/sports_science/conftest.py"
      to: "pytest.ini"
      via: "testpaths = tests enables fixture discovery"
      pattern: "testpaths"
    - from: "sports_science/types.py"
      to: "all sports_science modules"
      via: "ToolResult is the universal return contract (D-01)"
      pattern: "class ToolResult"
---

<objective>
Establish the Phase 1 test infrastructure and the two foundational source modules every other plan depends on: `sports_science/types.py` (the `ToolResult` contract, D-01) and `sports_science/constants.py` (all named constants, D-05/D-10). Scaffold the Python package, the test tree with stub test files, project config (pytest.ini, ruff.toml, requirements.txt, .env.example, .gitignore), a Python 3.12 venv, and install the Supabase CLI.

This is Wave 0: it unblocks parallel implementation of the tool functions in Wave 1 (plans 02, 03, 04). No tool logic is implemented here beyond the shared `ToolResult` type.

Purpose: Tests cannot be discovered without pytest.ini and the test tree; tool modules cannot return values without `ToolResult`; constants must exist before any function imports them. Front-loading this removes all setup friction from the parallel implementation plans.
Output: Importable `sports_science` package skeleton, complete test tree (stub tests that fail/skip cleanly, not collection errors), pinned dependency environment, Supabase CLI installed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sports-science-foundation/01-CONTEXT.md
@.planning/phases/01-sports-science-foundation/01-RESEARCH.md
@.planning/phases/01-sports-science-foundation/01-PATTERNS.md
@.planning/phases/01-sports-science-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, dependency environment, and config files</name>
  <files>requirements.txt, pytest.ini, ruff.toml, .env.example, .gitignore, sports_science/__init__.py, tests/__init__.py, tests/sports_science/__init__.py</files>
  <read_first>
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Standard Stack table, Installation Reference, Environment Availability, Project Constraints sections — pinned versions and Python 3.12 requirement)
    - .planning/phases/01-sports-science-foundation/01-CONTEXT.md (D-07, D-08, D-10 — Supabase setup, .env.example contract, package structure)
    - .planning/phases/01-sports-science-foundation/01-VALIDATION.md (Wave 0 Requirements checklist)
  </read_first>
  <behavior>
    - `python3.12 -m venv .venv` succeeds and `.venv/bin/python --version` reports 3.12.x
    - After `pip install -r requirements.txt`, `python -c "import numpy, scipy, pydantic, supabase, pytest"` exits 0
    - `supabase --version` reports a version after Homebrew install
  </behavior>
  <action>
    Create a Python 3.12 virtual environment at `.venv` using `python3.12 -m venv .venv` (local default Python is 3.14; Railway targets 3.12 per RESEARCH.md Project Constraints — the venv must target 3.12). If `python3.12` is not on PATH, install it via `brew install python@3.12` first.

    Write `requirements.txt` with these exact pinned versions (from RESEARCH.md Standard Stack): numpy==2.4.6, scipy==1.17.1, pydantic==2.13.4, supabase==2.31.0, pytest==9.1.1, pytest-asyncio==1.4.0, ruff==0.15.18. Install them into the venv with `.venv/bin/pip install -r requirements.txt`.

    Install the Supabase CLI via `brew install supabase/tap/supabase` (Pitfall 5 — CLI required at migration time in Wave 2). Confirm with `supabase --version`.

    Write `pytest.ini` containing a `[pytest]` section with `asyncio_mode = auto` (required for pytest-asyncio 1.4.0) and `testpaths = tests`.

    Write `ruff.toml` targeting Python 3.12 (`target-version = "py312"`) with line-length 100; enable the default rule set plus import sorting (per CLAUDE.md: ruff replaces black + flake8 + isort).

    Write `.env.example` (D-08) listing every required env var as KEY= with no value: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY. Add a comment line noting the service role key is backend-only and must never reach the frontend bundle.

    Write `.gitignore` (or append if exists) entries for: `.env`, `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.ruff_cache/`, `.DS_Store`.

    Create the package skeleton: empty `sports_science/__init__.py` (the full export list is added in plan 05 after all modules exist — for now leave it empty to avoid import errors for not-yet-written modules), empty `tests/__init__.py`, empty `tests/sports_science/__init__.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import numpy, scipy, pydantic, supabase, pytest" && .venv/bin/python --version | grep -q "3.12" && grep -q "asyncio_mode = auto" pytest.ini && grep -q "numpy==2.4.6" requirements.txt && supabase --version</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python --version` outputs a string containing `3.12`
    - `.venv/bin/python -c "import numpy, scipy, pydantic, supabase, pytest"` exits 0
    - `requirements.txt` contains the literal lines numpy==2.4.6, scipy==1.17.1, pydantic==2.13.4, supabase==2.31.0, pytest==9.1.1, pytest-asyncio==1.4.0, ruff==0.15.18
    - `pytest.ini` contains `asyncio_mode = auto` and `testpaths = tests`
    - `.env.example` contains SUPABASE_URL, SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY
    - `.gitignore` contains `.env` and `.venv/`
    - `supabase --version` exits 0
    - grep for the service-role env var in `.env.example` confirms a backend-only warning comment is present
  </acceptance_criteria>
  <done>Python 3.12 venv exists with all pinned deps installed; Supabase CLI installed; pytest.ini, ruff.toml, requirements.txt, .env.example, .gitignore written; empty package and test __init__ files created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToolResult contract and constants module</name>
  <files>sports_science/types.py, sports_science/constants.py, tests/sports_science/test_types.py</files>
  <read_first>
    - .planning/phases/01-sports-science-foundation/01-PATTERNS.md (sports_science/types.py and sports_science/constants.py full file patterns — copy these verbatim)
    - .planning/phases/01-sports-science-foundation/01-CONTEXT.md (D-01, D-02, D-05, D-09, D-10)
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Pattern 1, Assumption A1 on frozen config syntax)
  </read_first>
  <behavior>
    - Test 1: `ToolResult(value=1, unit="watts", methodology="m", inputs={})` constructs successfully
    - Test 2: `.model_dump()` returns a dict with exactly keys value, unit, methodology, inputs
    - Test 3: `.to_tool_response()` equals `.model_dump()`
    - Test 4: ToolResult is frozen — assigning to `.value` after construction raises a validation error
    - Test 5: `value` accepts None (sparse-data contract D-04 returns value=None)
  </behavior>
  <action>
    Write `sports_science/types.py` defining `ToolResult(BaseModel)` with fields `value: Any`, `unit: str`, `methodology: str`, `inputs: dict`, frozen config, and a `to_tool_response(self) -> dict` method returning `self.model_dump()` (D-01, TOOL-09). Use `model_config = {"frozen": True}`. If that dict form raises on Pydantic 2.13.4 (Assumption A1), fall back to `from pydantic import ConfigDict` and `model_config = ConfigDict(frozen=True)`.

    Write `sports_science/constants.py` as the single source of truth for all physiological constants (D-05, D-10 — no magic numbers anywhere else). Define: CTL_TC=42, ATL_TC=7, PMC_MIN_DAYS=28; POWER_ZONE_BOUNDARIES as the 7-zone list (zone, name, lower, upper as decimal FTP fractions, Z7 upper=None) per the PATTERNS.md table with zones Active Recovery 0.00-0.55, Endurance 0.55-0.75, Tempo 0.75-0.90, Threshold 0.90-1.05, VO2max 1.05-1.20, Anaerobic Capacity 1.20-1.50, Neuromuscular 1.50+; NP_SPIKE_MULTIPLIER=3.0, NP_SPIKE_FALLBACK_WATTS=600.0, NP_MIN_DURATION_SECS=600; QUALITY_EFFORT_MIN_DURATION_SECS=180, QUALITY_EFFORT_MIN_POWER_RATIO=0.85, QUALITY_EFFORT_FALLBACK_WATTS=150.0, MIN_QUALITY_EFFORTS=4.

    Write `tests/sports_science/test_types.py` with the five behaviors above as real assertions (this is the only fully-implemented test file in Wave 0; it must pass). Include a test named `test_returns_tool_result` style check is NOT needed here — that marker is used in the function-implementing plans.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sports_science/test_types.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `sports_science/types.py` contains `class ToolResult(BaseModel)` with fields value, unit, methodology, inputs and a `to_tool_response` method
    - `.venv/bin/python -c "from sports_science.types import ToolResult; print(ToolResult(value=None, unit='', methodology='m', inputs={}).model_dump())"` exits 0 and prints a dict with keys value, unit, methodology, inputs
    - `sports_science/constants.py` defines CTL_TC, ATL_TC, PMC_MIN_DAYS, POWER_ZONE_BOUNDARIES, NP_SPIKE_MULTIPLIER, NP_SPIKE_FALLBACK_WATTS, NP_MIN_DURATION_SECS, QUALITY_EFFORT_MIN_DURATION_SECS, QUALITY_EFFORT_MIN_POWER_RATIO, QUALITY_EFFORT_FALLBACK_WATTS, MIN_QUALITY_EFFORTS
    - `.venv/bin/python -c "from sports_science.constants import POWER_ZONE_BOUNDARIES; assert len(POWER_ZONE_BOUNDARIES)==7; assert POWER_ZONE_BOUNDARIES[6]['upper'] is None"` exits 0
    - `pytest tests/sports_science/test_types.py -x -q` exits 0 with all 5 tests passing
    - Mutating a constructed ToolResult instance raises (frozen enforced)
  </acceptance_criteria>
  <done>ToolResult contract and full constants module written and importable; test_types.py passes all 5 behaviors; constants are the single source for all physiological values.</done>
</task>

<task type="auto">
  <name>Task 3: Stub all remaining test files and shared fixtures</name>
  <files>tests/sports_science/conftest.py, tests/sports_science/test_zones.py, tests/sports_science/test_metrics.py, tests/sports_science/test_pmc.py, tests/sports_science/test_ftp.py, tests/sports_science/test_load.py, tests/sports_science/test_compliance.py, tests/sports_science/test_capability_gap.py, tests/sports_science/test_import_boundary.py</files>
  <read_first>
    - .planning/phases/01-sports-science-foundation/01-PATTERNS.md (conftest.py pattern, parametrize pattern, import boundary test pattern, test file list)
    - .planning/phases/01-sports-science-foundation/01-VALIDATION.md (Per-Task Verification Map — exact test function names each downstream plan must satisfy)
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Phase Requirements to Test Map — test names and edge cases)
  </read_first>
  <action>
    Write `tests/sports_science/conftest.py` with the four shared fixtures from PATTERNS.md: `sample_ftp` (returns 200.0), `flat_power_array` (returns [150.0]*3600), `variable_power_array` (returns [0.0]*300 + [250.0]*300, exercises NP-includes-zeros), `sample_quality_efforts` (4 efforts spanning 180-1200s as listed in PATTERNS.md).

    Write `tests/sports_science/test_import_boundary.py` as a FULLY IMPLEMENTED test (not a stub) named `test_sports_science_has_zero_anthropic_imports` that runs `grep -r anthropic sports_science/` via subprocess and asserts returncode != 0 (grep returncode 1 = no match = pass). This test must pass now (TRUST-01) because no anthropic imports exist yet, and continues to gate every downstream plan.

    Write the remaining seven test files as STUBS. Each stub file imports from the (not-yet-written) target module inside the test body or uses `pytest.importorskip`, and defines test functions with the exact names from the VALIDATION.md Per-Task Verification Map, each body marked `pytest.skip("implemented in plan 0N")` or using `@pytest.mark.skip`. The point: pytest must COLLECT these files without error (no module-level import of missing modules — defer the import), so the suite reports skips not collection errors. Required test function names by file:
      - test_zones.py: test_power_zones_ftp200, test_hr_zones_lthr155, test_zone_boundary_no_overlap
      - test_metrics.py: test_np_includes_zeros, test_np_spike_filter, test_tss_short_ride_null, test_all_zeros
      - test_pmc.py: test_ewma_values, test_cold_start_guard
      - test_ftp.py: test_insufficient_efforts_returns_none, test_short_effort_not_quality
      - test_load.py: test_back_constraints_cap
      - test_compliance.py: test_compliance_pct
      - test_capability_gap.py: test_user_message_no_method_name, test_db_insert

    Place a module-level `pytestmark = pytest.mark.skip(reason="implemented in downstream plan")` in each stub, OR per-function skips. Do NOT put a top-level `from sports_science.zones import ...` that would error at collection — defer it or use importorskip.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -q --co 2>&1 | grep -v '^#' | grep -qE "test_power_zones_ftp200|test_np_includes_zeros|test_ewma_values" && .venv/bin/pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/ --co -q` collects every named test function above with zero collection errors
    - `.venv/bin/pytest tests/sports_science/test_import_boundary.py -x` exits 0 (TRUST-01 holds: no anthropic in sports_science/)
    - `tests/sports_science/conftest.py` defines `sample_ftp`, `flat_power_array`, `variable_power_array`, `sample_quality_efforts`
    - `.venv/bin/pytest tests/ -q` reports test_types.py + import_boundary passing and the remaining tests as skipped (not errored)
    - Every stub test function name listed in the action exists and is discoverable via `pytest --co`
  </acceptance_criteria>
  <done>conftest fixtures written; import boundary test passes; all seven downstream test files stubbed with exact-named skipped tests that collect cleanly. The whole suite runs green-or-skipped with no collection errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint, confirm Supabase project exists and suite collects cleanly</name>
  <action>Human verification gate. The executor pauses here. The developer confirms a Supabase cloud project exists and the test suite collects cleanly, per the how-to-verify steps below, before Wave 1 proceeds.</action>
  <what-built>Python 3.12 venv with pinned deps, Supabase CLI, project config, ToolResult contract, constants module, and the full test tree (test_types + import_boundary passing, all other tests stubbed and skipping cleanly).</what-built>
  <how-to-verify>
    1. Confirm a Supabase cloud project exists. If not, create one at https://supabase.com/dashboard (New project). Copy SUPABASE_URL, SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY from Project Settings -> API into a local `.env` (the schema migration in plan 05 needs the project to exist).
    2. Run `.venv/bin/pytest tests/ -q`. Expect: test_types.py and test_import_boundary.py pass; the remaining tests report as skipped; zero collection errors.
    3. Confirm `supabase --version` prints a version.
  </how-to-verify>
  <resume-signal>Type "approved" once the Supabase project exists and the test suite collects cleanly, or describe any setup blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sports_science/ -> anthropic SDK | Trust-model boundary: no import may ever cross from the calculation module to the LLM SDK (TRUST-01) |
| local config -> git | Secrets boundary: `.env` (real keys) must never be committed; only `.env.example` is tracked (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | sports_science/ import boundary | mitigate | `tests/sports_science/test_import_boundary.py` greps for `anthropic` and fails on any match; runs in every suite invocation (TRUST-01) |
| T-01-08 | Information Disclosure | Service role key in committed config | mitigate | `.gitignore` excludes `.env`; only `.env.example` (no values) is tracked; backend-only warning comment in `.env.example` (D-08) |
| T-01-SC | Tampering | pip/brew installs (numpy, scipy, pydantic, supabase, pytest, ruff, supabase CLI) | accept | RESEARCH.md Package Legitimacy Audit: all packages approved (SUS verdicts are PyPI download-count API gaps, not risk signals); no [ASSUMED]/[SUS] packages requiring a blocking checkpoint |
</threat_model>

<verification>
- `.venv/bin/pytest tests/ -q` runs with zero collection errors; test_types.py and test_import_boundary.py pass; remaining tests skip cleanly
- `.venv/bin/python --version` reports 3.12.x
- `supabase --version` exits 0
- `git status` shows `.env` is NOT staged (gitignored); `.env.example` IS tracked
- All constants importable from `sports_science.constants`; ToolResult importable from `sports_science.types`
</verification>

<success_criteria>
- Test infrastructure is in place: pytest discovers and collects all 25 planned test functions with no errors
- `ToolResult` contract (TOOL-09) is established and tested before any tool function is written
- `constants.py` is the single source of all physiological constants
- Python 3.12 venv with pinned deps and Supabase CLI ready for Waves 1 and 2
- Supabase cloud project confirmed to exist (human checkpoint)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- New files: `requirements.txt`, `pytest.ini`, `ruff.toml`, `.env.example`, `.gitignore`, `sports_science/__init__.py` (empty), `sports_science/types.py`, `sports_science/constants.py`, `tests/__init__.py`, `tests/sports_science/__init__.py`, `tests/sports_science/conftest.py`, and 8 test files (test_types, test_zones, test_metrics, test_pmc, test_ftp, test_load, test_compliance, test_capability_gap, test_import_boundary)
- Symbols: `ToolResult` (pydantic BaseModel: value, unit, methodology, inputs; `.to_tool_response()`)
- Constants: `CTL_TC`, `ATL_TC`, `PMC_MIN_DAYS`, `POWER_ZONE_BOUNDARIES`, `NP_SPIKE_MULTIPLIER`, `NP_SPIKE_FALLBACK_WATTS`, `NP_MIN_DURATION_SECS`, `QUALITY_EFFORT_MIN_DURATION_SECS`, `QUALITY_EFFORT_MIN_POWER_RATIO`, `QUALITY_EFFORT_FALLBACK_WATTS`, `MIN_QUALITY_EFFORTS`
- Fixtures: `sample_ftp`, `flat_power_array`, `variable_power_array`, `sample_quality_efforts`
- Environment: `.venv` (Python 3.12), Supabase CLI on PATH
</artifacts_produced>

<output>
Create `.planning/phases/01-sports-science-foundation/01-01-SUMMARY.md` when done
</output>
