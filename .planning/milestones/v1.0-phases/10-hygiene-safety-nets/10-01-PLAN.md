---
phase: 10-hygiene-safety-nets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/agent/test_sse.py
  - backend/sports_science/profile.py
  - tests/sports_science/conftest.py
  - tests/api/test_contracts.py
autonomous: true
requirements: [ITEM-01, ITEM-02, ITEM-04]
must_haves:
  truths:
    - "pytest tests/agent/test_sse.py runs green (0 failures across all 8 previously-401 tests)"
    - "The full suite passes regardless of test execution order (no capability-gap/profile singleton leak)"
    - "A backend field-name regression on rides/sessions/profile fails a pytest test (D-01 contract guard)"
  artifacts:
    - tests/agent/test_sse.py
    - backend/sports_science/profile.py
    - tests/sports_science/conftest.py
    - tests/api/test_contracts.py
  key_links:
    - "test_sse.py mock run_turn functions accept the user_id/conversation_id kwargs sse_generator forwards"
    - "profile._reset_client_for_tests() is invoked by the autouse fixture in tests/sports_science/conftest.py"
    - "test_contracts.py asserts the exact field names frontend/src/lib/api.ts dereferences"
---

<objective>
Make the backend test suite green and add the seam-level contract guard that would have caught the Ride/Profile/FTP-key field-name mismatches from Phases 6-9. Three narrow, verified fixes: (1) repair the 8 stale SSE tests in `tests/agent/test_sse.py` that fail 401 because JWT auth middleware was added after they were written; (2) give `backend/sports_science/profile.py` the same `_reset_client_for_tests()` test seam that `capability_gap.py` already has, closing a latent module-singleton leak; (3) add `tests/api/test_contracts.py` with targeted field-presence assertions per D-01.

Purpose: The full `pytest tests/ -q` run currently reports 8 failures (all in `test_sse.py`); this plan takes it to zero and adds a drift-detecting contract net. This is the backend test-hygiene half of Phase 10 (scope items 1, 2, 4).

Output: A green backend suite plus a new `test_contracts.py` that fails if the backend response shape drifts from what the frontend reads.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hygiene-safety-nets/10-CONTEXT.md
@.planning/phases/10-hygiene-safety-nets/10-RESEARCH.md
@.planning/phases/10-hygiene-safety-nets/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 8 stale SSE tests in tests/agent/test_sse.py</name>
  <files>tests/agent/test_sse.py</files>
  <read_first>
    - tests/agent/test_sse.py (the file being fixed; note the `TestSSEEventSequence` class vs the already-passing `TestAssistantSinkGating` class)
    - tests/api/conftest.py (reuse `TEST_USER_ID`, `TEST_JWT_SECRET`, `auth_headers()`, `parse_sse_frames()` — do not invent a new auth fixture or SSE parser)
    - backend/routes/chat.py (the `chat_stream` handler: the CR-03 `_resolve_conversation_id` gate at lines 119-139, imported at module scope from `backend.routes.onboarding`; and the `sse_generator(...)` call at line 157 that forwards `user_id=` and `conversation_id=` kwargs)
    - .planning/phases/10-hygiene-safety-nets/10-RESEARCH.md ("Pattern 1", Pitfall 1 — the three-part fix verified end-to-end this session)
    - .planning/phases/10-hygiene-safety-nets/10-PATTERNS.md ("tests/agent/test_sse.py" section — exact auth/bypass/kwargs snippets)
  </read_first>
  <action>
    Apply all THREE coordinated changes together (auth-headers alone is verified insufficient — it changes the failure to an `invalid_conversation_id` error frame):
    (1) AUTH: at the top of each failing test (or via a shared fixture), call `monkeypatch.setenv("SUPABASE_JWT_SECRET", TEST_JWT_SECRET)` and pass `headers=auth_headers()` on every `client.get("/chat/stream", ...)` call. Import `TEST_USER_ID`, `TEST_JWT_SECRET`, `auth_headers` from `tests.api.conftest`.
    (2) OWNERSHIP-CHECK BYPASS: these tests exercise SSE framing mechanics, not conversation ownership (ownership is already covered by `tests/api/test_chat.py`). Define an async helper `_bypass_resolve(user_id, conversation_id)` that returns `conversation_id` unchanged, and `monkeypatch.setattr(chat_module, "_resolve_conversation_id", _bypass_resolve)` — patch the CALLER's module binding (`chat.py` imports the name directly, so `chat_module._resolve_conversation_id` is the patchable binding). Do NOT modify any production code to do this.
    (3) MOCK SIGNATURES: add `**kwargs` to EVERY mock run_turn function so it accepts the `user_id`/`conversation_id` kwargs `sse_generator` now always forwards for a resolved conversation: `_mock_run_turn_text_only`, `_mock_run_turn_with_tools`, `_mock_run_turn_token_then_error`, `_mock_run_turn_token_then_done`. The signature becomes `(messages, client, model, trust_scanner, audit_log, **kwargs)`.
    NO-OP EXCEPTION: `test_sse_requires_conversation_id` (the 422 test) needs only the auth header — omitting `conversation_id` with valid auth still correctly returns 422 (FastAPI required-Query validation fires first); do NOT add the ownership bypass there.
    Reuse the existing `parse_sse_frames()` already defined in this file for frame assertions. Do not weaken any production-side CR-03 check.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/agent/test_sse.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/agent/test_sse.py -q` reports 0 failures (all tests in the file pass, including the previously-401 `TestSSEEventSequence` set).
    - `grep -n "invalid_conversation_id" tests/agent/test_sse.py` finds no assertion expecting that error frame in the sequence tests (proof the bypass is applied, not that tests were made to accept the error).
    - `grep -c "def _mock_run_turn" tests/agent/test_sse.py` mock functions each declare `**kwargs` (behavior assertion: no `TypeError: unexpected keyword argument` at runtime).
    - No file under `backend/` is modified by this task (source assertion: `git diff --name-only backend/routes/chat.py` is empty).
  </acceptance_criteria>
  <done>All 8 previously-failing SSE tests pass by exercising the real endpoint with valid auth and correct mock signatures; production code is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile.py test-reset seam and wire it into the sports_science autouse fixture</name>
  <files>backend/sports_science/profile.py, tests/sports_science/conftest.py</files>
  <read_first>
    - backend/sports_science/profile.py (the module-level `_supabase_client` cache at line 21 and `_get_async_supabase()` at lines 24-44 — the exact pre-fix pattern capability_gap.py once had)
    - backend/sports_science/capability_gap.py (the `_reset_client_for_tests()` function and its module-singleton pattern — the source of the fix to replicate)
    - tests/sports_science/conftest.py (the existing `_reset_capability_gap_client` autouse fixture to extend — do not add a parallel fixture)
    - .planning/phases/10-hygiene-safety-nets/10-PATTERNS.md ("tests/sports_science/conftest.py + backend/sports_science/profile.py" section — exact copy target)
    - .planning/phases/10-hygiene-safety-nets/10-RESEARCH.md (Pitfall 2 — why profile.py, not audit.py, is the real duplicate)
  </read_first>
  <action>
    In `backend/sports_science/profile.py`, add a module-level function `_reset_client_for_tests()` that does `global _supabase_client; _supabase_client = None`, mirroring `capability_gap.py`'s identical function exactly (same docstring intent, WR-04 rationale). Place it directly below the `_supabase_client` declaration.
    In `tests/sports_science/conftest.py`, extend the EXISTING autouse fixture (currently `_reset_capability_gap_client`) so it imports both `capability_gap` and `profile` from `backend.sports_science` and calls `capability_gap._reset_client_for_tests()` AND `profile._reset_client_for_tests()` both before the `yield` and after it. Do not create a second fixture — keep the single-fixture style already present.
    This is a preventive parity fix (Pitfall 2): no test currently exercises the leak, so the suite must stay green with no behavior change.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sports_science/ tests/agent/test_tools_phase3.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_reset_client_for_tests" backend/sports_science/profile.py` returns at least 1 (source assertion: the seam exists).
    - `grep -v '^#' tests/sports_science/conftest.py | grep -c "profile._reset_client_for_tests"` returns at least 2 (before and after `yield`).
    - `.venv/bin/pytest tests/sports_science/ tests/agent/test_tools_phase3.py -q` reports 0 failures (behavior assertion: parity fix introduces no regression).
  </acceptance_criteria>
  <done>profile.py has the same reset seam as capability_gap.py, wired into the shared autouse fixture, with the suite still green.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/api/test_contracts.py — targeted field-presence guards (D-01)</name>
  <files>tests/api/test_contracts.py</files>
  <read_first>
    - tests/api/conftest.py (`mock_supabase_factory`, `auth_headers`, `TEST_JWT_SECRET`, `TEST_USER_ID` — reuse these; do not introduce a new mocking approach)
    - tests/api/test_chat.py (the `httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test")` pattern to copy)
    - backend/routes/rides.py (confirm the response wrapper: GET /rides/ returns `{"rides": [...]}` and the `.select(...)` column list)
    - backend/routes/sessions.py (confirm GET /sessions/today returns a single session object or 404, and `_SESSION_COLUMNS`)
    - backend/routes/profiles.py (confirm GET /profiles/me returns the profile row object)
    - frontend/src/lib/api.ts (the fields each helper dereferences: `getRides` reads `data.rides` then per-ride `id, ride_date, duration_secs, avg_power, np_watts, tss, compliance_pct`; `getSessionToday` reads the object directly; `getProfileMe` returns `Profile` where `.ftp` is the load-bearing read)
    - .planning/phases/10-hygiene-safety-nets/10-RESEARCH.md ("Pattern 2" — the verified field table and the `assert required <= set(...)` subset approach)
  </read_first>
  <action>
    Create `tests/api/test_contracts.py` with three async test functions, each hitting the real route via `httpx.AsyncClient(ASGITransport(app=app))` with `headers=auth_headers()` and `monkeypatch.setenv("SUPABASE_JWT_SECRET", TEST_JWT_SECRET)`, mocking the Supabase layer with `mock_supabase_factory([realistic_row])` via `monkeypatch.setattr(<route_module>, "_get_async_supabase", ...)`:
    (1) `test_rides_contract`: GET /api/rides/ with a mocked ride row; assert `{"id", "ride_date", "duration_secs", "avg_power", "np_watts", "tss", "compliance_pct"}` is a subset of the keys of the first element of the response's `rides` list.
    (2) `test_sessions_today_contract`: GET /api/sessions/today with a mocked session row; assert `{"id", "objective", "structure", "type", "duration_mins", "scheduled_date", "rpe_target"}` is a subset of the returned session object's keys (match the exact wrapper shape the route returns — read sessions.py first).
    (3) `test_profile_me_contract`: GET /api/profiles/me with a mocked profile row; assert `"ftp"` is present in the response object's keys (the one field `DuringSessionScreen.tsx` reads).
    Use `assert required <= set(body.keys())` (subset), NOT exact equality, so the backend can add columns without breaking the test. Assert field PRESENCE by name — do not assert on values. Confirm each route module's `_get_async_supabase` import name before patching (it may differ per route file).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/api/test_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/api/test_contracts.py -q` collects and passes exactly 3 tests.
    - Behavior assertion: removing any one required field from a mocked row makes the corresponding test FAIL (the guard actually detects drift) — verify by temporary local edit, then revert.
    - `grep -c "assert .*<= set" tests/api/test_contracts.py` returns 3 (subset assertion used, not exact equality).
  </acceptance_criteria>
  <done>Three contract tests assert field-presence for rides/sessions/profile against the exact names the frontend reads, using the established mocked-Supabase + ASGITransport pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test double → production code | test_sse.py monkeypatches a production ownership check; the bypass must never reach prod |
| module singleton → cross-test state | profile.py's cached Supabase client can leak across tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-01 | Elevation | test_sse.py `_resolve_conversation_id` monkeypatch | medium | mitigate | Bypass exists only in test doubles; production `chat_stream` keeps the CR-03 ownership check unchanged (Task 1 acceptance asserts `backend/routes/chat.py` diff is empty) |
| T-10-01-02 | Tampering | profile.py module-level `_supabase_client` | low | mitigate | Add `_reset_client_for_tests()` + autouse fixture reset (parity with capability_gap.py) so a cached client cannot leak a stale/mocked state into an unrelated test |
| T-10-01-03 | Tampering | test_contracts.py false-green risk | low | mitigate | Use subset field-presence assertions verified to FAIL when a field is dropped (Task 3 acceptance) so the guard cannot pass vacuously |
</threat_model>

## Artifacts this phase produces

- `backend/sports_science/profile.py` — new function `_reset_client_for_tests() -> None` (module-singleton reset seam)
- `tests/sports_science/conftest.py` — extended autouse fixture now also resets `profile`'s singleton
- `tests/agent/test_sse.py` — mock run_turn functions gain `**kwargs`; tests gain `auth_headers()` + `_bypass_resolve` monkeypatch of `chat_module._resolve_conversation_id`
- `tests/api/test_contracts.py` (NEW) — `test_rides_contract`, `test_sessions_today_contract`, `test_profile_me_contract`

<verification>
- `.venv/bin/pytest tests/agent/test_sse.py -q` → 0 failures
- `.venv/bin/pytest tests/sports_science/ tests/agent/test_tools_phase3.py -q` → 0 failures
- `.venv/bin/pytest tests/api/test_contracts.py -q` → 3 passed
- `.venv/bin/pytest tests/ -q` → 0 unexpected failures (full-suite green target for the backend-test half of the phase)
</verification>

<success_criteria>
- The 8 previously-failing SSE tests pass with real auth and correct mock signatures; no production code changed.
- profile.py has the capability_gap.py reset seam, wired into the shared autouse fixture.
- test_contracts.py fails on a dropped backend field for rides/sessions/profile.
</success_criteria>

<output>
Create `.planning/phases/10-hygiene-safety-nets/10-01-SUMMARY.md` when done.
</output>
