---
phase: 10-hygiene-safety-nets
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-03, 10-04]
files_modified:
  - .github/workflows/ci.yml
  - .gitignore
autonomous: true
requirements: [ITEM-07, ITEM-08]
must_haves:
  truths:
    - "A push to the repo triggers a GitHub Actions run that executes ruff, pytest, and vitest and reports status (report-only, no branch protection, D-05)"
    - "Root node_modules/ and test-ride.fit are removed and gitignored so they cannot silently reappear untracked"
  artifacts:
    - .github/workflows/ci.yml
    - .gitignore
  key_links:
    - "ci.yml backend job runs ruff check . and pytest tests/ -q; frontend job runs npm ci + vitest --run in frontend/"
    - ".gitignore entries prevent node_modules/ and test-ride.fit from recurring"
---

<objective>
Add the CI safety net and finish the repo cleanup — the last two Phase 10 items, sequenced last so the suite they guard is already green (depends on Plans 01, 03, 04). Create `.github/workflows/ci.yml` running ruff + pytest (backend job) and vitest (frontend job) on push and PR, report-only with no branch protection (D-05). Remove the orphaned root `node_modules/` (no root `package.json` exists) and the stray `test-ride.fit` fixture, and add both to the root `.gitignore` so they cannot reappear untracked.

Purpose: Scope items 7 and 8. CI makes regressions like the 8 stale SSE tests visible on every push instead of only on a manual full run; the cleanup removes untracked cruft flagged in APP-REVIEW.

Output: A working report-only CI workflow and a clean, guarded repo root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hygiene-safety-nets/10-CONTEXT.md
@.planning/phases/10-hygiene-safety-nets/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add report-only GitHub Actions CI workflow (D-05)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - requirements.txt (backend deps installed in CI; confirms Python 3.12 target)
    - ruff.toml (the linter config CI invokes: `ruff check .`)
    - pytest.ini (the test config; `asyncio_mode = auto`)
    - frontend/package.json (confirm the `test` script invokes vitest; CI must pass `--run` to avoid watch mode)
    - frontend/package-lock.json (present — enables `npm ci` and the actions/setup-node cache)
    - .planning/phases/10-hygiene-safety-nets/10-RESEARCH.md ("Code Examples — GitHub Actions CI workflow" — the verified YAML; action versions checkout@v6 / setup-python@v6 / setup-node@v6, python 3.12, node 22)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` (the directory does not exist yet). Name it `CI`, trigger on `push` and `pull_request`. Two jobs:
    (1) `backend` on ubuntu-latest: `actions/checkout@v6`, `actions/setup-python@v6` with `python-version: '3.12'`, `pip install -r requirements.txt`, `ruff check .`, `pytest tests/ -q`.
    (2) `frontend` on ubuntu-latest with `defaults.run.working-directory: frontend`: `actions/checkout@v6`, `actions/setup-node@v6` with `node-version: '22'`, `cache: 'npm'`, `cache-dependency-path: frontend/package-lock.json`, then `npm ci`, then `npm run test -- --run` (the `--run` flag is REQUIRED — `npm run test` invokes vitest which defaults to watch mode and would hang CI).
    Do NOT add Playwright/e2e to CI (out of scope this phase per D-05 — browser-binary provisioning). Do NOT configure branch protection or required-check rules (D-05: report-only; commits land directly on main). Do NOT reference any repository secret in the workflow — the test suites run against fixtures, not real credentials.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('yaml ok')"</automated>
    <human-check>After this commit is pushed, open the repo's GitHub Actions tab and confirm the CI workflow triggers and both jobs (backend: ruff + pytest; frontend: vitest) run and report status. This cannot be observed from a local run.</human-check>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML (automated command prints `yaml ok`).
    - `grep -c "ruff check\|pytest\|vitest\|npm run test" .github/workflows/ci.yml` confirms all three tools are invoked (ruff, pytest, and the vitest `npm run test -- --run` step present).
    - `grep -c "playwright\|test:e2e\|branch.protection" .github/workflows/ci.yml` returns 0 (D-05 scope: no e2e, no branch protection).
    - `grep -c "secrets\." .github/workflows/ci.yml` returns 0 (no secrets referenced).
    - `grep -c -- "--run" .github/workflows/ci.yml` returns at least 1 (vitest runs once, not watch mode).
  </acceptance_criteria>
  <done>A report-only CI workflow runs ruff + pytest + vitest on push/PR with pinned action versions, no e2e, no branch protection, and no secret references.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned root node_modules/ and test-ride.fit; gitignore both</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (the existing root ignore file — note the `# Python`, `# Test artifacts`, and `# OS` section structure to append to consistently)
    - .planning/phases/10-hygiene-safety-nets/10-RESEARCH.md (Runtime State Inventory + Environment Availability — confirms root `node_modules/` is orphaned, no root package.json/package-lock.json exists, and `test-ride.fit` is a 66-byte stray fixture; both are currently untracked and unignored)
  </read_first>
  <action>
    Delete the orphaned artifacts at the repo root: `rm -rf node_modules` and `rm -f test-ride.fit` (both verified untracked, so this is not a git operation — plain filesystem removal). Confirm no root `package.json` exists first (it does not — the root node_modules is orphaned, not a real JS project; the real frontend project lives under `frontend/`).
    Add guard entries to the root `.gitignore` so neither can reappear untracked: append a root-level `/node_modules/` entry (anchored to root so `frontend/node_modules` handling is unaffected) and a `test-ride.fit` entry under an appropriately labeled comment (e.g. reuse or extend the `# Test artifacts` section). Do not remove or reorder existing `.gitignore` entries.
  </action>
  <verify>
    <automated>test ! -e node_modules && test ! -e test-ride.fit && echo "artifacts removed"</automated>
    <automated>git status --porcelain | grep -E "^\?\? (node_modules/|test-ride.fit)" && echo "STILL UNTRACKED" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e node_modules && test ! -e test-ride.fit` succeeds (both artifacts gone from the repo root).
    - `git status --porcelain` shows neither `node_modules/` nor `test-ride.fit` as untracked (the second automated command prints `clean`).
    - `git check-ignore node_modules test-ride.fit` returns both paths (source assertion: they are now ignored, so they cannot silently reappear untracked).
    - No pre-existing `.gitignore` line is removed (source assertion via `git diff .gitignore` shows only additions).
  </acceptance_criteria>
  <done>Root node_modules/ and test-ride.fit are removed and gitignored; git status is clean and both paths are ignored going forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → third-party actions | pinned GitHub Actions execute in CI; a compromised/floating action is a supply-chain risk |
| repo working tree → committed history | untracked artifacts can be accidentally committed or leak local state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-SC | Tampering (supply chain) | GitHub Actions checkout/setup-python/setup-node | medium | mitigate | Pin all actions to current major versions (`@v6`); use only first-party `actions/*` (no third-party actions); frontend deps installed via `npm ci` from the committed lockfile, backend via `requirements.txt` — no floating installs |
| T-10-05-01 | Information Disclosure | ci.yml (secrets in logs) | medium | mitigate | Workflow references no repository secrets (Task 1 asserts `grep secrets. == 0`); pytest/vitest/ruff run against fixtures, not real credentials |
| T-10-05-02 | Information Disclosure | root node_modules/ + test-ride.fit | low | mitigate | Remove the stray artifacts and gitignore both so local cruft cannot be committed or reappear untracked |
</threat_model>

## Artifacts this phase produces

- `.github/workflows/ci.yml` (NEW) — `CI` workflow, jobs `backend` (ruff + pytest) and `frontend` (npm ci + vitest `--run`), report-only, pinned action majors
- `.gitignore` — new root-anchored `/node_modules/` and `test-ride.fit` guard entries
- Removed: root `node_modules/`, `test-ride.fit`

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` → valid YAML
- `test ! -e node_modules && test ! -e test-ride.fit` → both removed
- `git status --porcelain` → neither artifact untracked
- `git check-ignore node_modules test-ride.fit` → both ignored
- Manual (human-check): GitHub Actions tab shows the CI workflow running both jobs after push
</verification>

<success_criteria>
- CI runs ruff + pytest + vitest on push/PR, report-only, no e2e, no branch protection, no secrets.
- Root node_modules/ and test-ride.fit are removed and gitignored; repo status is clean.
</success_criteria>

<output>
Create `.planning/phases/10-hygiene-safety-nets/10-05-SUMMARY.md` when done. Note the manual GitHub Actions tab verification (item 7) as a human-check for end-of-phase verification.
</output>
