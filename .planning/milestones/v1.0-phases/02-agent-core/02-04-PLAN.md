---
phase: 02-agent-core
plan: 04
type: tdd
wave: 4
depends_on: ["02-03"]
files_modified:
  - tests/agent/conftest.py
  - tests/agent/test_loop.py
  - tests/agent/test_trust.py
  - tests/agent/test_sse.py
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05, AGENT-06, TRUST-03, TRUST-04, TRUST-05]
must_haves:
  truths:
    - "The compliance suite proves the agent never emits an unsourced physiological number across representative scenarios"
    - "Multi-turn parallel tool dispatch via asyncio.gather completes without error in tests"
    - "Tool retry cap (3) and dedup by (name, args_hash) per turn are verified by tests"
    - "An injected unsourced number triggers a retry and a capability-gap log, and never reaches the SSE stream"
    - "The SSE endpoint emits the typed event sequence (token, tool_start, tool_result, done) for a complete turn"
    - "claude-agent-sdk is provably absent from the dependency tree"
    - "No test makes a live Anthropic API call"
  artifacts:
    - path: "tests/agent/conftest.py"
      provides: "mock Anthropic stream fixtures (text-only, tool_use, trust-violation)"
      contains: "AsyncMock"
    - path: "tests/agent/test_loop.py"
      provides: "AGENT-01..04 + TRUST-04 loop tests"
      contains: "asyncio.gather"
    - path: "tests/agent/test_trust.py"
      provides: "TRUST-03/05 scanner + violation-retry tests"
      contains: "scan_buffer"
    - path: "tests/agent/test_sse.py"
      provides: "AGENT-05 SSE event-sequence test via ASGITransport"
      contains: "text/event-stream"
  key_links:
    - from: "tests/agent/test_loop.py"
      to: "agent/loop.py"
      via: "drives run_turn with the mock stream fixtures"
      pattern: "run_turn"
    - from: "tests/agent/test_sse.py"
      to: "api/main.py"
      via: "httpx.AsyncClient(transport=ASGITransport(app)) hits /chat/stream"
      pattern: "ASGITransport"
---

<objective>
Build the `tests/agent/` compliance suite (D-15, D-16, AGENT-06) that proves every Phase 2 requirement end-to-end with mocked Anthropic responses and zero live API calls: the raw-SDK + stop_reason discipline (AGENT-01), parallel tool dispatch (AGENT-02), the 3-retry cap and surfaced failures (AGENT-03), dedup by (name, args_hash) (AGENT-04), the SSE event sequence (AGENT-05), the trust scanner catching injected unsourced numbers + capability-gap logging (TRUST-03/05), and the audit trail (TRUST-04). This is the phase's proof gate.

Purpose: Success Criterion 5 of Phase 2 is "the compliance test suite passes: the agent does not emit unsourced physiological numbers across a representative set of scenarios." This plan delivers that suite. It is the Nyquist anchor for the whole phase — earlier plans verified via inline python; this plan creates the durable regression tests.
Output: tests/agent/conftest.py (mock stream fixtures), test_loop.py, test_trust.py, test_sse.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@.planning/phases/02-agent-core/02-RESEARCH.md
@.planning/phases/02-agent-core/02-02-SUMMARY.md
@.planning/phases/02-agent-core/02-03-SUMMARY.md
@agent/loop.py
@agent/tools.py
@agent/trust.py
@api/main.py
@api/routes/chat.py
@pytest.ini
</context>

<feature>
  <name>Agent compliance test suite (AGENT-06)</name>
  <files>tests/agent/conftest.py, tests/agent/test_loop.py, tests/agent/test_trust.py, tests/agent/test_sse.py</files>
  <behavior>
    Red→Green per requirement, all against mocked streams (no live API):
    - test_no_agent_sdk (AGENT-01): asserts importing claude_agent_sdk raises ImportError AND `grep -ci claude-agent-sdk requirements.txt == 0`
    - test_stop_reason_tool_use (AGENT-01): with a mock stream whose final message stop_reason == "tool_use", run_turn dispatches a tool and yields tool_start/tool_result; with stop_reason == "end_turn" it yields done
    - test_parallel_tool_dispatch (AGENT-02): a mock final message with two distinct tool_use blocks results in both being dispatched (audit_log has 2 entries) via the gather path
    - test_retry_limit (AGENT-03): a mock stream that always produces a trust violation causes run_turn to stop after MAX_RETRIES with a final error event code "max_retries"; retries never exceed 3
    - test_failed_tool_surfaced (AGENT-03): a tool_use block for a raising/unknown tool yields a tool_result with is_error true; the error is present in the audit log (not swallowed)
    - test_tool_deduplication (AGENT-04): a mock final message with two identical tool_use blocks (same name + inputs) dispatches the tool exactly once (audit_log length 1)
    - test_audit_log (TRUST-04): after a successful tool turn the audit_log entry contains tool_use_id, name, and the tool result
    - test_trust_violation_triggers_retry (TRUST-03): scan_buffer flags "Your FTP is 250 watts" (unsourced); within run_turn this increments retries, appends a correction message (not the violating assistant message), and emits an error event with code "trust_violation"; the violating number never appears in a token/done frame forwarded to the consumer
    - test_attributed_number_passes (TRUST-03): a number present verbatim in a tool_result value this turn does NOT trigger a violation
    - test_capability_gap_fallback (TRUST-05): the trust violation path calls log_capability_gap (mocked) with the matched text; the user-facing surface never contains the internal method_name
    - test_sse_event_sequence (AGENT-05): hitting GET /chat/stream via httpx.AsyncClient(transport=ASGITransport(app)) with a mocked loop yields a valid text/event-stream body containing the typed events in order (token..., tool_start, tool_result, done)
    - No test performs a network call to api.anthropic.com (mock the AsyncAnthropic client / messages.stream)
  </behavior>
  <implementation>
    Author tests/agent/conftest.py with the mock stream fixtures from RESEARCH.md Pattern 6 (mock_stream_text_only, mock_stream_with_tool_use, mock_stream_trust_violation) plus a two-distinct-tool-use fixture and a two-identical-tool-use fixture for AGENT-02/AGENT-04. Each fixture builds an AsyncMock implementing the async context manager (__aenter__/__aexit__), an async __aiter__ over delta events, and `get_final_message` returning a MagicMock with the right stop_reason and content blocks. Patch the Anthropic client so run_turn consumes the mock stream — inject a fake client whose `messages.stream(...)` returns the fixture (run_turn already accepts `client` as a parameter, so pass the mock directly; no monkeypatching of the real SDK is required for loop tests). For SSE tests, patch run_turn (or the loop's client) inside api/routes/chat.py so the route drives a deterministic mock turn, then drive the app with `httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test")` and read the streamed body, asserting the SSE frame format `event: <type>\ndata: <json>\n\n` and event ordering. Use the real scan_buffer from agent/trust.py for trust tests (it is pure and deterministic) and mock only log_capability_gap (patch where it is imported) so no DB call occurs. Tests run under asyncio_mode=auto (pytest.ini) so async test functions need no explicit marker. Add tests/agent/__init__.py is already created in Plan 01 — do not recreate it. Keep every assertion exact: assert on stop_reason handling, audit_log length, event codes, and the absence of the violating number in forwarded frames. The whole suite must run green with `pytest tests/agent/ -x -q` and the full project suite green with `pytest tests/ -x -q`.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Mock stream fixtures + loop compliance tests (AGENT-01..04, TRUST-04)</name>
  <read_first>
    - .planning/phases/02-agent-core/02-RESEARCH.md (Pattern 6: pytest-asyncio mock fixtures; Validation Architecture → Phase Requirements → Test Map)
    - agent/loop.py (run_turn signature: messages, client, model, trust_scanner, audit_log; yielded event dict shape)
    - agent/tools.py (dispatch_tool, dedup_key, TOOL_REGISTRY for choosing real tool names + valid inputs)
    - pytest.ini (asyncio_mode = auto)
  </read_first>
  <files>tests/agent/conftest.py, tests/agent/test_loop.py</files>
  <behavior>
    - mock_stream_* fixtures implement the async context manager + async __aiter__ + get_final_message returning the documented stop_reason/content
    - test_no_agent_sdk: import claude_agent_sdk raises ImportError; grep of requirements.txt finds no claude-agent-sdk
    - test_stop_reason_tool_use: tool_use stream dispatches + yields tool_start/tool_result then continues to a done turn; end_turn stream yields done
    - test_parallel_tool_dispatch: two distinct tool_use blocks → audit_log length 2
    - test_tool_deduplication: two identical tool_use blocks → audit_log length 1
    - test_retry_limit: always-violating stream stops after 3 retries with final error code "max_retries"
    - test_failed_tool_surfaced: unknown/raising tool → is_error tool_result and an error entry in audit_log
    - test_audit_log: successful tool turn → audit entry has tool_use_id, name, result
    - No live API call occurs (the mock client is injected as run_turn's client arg)
  </behavior>
  <action>
    Write tests/agent/conftest.py with the RESEARCH.md Pattern 6 fixtures plus the two-distinct and two-identical tool_use fixtures, and a tiny helper that builds a fake client whose `messages.stream(**kwargs)` returns a given mock stream. Write tests/agent/test_loop.py implementing test_no_agent_sdk, test_stop_reason_tool_use, test_parallel_tool_dispatch, test_tool_deduplication, test_retry_limit, test_failed_tool_surfaced, and test_audit_log. Drive run_turn by passing the fake client and a trust_scanner; for non-trust tests inject a no-op scanner that always returns None; for test_retry_limit inject a scanner that always returns a TrustViolation so the retry cap is exercised. Collect run_turn's yielded events into a list via `[ev async for ev in run_turn(...)]` and assert on event ordering, codes, and audit_log contents. Choose real tool names + valid inputs from TOOL_REGISTRY (e.g. calculate_power_zones with {"ftp": 200}) so dispatch_tool actually executes Phase 1 code in the parallel/dedup/audit tests. Mock log_capability_gap where imported so no DB call occurs. Run the RED step first (tests fail because assertions are precise), then confirm GREEN against the implemented loop.
  </action>
  <acceptance_criteria>
    - `pytest tests/agent/test_loop.py -x -q` passes
    - `pytest tests/agent/test_loop.py::test_no_agent_sdk -x -q` passes
    - `pytest tests/agent/test_loop.py::test_parallel_tool_dispatch -x -q` passes
    - `pytest tests/agent/test_loop.py::test_tool_deduplication -x -q` passes
    - `pytest tests/agent/test_loop.py::test_retry_limit -x -q` passes
    - `pytest tests/agent/test_loop.py::test_audit_log -x -q` passes
    - `grep -ci 'anthropic.com' tests/agent/test_loop.py` returns 0 (no live endpoint referenced)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/agent/test_loop.py -x -q</automated>
  </verify>
  <done>Loop compliance tests prove raw-SDK stop_reason handling, parallel dispatch, dedup, the 3-retry cap, surfaced failures, and the audit trail — all against mocked streams with no live API call.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Trust scanner + SSE compliance tests (TRUST-03/05, AGENT-05, AGENT-06 gate)</name>
  <read_first>
    - agent/trust.py (scan_buffer, TrustViolation, async violation hook)
    - api/main.py and api/routes/chat.py (app, /chat/stream, sse_generator frame format)
    - .planning/phases/02-agent-core/02-RESEARCH.md (Don't Hand-Roll → httpx.AsyncClient + ASGITransport; Pattern 4 SSE frames; Pitfall 1 attribution)
    - tests/agent/conftest.py (fixtures from Task 1 to reuse)
  </read_first>
  <files>tests/agent/test_trust.py, tests/agent/test_sse.py</files>
  <behavior>
    - test_trust_violation_triggers_retry: an unsourced "Your FTP is 250 watts" turn increments retries, emits error code "trust_violation", and the violating number is absent from any forwarded token/done frame
    - test_attributed_number_passes: a number present in a tool_result value this turn does not trigger a violation
    - test_capability_gap_fallback: the violation path calls the mocked log_capability_gap with the matched text; the user-facing surface never contains the internal method_name "unsourced_physiological_number"
    - test_sse_event_sequence: GET /chat/stream via ASGITransport returns content-type text/event-stream and a body whose frames are well-formed and ordered (token..., tool_start, tool_result, done)
    - The whole tests/agent suite is the AGENT-06 gate: `pytest tests/agent/ -x -q` green
    - No live Anthropic call in any test
  </behavior>
  <action>
    Write tests/agent/test_trust.py using the real scan_buffer (pure/deterministic) for test_attributed_number_passes and the unit-level violation checks, and run_turn (driven by an always-violating mock stream) plus a mocked log_capability_gap to assert test_trust_violation_triggers_retry and test_capability_gap_fallback — asserting the violating number never appears in any forwarded token/done frame and that log_capability_gap was awaited with the matched text but its internal method_name never reaches the user surface. Write tests/agent/test_sse.py that patches the loop/client used by api/routes/chat.py so a deterministic mock turn runs, then drives the app with `httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test")`, GETs `/chat/stream?conversation_id=test`, asserts the response content-type is text/event-stream, and parses the streamed body to assert the SSE frames are well-formed (`event: <type>` / `data: <json>` / blank line) and appear in the expected order ending with `event: done`. Reuse conftest fixtures from Task 1. Run RED then GREEN. Finally run the full `pytest tests/ -x -q` to confirm the phase suite (sports_science + agent) is green together (AGENT-06 phase gate).
  </action>
  <acceptance_criteria>
    - `pytest tests/agent/test_trust.py -x -q` passes
    - `pytest tests/agent/test_sse.py -x -q` passes
    - `pytest tests/agent/test_trust.py::test_trust_violation_triggers_retry -x -q` passes
    - `pytest tests/agent/test_trust.py::test_capability_gap_fallback -x -q` passes
    - `pytest tests/agent/ -x -q` passes (full agent compliance suite green — AGENT-06)
    - `pytest tests/ -x -q` passes (whole project suite green)
    - `grep -ci 'anthropic.com' tests/agent/test_sse.py tests/agent/test_trust.py` returns 0
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/agent/ -x -q && pytest tests/ -x -q</automated>
  </verify>
  <done>Trust and SSE compliance tests prove unsourced numbers trigger retry + capability-gap logging and never reach the stream, the SSE event sequence is correct, and the full agent suite plus the whole project suite are green with no live API calls (AGENT-05, AGENT-06, TRUST-03/05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → external network | The suite must never reach api.anthropic.com or a real Supabase project; all I/O is mocked |
| assistant text → consumer (under test) | Tests assert the violating number never crosses to a forwarded SSE frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | tests/agent compliance suite | mitigate | test_trust_violation_triggers_retry injects a tampered assistant response with an unsourced number and asserts it triggers retry + capability-gap log and never reaches a forwarded frame (TRUST-03 proof) |
| T-02-12 | Information disclosure | tests (mocked clients) | mitigate | No live Anthropic or Supabase calls; AsyncAnthropic client and log_capability_gap are mocked so no real API key or service-role key is exercised (D-16) |
| T-02-13 | Repudiation | tests/agent/test_loop.py audit assertions | mitigate | test_audit_log asserts every dispatched call is recorded with tool_use_id, name, and result, proving the TRUST-04 traceability trail holds under test |
</threat_model>

<verification>
- `pytest tests/agent/ -x -q` passes (AGENT-06 gate)
- `pytest tests/ -x -q` passes (whole project green)
- `.venv/bin/python -c "import importlib; importlib.import_module('claude_agent_sdk')" ; test $? -ne 0` (claude-agent-sdk not importable / absent)
- `grep -rci 'anthropic.com' tests/agent/` returns 0 (no live endpoint)
</verification>

<success_criteria>
The compliance suite passes and proves, with mocked responses and no live API calls, that the agent uses the raw SDK with stop_reason discipline, dispatches in parallel, dedups and caps retries, surfaces failures, maintains an audit trail, catches injected unsourced physiological numbers (logging a capability gap and never forwarding the number), and streams the correct typed SSE event sequence — satisfying AGENT-01..06 and TRUST-03/04/05.
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-04-SUMMARY.md` when done
</output>
