---
phase: 02-agent-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - agent/__init__.py
  - api/__init__.py
  - api/routes/__init__.py
  - tests/agent/__init__.py
  - sports_science/capability_gap.py
  - tests/sports_science/test_capability_gap.py
  - tests/sports_science/test_import_boundary.py
autonomous: true
requirements: [TRUST-05]
must_haves:
  truths:
    - "anthropic, fastapi, uvicorn, python-multipart are installed and importable; claude-agent-sdk is absent"
    - "agent/ and api/ package directories exist and are importable"
    - "log_capability_gap is an async coroutine backed by the Supabase async client"
    - "sports_science/ still has zero anthropic and zero fastapi imports"
  artifacts:
    - path: "requirements.txt"
      provides: "Phase 2 backend dependencies; no claude-agent-sdk"
      contains: "anthropic"
    - path: "agent/__init__.py"
      provides: "agent package marker"
    - path: "api/__init__.py"
      provides: "api package marker"
    - path: "api/routes/__init__.py"
      provides: "api.routes package marker"
    - path: "tests/agent/__init__.py"
      provides: "tests.agent package marker"
    - path: "sports_science/capability_gap.py"
      provides: "async log_capability_gap via acreate_client"
      contains: "async def log_capability_gap"
  key_links:
    - from: "sports_science/capability_gap.py"
      to: "supabase"
      via: "acreate_client / AsyncClient async client"
      pattern: "acreate_client"
---

<objective>
Establish the Phase 2 foundation: add backend dependencies (anthropic, fastapi, uvicorn, python-multipart) while proving claude-agent-sdk is absent; create the `agent/` and `api/` package scaffolding (D-01, D-02); and upgrade `sports_science/capability_gap.py` to an async Supabase client so `log_capability_gap` becomes an awaitable coroutine the agent dispatcher can call (D-06, TRUST-05).

Purpose: Every downstream plan (tool registry, agent loop, trust scanner, SSE, compliance tests) imports from these packages and depends on the async capability_gap. Nothing can build until the deps install, the package skeleton exists, and the async upgrade lands without breaking the existing trust boundary.
Output: Updated requirements.txt; empty package markers for agent/, api/, api/routes/, tests/agent/; async log_capability_gap; updated capability_gap tests; extended import-boundary test asserting zero fastapi imports in sports_science/.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@.planning/phases/02-agent-core/02-RESEARCH.md
@sports_science/capability_gap.py
@sports_science/types.py
@tests/sports_science/test_import_boundary.py
@requirements.txt
@Makefile
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 backend dependencies and prove claude-agent-sdk is absent</name>
  <read_first>
    - requirements.txt (current pinned deps; no web/LLM libs yet)
    - .planning/phases/02-agent-core/02-RESEARCH.md (Standard Stack table; Package Legitimacy Audit — all four packages Approved)
    - Makefile (install/test/lint targets; venv at .venv)
  </read_first>
  <files>requirements.txt</files>
  <!-- planner-discipline-allow: claude-agent-sdk -->
  <action>
    Append four dependencies to requirements.txt with the versions from RESEARCH.md Standard Stack (per the CLAUDE.md backend/AI stack tables): `anthropic==0.67.*`, `fastapi==0.115.*`, `uvicorn==0.30.*`, and `python-multipart` (latest; required FastAPI multipart dependency installed now per D and the specifics section, used for FIT upload in Phase 3). Keep the existing pins (numpy, scipy, pydantic, supabase, pytest, pytest-asyncio, ruff) unchanged. Do NOT add the autonomous-execution Agent SDK, the separate supabase-py-async package, or any anthropic Bedrock extra: the Anthropic Agent SDK is forbidden by AGENT-01/D-11 because it executes tools autonomously (bypassing the trust model), and supabase-py-async is superseded by the async client already inside supabase==2.31.0. Install into the existing venv via `.venv/bin/python -m pip install -r requirements.txt`. The Package Legitimacy Audit in RESEARCH.md already approved all four packages (official org repos verified); no human-verify checkpoint is required.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import anthropic, fastapi, uvicorn, multipart"` exits 0
    - `grep -c '^anthropic==0.67' requirements.txt` returns at least 1
    - `grep -c '^fastapi==0.115' requirements.txt` returns at least 1
    - `grep -c '^uvicorn==0.30' requirements.txt` returns at least 1
    - `grep -ci 'python-multipart' requirements.txt` returns at least 1
    - `grep -ci 'claude-agent-sdk' requirements.txt` returns 0
    - `grep -ci 'supabase-py-async' requirements.txt` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import anthropic, fastapi, uvicorn, multipart" && test "$(grep -ci 'claude-agent-sdk' requirements.txt)" -eq 0</automated>
  </verify>
  <done>All four packages import from the venv; claude-agent-sdk and supabase-py-async are absent from requirements.txt; existing pins intact.</done>
</task>

<task type="auto">
  <name>Task 2: Create agent/ and api/ package skeleton</name>
  <read_first>
    - .planning/phases/02-agent-core/02-CONTEXT.md (D-01, D-02 — directory layout)
    - .planning/phases/02-agent-core/02-RESEARCH.md (Recommended Project Structure)
    - sports_science/__init__.py (existing package-marker style for reference)
  </read_first>
  <files>agent/__init__.py, api/__init__.py, api/routes/__init__.py, tests/agent/__init__.py</files>
  <action>
    Create the package directory skeleton from D-02: `agent/__init__.py`, `api/__init__.py`, `api/routes/__init__.py`, and `tests/agent/__init__.py`. Each is an empty package marker containing only a one-line module docstring naming its role (for example, agent/__init__.py: transport-agnostic agentic loop layer reusable by the Phase 2 Telegram bot; api/__init__.py: FastAPI HTTP transport layer; tests/agent/__init__.py: agent compliance test package). Do NOT create loop.py, tools.py, trust.py, main.py, or chat.py here — those are built in later plans; this task only establishes importable packages so `import agent` and `import api.routes` succeed. Do NOT place any anthropic or fastapi import inside any sports_science file.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import agent, api, api.routes, tests.agent"` exits 0
    - `test -f agent/__init__.py && test -f api/__init__.py && test -f api/routes/__init__.py && test -f tests/agent/__init__.py` exits 0
    - `ls agent/` lists only `__init__.py` (no loop.py/tools.py/trust.py yet)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import agent, api, api.routes, tests.agent"</automated>
  </verify>
  <done>All four package markers exist and import cleanly; no implementation modules created prematurely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Upgrade log_capability_gap to async Supabase client</name>
  <read_first>
    - sports_science/capability_gap.py (current sync implementation; _get_supabase pattern; GAP-02/GAP-03 guarantees to preserve)
    - .planning/phases/02-agent-core/02-RESEARCH.md (Pattern 5: Supabase Async Upgrade — VERIFIED acreate_client/AsyncClient exports in supabase==2.31.0; Pitfall 6: AsyncClient must be awaited)
    - sports_science/types.py (ToolResult contract; methodology field)
    - tests/sports_science/test_capability_gap.py (existing tests to convert to async)
    - tests/sports_science/test_import_boundary.py (boundary test to extend)
  </read_first>
  <files>sports_science/capability_gap.py, tests/sports_science/test_capability_gap.py, tests/sports_science/test_import_boundary.py</files>
  <behavior>
    - log_capability_gap is now `async def`; awaiting it returns a ToolResult with methodology "capability_gap_log"
    - The returned user-facing message still does NOT contain method_name (GAP-03 preserved)
    - inputs records only context key names, never values (no data leakage preserved)
    - DB write uses an awaited async client (`client = await acreate_client(url, key)` then `await client.table(...).insert(...).execute()`)
    - DB errors remain best-effort: an exception during insert does not prevent returning the fallback ToolResult (GAP-02 preserved)
    - sports_science/ has zero anthropic imports AND zero fastapi imports
  </behavior>
  <action>
    Convert `log_capability_gap` to `async def` following RESEARCH.md Pattern 5. Replace the sync `_get_supabase()` (create_client/Client) with an async helper `_get_async_supabase()` that does `client = await acreate_client(url, key)` using `from supabase import acreate_client, AsyncClient` (VERIFIED exports in supabase==2.31.0). Keep the env-var validation (SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY required; service-role key bypasses RLS; never expose to frontend). Keep the insert payload shape (user_id, method_name, description, context) and the best-effort try/except so a failed insert still returns the fallback ToolResult. The returned ToolResult must keep value={"status": "logged", "message": <generic message>}, unit="", methodology="capability_gap_log", inputs={"context_keys": list(context.keys())}. Preserve GAP-03: method_name must NOT appear in the user-facing message string. Use `await client.table("capability_gaps").insert({...}).execute()` (Pitfall 6: the client must be awaited before use). Then update tests/sports_science/test_capability_gap.py so each test awaits the coroutine (asyncio_mode=auto makes async test functions run without explicit marks), and mock acreate_client so no live DB call occurs; assert the fallback message never contains the method_name string and that methodology == "capability_gap_log". Finally extend tests/sports_science/test_import_boundary.py with a second test asserting `grep -r fastapi sports_science/` finds nothing (returncode != 0), mirroring the existing anthropic-boundary test, so the async upgrade cannot smuggle web-layer imports into the trust anchor.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import inspect, asyncio; from sports_science.capability_gap import log_capability_gap; assert asyncio.iscoroutinefunction(log_capability_gap)"` exits 0
    - `grep -c 'async def log_capability_gap' sports_science/capability_gap.py` returns 1
    - `grep -c 'acreate_client' sports_science/capability_gap.py` returns at least 1
    - `grep -ci 'def _get_supabase\b' sports_science/capability_gap.py` returns 0 (sync helper removed)
    - `pytest tests/sports_science/test_capability_gap.py -x -q` passes
    - `pytest tests/sports_science/test_import_boundary.py -x -q` passes (both anthropic and fastapi boundary tests green)
    - The user-facing fallback message asserted in tests does not contain the injected method_name
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/sports_science/test_capability_gap.py tests/sports_science/test_import_boundary.py -x -q</automated>
  </verify>
  <done>log_capability_gap is async, backed by acreate_client; GAP-02/GAP-03 guarantees preserved; capability_gap and import-boundary (anthropic + fastapi) tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package install → dependency tree | New PyPI packages enter the build; a forbidden autonomous-execution SDK must not slip in |
| backend → Supabase | service-role key is used for the capability_gaps insert; must never cross to frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | requirements.txt | mitigate | Negative-grep gate asserts claude-agent-sdk absent (AGENT-01/D-11); it would let the LLM self-execute tools, bypassing the trust model |
| T-02-02 | Information disclosure | sports_science/capability_gap.py | mitigate | SUPABASE_SERVICE_ROLE_KEY read from env only; never logged, never returned in ToolResult; inputs records only context key names, not values |
| T-02-SC | Tampering | pip install of anthropic/fastapi/uvicorn/python-multipart | accept | RESEARCH.md Package Legitimacy Audit verified all four against official org GitHub repos (anthropics, fastapi, encode, Kludex); no [ASSUMED]/[SUS] package requires a blocking checkpoint |
</threat_model>

<verification>
- `.venv/bin/python -c "import anthropic, fastapi, uvicorn, multipart, agent, api, api.routes, tests.agent"` exits 0
- `grep -ci 'claude-agent-sdk' requirements.txt` returns 0
- `pytest tests/sports_science/ -x -q` fully green (capability_gap async + both import-boundary tests)
</verification>

<success_criteria>
Backend deps installed with claude-agent-sdk provably absent; agent/ and api/ packages importable; log_capability_gap is an async coroutine backed by the Supabase async client with GAP-02/GAP-03 guarantees and the sports_science trust boundary (zero anthropic, zero fastapi) intact.
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-01-SUMMARY.md` when done
</output>
