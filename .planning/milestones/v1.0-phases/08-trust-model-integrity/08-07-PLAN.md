---
phase: 08-trust-model-integrity
plan: 07
type: execute
wave: 3
depends_on: [08-01, 08-03, 08-05]
files_modified:
  - backend/sports_science/profile.py
  - backend/routes/onboarding.py
  - tests/api/test_onboarding.py
autonomous: true
requirements: [ONBD-05]
must_haves:
  truths:
    - "The onboarding interview asks a heart-rate baseline question ('Do you know your lactate threshold heart rate, or your resting/max HR?') as part of the required fields, before the plan/HR-zone tool calls (D-05)"
    - "Branch 1 (LTHR given): used directly. Branch 2 (max HR only): the agent calls estimate_lthr_from_max_hr and uses its methodology-tagged result — never invents the number. Branch 3 (neither): save_profile is called with lthr_estimate=None and the agent skips calculate_hr_zones, falling back to the existing RPE-only cold-start path (Phase 3 D-07)"
    - "save_profile persists hr_zones_available on the profile, true when an LTHR (given or estimated) is present and false when neither is known (D-05 explicit flag)"
  artifacts:
    - backend/sports_science/profile.py
    - backend/routes/onboarding.py
    - tests/api/test_onboarding.py
  key_links:
    - "hr_zones_available is derived server-side in save_profile as (lthr_estimate is not None) and written to the profiles.hr_zones_available column added by Plan 01 — no new LLM-facing schema field, so tools.py is untouched"
    - "the max-HR branch relies on the estimate_lthr_from_max_hr tool registered by Plan 03; the prompt instructs the agent to call it, honouring the trust model"
    - "the neither-known branch reuses the existing RPE-only path (empty hr_zones -> generate_plan already falls back to RPE targets); no new plan-generation code needed"
---

<objective>
Close the onboarding LTHR gap (CONTEXT.md defect #5 / D-05). Today `calculate_hr_zones` is never called during onboarding and there is no LTHR/max-HR question at all, yet Phase 3's plan-generation order expects HR zones before `generate_plan` — so the LLM's only path is to invent a number. Add the interview question with three explicit branches, wire the max-HR branch to the `estimate_lthr_from_max_hr` tool (registered in Plan 03), and persist an explicit `hr_zones_available` flag so the "neither known" case falls back cleanly to the existing RPE-only cold-start path.

Purpose: ONBD-05 (D-05) — the onboarding half; combines with Plan 03's estimator tool.
Output: system-prompt changes, an `hr_zones_available` persistence in save_profile, and onboarding tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-trust-model-integrity/08-CONTEXT.md
@.planning/phases/08-trust-model-integrity/08-RESEARCH.md
@.planning/phases/08-trust-model-integrity/08-PATTERNS.md
@backend/routes/onboarding.py
@backend/sports_science/profile.py
@tests/api/test_onboarding.py
@.planning/phases/03-coaching-loop/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist hr_zones_available in save_profile</name>
  <files>backend/sports_science/profile.py</files>
  <read_first>
    - backend/sports_science/profile.py (save_profile lines 47-117 — the upsert dict with lthr_estimate/lthr keys at 98-102; the ToolResult return)
    - tests/api/test_onboarding.py (test_back_status_constraint ~line 259-306 — the pattern of calling save_profile directly with a mocked _get_async_supabase that captures the upsert payload)
    - .planning/phases/08-trust-model-integrity/08-PATTERNS.md (profile.py Pattern Assignments — follow the same param-then-dict-key convention)
  </read_first>
  <behavior>
    - save_profile writes hr_zones_available to the profiles upsert, derived as (lthr_estimate is not None): true when an LTHR (given or estimated) is present, false when None.
    - The rest of the upsert (goals/weekly_hours/preferred_days/back_status/equipment/rpe_baseline/lthr_estimate/lthr/constraints) is unchanged.
    - No new LLM-facing parameter is added (the flag is derived, so the tools.py save_profile schema needs no change).
  </behavior>
  <action>
    In `backend/sports_science/profile.py`, add `"hr_zones_available": lthr_estimate is not None` to the `profiles` upsert dict in `save_profile` (alongside the existing lthr/lthr_estimate keys). Do NOT add a new function parameter — deriving it from lthr_estimate presence keeps the registered tool schema in tools.py unchanged and matches D-05's three-branch semantics (LTHR given or max-HR-estimated -> lthr_estimate set -> true; neither known -> lthr_estimate None -> false). Leave the ToolResult return shape as-is.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/api/test_onboarding.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'hr_zones_available' backend/sports_science/profile.py`.
    - Behavior: a save_profile call with lthr_estimate=160.0 writes hr_zones_available=True in the captured upsert; a call with lthr_estimate=None writes hr_zones_available=False.
    - Test command: `.venv/bin/python -m pytest tests/api/test_onboarding.py -x` exits 0.
  </acceptance_criteria>
  <done>save_profile persists a correct hr_zones_available flag derived from LTHR presence.</done>
</task>

<task type="auto">
  <name>Task 2: Add the LTHR onboarding question and branch handling to the system prompt</name>
  <files>backend/routes/onboarding.py, tests/api/test_onboarding.py</files>
  <read_first>
    - backend/routes/onboarding.py (ONBOARDING_SYSTEM_PROMPT lines 58-84 — the 6-field list, the confirmation GATE, and the TOOL ORDER after approval; the existing question-flow string convention)
    - backend/agent/tools.py (the estimate_lthr_from_max_hr tool + description registered by Plan 03, so the prompt references it by the correct tool name)
    - tests/api/test_onboarding.py (test file structure; the structural/mock-based test style used for the confirmation gate)
    - .planning/phases/03-coaching-loop/03-CONTEXT.md (D-07 RPE-only cold-start path + D-08 tool order the new branch must respect)
  </read_first>
  <action>
    Edit `ONBOARDING_SYSTEM_PROMPT` in `backend/routes/onboarding.py` to add a heart-rate baseline question to the interview (phrased like "Do you know your lactate threshold heart rate (LTHR), or your max heart rate?"), collected alongside the existing required fields (update the field list/count wording accordingly). Specify the three branches explicitly:
    (1) LTHR known -> pass it as lthr_estimate to save_profile and proceed to calculate_hr_zones;
    (2) max HR known but not LTHR -> call the estimate_lthr_from_max_hr tool with the user's max HR, tell the user it is an estimate (not a measured value), then use that tool result's LTHR for save_profile and calculate_hr_zones;
    (3) neither known -> tell the user the plan will use RPE (effort-based) targets for now, call save_profile with no LTHR, and SKIP calculate_hr_zones (the plan falls back to the existing RPE-only cold-start path).
    Preserve the confirmation GATE ("Here is what I have" summary + explicit approval before save_profile) and fold the HR answer into that summary. Update the TOOL ORDER instruction so the max-HR branch calls estimate_lthr_from_max_hr before calculate_hr_zones, and the neither-known branch omits calculate_hr_zones, while keeping the D-08 order (save_profile -> progress_load -> [calculate_hr_zones] -> generate_plan). Keep the TRUST RULE paragraph. Do NOT touch the sse_generator call site (Plan 05 owns the conversation_id threading there).
    Add a prompt-contract test to `tests/api/test_onboarding.py` asserting ONBOARDING_SYSTEM_PROMPT mentions the LTHR/max-HR question, references the estimate_lthr_from_max_hr tool, and describes the neither-known RPE-only fallback (string-contains assertions, matching the existing structural test style). Record the live end-to-end conversational verification as Manual-Only per 08-VALIDATION.md.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/api/test_onboarding.py -x -q && .venv/bin/python -c "from backend.routes.onboarding import ONBOARDING_SYSTEM_PROMPT as p; assert 'estimate_lthr_from_max_hr' in p; low=p.lower(); assert 'lthr' in low or 'lactate threshold' in low; assert 'rpe' in low; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: ONBOARDING_SYSTEM_PROMPT contains the LTHR/max-HR question, the estimate_lthr_from_max_hr tool name, and the RPE-only fallback wording (inline python check prints ok).
    - Behavior: the prompt-contract test passes and the existing confirmation-gate tests still pass (the HR question does not break the "Here is what I have" gate contract).
    - Manual-Only (08-VALIDATION.md): a real onboarding conversation asks the HR question before any HR-zone/plan tool call and all three branches produce the expected profile state.
  </acceptance_criteria>
  <done>The onboarding prompt collects LTHR/max-HR/neither with correct branch behaviour and tool order; prompt-contract test green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `backend/sports_science/profile.py`: `save_profile` writes `hr_zones_available` (derived from lthr_estimate presence) to the profiles upsert
- `backend/routes/onboarding.py`: `ONBOARDING_SYSTEM_PROMPT` gains the LTHR/max-HR/neither question, the three-branch handling, and the estimate_lthr_from_max_hr tool-order instruction
- `tests/api/test_onboarding.py`: hr_zones_available persistence test + ONBOARDING_SYSTEM_PROMPT contract test
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-reported HR → profile / plan | an LTHR must be user-given or tool-derived, never invented by the LLM |
| missing HR data → plan generation | the neither-known case must degrade to a safe RPE-only plan, not a fabricated HR-zone plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Tampering | LLM inventing an LTHR to satisfy the plan-order dependency | high | mitigate | Explicit three-branch prompt: LTHR used as-is, max-HR routed through the methodology-tagged estimate_lthr_from_max_hr tool, neither-known routed to the RPE-only fallback with hr_zones_available=false — the LLM never emits the number |
| T-08-13 | Repudiation | max-HR-derived LTHR presented as measured | medium | mitigate | Prompt instructs the agent to state the value is an estimate; the tool's methodology string carries the same caveat |
| T-08-SC | Tampering | package installs | low | accept | No new packages; prompt string + one derived column |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/api/test_onboarding.py -x -q` green.
- ONBOARDING_SYSTEM_PROMPT references the estimate tool + LTHR/max-HR + RPE fallback.
- Full suite shows no NEW failures beyond the 9 documented pre-existing baseline failures.
- Manual-Only: live onboarding conversation exercises all three HR branches (08-VALIDATION.md).
</verification>

<success_criteria>
- Onboarding collects LTHR / max-HR-estimate / neither with correct tool order and RPE-only fallback.
- hr_zones_available persisted correctly.
- No new full-suite regressions.
</success_criteria>

<output>
Create `.planning/phases/08-trust-model-integrity/08-07-SUMMARY.md` when done.
</output>
