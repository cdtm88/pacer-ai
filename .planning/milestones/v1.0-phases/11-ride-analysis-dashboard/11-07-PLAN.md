---
phase: 11-ride-analysis-dashboard
plan: 07
type: execute
wave: 5
depends_on: ["11-05", "11-06"]
files_modified: []
autonomous: false
requirements: [RIDE-12]
must_haves:
  truths:
    - "The full backend suite (pytest) and full frontend suite (vitest) are green with the phase's new tests included"
    - "Both fixtures are present in tests/fixtures/ and drive the backend channel/lap/zone assertions"
    - "A human confirms the Analysis screen renders correctly on mobile and desktop, hover sync works, and absent channels hide"
    - "The 5-tab nav (Today/Agenda/Progress/Analysis/Coach) looks correct and every tab is >=44px tall"
  artifacts:
    - ".planning/phases/11-ride-analysis-dashboard/11-07-SUMMARY.md"
  key_links:
    - "Aggregates the test files created by 11-01/11-02/11-03 (backend) and 11-05 (frontend) into one phase-gate green run"
    - "Human-verify checkpoint covers the visual concerns that cannot be automated (Pitfall 6, hover sync, viewport)"
---

<objective>
Close Phase 11 (RIDE-12): run the full backend and frontend suites green with the phase's new tests included, confirm the two fixtures drive the required backend assertions, and gate the visual/interaction quality with a human-verify checkpoint (the parts that cannot be automated: cross-chart hover sync feel, absent-channel hiding on a real Zwift ride, and the 4→5 nav-tab proportions on mobile and desktop).

Purpose: The feature's correctness is proven by the tests written test-first across 11-01..11-06; this plan is the phase gate that (1) proves those tests are all green together and (2) verifies the experiential quality a screenshot/test cannot assert.
Output: `11-07-SUMMARY.md` recording the green run and the human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ride-analysis-dashboard/11-CONTEXT.md
@.planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md
@.planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces no source files. It produces:
- A verified green full-suite run (backend pytest + frontend vitest).
- `11-07-SUMMARY.md` documenting the phase gate and the human sign-off.
(The two fixtures `tests/fixtures/zwift_ride_30min.fit` and `tests/fixtures/hilly_ride_30min.fit` already exist — no fixture creation needed.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm fixtures present and run the full backend + frontend suites green</name>
  <files>tests/fixtures/zwift_ride_30min.fit, tests/fixtures/hilly_ride_30min.fit</files>
  <read_first>
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Validation Architecture: quick + full-suite commands; Pitfall 4 — six laps)
    - tests/api/test_rides_stream.py (the parser + endpoint tests from 11-01/11-03)
    - tests/sports_science/test_zones.py (time_in_hr_zones tests from 11-02)
    - frontend/src/tests/rideChart.test.tsx (component tests from 11-05)
  </read_first>
  <action>
    Confirm both fixtures exist (`ls tests/fixtures/zwift_ride_30min.fit tests/fixtures/hilly_ride_30min.fit`). Run the phase's targeted tests first: `python -m pytest tests/sports_science/test_zones.py tests/api/test_rides_stream.py -x` and `cd frontend && npx vitest run src/tests/rideChart.test.tsx`. Then run the full suites to prove no regression: `python -m pytest` (backend) and `cd frontend && npx vitest run` (frontend). Confirm the backend stream tests assert `channels["altitude"] is False` for the Zwift fixture, `True` for the hilly fixture, and `len(laps) == 6` for both (RESEARCH Pitfall 4). If any test fails, stop and report the failure for a gap-closure plan — do NOT edit source in this verification plan.
  </action>
  <verify>
    <automated>python -m pytest -q && cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Both fixtures exist under `tests/fixtures/`.
    - `python -m pytest` exits 0 (full backend suite green, including test_zones.py and test_rides_stream.py).
    - `cd frontend && npx vitest run` exits 0 (full frontend suite green, including rideChart.test.tsx).
    - The backend stream tests include the altitude-present/absent and `len(laps) == 6` assertions for the two fixtures.
  </acceptance_criteria>
  <done>Fixtures confirmed and both full suites are green with the phase's new tests included.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual + interaction smoke-check of the Analysis feature</name>
  <action>Blocking human-verify checkpoint. Present the how-to-verify steps below to the developer, wait for the resume-signal, and do not mark the plan complete until they respond "approved" (or capture their reported issues for a gap-closure plan). Do not edit source in this plan.</action>
  <what-built>
    The Ride Analysis feature: a new Analysis tab (5th nav item), an AnalysisScreen at /analysis (latest ride) and /rides/:rideId, per-channel Recharts line charts with cross-chart hover sync, lap reference lines, a synced readout row (time / lap / per-channel values), and a server-computed time-in-HR-zone breakdown. Absent channels (e.g. elevation on an indoor Zwift ride) render no chart. A "View analysis" link was added to each ride in the Progress screen's Ride log.
  </what-built>
  <how-to-verify>
    1. Run the app locally (`cd frontend && npm run dev`, backend running per the repo's usual dev command) and sign in as a user with at least one uploaded ride, including one indoor Zwift ride (no elevation).
    2. Mobile viewport (or DevTools device mode): confirm the bottom tab bar shows five tabs in order Today, Agenda, Progress, Analysis, Coach; each tab is comfortably tappable (>=44px tall) and the labels are not clipped (Pitfall 6).
    3. Tap Analysis: the latest ride's charts load; the header reads "Analysis". Confirm an indoor Zwift ride shows NO Elevation chart (and no empty placeholder), while an outdoor ride shows one.
    4. Hover/drag across any chart: the synced readout row and the crosshair move together across ALL charts (D-11-06 syncId), and the time shows the "Mm SSs" format (e.g. "5m 32s"), with a "Lap N" chip.
    5. Confirm the time-in-zone bar + rows appear for a ride with HR when your profile has an LTHR; confirm they are absent for a user with no LTHR or a ride with no HR (no disabled/empty variant).
    6. Go to Progress → Ride log, tap "View analysis" on a row: it navigates to that ride's analysis without toggling the row's expand state.
    7. Desktop viewport: confirm the sidebar shows the Analysis item with the same active-state treatment as the other tabs, and the charts/readout render cleanly at desktop width.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the visual/interaction issues to open a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification only) | This plan runs tests and a human review; it introduces no new code and crosses no new trust boundary. The phase's boundaries are covered by 11-03's threat model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | manual verification | low | mitigate | The human-verify step includes editing a `:rideId` in the URL is out of scope for a non-owned ride; ownership enforcement was proven by 11-03's automated IDOR test. No new surface here. |
</threat_model>

<verification>
- `python -m pytest` — full backend suite green.
- `cd frontend && npx vitest run` — full frontend suite green.
- Human sign-off on the visual/interaction checklist above.
</verification>

<success_criteria>
- Both full suites are green with the phase's new tests, fixtures drive the required assertions, and a human has confirmed the Analysis screen's visual/interaction quality on mobile and desktop.
</success_criteria>

<output>
Create `.planning/phases/11-ride-analysis-dashboard/11-07-SUMMARY.md` when done.
</output>
