---
phase: 11-ride-analysis-dashboard
plan: 06
type: execute
wave: 4
depends_on: ["11-04", "11-05"]
files_modified:
  - frontend/src/screens/AnalysisScreen.tsx
  - frontend/src/router.tsx
  - frontend/src/components/AppLayout.tsx
  - frontend/src/components/nav/BottomTabBar.tsx
  - frontend/src/components/nav/DesktopSidebar.tsx
  - frontend/src/components/history/RideRow.tsx
autonomous: true
requirements: [RIDE-10, RIDE-11]
must_haves:
  truths:
    - "Visiting /analysis shows the most recent ride's analysis; visiting /rides/:rideId shows that ride"
    - "An Analysis tab appears in both the mobile bottom bar and the desktop sidebar, ordered Today, Agenda, Progress, Analysis, Coach"
    - "Each RideRow shows a 'View analysis' link to /rides/:id without changing the existing row layout"
    - "AnalysisScreen shows loading, empty (no rides), and three distinct error states per the UI-SPEC"
    - "The header title reads 'Analysis' on both /analysis and /rides/:rideId"
  artifacts:
    - "frontend/src/screens/AnalysisScreen.tsx"
    - "frontend/src/router.tsx routes rides/:rideId and analysis"
    - "frontend/src/components/AppLayout.tsx ROUTE_TITLES + /rides/ startsWith fallback"
    - "Analysis tab in BottomTabBar.tsx and DesktopSidebar.tsx"
    - "View analysis link in RideRow.tsx"
  key_links:
    - "AnalysisScreen resolves :rideId or falls back to getRides()[0].id (no new backend endpoint) and queries ['ride-stream', rideId] with staleTime Infinity"
    - "AnalysisScreen renders RideChart (11-05) with the fetched RideStream"
    - "Routes sit inside the existing AuthGate → FirstRunGate → AppLayout chain with RouteErrorFallback"
---

<objective>
Wire the Analysis feature into the app (RIDE-10, RIDE-11): a new `AnalysisScreen` at `/rides/:rideId` and default `/analysis` (latest ride), a fifth Analysis nav tab in both nav components, a header title mapping, and a "View analysis" link on each `RideRow`.

Purpose: Keeps the ride log as the list and Analysis as the deep-dive (D-11-05). Reuses `getRides()` for the "latest ride" default (no new backend endpoint) and mirrors ProgressScreen's loading/empty/error affordances so the screen is consistent with the shipped design system (not the Phase 12 athletic target).
Output: new `AnalysisScreen.tsx`; edits to `router.tsx`, `AppLayout.tsx`, `BottomTabBar.tsx`, `DesktopSidebar.tsx`, `RideRow.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md
@.planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md
@.planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `frontend/src/screens/AnalysisScreen.tsx` — `export function AnalysisScreen()`; reads `:rideId` via `useParams`, falls back to latest `getRides()[0].id`; queries `['ride-stream', rideId]` (staleTime Infinity, enabled when rideId present); renders `RideChart`, loading spinner, empty state, and three error states.
- `frontend/src/router.tsx` — two routes under `AppLayout`: `rides/:rideId` and `analysis`, both with `ErrorBoundary: RouteErrorFallback`.
- `frontend/src/components/AppLayout.tsx` — `ROUTE_TITLES['/analysis'] = 'Analysis'` and a `pathname.startsWith('/rides/')` title fallback.
- `frontend/src/components/nav/BottomTabBar.tsx` + `DesktopSidebar.tsx` — an `Activity`-icon Analysis tab at `/analysis`, ordered between Progress and Coach.
- `frontend/src/components/history/RideRow.tsx` — a "View analysis" `Link` to `/rides/${ride.id}`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: AnalysisScreen with query, loading/empty/error states, and RideChart</name>
  <files>frontend/src/screens/AnalysisScreen.tsx</files>
  <read_first>
    - frontend/src/screens/ProgressScreen.tsx (useQuery({queryKey:['rides'],queryFn:getRides}); loading/empty/error markup at lines ~208-247 — "No rides yet", "Could not load history. Tap to retry.")
    - frontend/src/router.tsx (AuthGate/FirstRunGate spinner markup lines 51-60/86-96 — the exact loading affordance to reuse)
    - frontend/src/components/rides/RideChart.tsx (RideChart prop contract from 11-05)
    - frontend/src/lib/api.ts (getRides, getRideStream, RideStream)
    - .planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md (AnalysisScreen contract + Copywriting Contract: exact empty/error copy)
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Don't Hand-Roll "latest ride" via getRides()[0]; Open Question 2 empty state)
  </read_first>
  <action>
    Create `frontend/src/screens/AnalysisScreen.tsx` exporting `AnalysisScreen`. Import `useParams` from `react-router`, `useQuery` from `@tanstack/react-query`, and `getRides`, `getRideStream` from `../lib/api`, and `RideChart` from `../components/rides/RideChart`. Read `const { rideId: routeRideId } = useParams()`. Query `const ridesQuery = useQuery({ queryKey: ['rides'], queryFn: getRides })`. Resolve `const rideId = routeRideId ?? ridesQuery.data?.[0]?.id`. Query `const streamQuery = useQuery({ queryKey: ['ride-stream', rideId], queryFn: () => getRideStream(rideId!), enabled: !!rideId, staleTime: Infinity })`.

    States (match ProgressScreen/router markup exactly):
    - Loading (ridesQuery loading, or streamQuery loading while rideId is set): the exact spinner `<div className="w-8 h-8 rounded-full border-2 border-t-transparent animate-spin" style={{ borderColor: 'var(--color-blue-6)', borderTopColor: 'transparent' }} aria-label="Loading" />`, centered in the scroll area.
    - Empty (ridesQuery done, no rides, no routeRideId): centered block, `textAlign: center`, `paddingTop: 24`, Heading-role "No rides yet" + Body-role "Upload a .FIT file to see your ride analysis." (UI-SPEC Copywriting Contract — verbatim).
    - Error states (three distinct, per UI-SPEC): if streamQuery error looks like a 404 not-found → "This ride couldn't be found." plus a text link "Back to ride log" to `/progress`; if it looks like a 422 corrupt-file → "Could not read this ride file. It may be damaged or in an unsupported format." with NO retry; otherwise generic → a tappable "Could not load ride data. Tap to retry." button (`--color-bad`, 14px, centered) calling `streamQuery.refetch()`. (getRideStream throws `Error('getRideStream failed: <status>')`, so branch on the status number in the message.)
    - Success: render `<RideChart stream={streamQuery.data} />` inside the scrolling content region. Use the same page padding container ProgressScreen uses.
    No em dashes in any copy (use commas / separate sentences).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -Eiq "error" && echo TYPECHECK-FAIL || echo TYPECHECK-OK</automated>
  </verify>
  <acceptance_criteria>
    - `AnalysisScreen` exists, uses `useParams`, resolves `routeRideId ?? getRides()[0].id`, and queries `['ride-stream', rideId]` with `staleTime: Infinity` and `enabled: !!rideId`.
    - Renders the shared spinner, the "No rides yet" empty state, and three distinct error states (not-found, corrupt, generic-with-retry) with the exact UI-SPEC copy.
    - Renders `<RideChart stream={...} />` on success.
    - No em dashes in any copy; `npx tsc --noEmit` prints `TYPECHECK-OK`.
  </acceptance_criteria>
  <done>AnalysisScreen fetches and renders a ride's analysis with correct loading/empty/error states and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Add routes + header title mapping</name>
  <files>frontend/src/router.tsx, frontend/src/components/AppLayout.tsx</files>
  <read_first>
    - frontend/src/router.tsx (AppLayout children block lines 172-204; screen import block lines 12-17; RouteErrorFallback usage)
    - frontend/src/components/AppLayout.tsx (ROUTE_TITLES map lines 8-14; title resolution line 23)
    - .planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md (router + AppLayout ROUTE_TITLES blocks)
    - .planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md (header title note: startsWith('/rides/') fallback)
  </read_first>
  <action>
    In `frontend/src/router.tsx`, import `AnalysisScreen` from `./screens/AnalysisScreen` alongside the other screen imports. Add two route objects inside the existing `AppLayout` children array (same shape as `progress`): `{ path: 'rides/:rideId', element: <AnalysisScreen />, ErrorBoundary: RouteErrorFallback }` and `{ path: 'analysis', element: <AnalysisScreen />, ErrorBoundary: RouteErrorFallback }`. Do not touch the existing `history` → `/progress` redirect or any other route.

    In `frontend/src/components/AppLayout.tsx`, add `'/analysis': 'Analysis'` to `ROUTE_TITLES`, and change the title resolution to `const title = pathname.startsWith('/rides/') ? 'Analysis' : (ROUTE_TITLES[pathname] ?? 'PacerAI')` so both `/analysis` and the dynamic `/rides/:rideId` render "Analysis" in the header (ROUTE_TITLES is keyed by exact pathname and will not match the dynamic segment on its own).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -Eiq "error" && echo TYPECHECK-FAIL || echo TYPECHECK-OK</automated>
  </verify>
  <acceptance_criteria>
    - `router.tsx` has `rides/:rideId` and `analysis` routes under `AppLayout`, both with `ErrorBoundary: RouteErrorFallback`; the `history`→`/progress` redirect is unchanged.
    - `AppLayout.tsx` `ROUTE_TITLES` includes `'/analysis': 'Analysis'` and the title resolver returns "Analysis" for any `/rides/...` pathname.
    - `npx tsc --noEmit` prints `TYPECHECK-OK`.
  </acceptance_criteria>
  <done>Both routes render AnalysisScreen inside the app chrome and the header reads "Analysis".</done>
</task>

<task type="auto">
  <name>Task 3: Add Analysis nav tab (both nav components) + RideRow "View analysis" link</name>
  <files>frontend/src/components/nav/BottomTabBar.tsx, frontend/src/components/nav/DesktopSidebar.tsx, frontend/src/components/history/RideRow.tsx</files>
  <read_first>
    - frontend/src/components/nav/BottomTabBar.tsx (TABS array + lucide import; flex-1 per tab, active dot indicator)
    - frontend/src/components/nav/DesktopSidebar.tsx (NAV_ITEMS array + lucide import; active left-border treatment)
    - frontend/src/components/history/RideRow.tsx (collapsed-row button block lines 67-128; ComplianceChip inline-style convention)
    - .planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md (nav order Today/Agenda/Progress/Analysis/Coach; "View analysis" link copy + placement)
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Pitfall 6: 4→5 tabs, keep it conventional; RideRow now lives in ProgressScreen's Ride log)
  </read_first>
  <action>
    In `BottomTabBar.tsx` and `DesktopSidebar.tsx`: add `Activity` to the `lucide-react` import and insert `{ to: '/analysis', label: 'Analysis', Icon: Activity }` into the `TABS`/`NAV_ITEMS` array BETWEEN the Progress entry and the Coach entry, so the order is Today, Agenda, Progress, Analysis, Coach. Change nothing else — the existing `.map` rendering and active-state styling apply to the new tab automatically (no new styling; do NOT adopt Phase 12's filled-pill nav).

    In `RideRow.tsx`: add `import { Link } from 'react-router'`. Add a "View analysis" `Link` to `/rides/${ride.id}` inside the collapsed row, alongside the existing date / compliance chip / stats — as a SEPARATE element, not nested inside the existing expand `<button>` (avoid nesting two interactive elements). Style it inline: `fontSize: 13, fontWeight: 600, color: 'var(--color-brand)'`, right-aligned. Do NOT remove, restyle, or reorder any existing element in this file (the table→bars redesign is Phase 12's job). Because the row's expand toggle is a full-width `<button>`, place the Link outside that button (e.g. as a sibling in the row container) so clicking "View analysis" navigates without toggling the row; use `e.stopPropagation()` only if the Link ends up inside a clickable ancestor.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/tests/AppLayout.test.tsx src/tests/history.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both nav components import `Activity` and contain an `/analysis` "Analysis" entry ordered between Progress and Coach.
    - `RideRow.tsx` renders a `Link` to `/rides/<id>` with text "View analysis", brand-colored, without removing or restyling any existing element.
    - `grep -c "View analysis" frontend/src/components/history/RideRow.tsx` returns 1.
    - Existing `AppLayout.test.tsx` and `history.test.tsx` still pass; `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>The Analysis tab appears in both navs and each RideRow links to its analysis, with no regression to existing nav/row tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser routing / render | Client-side SPA routing and rendering only; the data boundary is the already-secured stream endpoint (11-03) consumed via getRideStream (11-04). No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | AnalysisScreen / RideRow link | low | mitigate | The screen requests rides by id through the server-scoped endpoint; a user cannot view another user's ride even by editing the `:rideId` in the URL because 11-03 returns 404 for non-owned ids. New routes sit inside the existing AuthGate → FirstRunGate chain, so unauthenticated access redirects to /login. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — no type errors.
- `cd frontend && npx vitest run src/tests/AppLayout.test.tsx src/tests/history.test.tsx` — no regression.
- Manual: the deep visual smoke-check (5-tab nav proportions, hover sync, absent-channel hiding) is the human-verify gate in 11-07.
</verification>

<success_criteria>
- /analysis and /rides/:rideId render the deep-dive; a fifth Analysis tab exists in both navs; each RideRow links to its analysis; existing tests stay green.
</success_criteria>

<output>
Create `.planning/phases/11-ride-analysis-dashboard/11-06-SUMMARY.md` when done.
</output>
