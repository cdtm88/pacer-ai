---
phase: 04-ui-and-calendar
plan: 10
type: execute
wave: gap_closure
depends_on: []
files_modified:
  - frontend/tests/e2e/phase4.spec.ts
  - frontend/src/components/history/FitUploadZone.tsx
  - frontend/src/components/session/TsbChip.tsx
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - UI-02
  - UI-04
  - UI-06
  - CAL-01

must_haves:
  truths:
    - "All 34 Playwright E2E tests pass in one run without retries"
    - "TSB chip labels are sentence-cased (Fresh / Balanced / Fatigued)"
    - "FIT upload zone has data-testid='fit-upload-zone' for E2E targeting"
  artifacts:
    - path: "frontend/tests/e2e/phase4.spec.ts"
      provides: "Playwright E2E test suite (34 tests across T02-T18)"
    - path: "frontend/src/components/history/FitUploadZone.tsx"
      provides: "Drop zone with data-testid attribute for E2E targeting"
    - path: "frontend/src/components/session/TsbChip.tsx"
      provides: "TSB chip with sentence-cased labels matching test assertions"
  key_links:
    - from: "frontend/playwright.config.ts"
      to: "frontend/tests/e2e/phase4.spec.ts"
      via: "Playwright testDir + webServer on port 5174"
      pattern: "testDir.*tests/e2e"
---

<objective>
Commit and verify the in-flight E2E test fixes so the Phase 4 Playwright suite reaches 34/34 passing.

Three source files have uncommitted working-tree changes that fix known test failures:

- `phase4.spec.ts`: route registration order fixed (LIFO), `.first()` added to multi-match selectors, compliance text assertion updated, onboarding profile-404 route ordering corrected, Settings assertions changed to `getByRole`, URL port corrected (5173 -> 5174)
- `FitUploadZone.tsx`: `data-testid="fit-upload-zone"` added for T13 click target
- `TsbChip.tsx`: labels capitalised to Fresh / Balanced / Fatigued to match test assertions

Purpose: All four code gaps from VERIFICATION.md (router stubs, PATCH endpoint, training_sessions table name, conversation_id loss) were fixed in prior commits (WR-005 through WR-009). The E2E suite was built mid-session to validate those fixes; this plan finalises it.

Output: 34/34 green Playwright tests, committed and passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage and commit in-flight E2E test fixes</name>
  <files>
    frontend/tests/e2e/phase4.spec.ts,
    frontend/src/components/history/FitUploadZone.tsx,
    frontend/src/components/session/TsbChip.tsx
  </files>
  <read_first>
    frontend/tests/e2e/phase4.spec.ts
    frontend/src/components/history/FitUploadZone.tsx
    frontend/src/components/session/TsbChip.tsx
    frontend/playwright.config.ts
  </read_first>
  <action>
    These three files have working-tree modifications that are correct but uncommitted. Verify each change is present, then stage and commit.

    For phase4.spec.ts, confirm the following changes are present before committing:
    - Route registration order: `pmc_history/` general handler registered before `pmc_history/latest` specific handler; `rides/` general before `rides/upload` specific. Playwright LIFO means the last-registered route wins, so specific routes must come last.
    - Multi-match text selectors use `.first()` to avoid strict-mode errors: `getByText('tempo', { exact: false }).first()` and `getByText('recovery', { exact: false }).first()`.
    - History screen ride assertion changed from `getByText('morning_ride.fit')` to `getByText('95% on target')` which matches the compliance chip text rendered by RideRow.
    - T15 (Onboarding): `mockBackendApis` called before the `profiles/me` 404 route so the 404 handler is registered last (wins in LIFO order).
    - T16 (Settings): assertions changed to `getByRole('heading', { name: 'Settings' })` and `getByRole('heading', { name: /Google Calendar/i })`.
    - T18 (During-Session End Session): redirect URL changed from `http://localhost:5173/` to `http://localhost:5174/` to match the test server port in playwright.config.ts.

    For FitUploadZone.tsx, confirm `data-testid="fit-upload-zone"` is present on the outer `role="button"` div.

    For TsbChip.tsx, confirm STATE_STYLE labels are `'Fresh'`, `'Balanced'`, `'Fatigued'` (sentence case).

    If any change is missing, apply it. Then:
    ```
    cd /Users/christianmoore/ai/pacer-ai
    git add frontend/tests/e2e/phase4.spec.ts frontend/src/components/history/FitUploadZone.tsx frontend/src/components/session/TsbChip.tsx
    git commit -m "fix(04-e2e): commit E2E test fixes — LIFO route order, selectors, testid, label casing"
    ```
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && git log --oneline -1 | grep -i "e2e\|fix\|test"</automated>
  </verify>
  <done>All three files committed; git status shows no unstaged changes for these paths.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full Playwright E2E suite and confirm 34/34 passing</name>
  <files>
    frontend/tests/e2e/phase4.spec.ts
  </files>
  <read_first>
    frontend/playwright.config.ts
    frontend/package.json
  </read_first>
  <action>
    Run the full Playwright suite from the frontend directory. The config starts a Vite dev server on port 5174 in --mode test using frontend/.env.test (fake Supabase creds). All 34 tests are headless.

    ```
    cd /Users/christianmoore/ai/pacer-ai/frontend && npx playwright test --reporter=line
    ```

    If tests fail, triage by group:

    - Auth / nav tests (T02-T05): check that `SUPABASE_KEY` localStorage injection in `injectAuth()` matches the key derived from `VITE_SUPABASE_URL=https://test-pacer.supabase.co` (key = `sb-test-pacer-auth-token`).
    - Session card tests (T06-T09): confirm the PATCH `/sessions/[^/]+$` route handler calls `route.fallback()` (not `route.continue()`) for non-PATCH verbs.
    - History / compliance (T12): confirm fixture `fixtureRides[0].compliance_pct` is a number (e.g. 95) and that `RideRow` renders a compliance chip when `compliance_pct` is non-null.
    - T13 FIT upload: confirm `data-testid="fit-upload-zone"` is present in the rendered HTML before clicking.
    - T15 Onboarding: confirm the 404 profiles/me route is registered AFTER `mockBackendApis()` so it wins in LIFO.
    - T16 Settings: confirm SettingsScreen renders an `<h1>` or `<h2>` with text "Settings" and a section heading "Google Calendar".
    - T18 During-Session End: confirm redirect lands at `http://localhost:5174/` not `http://localhost:5173/`.

    Do not modify tests to lower their assertions. Fix only the production code or mock setup if a test legitimately fails.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx playwright test --reporter=line 2>&1 | tail -5</automated>
  </verify>
  <done>Playwright output shows "34 passed" with 0 failed and 0 flaky. If retries are needed for a single test, investigate and fix the underlying race rather than accepting a flaky pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> frontend | Playwright injects fake localStorage tokens; no real credentials used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Tampering | phase4.spec.ts fixture data | accept | Test-only file, fake tokens, no production secrets; CI environment only |
| T-04-10-SC | Tampering | npm/playwright install | accept | @playwright/test already present in package-lock.json from prior session work |
</threat_model>

<verification>
```bash
cd /Users/christianmoore/ai/pacer-ai/frontend && npx playwright test --reporter=line 2>&1 | grep -E "passed|failed|flaky"
```
Expected: `34 passed` line, no `failed`, no `flaky`.

```bash
cd /Users/christianmoore/ai/pacer-ai && git log --oneline -3
```
Expected: commit for E2E fixes in top 3 entries.
</verification>

<success_criteria>
- 34/34 Playwright tests pass in a single run
- TSB chip renders "Fresh", "Balanced", or "Fatigued" (sentence case)
- FIT upload zone is reachable via `data-testid="fit-upload-zone"` in the rendered DOM
- E2E fix commit is present in git history
</success_criteria>

<output>
Create `.planning/phases/04-ui-and-calendar/04-10-SUMMARY.md` when done
</output>
