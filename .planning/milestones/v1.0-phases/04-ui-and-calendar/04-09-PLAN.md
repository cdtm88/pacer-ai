---
phase: 04-ui-and-calendar
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/router.tsx
  - api/routes/sessions.py
  - api/routes/rides.py
  - api/routes/onboarding.py
autonomous: false
gap_closure: true
requirements: [UI-01, UI-02, UI-04, UI-06, UI-07, CAL-01, CAL-03, CAL-04]
user_setup:
  - service: google-cloud
    why: "CAL-03 requires the OAuth consent screen to be published in Production, not Testing. Cannot be verified or changed from code."
    dashboard_config:
      - task: "Verify OAuth consent screen status is 'In production' (not 'Testing'); confirm calendar.events scope is approved"
        location: "Google Cloud Console -> APIs & Services -> OAuth consent screen"

must_haves:
  truths:
    - "Navigating to /onboarding renders the real conversational interview UI, not a plain text div"
    - "Navigating to /history renders the FIT upload zone and ride list, not a plain text div"
    - "Navigating to /chat renders the SSE coaching chat UI, not a plain text div"
    - "Navigating to /settings renders the Profile, Google Calendar, and Account sections, not a plain text div"
    - "Clicking Mark Done on today's session card sets the session status to completed without throwing an error"
    - "compliance_pct is computed from a matching planned session and stored on the ride row instead of always being null"
    - "Onboarding preserves multi-turn interview context across SSE replies"
  artifacts:
    - path: "frontend/src/router.tsx"
      provides: "Real screen imports wired into route tree for onboarding, history, chat, settings"
      contains: "import { OnboardingScreen }"
    - path: "api/routes/sessions.py"
      provides: "PATCH /sessions/{session_id} endpoint setting status='completed'"
      contains: "@router.patch"
    - path: "api/routes/rides.py"
      provides: "compliance lookup against the correct sessions table"
      contains: "table(\"sessions\")"
    - path: "api/routes/onboarding.py"
      provides: "conversation_id captured from create_conversation and used to seed onboarding turns"
  key_links:
    - from: "frontend/src/router.tsx"
      to: "frontend/src/screens/OnboardingScreen.tsx"
      via: "ES import of the real screen replacing the inline stub"
      pattern: "import.*OnboardingScreen.*from './screens/OnboardingScreen'"
    - from: "frontend/src/components/session/SessionCard.tsx"
      to: "api/routes/sessions.py"
      via: "markSessionDone -> PATCH /sessions/{id}"
      pattern: "@router\\.patch\\(\"/sessions/"
    - from: "api/routes/rides.py"
      to: "sessions table"
      via: "compliance lookup query"
      pattern: "table\\(\"sessions\"\\)"
---

<objective>
Close the four code gaps and one human-verify gap found in 04-VERIFICATION.md for the UI and Calendar phase. All gaps are wiring/correctness defects in otherwise-complete implementations; no new features are introduced.

Purpose: The phase goal (all screens functional; calendar in sync) is blocked by three BLOCKER gaps and one WARNING gap. Four real screens are unreachable, Mark Done has no backend route, compliance is silently always null, and onboarding loses multi-turn context.

Output: router.tsx imports the four real screens; sessions.py exposes PATCH /sessions/{id}; rides.py queries the correct table; onboarding.py preserves conversation context; user confirms Google OAuth production status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the four real screens into router.tsx (Gap 1 — BLOCKER)</name>
  <files>frontend/src/router.tsx</files>
  <read_first>
    - frontend/src/router.tsx (current state: lines 114-131 define inline stub components OnboardingScreen, HistoryScreen, ChatScreen, SettingsScreen that return a div with literal text; DuringSessionScreen already delegates to the real impl)
    - frontend/src/screens/OnboardingScreen.tsx (confirm the named export is OnboardingScreen)
    - frontend/src/screens/HistoryScreen.tsx (confirm the named export is HistoryScreen)
    - frontend/src/screens/ChatScreen.tsx (confirm the named export is ChatScreen)
    - frontend/src/screens/SettingsScreen.tsx (confirm the named export is SettingsScreen)
  </read_first>
  <action>
    Replace the four inline stub component definitions in router.tsx (lines 114-131: OnboardingScreen, HistoryScreen, ChatScreen, SettingsScreen) with top-level ES imports from ./screens/, following the existing import pattern already used for TodayScreen, AgendaScreen, LoginScreen, and DuringSessionScreenImpl at the top of the file.

    Concretely:
    - Add the import for OnboardingScreen from './screens/OnboardingScreen' near the other screen imports, after the existing AgendaScreen import line.
    - Add the import for HistoryScreen from './screens/HistoryScreen'.
    - Add the import for ChatScreen from './screens/ChatScreen'.
    - Add the import for SettingsScreen from './screens/SettingsScreen'.
    - Delete the four inline stub function definitions (the OnboardingScreen, HistoryScreen, ChatScreen, SettingsScreen functions that return a div containing only literal text).
    - Keep the existing DuringSessionScreen wrapper function that delegates to DuringSessionScreenImpl exactly as-is; it is correct and must not be touched.

    Verify each real screen's actual export name in its file before writing the import. If a real screen uses a default export instead of a named export, adjust the import form to the default-import shape for that file. The route element references in the route tree at lines 138-201 stay exactly as they are; only the source of those four symbols changes from inline stub to import.

    Resolves verification gap 1 (truth: all six screens are implemented and navigable). Reason from gap: real screen files exist at frontend/src/screens/ but router.tsx defines its own stubs, so UI-01, UI-04, UI-06, and the UI-07 settings destination are non-functional.
  </action>
  <verify>
    <automated>cd frontend && grep -q "import { OnboardingScreen } from './screens/OnboardingScreen'" src/router.tsx && grep -q "import { HistoryScreen } from './screens/HistoryScreen'" src/router.tsx && grep -q "import { ChatScreen } from './screens/ChatScreen'" src/router.tsx && grep -q "import { SettingsScreen } from './screens/SettingsScreen'" src/router.tsx && ! grep -qE "function (OnboardingScreen|HistoryScreen|ChatScreen|SettingsScreen)\(\)" src/router.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - router.tsx contains a top-level import for each of OnboardingScreen, HistoryScreen, ChatScreen, SettingsScreen from the corresponding ./screens/ path
    - router.tsx no longer defines inline function components named OnboardingScreen, HistoryScreen, ChatScreen, or SettingsScreen (grep for the parameterless function declarations returns no match)
    - The DuringSessionScreen wrapper that delegates to DuringSessionScreenImpl is still present and unchanged
    - npm run build succeeds with no unresolved-import or duplicate-identifier errors
  </acceptance_criteria>
  <done>Navigating to /onboarding, /history, /chat, and /settings renders the real screen implementations instead of plain text divs; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add PATCH /sessions/{session_id} endpoint (Gap 2 — BLOCKER)</name>
  <files>api/routes/sessions.py</files>
  <read_first>
    - api/routes/sessions.py (current state: only GET endpoints defined — /sessions/today, /sessions/upcoming, /pmc_history/latest, /pmc_history/, /profiles/me; router mounted with no prefix; uses get_current_user dependency and _get_async_supabase singleton; 404 shape used by /profiles/me is detail of error profile_not_found)
    - frontend/src/lib/api.ts lines 179-187 (markSessionDone sends PATCH /sessions/{sessionId} with JSON body status completed and throws on non-ok response)
    - api/auth.py (get_current_user contract returns a dict with a user_id key)
    - api/utils.py (validate_uuid helper and the HTTPException error-detail shape with error and detail keys)
  </read_first>
  <action>
    Add a PATCH endpoint at path /sessions/{session_id} to api/routes/sessions.py that sets the matching session status to completed for the authenticated user.

    Concretely:
    - Define the route with the patch decorator on path /sessions/{session_id}, decorating an async handler named update_session with signature parameters session_id (str, path param) and current_user (dict) sourced from Depends(get_current_user).
    - Source user_id from current_user index user_id (never from a path, query, or body param), matching the T-04-01 pattern used by the existing GET handlers in this file.
    - Validate session_id is a UUID using the validate_uuid helper from api.utils (import it if not already imported); on failure raise HTTPException 400 with the structured detail shape (error key and detail key) used elsewhere in the backend.
    - Acquire the singleton via supabase = await _get_async_supabase().
    - Execute an update filtered by BOTH id and user_id so a user can never complete another user's session (T-04-03): update the sessions table setting status to completed, with an eq filter on id equal to session_id and an eq filter on user_id equal to user_id, then execute.
    - If the update returns no rows (result data is empty), raise HTTPException 404 with detail whose error is session_not_found and detail is a human-readable no-session message, matching the 404 shape used by /profiles/me.
    - Return the updated row dict (result data first element) on success.

    The frontend already calls this endpoint (markSessionDone in api.ts sends the PATCH with a status-completed body) and throws on a non-2xx response. The request body is informational only; the handler derives the new status server-side as completed and does not need to trust the request body.

    Resolves verification gap 2 (truth: Today screen shows session card with Mark Done). Reason from gap: SessionCard.markSessionDone calls PATCH /sessions/{id} which does not exist in sessions.py (only GET endpoints present), so every click throws internally.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && grep -qE '@router\.patch\("/sessions/\{session_id\}"\)' api/routes/sessions.py && .venv/bin/python -c "import ast; ast.parse(open('api/routes/sessions.py').read()); print('parse ok')" && .venv/bin/pytest tests/api/test_sessions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - sessions.py defines the patch route on path /sessions/{session_id} decorating an async handler
    - The handler sources user_id from current_user user_id, not from any path, query, or body param
    - The update query filters on both id equal to session_id and user_id equal to user_id
    - The update sets status to completed
    - A no-match update returns HTTP 404 with the structured error detail shape (error session_not_found)
    - api/routes/sessions.py parses without syntax error and existing test_sessions.py tests still pass
  </acceptance_criteria>
  <done>PATCH /sessions/{id} exists; Mark Done updates a session to completed for the owning user and returns 404 for a non-existent or non-owned session; no error is thrown by the frontend on a valid completion.</done>
</task>

<task type="auto">
  <name>Task 3: Fix compliance table name in rides.py (Gap 3 — BLOCKER)</name>
  <files>api/routes/rides.py</files>
  <read_first>
    - api/routes/rides.py lines 304-335 (current state: the FIT-05 compliance block at line 309 queries the training_sessions table; the exception is swallowed at line 321-322 via an except clause that logs at info level, so compliance_pct is silently always null)
    - api/routes/sessions.py lines 96-141 (confirms the real Supabase table is named sessions — the GET handlers query the sessions table; _SESSION_COLUMNS selects tss_target and type, not tss and session_type)
  </read_first>
  <action>
    On line 309 of api/routes/rides.py, change the table name in the compliance-lookup query from the non-existent training_sessions table to the real sessions table.

    Concretely: replace the table reference training_sessions with sessions inside the try block of the Step 4 (FIT-05) compliance check. Do not alter the user_id and scheduled_date eq filters, the validate_session_vs_actual call, or the swallowing except clause beyond what the column-name reconciliation below requires.

    The planned-session select on line 310 currently selects tss and session_type. Cross-reference _SESSION_COLUMNS in sessions.py: the sessions table exposes tss_target (planned TSS) and type (session type), not tss and session_type. Update the select on line 310 to request tss_target and type, and update the subsequent read on line 318 (planned_session get tss default 0) to read from tss_target so the compliance percentage is computed against the real planned TSS value rather than defaulting to 0. If a column name cannot be confirmed against sessions.py, prefer the name used in _SESSION_COLUMNS.

    Resolves verification gap 3 (truth: calendar sync failures surface gracefully — compliance). Reason from gap: rides.py line 309 queries training_sessions (a table that does not exist); the exception is swallowed at line 322 so compliance_pct is silently always null, breaking UI-04 compliance chips in HistoryScreen.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && ! grep -q "training_sessions" api/routes/rides.py && grep -q 'table("sessions")' api/routes/rides.py && .venv/bin/python -c "import ast; ast.parse(open('api/routes/rides.py').read()); print('parse ok')" && .venv/bin/pytest tests/api/test_rides.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The string training_sessions no longer appears anywhere in api/routes/rides.py
    - The compliance-lookup query references the sessions table
    - The planned-session select and the planned-TSS read use the actual sessions columns (tss_target, type) confirmed against sessions.py _SESSION_COLUMNS
    - api/routes/rides.py parses without syntax error and existing test_rides.py tests still pass
  </acceptance_criteria>
  <done>The compliance lookup queries the sessions table; when a matching planned session exists, compliance_pct is computed from its planned TSS and stored on the ride row instead of always being null.</done>
</task>

<task type="auto">
  <name>Task 4: Preserve onboarding conversation context (Gap 4 — WARNING, WR-05)</name>
  <files>api/routes/onboarding.py</files>
  <read_first>
    - api/routes/onboarding.py lines 234-274 (current state of onboarding_start: line 261 awaits create_conversation but discards the return value; the seeded messages list is always a single hardcoded user message; sse_generator is called without any conversation reference)
    - api/routes/onboarding.py lines 119-180 (create_conversation returns the new conversation UUID string; load_conversation(conversation_id, user_id, limit) returns prior messages in chronological order)
    - api/routes/chat.py lines 70-105 (reference pattern: chat.py captures conversation_id from a query param, calls load_conversation(conversation_id, user_id, limit) to build the messages list, then passes that list to sse_generator)
    - api/routes/_sse.py lines 34-39 (sse_generator signature: messages, model, system_prompt, _run_turn — it does NOT accept a conversation_id; conversation state is carried through the messages list, not a separate id param)
  </read_first>
  <action>
    Capture the conversation_id returned by create_conversation in onboarding_start and use it so multi-turn interview state is preserved instead of every reply starting from a single hardcoded message.

    Concretely:
    - On line 261, capture the return value: assign the awaited create_conversation result to a conversation_id variable instead of discarding it. Keep the best-effort try/except so a DB failure does not block the stream; on the except branch, set conversation_id to None.
    - After conversation creation, when conversation_id is not None, load any existing turns for this conversation via load_conversation(conversation_id, user_id) (the same helper chat.py uses) and use the returned list as the messages passed to sse_generator. When load_conversation returns an empty list (brand-new conversation) or conversation_id is None, fall back to the existing single seed message list so the interview opener still fires.
    - Pass the resulting messages list (loaded turns when present, else the seed) into the existing sse_generator(...) call. Do not add a conversation_id parameter to sse_generator — its signature is messages, model, system_prompt, _run_turn, and conversation context flows through the messages list (mirror chat.py exactly, which builds messages via load_conversation and passes only the list).

    This matches the verification gap's stated fix (capture conversation_id from create_conversation so it is no longer discarded and multi-turn interview state is preserved) and uses the established chat.py pattern rather than inventing a new sse_generator parameter.

    Resolves verification gap 4 (truth: Google Calendar / onboarding wiring — WR-05). Reason from gap: line 261 awaits create_conversation but does not capture the return value, and sse_generator is called without it, so each SSE reply starts a fresh context and multi-turn interviews lose prior turns.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && grep -qE "conversation_id\s*=\s*await create_conversation" api/routes/onboarding.py && grep -q "load_conversation" api/routes/onboarding.py && .venv/bin/python -c "import ast; ast.parse(open('api/routes/onboarding.py').read()); print('parse ok')" && .venv/bin/pytest tests/api/test_onboarding.py -q</automated>
  </verify>
  <acceptance_criteria>
    - onboarding_start captures the create_conversation return value into a conversation_id variable (it is no longer discarded)
    - When conversation_id is set, the messages passed to sse_generator come from load_conversation(conversation_id, user_id) when prior turns exist
    - When no prior turns exist or conversation_id is None, the existing single seed message is used so the interview opener still fires
    - sse_generator is still called with the messages-list signature (no new conversation_id parameter added to sse_generator)
    - api/routes/onboarding.py parses without syntax error and existing test_onboarding.py tests still pass
  </acceptance_criteria>
  <done>The onboarding conversation_id is captured and used to load prior turns, so multi-turn interview context is preserved across SSE replies instead of resetting on every turn.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Human verify Google OAuth production status (Gap 5 — CAL-03)</name>
  <files>(none — Google Cloud Console dashboard setting)</files>
  <read_first>
    - .planning/phases/04-ui-and-calendar/04-VERIFICATION.md (behavior_unverified_items: CAL-03 production-credentials check)
    - .planning/phases/04-ui-and-calendar/04-07-PLAN.md user_setup block (the original CAL-03 setup task)
  </read_first>
  <what-built>
    CAL-03 requires the Google Cloud OAuth consent screen to be published in Production (not Testing) with the calendar.events scope approved. This cannot be verified or changed from code — it is a Google Cloud Console dashboard setting. No prior task touches this; it is a manual confirmation.
  </what-built>
  <action>
    Human-only confirmation; no code change. Ask the developer to verify in Google Cloud Console that the PacerAI OAuth consent screen is published "In production" (not "Testing") and that the calendar.events scope is approved. Do not auto-approve this checkpoint (workflow.auto_advance does not apply to blocking-human gates). Pause execution until the developer responds.
  </action>
  <how-to-verify>
    1. Log in to Google Cloud Console for the PacerAI project.
    2. Navigate to APIs & Services -> OAuth consent screen.
    3. Confirm the publishing status reads "In production" (NOT "Testing").
    4. Confirm the scope list includes https://www.googleapis.com/auth/calendar.events.
    5. Confirm a real user outside any test-user allowlist can authorize the calendar connection.
    Expected: status shows In production; calendar.events scope approved; no per-user test whitelist required.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the OAuth consent screen shows "In production" and the calendar.events scope is approved.</human-check>
  </verify>
  <acceptance_criteria>
    - Developer confirms OAuth consent screen publishing status is "In production"
    - Developer confirms the calendar.events scope is listed and approved
  </acceptance_criteria>
  <resume-signal>Type "approved" once production status is confirmed, or describe what the consent screen currently shows.</resume-signal>
  <done>Developer has confirmed the Google OAuth consent screen is in Production mode with the calendar.events scope approved.</done>
</task>

</tasks>

<verification>
- Frontend builds: cd frontend && npm run build succeeds
- router.tsx imports the four real screens and defines no inline stub screen functions
- PATCH /sessions/{session_id} exists in sessions.py and existing session tests pass
- training_sessions no longer appears in rides.py; the sessions table is queried for compliance
- onboarding.py captures conversation_id and loads prior turns via load_conversation
- Backend suites pass: .venv/bin/pytest tests/api/test_sessions.py tests/api/test_rides.py tests/api/test_onboarding.py -q
- Human confirms Google OAuth consent screen is In production (CAL-03)
</verification>

<success_criteria>
- All four code gaps from 04-VERIFICATION.md are closed (router stubs replaced, PATCH endpoint added, table name fixed, conversation_id captured)
- /onboarding, /history, /chat, /settings render real screens (UI-01, UI-04, UI-06, UI-07)
- Mark Done completes a session via a real backend route (UI-02)
- compliance_pct is computed and stored from a matching planned session (UI-04, CAL-04)
- Multi-turn onboarding interview context is preserved (CAL-01 onboarding flow)
- Google OAuth production status is human-confirmed (CAL-03)
</success_criteria>

<artifacts_produced>
This gap-closure plan produces no new files. It modifies:
- frontend/src/router.tsx (imports four real screens, removes inline stubs)
- api/routes/sessions.py (adds PATCH /sessions/{session_id})
- api/routes/rides.py (corrects compliance table name and columns)
- api/routes/onboarding.py (captures and uses conversation_id)
And surfaces one human-verify confirmation for CAL-03 (no file output).
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-09-SUMMARY.md` when done
</output>
