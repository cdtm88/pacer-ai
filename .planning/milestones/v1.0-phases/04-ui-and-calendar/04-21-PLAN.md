---
phase: 04-ui-and-calendar
plan: 21
type: execute
wave: 8
depends_on: [04-16]
files_modified:
  - frontend/tests/e2e/full-uat.spec.ts
autonomous: true
gap_closure: true
requirements: [UI-04]
must_haves:
  truths:
    - "In the e2e mock setup, an upload request to /rides/upload is matched by the /rides/upload handler (returning the success fixture), not swallowed by the general /rides/ handler"
    - "A GET to /rides/ is still matched by the /rides/ list handler"
    - "The History upload e2e flow reflects a successful upload against the mock backend"
  artifacts:
    - path: "frontend/tests/e2e/full-uat.spec.ts"
      provides: "Route registration ordered so the specific /rides/upload handler wins under Playwright LIFO matching"
      contains: "rides/upload"
  key_links:
    - from: "frontend/tests/e2e/full-uat.spec.ts"
      to: "frontend/src/lib/api.ts"
      via: "uploadRide POSTs /rides/upload; the mock must intercept it with the upload fixture, not the list handler"
      pattern: "rides/upload"
---

<objective>
Close the test-harness half of UAT GAP 4 (Tests 12-13: "uploadRide failed: 422"). Playwright matches page.route handlers in LIFO order — the LAST-registered matching handler wins. In full-uat.spec.ts the general /rides/ handler is registered AFTER the specific /rides/upload handler, so /rides/upload requests are matched by the general /rides/ handler and never receive the upload success fixture; against the real backend an upload of a short/invalid file then returns 422.

The diagnosis is explicit: register /rides/ FIRST and /rides/upload LAST so the specific upload handler wins under LIFO. This is a test-harness ordering fix only.

Note on the real (non-test) 422: the runtime 422 happens when a user uploads a FIT file shorter than the minimum duration; the 04-16 fix already made the duration parse accurate and surfaces the backend error reason. The real-device remedy is to upload a valid FIT file with 10+ minutes of data (test-ride.fit in the repo root is a 66-byte stub and is NOT a valid 10-minute ride — it is only useful for unit-level parser tests, not as a UAT upload). This plan does not change backend code; it only corrects the e2e route ordering so the mocked History upload flow passes.

Purpose: UI-04 (History FIT upload). The e2e suite must exercise a successful upload path so the upload flow is covered and green.

Output: full-uat.spec.ts registers /rides/ before /rides/upload so the upload handler wins; the History upload test reflects success.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UAT.md
@frontend/tests/e2e/full-uat.spec.ts
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder the rides route handlers so /rides/upload wins under Playwright LIFO</name>
  <files>frontend/tests/e2e/full-uat.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/full-uat.spec.ts lines 231-232: the two rides handlers inside mockBackendApis — currently page.route(/\/rides\/upload/, ...) is registered BEFORE page.route(/\/rides\//, ...). Under Playwright LIFO, the LAST matching handler wins, so the /rides/ handler (registered second) intercepts /rides/upload requests and returns the rides list fixture instead of the upload fixture.
    - frontend/tests/e2e/full-uat.spec.ts around lines 159 and the fixture block: fixtureRides[0] is used as the upload success fixture and fixtureRides as the list fixture; do not change the fixtures, only the registration order.
    - frontend/src/lib/api.ts lines 277-309: uploadRide POSTs multipart to /rides/upload and throws with the backend detail on non-ok. The mock must intercept /rides/upload with a 2xx fixture so this resolves.
  </read_first>
  <action>
    In mockBackendApis (full-uat.spec.ts, lines 231-232), swap the registration order of the two rides handlers so the GENERAL /rides/ handler is registered FIRST and the SPECIFIC /rides/upload handler is registered LAST. Concretely, the resulting order is:
      1. page.route(/\/rides\//, (route) => route.fulfill(respond(overrides.rides ?? fixtureRides)))   // general list — registered first
      2. page.route(/\/rides\/upload/, (route) => route.fulfill(respond(fixtureRides[0])))            // specific upload — registered last, wins LIFO

    Because Playwright evaluates routes last-registered-first, the /rides/upload handler now wins for upload POSTs while a GET /rides/ still falls through to the list handler (the upload regex does not match a bare /rides/). Keep both handler bodies, the respond() helper, and the fixtures exactly as they are — only the two lines' order changes.

    This mirrors the established project convention recorded in STATE.md: "Playwright LIFO: specific route handlers registered after general ones to win the match." Apply the same convention here for the rides pair. Do not reorder any other route registrations.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test full-uat --grep "History"</automated>
  </verify>
  <acceptance_criteria>
    - In mockBackendApis, the /rides/ general handler is registered before the /rides/upload specific handler
    - The /rides/upload handler returns the upload success fixture (fixtureRides[0]); the /rides/ handler returns the list fixture
    - The History e2e tests (including the upload flow) pass
    - No other route registrations or fixtures were changed
  </acceptance_criteria>
  <done>The mocked /rides/upload request is intercepted by the specific upload handler (not the general list handler), so the History upload e2e flow reflects success and the test-harness half of GAP 4 is closed.</done>
</task>

</tasks>

<verification>
- /rides/ is registered before /rides/upload so the upload handler wins under LIFO (read confirms order)
- History e2e tests pass: cd frontend && npx playwright test full-uat --grep "History"
- Note recorded in SUMMARY: the real-device 422 is resolved by uploading a valid 10+ minute FIT file, not a code change; test-ride.fit (66 bytes) is a parser stub, not a valid UAT upload
</verification>

<success_criteria>
- The e2e upload flow is intercepted correctly and the History tests pass (test-harness half of UAT GAP 4 closed)
- The real-device upload remedy (valid FIT file) is documented for the human re-test
</success_criteria>

<artifacts_produced>
This gap-closure plan produces no new files. It modifies:
- frontend/tests/e2e/full-uat.spec.ts (swap rides route registration order so /rides/upload wins LIFO)
No new exported symbols.
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-21-SUMMARY.md` when done
</output>
