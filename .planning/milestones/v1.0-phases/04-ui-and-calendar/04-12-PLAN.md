---
phase: 04-ui-and-calendar
plan: 12
type: execute
wave: 6
depends_on: [04-06, 04-09]
files_modified:
  - api/routes/onboarding.py
  - api/routes/_sse.py
autonomous: true
gap_closure: true
requirements: [UI-01]
must_haves:
  truths:
    - "After the onboarding agent replies, both the user message and the assistant reply are persisted to the messages table"
    - "On the next onboarding turn, load_conversation returns the prior user and assistant turns so the agent does not re-ask an already-answered question"
    - "Answering 'What are your main fitness goals?' advances the interview to the next field instead of looping back to the same question"
  artifacts:
    - path: "api/routes/onboarding.py"
      provides: "save_messages() invoked after each onboarding turn with the user message and the assembled assistant reply"
      contains: "save_messages"
    - path: "api/routes/_sse.py"
      provides: "sse_generator captures the assistant text so the caller can persist it"
      contains: "assistant"
  key_links:
    - from: "api/routes/onboarding.py"
      to: "messages table"
      via: "save_messages persists user + assistant turns after the stream"
      pattern: "save_messages\\("
    - from: "api/routes/onboarding.py"
      to: "api/routes/_sse.py"
      via: "sse_generator surfaces accumulated assistant text for persistence"
      pattern: "sse_generator\\("
---

<objective>
Close UAT GAP 6: the onboarding interview loops, re-asking "What are your main fitness goals?" regardless of the answer. Root cause (diagnosed in 04-UAT.md): `save_messages()` is defined in `onboarding.py` but never called, so neither the user's reply nor the agent's response is persisted. Every turn `load_conversation` returns an empty/stale history and the agent repeats its opening question.

Purpose: Without persistence the multi-turn interview cannot progress. UI-01 (onboarding conversational flow) is non-functional in practice even though the streaming wiring works.

Output: `onboarding.py` persists both the incoming user message and the assembled assistant reply after each `/onboarding/start` turn, so subsequent turns load real prior context. `_sse.py` exposes the accumulated assistant text to the caller so it can be saved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UAT.md
@.planning/phases/04-ui-and-calendar/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make sse_generator surface the accumulated assistant text</name>
  <files>api/routes/_sse.py</files>
  <read_first>
    - api/routes/_sse.py (current state: sse_generator is an async generator that yields SSE frames; it iterates `fn(messages, client, model, scan_buffer, audit_log, **kwargs)` and yields `event: <type>\ndata: <json>\n\n` per event; it does NOT retain the assistant text after yielding — token events carry {"text": ...} deltas)
    - api/agent/loop.py (confirm the run_turn event shape: each yielded event is a dict with keys "event" and "data"; the assistant text is delivered incrementally via event type "token" with data {"text": "..."}; confirm whether run_turn yields a terminal event such as "done")
    - api/routes/onboarding.py lines 263-279 (the caller's _stream_with_metadata wraps sse_generator; this is where the accumulated assistant text must be captured for save_messages)
  </read_first>
  <action>
    Give the caller a way to obtain the full assistant text that was streamed, without changing the SSE frame format the frontend consumes.

    Concretely, add an optional mutable sink parameter to sse_generator named `assistant_sink` (default None) typed as a list. While iterating events from `fn(...)`, when an event of type "token" is seen, append its `data["text"]` value to a local accumulator string. After the async-for loop completes normally (and inside the same scope, before the generator returns), if `assistant_sink is not None`, append the fully accumulated assistant text as a single string to `assistant_sink` (i.e. `assistant_sink.append(accumulated_text)`). Do not append on the exception path (a server_error frame means there is no complete assistant turn worth persisting).

    Keep the existing yield behavior byte-for-byte identical: still yield `event: {event_type}\ndata: {data}\n\n` for every event, and still emit the `event: error` frame on exception. The sink is write-only side output; it must never alter, delay, or drop any yielded frame.

    Rationale: token events carry incremental deltas, so the assistant's full reply only exists once all token events have been concatenated. The caller (onboarding.py) needs that concatenated string to persist the assistant turn via save_messages. A list sink is used because an async generator cannot cleanly return a value to a caller that drives it with `async for`.

    Do not import anything new beyond what is already imported. Do not change the function's positional parameters (messages, model, system_prompt, _run_turn); add assistant_sink as a keyword-only or trailing keyword argument with a None default so existing callers (chat.py) are unaffected.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && grep -q "assistant_sink" api/routes/_sse.py && .venv/bin/python -c "import ast; ast.parse(open('api/routes/_sse.py').read()); print('parse ok')" && .venv/bin/pytest tests/api/test_sse.py -q</automated>
  </verify>
  <acceptance_criteria>
    - sse_generator accepts an optional assistant_sink parameter defaulting to None
    - While iterating events, the text from every "token" event is concatenated into a local accumulator
    - After a successful run, when assistant_sink is not None, the full accumulated assistant string is appended to assistant_sink exactly once
    - On the exception (error) path, nothing is appended to assistant_sink
    - Every SSE frame yielded before this change is still yielded identically (no frame added, removed, reordered, or altered)
    - api/routes/_sse.py parses without syntax error and existing test_sse.py tests still pass
  </acceptance_criteria>
  <done>sse_generator can hand the caller the complete streamed assistant text via assistant_sink while emitting the exact same SSE frames to the client.</done>
</task>

<task type="auto">
  <name>Task 2: Persist user and assistant turns after each onboarding stream (UAT GAP 6)</name>
  <files>api/routes/onboarding.py</files>
  <read_first>
    - api/routes/onboarding.py lines 205-279 (current state of onboarding_start: it loads prior_turns via load_conversation, appends body.message to prior_turns when present, builds `messages`, then `_stream_with_metadata` yields a metadata frame and delegates to sse_generator; save_messages is NEVER called anywhere in this function)
    - api/routes/onboarding.py lines 148-173 (save_messages signature: save_messages(conversation_id, user_id, new_messages) where new_messages is a list of {role, content} dicts; it is a no-op when new_messages is empty and inserts rows into the messages table)
    - api/routes/onboarding.py lines 108-145 (load_conversation returns prior messages as {role, content} dicts in chronological order, scoped to conversation_id AND user_id)
    - api/routes/_sse.py (the assistant_sink parameter added in Task 1)
  </read_first>
  <action>
    After the onboarding stream finishes, persist the new user message (when one was sent) and the assistant reply so the next turn loads real history. Do this without breaking the streaming response to the client.

    Concretely, inside onboarding_start, modify the `_stream_with_metadata` async generator (lines 263-279):
    - Before the async-for over sse_generator, create a local list: `assistant_sink: list[str] = []`.
    - Pass `assistant_sink=assistant_sink` into the existing sse_generator(...) call (added in Task 1).
    - Keep yielding every chunk from sse_generator unchanged (the metadata frame first, then all streamed frames).
    - After the async-for loop completes (the stream is done), build the list of new turns to persist and call save_messages. The turns to persist are: (a) the incoming user message, only when body.message is a non-empty string — as {"role": "user", "content": body.message}; and (b) the assistant reply, only when assistant_sink is non-empty and assistant_sink[0] is a non-empty string — as {"role": "assistant", "content": assistant_sink[0]}. Persist via `await save_messages(conversation_id, user_id, new_turns)` only when conversation_id is not None and new_turns is non-empty. Wrap the save in a try/except that swallows exceptions (best-effort persistence must never break the already-completed stream), mirroring the existing best-effort try/except style used around create_conversation and load_conversation in this same function.

    Important ordering note for the FIRST turn: on the very first call there is no body.message (the frontend opens the stream on mount with an empty body), so only the assistant opener is persisted. On subsequent turns body.message carries the user's reply and both the user reply and the assistant response are persisted. This is exactly what makes load_conversation on the next turn return the real interview history instead of re-seeding, which stops the "What are your main fitness goals?" loop.

    Do NOT change how `messages` is assembled for sse_generator (the existing prior_turns + body.message logic stays). Do NOT add a second create_conversation call. Do NOT persist the hardcoded seed_messages content — only persist the genuine user message (body.message) and the assistant_sink reply, so the messages table reflects the true conversation.

    Resolves UAT GAP 6 (truth: onboarding interview advances without looping). Reason from UAT.md: save_messages is defined but never called, so every turn loads empty history and the agent repeats its opening question.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && grep -q "assistant_sink" api/routes/onboarding.py && grep -qE "await save_messages\(" api/routes/onboarding.py && .venv/bin/python -c "import ast; ast.parse(open('api/routes/onboarding.py').read()); print('parse ok')" && .venv/bin/pytest tests/api/test_onboarding.py -q</automated>
  </verify>
  <acceptance_criteria>
    - _stream_with_metadata creates an assistant_sink list and passes it to sse_generator
    - After the stream completes, save_messages is awaited with the new turns when conversation_id is set and there is at least one turn to persist
    - The incoming user message (body.message) is persisted only when it is a non-empty string
    - The assistant reply (assistant_sink[0]) is persisted only when it is non-empty
    - The save is wrapped in a best-effort try/except so a persistence failure does not break the completed stream
    - The hardcoded seed_messages opener text is not itself persisted as a user message
    - api/routes/onboarding.py parses without syntax error and existing test_onboarding.py tests still pass
  </acceptance_criteria>
  <done>Each onboarding turn persists the real user message and assistant reply, so the next turn loads genuine prior context and the interview advances past the fitness-goals question instead of looping.</done>
</task>

</tasks>

<verification>
- sse_generator exposes an assistant_sink that receives the full streamed assistant text without altering emitted frames
- onboarding_start calls save_messages after the stream with the user message and assistant reply
- Backend suites pass: .venv/bin/pytest tests/api/test_sse.py tests/api/test_onboarding.py -q
- Manual smoke (post-deploy, human): completing two onboarding turns no longer re-asks the same question
</verification>

<success_criteria>
- save_messages is invoked after each onboarding turn (UAT GAP 6 closed)
- Prior user and assistant turns are loaded on the next turn via load_conversation
- The onboarding interview advances through fields instead of looping (UI-01)
</success_criteria>

<artifacts_produced>
This gap-closure plan produces no new files. It modifies:
- api/routes/_sse.py (adds optional assistant_sink output parameter to sse_generator)
- api/routes/onboarding.py (calls save_messages after each stream with user + assistant turns)
New symbols: assistant_sink parameter on sse_generator.
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-12-SUMMARY.md` when done
</output>
