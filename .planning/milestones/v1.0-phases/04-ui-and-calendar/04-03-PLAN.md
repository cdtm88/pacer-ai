---
phase: 04-ui-and-calendar
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/index.html
  - frontend/components.json
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/router.tsx
  - frontend/src/vite-env.d.ts
autonomous: true
requirements: [UI-08, UI-10]
must_haves:
  truths:
    - "frontend/ is a working Vite + React 19 + TypeScript project that builds"
    - "Tailwind v4 is configured via @tailwindcss/vite and the design tokens from UI-SPEC are declared in @theme"
    - "shadcn/ui is initialized (components.json with CSS variables) and at least the button component installs"
    - "vite-plugin-pwa is configured with the PacerAI manifest and workbox navigateFallback"
    - "A React Router v7 createBrowserRouter skeleton renders placeholder routes for all screens"
    - "No pure black (#000000) appears in the token set; no dark: variant classes are used"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 @theme design tokens"
      contains: "--color-blue-6"
    - path: "frontend/vite.config.ts"
      provides: "Vite + Tailwind + PWA config"
      contains: "VitePWA"
    - path: "frontend/src/router.tsx"
      provides: "Route skeleton for all screens"
      contains: "createBrowserRouter"
    - path: "frontend/src/main.tsx"
      provides: "React root with providers"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/router.tsx"
      via: "RouterProvider router={router}"
      pattern: "RouterProvider"
    - from: "frontend/vite.config.ts"
      to: "frontend/src/index.css"
      via: "@tailwindcss/vite plugin processes @theme tokens"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Scaffold the entire React PWA frontend from scratch in a `frontend/` subdirectory: Vite 6 + React 19 + TypeScript + Tailwind v4 + shadcn/ui + vite-plugin-pwa + React Router v7, plus the design-token foundation and the routing skeleton. Zero frontend code exists today.

Purpose: Every subsequent frontend plan (auth shell, screens, calendar, PWA polish) depends on this scaffold existing with the design system wired in. The token foundation enforces UI-08 (design system) and UI-10 (light mode only, no pure black) from the very first commit.
Output: A buildable `frontend/` project with all dependencies installed, design tokens in `src/index.css`, PWA config in `vite.config.ts`, and a `createBrowserRouter` skeleton with placeholder components for every route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-RESEARCH.md
@.planning/phases/04-ui-and-calendar/04-UI-SPEC.md
@.planning/phases/04-ui-and-calendar/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TS project and install all dependencies</name>
  <read_first>
    - .planning/phases/04-ui-and-calendar/04-RESEARCH.md (Standard Stack versions table; Installation section lines 187-198; Recommended Project Structure)
    - .claude/CLAUDE.md (Technology Stack frontend table for exact library list)
  </read_first>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/index.html, frontend/vite-env.d.ts</files>
  <action>
    From the repo root, scaffold the project with `npm create vite@latest frontend -- --template react-ts`. Then in frontend/ install the runtime deps pinned to the RESEARCH.md Standard Stack versions: react@19.1.x, react-dom@19.1.x, react-router (v7, package name `react-router`), @tanstack/react-query@5.x, zustand@5.x, @supabase/supabase-js@2.x, sonner@2.x, recharts@3.x, lucide-react, plus dev/build deps tailwindcss@4.x, @tailwindcss/vite, vite-plugin-pwa@1.x, and the Vitest stack (vitest, @testing-library/react, @testing-library/jest-dom, jsdom). Set the frontend index.html title to "PacerAI" and add the Inter font via a Google Fonts <link> in the head (or document a self-hosted fallback). Add a `frontend/src/vite-env.d.ts` declaring the import.meta.env vars used later (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_URL) as string types on ImportMetaEnv. Ensure `npm run build` and `npm test` scripts exist in package.json (vite build; vitest).
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && test -f package.json && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; ['react','react-router','@tanstack/react-query','zustand','@supabase/supabase-js','sonner','recharts','lucide-react','tailwindcss','@tailwindcss/vite','vite-plugin-pwa','vitest'].forEach(k=>{if(!d[k])throw new Error('missing '+k)}); console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json exists with all listed dependencies present
    - frontend/index.html <title> is "PacerAI" and references the Inter font
    - frontend/src/vite-env.d.ts declares VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_URL
    - The dependency-check node command prints "deps ok"
  </acceptance_criteria>
  <done>Vite React-TS project scaffolded in frontend/ with all Standard Stack dependencies installed and env-var types declared.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4, design tokens, and vite-plugin-pwa</name>
  <read_first>
    - .planning/phases/04-ui-and-calendar/04-UI-SPEC.md (Color section @theme block lines 78-119; PWA Manifest Contract lines 540-561)
    - .planning/phases/04-ui-and-calendar/04-RESEARCH.md (Pattern 5 design tokens; Pattern 7 vite-plugin-pwa config; Pitfall 7 Tailwind v4 vs v3)
    - .planning/phases/04-ui-and-calendar/04-PATTERNS.md (frontend/src/index.css section; frontend/vite.config.ts section)
  </read_first>
  <files>frontend/vite.config.ts, frontend/src/index.css, frontend/components.json</files>
  <action>
    Configure frontend/vite.config.ts to register three plugins: `react()`, `tailwindcss()` (from @tailwindcss/vite), and `VitePWA({...})`. The VitePWA config must use registerType 'autoUpdate', includeAssets ['favicon.ico','apple-touch-icon.png'], the PacerAI manifest from UI-SPEC (name PacerAI, short_name PacerAI, theme_color #228BE6, background_color #F9F9FA, display standalone, start_url /, and the three icon entries apple-touch-icon 180x180, pwa-192x192, pwa-512x512), and a workbox block with navigateFallback '/index.html' and a NetworkFirst runtimeCaching rule for the session route (cacheName 'session-cache') so the during-session view can work offline (UI-09).

    Write frontend/src/index.css beginning with `@import "tailwindcss";` followed by the full `@theme` block from UI-SPEC Color section (per D-11 Inter font + blue scale + zone colors, and D-13 no-em-dash copy enforced in all UI copy): the blue scale blue-0..blue-9 (with --color-blue-6 #228BE6 and --color-blue-7 #1B73C0), neutrals (--color-ink #1A2230, --color-ink-2, --color-ink-3, --color-line, --color-line-2, --color-surface #FFFFFF, --color-bg #F9F9FA, --color-bg-2), semantic colors (--color-warm, --color-warm-soft, --color-amber, --color-good #2B8A5B, --color-warn, --color-bad), the five zone colors (--color-zone-recovery #2B8A5B, --color-zone-endurance #228BE6, --color-zone-tempo #F0A030, --color-zone-threshold #E8590C, --color-zone-vo2 #C92A2A), and --font-family-sans set to "Inter", ui-sans-serif, system-ui, sans-serif. Set the body/background base styles to use --color-bg and --color-ink. The literal #000000 must never appear in this file.

    Initialize shadcn/ui: run `npx shadcn@canary init` accepting the Tailwind v4 CSS-variables path so frontend/components.json is created with `"cssVariables": true`. Then install the button component (`npx shadcn@canary add button`) as a smoke test that the registry wiring works. If the interactive init cannot run non-TTY, create components.json manually per the shadcn v4 docs with cssVariables true, baseColor neutral, and the correct path aliases pointing at src/components/ui and src/lib/utils.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && grep -q "VitePWA" vite.config.ts && grep -q "@import \"tailwindcss\"" src/index.css && grep -q -- "--color-blue-6" src/index.css && grep -q "228BE6" src/index.css && test -f components.json && ! grep -q "#000000" src/index.css && echo "config ok"</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts registers react(), tailwindcss(), and VitePWA with the PacerAI manifest and navigateFallback '/index.html'
    - src/index.css starts with `@import "tailwindcss";` and declares all five zone tokens plus --color-blue-6 #228BE6 and --color-blue-7 #1B73C0
    - The literal `#000000` does NOT appear in src/index.css (UI-10 / D-12 enforcement)
    - components.json exists with cssVariables true
    - The verify command prints "config ok"
  </acceptance_criteria>
  <done>Tailwind v4 + design tokens + PWA manifest configured; shadcn/ui initialized with CSS variables; no pure black token present.</done>
</task>

<task type="auto">
  <name>Task 3: Build the router skeleton and React root with providers</name>
  <read_first>
    - .planning/phases/04-ui-and-calendar/04-RESEARCH.md (Pattern 2 React Router v7 layout with auth gate; Recommended Project Structure routes list lines 253-268)
    - .planning/phases/04-ui-and-calendar/04-UI-SPEC.md (Screen-by-Screen Contract route paths: /login, /onboarding, /, /agenda, /history, /chat, /session, /settings)
    - .planning/phases/04-ui-and-calendar/04-PATTERNS.md (frontend/src/main.tsx and router.tsx sections)
  </read_first>
  <files>frontend/src/main.tsx, frontend/src/router.tsx</files>
  <action>
    Create frontend/src/router.tsx exporting `router = createBrowserRouter([...])` (from react-router) with the full route tree but placeholder leaf components (each a function returning a div with the screen name, to be replaced by later plans). Routes: `/login`, `/onboarding` (no nav chrome), and a root `/` branch that will later host AuthGate + FirstRunGate + AppLayout, with children: index (Today), `agenda`, `history`, `chat`, `session`, `settings`. For this scaffold, define the AuthGate/FirstRunGate/AppLayout as thin pass-through components rendering `<Outlet />` (real gating logic lands in plan 04-04). Export placeholder screen components named LoginScreen, OnboardingScreen, TodayScreen, AgendaScreen, HistoryScreen, ChatScreen, DuringSessionScreen, SettingsScreen from a temporary location (inline in router.tsx is acceptable for the skeleton; later plans replace them with imports from src/screens/).

    Create frontend/src/main.tsx as the React root: import './index.css', create a QueryClient, and render `<QueryClientProvider><RouterProvider router={router} /><Toaster /></QueryClientProvider>` into the #root element using React 19 createRoot. Import Toaster from sonner. The provider order must be QueryClientProvider outermost so all routes have query + toast access.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && grep -q "createBrowserRouter" src/router.tsx && grep -q "RouterProvider" src/main.tsx && grep -q "QueryClientProvider" src/main.tsx && grep -q "Toaster" src/main.tsx && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/router.tsx exports `router` built with createBrowserRouter and declares routes for /login, /onboarding, /, /agenda, /history, /chat, /session, /settings
    - src/main.tsx renders RouterProvider inside QueryClientProvider and mounts a sonner Toaster
    - `npm run build` completes successfully (exit 0) producing a dist/ bundle
  </acceptance_criteria>
  <done>The frontend builds; createBrowserRouter skeleton covers every screen route; React root wires QueryClient, Router, and Toaster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → bundle | Frontend env vars (VITE_*) are embedded in the client bundle; only public values (anon key, API URL) belong here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | VITE_* env vars in client bundle | mitigate | Only the Supabase anon key and public API URL are exposed via VITE_; the service-role key and JWT secret never enter frontend/ |
| T-04-09 | Tampering | npm package installs (Vite/React/shadcn) | mitigate | All packages are RESEARCH.md Package Legitimacy Audit "Approved" (official orgs, years of history); no [SLOP]; no [ASSUMED] frontend blocker |
| T-04-SC | Tampering | npx shadcn@canary / npm installs | mitigate | slopcheck on install; shadcn official registry only (no third-party registries per UI-SPEC Registry Safety) |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- `grep -- "--color-blue-6" frontend/src/index.css` matches and `grep "#000000" frontend/src/index.css` does not
- components.json exists with cssVariables true
- router.tsx and main.tsx contain createBrowserRouter and RouterProvider
</verification>

<success_criteria>
- frontend/ is a buildable Vite + React 19 + TS + Tailwind v4 + shadcn/ui project
- Design tokens match UI-SPEC exactly; no pure black; light mode only
- PWA manifest + workbox configured
- Router skeleton renders all routes; React root wires providers
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 04-03)

- **Project:** `frontend/` (Vite + React 19 + TypeScript)
- **Config files:** `frontend/vite.config.ts`, `frontend/tsconfig.json`, `frontend/components.json`, `frontend/package.json`
- **Design tokens:** `frontend/src/index.css` (@theme: blue scale, neutrals, semantic, 5 zone colors, Inter font)
- **Routing:** `frontend/src/router.tsx` exporting `router` (createBrowserRouter), placeholder gates AuthGate/FirstRunGate/AppLayout
- **Root:** `frontend/src/main.tsx` (QueryClientProvider, RouterProvider, sonner Toaster)
- **Env types:** `frontend/src/vite-env.d.ts` (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_URL)
- **shadcn:** button component installed
- **PWA:** manifest + workbox navigateFallback + session-cache runtime caching
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-03-SUMMARY.md` when done
</output>
