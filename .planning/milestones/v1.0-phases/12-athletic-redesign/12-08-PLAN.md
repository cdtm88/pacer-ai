---
phase: 12-athletic-redesign
plan: 08
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - frontend/src/screens/SettingsScreen.tsx
  - frontend/src/tests/SettingsScreen.test.tsx
  - frontend/src/components/session/ZoneChip.tsx
  - frontend/src/tests/rideChart.test.tsx
  - frontend/src/screens/OnboardingScreen.tsx
  - frontend/src/screens/ChatScreen.tsx
autonomous: true
requirements: [D-12, D-8]
must_haves:
  truths:
    - "Settings is card-grouped with real Button instances and on-token colors (resend = brand link, sign out = bad/destructive)"
    - "Settings renders without throwing and exposes a Sign out button (first automated coverage for the screen)"
    - "ZoneChip reads the single lib/zones map (keeps its ZoneType export so RideChart is unbroken); the vo2 label unifies to 'VO2 Max' with its dependent test updated in the same commit"
    - "Onboarding and Chat use the shared PromptChip; both local copies are deleted"
  artifacts:
    - "SettingsScreen.tsx card redesign + SettingsScreen.test.tsx smoke test"
    - "ZoneChip.tsx importing from lib/zones (local ZONE_VAR/ZONE_LABEL removed)"
  key_links:
    - "SettingsScreen uses card.tsx (from 12-03) and Button variants link/destructive on the fixed tokens (from 12-01)"
    - "ZoneChip keeps `export type ZoneType = ZoneKey` so RideChart's import stays valid"
    - "OnboardingScreen/ChatScreen import PromptChip from @/components/ui/PromptChip (from 12-03)"
---

<objective>
Slice E, part 2: secondary surfaces (D-12, D-8). Redesign the weakest screen (Settings) into card-grouped sections with real `<Button>`s and on-token colors, and add its first smoke test. Migrate `ZoneChip` onto the single `lib/zones` map (preserving its `ZoneType` export for the live RideChart consumer), and migrate `OnboardingScreen`/`ChatScreen` onto the shared `PromptChip`, deleting both local copies.

Purpose: Closes out the component-unification decision (D-8) and fixes the last off-token colors; Settings goes from bare divs + hardcoded hex to the shared card/button system.
Output: Redesigned `SettingsScreen` + smoke test; `ZoneChip` on `lib/zones` with its coupled test update; Onboarding/Chat on shared `PromptChip`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-CONTEXT.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
@.planning/phases/12-athletic-redesign/12-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- New file `frontend/src/tests/SettingsScreen.test.tsx`
- `SettingsScreen.tsx` Card-based sections + `<Button variant="link">` / `<Button variant="destructive">`
- `ZoneChip.tsx` retains `export type ZoneType` (now aliased to `ZoneKey`) while sourcing color/label from `@/lib/zones`
- Updated vo2 label assertion in `rideChart.test.tsx` (VO2max -> VO2 Max)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Settings card redesign + smoke test (D-12, D-8)</name>
  <files>frontend/src/screens/SettingsScreen.tsx, frontend/src/tests/SettingsScreen.test.tsx</files>
  <read_first>
    - frontend/src/screens/SettingsScreen.tsx (FULL — sections at 65/98/129, manual dividers at 95/126, magic-link button 115-120 with color var(--color-accent), sign-out 133-141 with var(--color-destructive, #dc2626), auth calls getSession/signOut unchanged)
    - frontend/src/components/ui/card.tsx (Card/CardHeader/CardTitle/CardContent from 12-03)
    - frontend/src/components/ui/button.tsx (variant="link" / variant="destructive")
    - frontend/src/tests/today.test.tsx (Wrapper pattern: QueryClient + MemoryRouter + TooltipProvider — reuse for the Settings smoke test)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Foundation Fixes 4; Screen-by-Screen E SettingsScreen)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (SettingsScreen card redesign, lines 334-355)
  </read_first>
  <action>
    In `SettingsScreen.tsx`: convert each of the three `<section>` blocks (Training ~65, Profile ~98, Account ~129) into `<Card><CardHeader><CardTitle>...</CardTitle></CardHeader><CardContent>...</CardContent></Card>`, removing the two manual divider `<div style={{ height: 1, background: 'var(--color-line)' }}>` (lines 95, 126) — card boundaries replace them. Do NOT change the auth logic (`supabase.auth.getSession()` / `signOut()` calls and the resend-magic-link handler stay exactly as-is; only their surrounding markup and the button elements change).

    Fix the two off-token colors (Foundation Fixes 4): the "Re-send magic link" control becomes `<Button variant="link">` colored `var(--color-brand)` (it is an interactive link, not destructive — resolves the D-8 ambiguity per UI-SPEC: resend = brand, sign out = bad). The "Sign out" control becomes `<Button variant="destructive">` (now correctly token-mapped by 12-01, so drop the hardcoded `#dc2626` fallback and the `var(--color-destructive, #dc2626)` inline styles entirely). Keep the exact visible button text ("Re-send magic link", "Sign out").

    Create `frontend/src/tests/SettingsScreen.test.tsx` (Wave 0 gap from 12-VALIDATION.md): a minimal smoke test that renders `SettingsScreen` inside the same Wrapper pattern used in `today.test.tsx` (QueryClient + MemoryRouter + TooltipProvider), mocks the supabase auth client as needed so it does not throw, and asserts (a) the component renders without throwing and (b) a button with accessible name matching /sign out/i is present.
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "#dc2626" src/screens/SettingsScreen.tsx && ! grep -q "var(--color-accent)" src/screens/SettingsScreen.tsx && grep -q "CardHeader" src/screens/SettingsScreen.tsx && npx vitest run src/tests/SettingsScreen.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three settings sections is a `<Card>` with `CardHeader`/`CardTitle`/`CardContent`; the two manual 1px divider divs are gone.
    - "Re-send magic link" is a `<Button variant="link">` with `var(--color-brand)`; "Sign out" is a `<Button variant="destructive">`; no `#dc2626` and no `var(--color-accent)` remain in the file.
    - The supabase auth calls (`getSession`, `signOut`, resend handler) are unchanged.
    - `frontend/src/tests/SettingsScreen.test.tsx` exists and passes: renders without throwing and finds a /sign out/i button.
  </acceptance_criteria>
  <done>Settings is card-grouped with real Buttons and on-token colors; first automated smoke coverage added and green.</done>
</task>

<task type="auto">
  <name>Task 2: ZoneChip migration to lib/zones + coupled rideChart test update (D-8)</name>
  <files>frontend/src/components/session/ZoneChip.tsx, frontend/src/tests/rideChart.test.tsx</files>
  <read_first>
    - frontend/src/components/session/ZoneChip.tsx (FULL — export type ZoneType, local ZONE_VAR 7-13 + ZONE_LABEL 15-21 with vo2 'VO2max', ZoneChip({zone,label}) badge JSX with color-mix)
    - frontend/src/components/rides/RideChart.tsx (line 12 imports { ZoneChip, type ZoneType }; renders <ZoneChip zone={zone}/> at 233 with no label override)
    - frontend/src/tests/rideChart.test.tsx (line 85 mock name 'VO2max'; line 94 asserts getByText('VO2max') — this targets the ZoneChip label)
    - frontend/src/lib/zones.ts (zoneColor, zoneLabel, ZoneKey — canonical vo2 label 'VO2 Max')
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (ZoneChip migration, lines 75-113)
  </read_first>
  <action>
    In `ZoneChip.tsx`: delete the local `ZONE_VAR` (7-13) and `ZONE_LABEL` (15-21) maps. Import `zoneColor`, `zoneLabel`, and `type ZoneKey` from `@/lib/zones`. Keep the public export `export type ZoneType = ZoneKey` (aliasing) so `RideChart.tsx`'s `import { ZoneChip, type ZoneType }` keeps compiling with no change to RideChart. In the badge JSX, source the color from `zoneColor(zone)` (unchanged color-mix 15% background + zone-color text treatment) and the fallback label from `zoneLabel(zone)` (still honoring an explicit `label` prop override when provided). This unifies the vo2 label from 'VO2max' to the canonical 'VO2 Max'.

    Because RideChart renders `<ZoneChip zone={zone} />` without a label override, its vo2 chip now displays 'VO2 Max'. Update `rideChart.test.tsx` in the SAME task/commit: change the assertion at line 94 from `getByText('VO2max')` to `getByText('VO2 Max')` (leave the mock `hr_zone_distribution` `name` field as-is; the assertion targets the ZoneChip label, not `row.name`). Do not touch RideChart.tsx.
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "ZONE_VAR" src/components/session/ZoneChip.tsx && grep -q "export type ZoneType = ZoneKey" src/components/session/ZoneChip.tsx && grep -q "@/lib/zones" src/components/session/ZoneChip.tsx && npx vitest run src/tests/rideChart.test.tsx 2>&1 | tail -8 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ZoneChip.tsx` defines no local `ZONE_VAR`/`ZONE_LABEL`; it imports `zoneColor`/`zoneLabel`/`ZoneKey` from `@/lib/zones` and still exports `type ZoneType` (aliased to `ZoneKey`).
    - `RideChart.tsx` is unchanged and still compiles (its `ZoneChip`/`ZoneType` import resolves).
    - `rideChart.test.tsx` asserts `getByText('VO2 Max')` and passes; the update lands in the same commit as the ZoneChip change.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>ZoneChip on the single zone map; RideChart unbroken; vo2 label unified with its dependent test updated together.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Onboarding + Chat to the shared PromptChip (D-8)</name>
  <files>frontend/src/screens/OnboardingScreen.tsx, frontend/src/screens/ChatScreen.tsx</files>
  <read_first>
    - frontend/src/screens/OnboardingScreen.tsx (local PromptChip def lines 110-147; call site line 673)
    - frontend/src/screens/ChatScreen.tsx (local PromptChip def lines 83-119; call site line 385)
    - frontend/src/components/ui/PromptChip.tsx (shared component from 12-03)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (shared PromptChip, lines 224-266)
  </read_first>
  <action>
    In both `OnboardingScreen.tsx` and `ChatScreen.tsx`: delete the local `PromptChip` function definition and its explanatory comment, and add `import { PromptChip } from '@/components/ui/PromptChip'`. Leave every `<PromptChip ... />` call site (same props: `label`, `onClick`, `disabled`) exactly as-is — the shared component is byte-identical, so no visual or behavioral change. Remove any now-unused `useState` import only if it becomes unused after deleting the local component (verify it is not used elsewhere in the file before removing).
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "function PromptChip" src/screens/OnboardingScreen.tsx && ! grep -q "function PromptChip" src/screens/ChatScreen.tsx && grep -q "@/components/ui/PromptChip" src/screens/OnboardingScreen.tsx && grep -q "@/components/ui/PromptChip" src/screens/ChatScreen.tsx && npx tsc --noEmit 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Neither `OnboardingScreen.tsx` nor `ChatScreen.tsx` defines a local `PromptChip`; both import it from `@/components/ui/PromptChip`.
    - All existing `<PromptChip>` call sites render unchanged (same props).
    - `npx tsc --noEmit` clean; `npm test -- --run` full suite green (onboarding + chat tests included).
  </acceptance_criteria>
  <done>Onboarding and Chat use the single shared PromptChip; both local copies removed; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Auth (existing) -> SettingsScreen | `SettingsScreen` reads the authenticated session (`getSession`) and triggers `signOut` / resend-magic-link. This plan changes ONLY the surrounding card/button presentation; the auth calls and their handlers are untouched (RESEARCH.md Security Domain V3 Session Management: no change). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08 | Elevation of Privilege / Session Management | SettingsScreen auth actions | low | accept | Sign-out / resend / getSession logic is preserved verbatim; only markup + button variants change. New smoke test asserts the screen renders and Sign out is present, guarding against an accidental control removal. RESEARCH.md Security Domain: no auth-flow change, ASVS L1, no new attack surface. |
| T-12-08b | Information Disclosure | ZoneChip / PromptChip presentation | low | accept | Presentation-layer-only; no new inputs/auth/persistence/endpoints. Carries forward RESEARCH.md Security Domain conclusion. |

No high/critical threats (security_block_on: high). No package installs — no supply-chain gate (`card` was added registry-side in 12-03).
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/SettingsScreen.test.tsx src/tests/rideChart.test.tsx` green.
- `cd frontend && npx tsc --noEmit` clean; `cd frontend && npm test -- --run` full suite green.
</verification>

<success_criteria>
Settings is card-grouped with real `<Button>`s and on-token colors (resend = brand link, sign out = destructive) plus a passing smoke test; `ZoneChip` sources from `@/lib/zones` while preserving RideChart's `ZoneType` import (vo2 label unified, dependent test updated together); Onboarding + Chat use the shared `PromptChip`; full suite green.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-08-SUMMARY.md` when done.
</output>
