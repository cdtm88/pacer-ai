---
phase: 12-athletic-redesign
plan: 07
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - frontend/src/lib/zones.ts
  - frontend/src/screens/LoginScreen.tsx
  - frontend/src/components/AppLayout.tsx
  - frontend/src/components/nav/BottomTabBar.tsx
  - frontend/src/components/nav/DesktopSidebar.tsx
autonomous: true
requirements: [D-10, D-8]
must_haves:
  truths:
    - "Screen titles render at 28px display weight with the date as an eyebrow ABOVE the title"
    - "Bottom tab labels are 11px/600 with a filled-pill active state (no dot)"
    - "The desktop sidebar active state is the same filled pill (no 3px left stripe), including the Settings row"
    - "The zone-spectrum wordmark is the app-wide brand mark in the sidebar; ZONE_SPECTRUM lives in lib/zones.ts"
    - "AppLayout's h-dvh height-chain wrapping divs are untouched"
  artifacts:
    - "ZONE_SPECTRUM exported from lib/zones.ts"
    - "AppLayout header 28px/600 title + date eyebrow"
    - "BottomTabBar + DesktopSidebar filled-pill active state"
  key_links:
    - "LoginScreen + DesktopSidebar import ZONE_SPECTRUM from lib/zones.ts (page-local const hoisted)"
    - "AppLayout header edits scoped to <header> only; lines 32 and 39 (h-dvh divs) frozen"
---

<objective>
Slice E, part 1: shell chrome (D-10). Raise `AppLayout` screen titles to 28px display weight with the date moved above as an eyebrow; convert both navs to a filled-pill active state (bottom tab 11px/600, desktop sidebar replacing its 3px left stripe); and adopt the zone-spectrum wordmark as the app-wide brand mark in the sidebar by hoisting `ZONE_SPECTRUM` from `LoginScreen` into `lib/zones.ts` (D-8 dedup). The `AppLayout` height-chain wrapping divs stay frozen (Pitfall 5).

Purpose: The persistent shell is seen on every screen; the pill nav and display titles are what make the whole app read as one athletic system.
Output: `ZONE_SPECTRUM` in `lib/zones.ts`; updated `LoginScreen`, `AppLayout` header, `BottomTabBar`, `DesktopSidebar`. `AppLayout.test.tsx` stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-CONTEXT.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
@.planning/phases/12-athletic-redesign/12-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- New `ZONE_SPECTRUM` constant exported from `frontend/src/lib/zones.ts`
- `AppLayout.tsx` header: 28px/600 `<h1>` + date eyebrow above title
- `BottomTabBar.tsx` / `DesktopSidebar.tsx` filled-pill active-state JSX
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Hoist ZONE_SPECTRUM to lib/zones.ts and adopt AppLayout display title + eyebrow</name>
  <files>frontend/src/lib/zones.ts, frontend/src/screens/LoginScreen.tsx, frontend/src/components/AppLayout.tsx</files>
  <read_first>
    - frontend/src/lib/zones.ts (append target for ZONE_SPECTRUM)
    - frontend/src/screens/LoginScreen.tsx (ZONE_SPECTRUM const lines 10-11; BrandMark lines 15-30 using text-4xl + gradient bar)
    - frontend/src/components/AppLayout.tsx (wrapping divs at 32 and 39 FROZEN; header lines 41-72; h1 at 50-52 fontSize:20/fontWeight:700; date <p> at 57 rendered BELOW the title)
    - frontend/src/tests/AppLayout.test.tsx (asserts both wrappers use h-dvh, neither uses min-h-screen)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Typography Display role; Screen-by-Screen E AppLayout header)
  </read_first>
  <action>
    In `frontend/src/lib/zones.ts`: add `export const ZONE_SPECTRUM = 'linear-gradient(90deg, var(--color-zone-recovery), var(--color-zone-endurance), var(--color-zone-tempo), var(--color-zone-threshold), var(--color-zone-vo2))'` (moved verbatim from LoginScreen). In `LoginScreen.tsx`: delete the local `ZONE_SPECTRUM` const (lines 10-11) and import it from `@/lib/zones`; `BrandMark` otherwise unchanged (it still renders the 36px "Pace" wordmark + gradient bar).

    In `AppLayout.tsx` header ONLY (lines 41-72): change the `<h1>` style from `fontSize: 20, fontWeight: 700` to `fontSize: 28, fontWeight: 600` (Display role; keep `letterSpacing`/`lineHeight` reasonable per the 1.15 line-height guidance). Move the per-route date `<p>` (line 57) so it renders ABOVE the `<h1>` as an eyebrow: 12px, fontWeight 600, `var(--color-ink-3)`, sentence case (it already renders a date string). Do NOT touch the two wrapping `<div>`s at lines 32 and 39 (the `h-dvh` / `md:ml-60` height-chain fix from Phase 9) — Pitfall 5.
  </action>
  <verify>
    <automated>cd frontend && grep -q "ZONE_SPECTRUM" src/lib/zones.ts && grep -q "from '@/lib/zones'" src/screens/LoginScreen.tsx && grep -q "fontSize: 28" src/components/AppLayout.tsx && npx vitest run src/tests/AppLayout.test.tsx 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/zones.ts` exports `ZONE_SPECTRUM`; `LoginScreen.tsx` imports it and no longer defines it locally.
    - `AppLayout.tsx` `<h1>` uses `fontSize: 28, fontWeight: 600`; the date renders above the title as a 12px/600 `--color-ink-3` eyebrow.
    - The two wrapping `<div>`s (lines 32, 39) still carry `h-dvh` / `md:ml-60`; `npx vitest run src/tests/AppLayout.test.tsx` is green.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>ZONE_SPECTRUM hoisted; display-weight titles with date eyebrow; height-chain divs untouched; AppLayout test green.</done>
</task>

<task type="auto">
  <name>Task 2: Filled-pill active state for BottomTabBar and DesktopSidebar + sidebar brand mark</name>
  <files>frontend/src/components/nav/BottomTabBar.tsx, frontend/src/components/nav/DesktopSidebar.tsx</files>
  <read_first>
    - frontend/src/components/nav/BottomTabBar.tsx (label fontSize:10/fontWeight:500 at line 47; active dot lines 36-46; isActive color at 30)
    - frontend/src/components/nav/DesktopSidebar.tsx (logotype "PacerAI" text at line 27; active borderLeft 3px stripe at lines 42 and 62; Settings row 53-68)
    - frontend/src/lib/zones.ts (ZONE_SPECTRUM to import for the sidebar brand mark)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen E: BottomTabBar + DesktopSidebar pill specs; sidebar wordmark scale)
  </read_first>
  <action>
    In `BottomTabBar.tsx` (D-10): bump the label to `fontSize: 11, fontWeight: 600` (from 10/500). Remove the small active dot (lines 36-46) entirely. Replace the active state with a filled pill around the icon+label group: `backgroundColor: color-mix(in srgb, var(--color-brand) 12%, transparent)`, `borderRadius: 999px`, padding `6px 14px`, icon and text color `var(--color-brand)`; inactive stays `var(--color-ink-3)` with no pill. Keep tab hit targets >= 44px.

    In `DesktopSidebar.tsx` (D-10): replace the active `borderLeft: 3px solid var(--color-brand)` indicator (lines 42 and 62) with the SAME filled-pill treatment as the bottom tab: on the active nav row use `borderRadius: 999px` (instead of the current `rounded-md`), `backgroundColor: color-mix(in srgb, var(--color-brand) 12%, transparent)`, no left border, text/icon `var(--color-brand)`. Apply identically to the Settings row at the bottom (lines 53-68). Replace the plain "PacerAI" logotype text (line 27) with the zone-spectrum brand mark scaled for the sidebar: a "Pace" wordmark at ~20px (down from Login's 36px) plus the thin `ZONE_SPECTRUM` gradient bar beneath it, importing `ZONE_SPECTRUM` from `@/lib/zones` (do not redefine it). Scope the brand-mark adoption to the sidebar only (the mobile header keeps contextual page titles).
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "3px solid var(--color-brand)" src/components/nav/DesktopSidebar.tsx && grep -q "ZONE_SPECTRUM" src/components/nav/DesktopSidebar.tsx && grep -q "fontSize: 11" src/components/nav/BottomTabBar.tsx && grep -q "color-mix(in srgb, var(--color-brand) 12%" src/components/nav/BottomTabBar.tsx && npx tsc --noEmit 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `BottomTabBar` labels are `fontSize: 11, fontWeight: 600`; the active state is a `color-mix(... --color-brand 12% ...)` `999px` pill with `--color-brand` icon/text; no dot indicator remains.
    - `DesktopSidebar` has no `3px solid var(--color-brand)` left stripe; active nav rows AND the Settings row use the same filled pill (999px, 12% brand mix, no left border).
    - `DesktopSidebar` renders the zone-spectrum brand mark (imports `ZONE_SPECTRUM` from `@/lib/zones`) in place of the plain "PacerAI" text.
    - `npx tsc --noEmit` clean; `npm test -- --run` full suite green.
  </acceptance_criteria>
  <done>Both navs use the filled-pill active state; sidebar shows the app-wide brand mark; 11px/600 tab labels; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Shell chrome + `LoginScreen` visual treatment only. `LoginScreen.tsx`'s auth form fields and submit logic (lines 45-108) are untouched — only the `BrandMark`/`ZONE_SPECTRUM` presentation and its reuse in the sidebar change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Spoofing / Authentication | LoginScreen BrandMark region | low | accept | Only the visual brand-mark treatment is touched; the auth form fields, submit handler, and magic-link flow are unchanged (RESEARCH.md Security Domain: V2 Authentication does not apply). ASVS L1, no new attack surface. |
| T-12-07b | Tampering | AppLayout height-chain divs | low | mitigate | Header edits are scoped to `<header>`; the frozen `h-dvh` wrapping divs are guarded by `AppLayout.test.tsx` (run in Task 1) so the Phase-9 iOS height fix cannot regress. |

No high/critical threats (security_block_on: high). No package installs — no supply-chain gate.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/AppLayout.test.tsx` green (height-chain preserved).
- `cd frontend && npx tsc --noEmit` clean; `cd frontend && npm test -- --run` full suite green.
</verification>

<success_criteria>
Display-weight (28px/600) screen titles with a date eyebrow; 11px/600 bottom-tab labels with filled-pill active state; desktop sidebar filled-pill active state (incl. Settings) with the zone-spectrum brand mark; `ZONE_SPECTRUM` hoisted to `lib/zones.ts`; `AppLayout` height-chain divs untouched; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-07-SUMMARY.md` when done.
</output>
