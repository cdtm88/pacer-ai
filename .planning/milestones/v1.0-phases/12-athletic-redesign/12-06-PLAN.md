---
phase: 12-athletic-redesign
plan: 06
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - frontend/src/components/progress/WeeklyLoadChart.tsx
  - frontend/src/components/history/RideRow.tsx
  - frontend/src/screens/AgendaScreen.tsx
autonomous: true
requirements: [D-9, D-8, D-7]
must_haves:
  truths:
    - "Weekly load history bars are neutral gray with only the current ISO week in brand blue"
    - "RideRow's planned-vs-actual HTML table is replaced by paired horizontal bars + the existing ComplianceChip"
    - "Agenda row zone dots become short zone-colored intensity bars, matching the Today-hub treatment"
    - "AgendaScreen's local zone map duplicate is removed in favor of the single lib/zones source"
  artifacts:
    - "WeeklyLoadChart.tsx two-tone current-week highlight"
    - "RideRow.tsx paired-bar planned/actual block"
    - "AgendaScreen.tsx mini zone bars + single zone import"
  key_links:
    - "WeeklyLoadChart reuses the already-imported weekStartOf() to detect the current week"
    - "RideRow paired bars reuse the in-file ComplianceChip and its >=90 good/warn threshold"
    - "AgendaScreen row bar uses zoneColor() from lib/zones (local ZONE_VAR deleted)"
---

<objective>
Slice D: Progress + Agenda polish (D-9), on the consolidated zone system (D-7/D-8). Replace `WeeklyLoadChart`'s jump-detection amber coloring with a neutral-gray history plus a current-week brand-blue highlight; replace `RideRow`'s HTML planned-vs-actual table with paired horizontal bars and the existing `ComplianceChip`; convert `AgendaScreen`'s row zone dots to mini zone bars and delete its local zone-map duplicate.

Purpose: The history and agenda surfaces are where the redesign's zone-color-as-data-encoding language reads most clearly; these three edits remove the last off-pattern chart/table treatments.
Output: Updated `WeeklyLoadChart.tsx`, `RideRow.tsx`, `AgendaScreen.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-CONTEXT.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- Removal of `JUMP_FACTOR` and jump-detection `<Cell>` coloring in `WeeklyLoadChart.tsx`; new current-week two-tone fill
- Replacement of the `<table>` planned-vs-actual block in `RideRow.tsx` with a paired-bar JSX block
- Removal of the local `ZONE_VAR`/`isValidZone` in `AgendaScreen.tsx`; new mini zone-bar JSX
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: WeeklyLoadChart two-tone current-week highlight (D-9)</name>
  <files>frontend/src/components/progress/WeeklyLoadChart.tsx</files>
  <read_first>
    - frontend/src/components/progress/WeeklyLoadChart.tsx (FULL — JUMP_FACTOR line 23, per-bar Cell coloring 116-125, weekStartOf import line 13 already used at line 45, ReferenceLine 107-114)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen D: WeeklyLoadChart spec)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (WeeklyLoadChart replacement pattern, lines 305-320)
  </read_first>
  <action>
    Remove the `JUMP_FACTOR = 1.5` constant (line 23) and the jump-detection per-`<Cell>` coloring (lines 116-125, currently `fill={jump ? 'var(--color-zone-tempo)' : 'var(--color-zone-endurance)'}`). Replace with a two-tone scheme keyed on whether each bucket is the current ISO week: compute `isCurrentWeek` by comparing each bucket's week-start against `weekStartOf(new Date())` (the `weekStartOf` helper is already imported from `./week` at line 13 and already used at line 45 to build the 8-week bucket range — reuse it, add no new import). Fill non-current-week bars with `var(--color-ink-3)` (neutral gray) and the current-week bar with `var(--color-brand)`. Leave the `ReferenceLine` average marker (lines 107-114) exactly as-is.
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "JUMP_FACTOR" src/components/progress/WeeklyLoadChart.tsx && grep -q "var(--color-brand)" src/components/progress/WeeklyLoadChart.tsx && grep -q "weekStartOf(new Date" src/components/progress/WeeklyLoadChart.tsx && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `JUMP_FACTOR` no longer appears in `WeeklyLoadChart.tsx`; no `jump ?` cell coloring remains.
    - Each bar's fill is `var(--color-brand)` when its bucket equals `weekStartOf(new Date())`, else `var(--color-ink-3)`.
    - The `ReferenceLine` average marker is unchanged.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Weekly load history is neutral gray with the current week highlighted in brand blue.</done>
</task>

<task type="auto">
  <name>Task 2: RideRow paired-bar planned/actual block (D-9)</name>
  <files>frontend/src/components/history/RideRow.tsx</files>
  <read_first>
    - frontend/src/components/history/RideRow.tsx (FULL — in-file ComplianceChip def lines 19-46 with threshold pct >= 90 good else warn; the <table> block lines 245-314 with hardcoded "Planned: 100%")
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen D: RideRow paired-bar concrete spec)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (RideRow replacement spec, lines 324-330)
  </read_first>
  <action>
    Replace the entire `<table>`/`<thead>`/`<tbody>` block (lines 245-314, rendered only when `ride.compliance_pct != null`) with paired horizontal bars plus the existing in-file `ComplianceChip`. Concrete spec: two stacked bars, each 8px tall, `borderRadius: 4`. "Planned" is the track (fill `var(--color-line)`, width 100%, label "Planned" left-aligned above). "Actual" is the filled bar, width `min(150, compliance_pct)%` of the track (cap the visual width at 150% so over-target rides do not blow out the layout), color `var(--color-good)` when `compliance_pct >= 90` else `var(--color-warn)` (same threshold `ComplianceChip` already uses at its line 34). Render `<ComplianceChip pct={ride.compliance_pct} />` beside/below the bars. Remove all `<table>` markup entirely; do not introduce a new compliance-indicator component (reuse `ComplianceChip`).
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "<table" src/components/history/RideRow.tsx && grep -q "min(150" src/components/history/RideRow.tsx && grep -q "ComplianceChip" src/components/history/RideRow.tsx && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/tests/history.test.tsx 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - No `<table>`/`<thead>`/`<tbody>` markup remains in `RideRow.tsx`.
    - A Planned track (100% width, `--color-line`) and an Actual bar (width `min(150, compliance_pct)%`, `--color-good` when `>= 90` else `--color-warn`, 8px tall, 4px radius) render when `compliance_pct != null`.
    - `ComplianceChip` is still used (not duplicated by a new component).
    - `npx tsc --noEmit` clean; `npx vitest run src/tests/history.test.tsx` green.
  </acceptance_criteria>
  <done>RideRow shows paired planned/actual bars + ComplianceChip; the HTML table is gone.</done>
</task>

<task type="auto">
  <name>Task 3: AgendaScreen mini zone bars + zone-map dedup (D-9, D-8)</name>
  <files>frontend/src/screens/AgendaScreen.tsx</files>
  <read_first>
    - frontend/src/screens/AgendaScreen.tsx (FULL — line 12 imports sessionTypeLabel/ZONE_META/ZoneKey from @/lib/format; local ZONE_VAR/isValidZone lines 14-22; legend at 193/196 uses ZONE_META; row dot at ~280-288 uses ZONE_VAR)
    - frontend/src/lib/zones.ts (zoneColor / ZONE_META to import)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen D: Agenda mini zone bar 24x4px, 2px radius)
  </read_first>
  <action>
    In `AgendaScreen.tsx`: delete the local `ZONE_VAR` map and `isValidZone` type-guard (lines 14-22). Keep importing `sessionTypeLabel` from `@/lib/format` (it stays in format.ts), and import `ZONE_META`, `zoneColor`, and `type ZoneKey` from `@/lib/zones` (the legend at lines 193/196 keeps using `ZONE_META[z].color`/`.label`). Replace the per-row 12px zone dot (lines ~280-288) with a mini zone-colored duration bar matching the Today-hub treatment: `width: 24, height: 4, borderRadius: 2, backgroundColor: zoneColor(zoneType)`. This gives one consistent mini-bar treatment across Today and Agenda.
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "ZONE_VAR" src/screens/AgendaScreen.tsx && grep -q "@/lib/zones" src/screens/AgendaScreen.tsx && npx tsc --noEmit 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `AgendaScreen.tsx` no longer defines a local `ZONE_VAR`/`isValidZone`; it imports the zone map from `@/lib/zones` and `sessionTypeLabel` from `@/lib/format`.
    - The per-row indicator is a 24x4px, 2px-radius zone-colored bar (no 12px round dot remains); the intensity legend still renders from `ZONE_META`.
    - `npx tsc --noEmit` clean; `npm test -- --run` full suite green.
  </acceptance_criteria>
  <done>Agenda rows use mini zone bars; the local zone duplicate is gone; single-source consolidation extends to Agenda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation of existing read-only ride/PMC/session data; no new input, auth flow, persistence, or endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Information Disclosure | Progress/Agenda presentation | low | accept | Presentation-layer-only; no new inputs/auth/persistence/endpoints. Carries forward RESEARCH.md Security Domain conclusion (ASVS L1, no new attack surface). |

No high/critical threats (security_block_on: high). No package installs — no supply-chain gate.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `cd frontend && npm test -- --run` full suite green.
- `cd frontend && npx vitest run src/tests/history.test.tsx` green (RideRow).
</verification>

<success_criteria>
Weekly load history is neutral gray with a current-week brand-blue bar; RideRow shows paired planned/actual bars + ComplianceChip (no table); Agenda rows use mini zone bars from `@/lib/zones` with the local duplicate removed; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-06-SUMMARY.md` when done.
</output>
