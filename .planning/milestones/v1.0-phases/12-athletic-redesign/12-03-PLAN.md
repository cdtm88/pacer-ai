---
phase: 12-athletic-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/PromptChip.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/session/SessionStepList.tsx
autonomous: true
requirements: [D-8, D-12]
must_haves:
  truths:
    - "A single shared PromptChip component exists for Onboarding and Chat to import"
    - "The shadcn Card primitive is available for the Settings redesign (D-12)"
    - "The dead SessionStepList component (a 5th duplicate zone map) is removed"
  artifacts:
    - "frontend/src/components/ui/PromptChip.tsx (extracted, visually identical)"
    - "frontend/src/components/ui/card.tsx (shadcn card block)"
  key_links:
    - "PromptChip.tsx <- consumed by OnboardingScreen/ChatScreen in 12-08"
    - "card.tsx <- consumed by SettingsScreen redesign in 12-08"
---

<objective>
Foundation Slice A, part 3: stand up the shared building blocks that Slice E needs (D-8 component unification, D-12 Settings). Extract the byte-identical `PromptChip` into a shared component, add the shadcn `card` primitive, and delete the confirmed-dead `SessionStepList` (removing a fifth duplicate zone map for free).

Purpose: `PromptChip.tsx` and `card.tsx` must exist before 12-08 migrates Onboarding/Chat and redesigns Settings. Doing them here (creation-only, no conflicts) keeps Slice E focused on the screen wiring.
Output: New `PromptChip.tsx`, new `card.tsx`, deleted `SessionStepList.tsx`. This plan creates but does not yet consume the shared components; the consuming screen edits happen in 12-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- New file `frontend/src/components/ui/PromptChip.tsx` exporting `PromptChip({ label, onClick, disabled })`
- New file `frontend/src/components/ui/card.tsx` exporting `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter` (shadcn defaults)
- Deletion of `frontend/src/components/session/SessionStepList.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared PromptChip and delete dead SessionStepList</name>
  <files>frontend/src/components/ui/PromptChip.tsx, frontend/src/components/session/SessionStepList.tsx (deleted)</files>
  <read_first>
    - frontend/src/screens/ChatScreen.tsx (local PromptChip, lines 85-119 — the source to extract; byte-identical to OnboardingScreen's copy)
    - frontend/src/screens/OnboardingScreen.tsx (local PromptChip, lines 113-147 — confirm byte-identical)
    - frontend/src/components/session/SessionStepList.tsx (dead code to delete)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (exact PromptChip pattern to extract, lines 224-266)
  </read_first>
  <action>
    Create `frontend/src/components/ui/PromptChip.tsx` containing the exact `PromptChip` function currently duplicated in `ChatScreen.tsx` and `OnboardingScreen.tsx` — same props (`label: string`, `onClick: () => void`, `disabled?: boolean`), same local `hover` state, same inline style object (padding `8px 14px`, `999px` radius, `1px solid var(--color-line)` border, hover swaps `var(--color-surface)` to `var(--color-bg-2)`, text `var(--color-ink-2)` at `14px`, `var(--font-family-sans)`, disabled opacity 0.6, `whiteSpace: nowrap`). Export it as a named export `PromptChip`. Do NOT restyle it — it is already on-token; this is a pure copy-out. Do NOT edit ChatScreen.tsx or OnboardingScreen.tsx in this plan (their migration to the shared import happens in 12-08).

    Before deleting, re-confirm `SessionStepList` has zero render sites: run `grep -rn "SessionStepList" frontend/src`. Expect matches only inside `SessionStepList.tsx` itself (definition + its own `import type { ZoneType } from './ZoneChip'`). If any OTHER file imports or renders `<SessionStepList`, STOP and do not delete — surface it. If confirmed dead (no external references), delete `frontend/src/components/session/SessionStepList.tsx` entirely; this removes the 5th duplicate zone map. Note: `ZoneChip.tsx` stays (it is LIVE, imported by `RideChart.tsx`); only `SessionStepList.tsx` is deleted.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/ui/PromptChip.tsx && ! test -f src/components/session/SessionStepList.tsx && grep -rq "SessionStepList" src && echo "UNEXPECTED refs remain" || echo "PASS" ; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/PromptChip.tsx` exists and exports `PromptChip` with the exact visual treatment (padding `8px 14px`, radius `999px`, `--color-line` border, `--color-ink-2` 14px text, hover `--color-surface`->`--color-bg-2`).
    - `frontend/src/components/session/SessionStepList.tsx` no longer exists.
    - `grep -rn "SessionStepList" frontend/src` returns no matches after deletion.
    - `npx tsc --noEmit` reports no new errors (nothing depended on SessionStepList).
    - `ChatScreen.tsx` and `OnboardingScreen.tsx` are unchanged in this plan.
  </acceptance_criteria>
  <done>Shared PromptChip exists; dead SessionStepList removed; no dangling references; types clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the shadcn card primitive</name>
  <files>frontend/src/components/ui/card.tsx</files>
  <read_first>
    - frontend/components.json (confirms shadcn is initialized: new-york / neutral / cssVariables true)
    - frontend/src/components/ui/button.tsx (reference for the existing shadcn block style/location)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Registry Safety table — card is official-registry, no gate)
  </read_first>
  <action>
    Add the shadcn `card` block by running `cd frontend && npx shadcn add card` (official registry per UI-SPEC Registry Safety — no third-party registry, no legitimacy gate required; this copies a local component file and adds no new npm dependency per 12-RESEARCH.md Package Legitimacy Audit). This produces `frontend/src/components/ui/card.tsx` exporting `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`.

    If the shadcn CLI is unavailable in the execution environment, hand-write `frontend/src/components/ui/card.tsx` matching the current shadcn new-york Card template (plain styled `div` wrappers using the `cn` util from `@/lib/utils`, no additional Radix dependency), consistent with the 7 already-installed blocks. Do not consume Card in any screen in this plan; the Settings redesign that uses it is 12-08.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/ui/card.tsx && grep -q "CardHeader" src/components/ui/card.tsx && grep -q "CardContent" src/components/ui/card.tsx && npx tsc --noEmit 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/card.tsx` exists and exports at least `Card`, `CardHeader`, `CardTitle`, `CardContent` (plus `CardDescription`, `CardFooter`).
    - No new runtime dependency appears in `package.json` diff (card is composed from local primitives per the audit).
    - `npx tsc --noEmit` clean; `npm test -- --run` full suite green.
  </acceptance_criteria>
  <done>shadcn Card primitive available for the Settings redesign; no new deps; types + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn CLI / npm registry -> repo | `npx shadcn add card` copies a component template from the official shadcn registry; no new runtime package is installed (per 12-RESEARCH.md Package Legitimacy Audit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | shadcn card block source | low | mitigate | Official shadcn registry only (UI-SPEC Registry Safety); no third-party registry; card adds no npm dependency, so no package-legitimacy checkpoint is triggered. Verify no package.json runtime-dep delta after the add. |
| T-12-03b | Information Disclosure | PromptChip / card presentation | low | accept | Presentation-layer-only; no new inputs/auth/persistence/endpoints. Carries forward RESEARCH.md Security Domain conclusion (ASVS L1). |

No high/critical threats (security_block_on: high). `card` is a registry-template copy, not a package install — no `[ASSUMED]`/`[SUS]` install checkpoint required.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `cd frontend && npm test -- --run` fully green.
- `grep -rn "SessionStepList" frontend/src` returns nothing.
</verification>

<success_criteria>
Shared `PromptChip.tsx` and shadcn `card.tsx` exist and type-check; dead `SessionStepList.tsx` deleted with no dangling references; full suite green; no new npm dependency.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-03-SUMMARY.md` when done.
</output>
