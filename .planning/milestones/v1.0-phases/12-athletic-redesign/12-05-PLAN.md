---
phase: 12-athletic-redesign
plan: 05
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - frontend/src/components/session/SessionCard.tsx
  - frontend/src/components/session/ZwoExportModal.tsx
  - frontend/src/tests/today.test.tsx
  - frontend/src/screens/TodayScreen.tsx
  - frontend/src/components/session/WorkoutProfileChart.tsx
autonomous: true
requirements: [D-6, D-7, D-8]
must_haves:
  truths:
    - "Today's session card leads with a 28px objective, a stat tile row (duration / est TSS / IF), and a taller centered workout profile chart"
    - "One fat full-width 'Start ride' primary and an 'Export .zwo' secondary; the inline brand-fill override is gone (tokens carry it)"
    - "Mark done / Mark missed collapse into a single quiet overflow affordance, keeping their accessible names"
    - "'Coming up' rows show mini zone-colored duration bars instead of 8px dots, in both strip states"
    - "The Export copy rename and its two dependent test assertions land in the same commit"
  artifacts:
    - "SessionCard.tsx redesigned card + StatTile row + renamed CTAs"
    - "today.test.tsx assertions updated for 'Export .zwo'"
  key_links:
    - "SessionData gains tss_target so est TSS renders; IF derived as sqrt(tss_target / (durationMin/60 * 100))"
    - "TodayScreen passes ftp + tss through to SessionCard; both strips use lib/zones mini-bars"
    - "WorkoutProfileChart height param grows to 56-64px for the Today hub"
---

<objective>
Slice C: turn Today into the hub (D-6). Rebuild `SessionCard` with a 28px objective, a `StatTile` row (duration / est TSS / IF), a taller centered profile chart, one fat "Start ride" primary and an "Export .zwo" secondary, and a collapsed Mark-done/Mark-missed overflow. Update `TodayScreen`'s two "Coming up" strips to mini zone-colored bars and repoint its local zone map at `@/lib/zones` (D-7/D-8). The Export copy rename and its two dependent `today.test.tsx` assertions ship together (D-6, Pitfall 1).

Purpose: Today is the daily entry point; the stat tiles and single dominant Start CTA make the next action unmistakable.
Output: Redesigned `SessionCard`, updated `ZwoExportModal` heading, updated `today.test.tsx`, restyled `TodayScreen`, height-parameterized `WorkoutProfileChart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-CONTEXT.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
@.planning/phases/12-athletic-redesign/12-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- New `tss_target?: number | null` field on the `SessionData` interface in `SessionCard.tsx`
- New optional `height` prop on `WorkoutProfileChart`
- Renamed CTA copy: "Start session" -> "Start ride", "Export to Zwift" -> "Export .zwo" (button + aria-label + `ZwoExportModal` heading)
- New stat-tile row and overflow affordance JSX inside `SessionCard`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SessionCard redesign + Export copy rename + coupled test update</name>
  <files>frontend/src/components/session/SessionCard.tsx, frontend/src/components/session/ZwoExportModal.tsx, frontend/src/tests/today.test.tsx</files>
  <read_first>
    - frontend/src/components/session/SessionCard.tsx (FULL — current objective 141-146, WorkoutProfileChart at 160, Start btn 212-220 with inline override, Export btn 222-230, mark done/missed 249-272, "Log without riding" label 232-248)
    - frontend/src/components/ui/StatTile.tsx (prop-driven: label/value/unit/delta/tone; already uses .stat-num-hero after 12-01)
    - frontend/src/components/session/ZwoExportModal.tsx (internal heading text "Export to Zwift", line ~51)
    - frontend/src/tests/today.test.tsx (assertions at lines 154 and 159 that break on rename; MOCK_SESSION shape lines 48-57)
    - frontend/src/components/session/WorkoutProfileChart.tsx (height=34 default to parameterize)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen C; Typography Display role; Copywriting Contract; Open Question 1 resolution)
  </read_first>
  <action>
    In `SessionCard.tsx`:
    - Objective heading (lines 141-146): change `fontSize: 20` to `fontSize: 28`, keep `fontWeight: 600`, `lineHeight: 1.15` (Display role per Typography table). Do not introduce a new size token.
    - Add `tss_target?: number | null` to the `SessionData` interface (the backend `/sessions/today` already returns `tss_target`; the frontend type simply did not declare it).
    - Add a StatTile row (D-6) directly under the `WorkoutProfileChart` (line 160): three `StatTile`s — Duration (value = `getDuration(session)`, unit "min"), est TSS (value = `session.tss_target` rounded, or a placeholder "—" when null), IF (value = derived intensity factor). Compute IF by inverting the planned-TSS formula: `IF = sqrt(tss_target / ((durationMin/60) * 100))`, rounded to 2 decimals; render "—" when `tss_target` or duration is null/zero. This needs no backend change and no FTP.
    - Grow the profile chart: pass a new `height` prop to `WorkoutProfileChart` at line 160 set to 60 (per D-6 "taller and central", 56-64px range) and position it as the card centerpiece.
    - Start CTA (lines 212-220): remove the inline `style={{ backgroundColor: 'var(--color-brand)', color: '#fff' }}` override entirely (the 12-01 token mapping now makes `variant="default"` render brand-blue), keep `className="w-full"` (fat full-width primary), and rename the label from "Start session" to "Start ride".
    - Export CTA (lines 222-230): rename visible text and `aria-label` from "Export to Zwift" to "Export .zwo"; keep `variant="outline"`.
    - Mark done / Mark missed (lines 232-272): collapse the two always-visible ghost buttons into a single quiet overflow affordance (e.g. a "Log without riding" disclosure row that expands to reveal both actions). BOTH actions must remain in the DOM with their exact accessible names "Mark done" and "Mark missed" (the `today.test.tsx` "mark missed" tests query `getByRole('button', { name: /mark missed/i })`). Keep the existing Mark Missed `AlertDialog` and its copy unchanged.

    In `ZwoExportModal.tsx`: rename the internal panel heading text from "Export to Zwift" to "Export .zwo" (Open Question 1 resolved: rename both for consistency, button = action, modal = same label).

    In `today.test.tsx` (SAME task/commit as the copy change — D-6, Pitfall 1): update the button-name assertion at line 154 from `/export to zwift/i` to `/export \.zwo/i`; the multi-match assertion at line 159 changes `getAllByText('Export to Zwift')` to `getAllByText('Export .zwo')` (still `> 1` because both the button and the modal heading now read "Export .zwo"). Add `tss_target: 55` to `MOCK_SESSION` so the est TSS / IF stat tiles have data, and add an assertion that the three stat-tile labels (Duration, TSS, IF) render.
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "Export to Zwift" src/components/session/SessionCard.tsx && ! grep -q "Export to Zwift" src/components/session/ZwoExportModal.tsx && grep -q "Start ride" src/components/session/SessionCard.tsx && npx vitest run src/tests/today.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `SessionCard.tsx` objective renders at `fontSize: 28`; `SessionData` declares `tss_target?: number | null`.
    - A three-tile StatTile row (Duration / est TSS / IF) renders under the profile chart; IF is derived via `sqrt(tss_target / ((durationMin/60)*100))` and shows "—" when data is missing.
    - Start CTA text is "Start ride" with NO inline `backgroundColor` override; Export CTA text and aria-label are "Export .zwo".
    - `ZwoExportModal.tsx` heading is "Export .zwo"; `grep "Export to Zwift"` returns nothing in SessionCard.tsx or ZwoExportModal.tsx.
    - Mark done and Mark missed remain queryable by accessible name; the Mark Missed AlertDialog copy is unchanged.
    - `WorkoutProfileChart` is rendered with `height={60}`.
    - `npx vitest run src/tests/today.test.tsx` passes with the updated assertions (Export .zwo + stat tiles) — no `/export to zwift/i` reference remains.
  </acceptance_criteria>
  <done>SessionCard redesigned; Export renamed everywhere with tests updated in the same commit; stat tiles + taller chart live.</done>
</task>

<task type="auto">
  <name>Task 2: WorkoutProfileChart height prop + TodayScreen mini-bars + zone dedup</name>
  <files>frontend/src/components/session/WorkoutProfileChart.tsx, frontend/src/screens/TodayScreen.tsx</files>
  <read_first>
    - frontend/src/components/session/WorkoutProfileChart.tsx (fixed height: 34 at line ~73 to make an optional prop with default 34)
    - frontend/src/screens/TodayScreen.tsx (FULL — local ZONE_VAR/isValidZone 21-31; empty-state strip zone dot 181-191; post-session strip zone dot 259-269; SessionCard render at 216 with no ftp/tss props)
    - frontend/src/lib/zones.ts (zoneColor to import)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen C: mini zone-colored duration bar 24x4px, 2px radius)
  </read_first>
  <action>
    In `WorkoutProfileChart.tsx`: add an optional `height?: number` prop defaulting to `34` (backward compatible), and use it where the fixed `height: 34` currently is. No other change to the component's rendering or its `zoneColor` import.

    In `TodayScreen.tsx`:
    - Delete the local `ZONE_VAR` map and `isValidZone` type-guard (lines 21-31); import `zoneColor` from `@/lib/zones` instead. Keep a lightweight local validity check only if needed for typing, or use `zoneColor(type)` directly (it already falls back for unknown types).
    - In BOTH "Coming up" strips (empty-state at 181-191 and post-session at 259-269), replace the 8px round zone dot with a mini zone-colored duration bar: `width: 24, height: 4, borderRadius: 2, backgroundColor: zoneColor(type)`. Apply the identical treatment to both strips (they are duplicated JSX).
    - Pass the session's `ftp` and (implicitly via `session`) `tss_target` through to `SessionCard` at line 216 so its IF/TSS stat tiles have data: the `getSessionToday` response already carries `tss_target`; pass `ftp` if available on the loaded profile/session (if TodayScreen has no ftp query, add `ftp={null}` explicitly so the prop is intentional — SessionCard's IF derivation does not require ftp, only tss_target + duration).
  </action>
  <verify>
    <automated>cd frontend && ! grep -q "ZONE_VAR" src/screens/TodayScreen.tsx && grep -q "@/lib/zones" src/screens/TodayScreen.tsx && grep -q "height?: number" src/components/session/WorkoutProfileChart.tsx && npx tsc --noEmit 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `WorkoutProfileChart` accepts an optional `height` prop (default 34) and uses it for the bar row height.
    - `TodayScreen.tsx` no longer defines a local `ZONE_VAR`; it imports `zoneColor` from `@/lib/zones`.
    - Both "Coming up" strips render a 24x4px, 2px-radius zone-colored bar (no 8px round dot remains).
    - `SessionCard` is rendered from TodayScreen with a `tss_target`-bearing session (stat tiles populated).
    - `npx tsc --noEmit` clean; `npm test -- --run` full suite green.
  </acceptance_criteria>
  <done>Chart height parameterized; TodayScreen strips use lib/zones mini-bars; local zone map removed; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation of existing read-only session/PMC data; no new input field, auth flow, persistence, or endpoint. `tss_target` was already returned by the existing `/sessions/today` endpoint; only the frontend type declaration is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Tampering | today.test.tsx copy-coupled assertions | low | mitigate | The Export rename and its two dependent assertions land in one task/commit (Pitfall 1), so the regression cannot ship silently. |
| T-12-05b | Information Disclosure | Today hub presentation | low | accept | Presentation-layer-only; no new inputs/auth/persistence/endpoints. Carries forward RESEARCH.md Security Domain conclusion (ASVS L1). |

No high/critical threats (security_block_on: high). No package installs — no supply-chain gate.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/today.test.tsx` passes with updated assertions.
- `cd frontend && npx tsc --noEmit` clean; `cd frontend && npm test -- --run` full suite green.
</verification>

<success_criteria>
Today hub leads with a 28px objective, populated stat tiles (duration/est TSS/IF), a taller centered profile chart, a fat "Start ride" primary and "Export .zwo" secondary (no inline override), a collapsed log-without-riding overflow, and mini zone bars in both strips; Export rename + tests committed together; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-05-SUMMARY.md` when done.
</output>
