---
phase: 13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ADAPT-04, TRANSP-03]
security_asvs_level: 1
security_block_on: high
must_haves:
  truths:
    - "REQUIREMENTS.md's traceability table records that ADAPT-04 and TRANSP-03 are genuinely satisfied by Phase 13 (the phase that added the real caller + UI consumer), correcting the stale 'Phase 3 / Complete' mapping surfaced by the v1.0 milestone audit"
    - "ROADMAP.md's Phase 13 entry Goal and Requirements fields are no longer placeholders ([To be planned] / TBD) and reflect the real goal + ADAPT-04, TRANSP-03"
  artifacts:
    - ".planning/REQUIREMENTS.md (updated traceability rows for ADAPT-04, TRANSP-03)"
    - ".planning/ROADMAP.md (Phase 13 Goal + Requirements finalized)"
---

<objective>
Correct the documentation audit trail so it matches reality. The v1.0 milestone audit found ADAPT-04 and TRANSP-03 were marked "Phase 3 / Complete" in REQUIREMENTS.md on backend-only evidence, while the caller (ADAPT-04) and UI consumer (TRANSP-03) did not exist until Phase 13. This plan updates the traceability table to attribute genuine satisfaction to Phase 13, and finalizes ROADMAP.md's Phase 13 Goal/Requirements fields (per 13-RESEARCH.md Open Question 2 recommendation: keep the audit trail accurate rather than reproducing the exact blind spot the audit exists to catch).

Purpose: leaving the mapping stale would repeat the milestone-audit blind spot. Note: the underlying detection/logging logic legitimately shipped in Phase 3/6; Phase 13 is what makes these two requirements reachable by a user.
Output: corrected REQUIREMENTS.md traceability rows + finalized ROADMAP.md Phase 13 fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct REQUIREMENTS.md traceability for ADAPT-04 and TRANSP-03</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (traceability table — ADAPT-04 at line 199, TRANSP-03 at line 203, both currently "Phase 3 | Complete")
    - .planning/v1.0-MILESTONE-AUDIT.md (the evidence trail: both were SATISFIED on endpoint+unit-test alone, with no caller/consumer)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-RESEARCH.md (lines 39-47, 474-477 — the traceability-note recommendation)
  </read_first>
  <action>
    Use scoped Edit (not Write) on .planning/REQUIREMENTS.md. Update the ADAPT-04 and TRANSP-03 rows so the Phase column reflects that Phase 13 is where they became genuinely satisfied (the endpoint/log existed since Phase 3, but per the milestone audit "Complete" was previously true only for the backend half). Keep the change minimal and factual: change the Phase attribution for these two rows to Phase 13 (or, if you prefer to preserve history, annotate as "Phase 3 (backend) / Phase 13 (wired)") and keep Status Complete. Add a brief one-line note under the traceability table (or in the coverage note) that Phase 13 closed the ADAPT-04/TRANSP-03 integration gap found in the v1.0 milestone audit. Do not alter any other requirement rows. No em dashes (CLAUDE.md).
  </action>
  <verify>
    <automated>grep -nE 'ADAPT-04|TRANSP-03' .planning/REQUIREMENTS.md | grep -i 'Phase 13'</automated>
  </verify>
  <acceptance_criteria>
    - The ADAPT-04 and TRANSP-03 traceability rows reference Phase 13 (no longer attributing genuine satisfaction to Phase 3 alone)
    - A one-line note records that Phase 13 closed the milestone-audit integration gap
    - No other requirement rows are modified
  </acceptance_criteria>
  <done>REQUIREMENTS.md traceability accurately attributes ADAPT-04/TRANSP-03 wiring to Phase 13.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize ROADMAP.md Phase 13 Goal + Requirements fields</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 13 entry ~lines 413-423 — Goal currently "[To be planned]", Requirements currently "TBD"; the planner may have pre-filled these during plan creation)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-CONTEXT.md (phase boundary statement for the goal wording)
  </read_first>
  <action>
    Use scoped Edit (not Write) on .planning/ROADMAP.md. Verify the Phase 13 entry's Goal field reflects the real goal (a client-initiated weekly adaptation check wired at AppLayout closes ADAPT-04, and a readable Adaptations log section in ProgressScreen closes TRANSP-03 — the two integration gaps from the v1.0 milestone audit) and the Requirements field reads ADAPT-04, TRANSP-03. If the planner already replaced the [To be planned] / TBD placeholders during plan creation, confirm they are correct and leave them; if either placeholder remains, replace it now. Do not touch other phases' entries. No em dashes (CLAUDE.md).
  </action>
  <verify>
    <automated>grep -nA3 'Phase 13:' .planning/ROADMAP.md | grep -Ei 'ADAPT-04|TRANSP-03' && ! grep -n 'Phase 13' .planning/ROADMAP.md | grep -q 'To be planned'</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 13 Goal is a real goal statement (no "[To be planned]" placeholder remaining)
    - ROADMAP.md Phase 13 Requirements reads "ADAPT-04, TRANSP-03" (no "TBD")
    - No other phase entries are modified
  </acceptance_criteria>
  <done>ROADMAP.md Phase 13 Goal and Requirements are finalized and placeholder-free.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 04)

- `.planning/REQUIREMENTS.md` — traceability rows for ADAPT-04 + TRANSP-03 corrected to attribute genuine satisfaction to Phase 13
- `.planning/ROADMAP.md` — Phase 13 Goal + Requirements finalized (placeholder-free)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Documentation-only plan: no code, no runtime surface, no attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-DOC | Repudiation | traceability audit trail | low | accept | This plan improves auditability by correcting a stale mapping; the only "risk" is an inaccurate edit, mitigated by scoped Edits and the grep acceptance checks. No runtime attack surface. |

No endpoints, auth paths, or package installs. ASVS level 1; no `high`-severity threat, so no blocking checkpoint.
</threat_model>

<verification>
- `grep -nE 'ADAPT-04|TRANSP-03' .planning/REQUIREMENTS.md | grep -i 'Phase 13'` returns both rows
- ROADMAP.md Phase 13 Goal/Requirements are placeholder-free
</verification>

<success_criteria>
- The documentation audit trail no longer misattributes ADAPT-04/TRANSP-03 satisfaction to the backend-only Phase 3 state
- ROADMAP.md Phase 13 fields are finalized
</success_criteria>

<output>
Create `.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-04-SUMMARY.md` when done
</output>
