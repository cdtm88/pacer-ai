---
phase: 13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - frontend/src/screens/ProgressScreen.tsx
  - frontend/src/tests/progress.test.tsx
autonomous: true
requirements: [TRANSP-03]
security_asvs_level: 1
security_block_on: high
must_haves:
  truths:
    - "ProgressScreen renders an 'Adaptations' section as the 5th and final block, after the Ride log (D-06, D-07)"
    - "The section fetches via useQuery({ queryKey: ['adaptations'], queryFn: getAdaptations }) — same shape as the existing rides/pmc queries (D-08)"
    - "Each row shows the humanized trigger (triggerLabel), the explanation_text verbatim, and the formatted created_at, newest first with no client sort (backend orders created_at desc) (D-09, D-10)"
    - "When getAdaptations() returns [], the section shows exactly: No adaptations yet. Your plan hasn't needed adjustment. (D-11, em-dash-free per CLAUDE.md)"
    - "Loading renders 2 SkeletonRows; error renders a retry button with copy 'Could not load adaptations. Tap to retry.'"
    - "A malformed/partial row (nullable fields absent) does not crash the render (T-13-01)"
  artifacts:
    - "frontend/src/screens/ProgressScreen.tsx (Adaptations section)"
    - "frontend/src/tests/progress.test.tsx"
  key_links:
    - "Imports getAdaptations + the corrected Adaptation type from lib/api (13-01) and triggerLabel + formatDate from lib/format (13-01)"
---

<objective>
Give TRANSP-03 a UI consumer. `getAdaptations()` exists but no screen calls it, so a user cannot review past adaptation decisions. This plan adds an "Adaptations" section to ProgressScreen (D-06 — it belongs to the "am I getting fitter?" narrative, not a new nav tab), placed after the Ride log (D-07), following the exact useQuery + loading/error/empty/data state machine the Ride log already demonstrates (D-08). Rows render the humanized `trigger` (triggerLabel), the `explanation_text` verbatim, and the formatted `created_at` (D-09), full list, no pagination (D-10), with the locked empty-state sentence (D-11).

Purpose: closes the read half of the milestone-audit gap; the log becomes readable per TRANSP-03. Built against the corrected `Adaptation` interface from 13-01 so rows render real data, not undefined (13-RESEARCH.md Pitfall 1).
Output: Adaptations section in ProgressScreen + progress.test.tsx (which also gives ProgressScreen its first test coverage).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-RESEARCH.md
@.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-PATTERNS.md
@.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-UI-SPEC.md
@.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Adaptations section to ProgressScreen</name>
  <files>frontend/src/screens/ProgressScreen.tsx</files>
  <read_first>
    - frontend/src/screens/ProgressScreen.tsx (target — the local SectionLabel/SkeletonRow at lines 24-53; the useQuery block at 96-98; the "4. Ride log" section at 189-269 with its loading/error/empty/data states to replicate; import line at line 3)
    - frontend/src/lib/api.ts (getAdaptations + corrected Adaptation type from 13-01)
    - frontend/src/lib/format.ts (triggerLabel + formatDate from 13-01)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-UI-SPEC.md (full — the visual/interaction contract: 2 SkeletonRows, static non-tappable rows, exact copy, exact tokens, NO scope badge, NO brand-blue element)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-PATTERNS.md (lines 198-264 — the exact section markup to replicate)
  </read_first>
  <action>
    Extend the ProgressScreen import at line 3 to add getAdaptations (and the Adaptation type if needed for typing); add `import { triggerLabel, formatDate } from '../lib/format'` (classifyTsb import already present). Add adaptationsQuery = useQuery({ queryKey: ['adaptations'], queryFn: getAdaptations }) and const adaptations = adaptationsQuery.data ?? [] alongside the existing queries (D-08).

    Insert a new top-level div directly AFTER the "4. Ride log" div block (after line 269, inside the max-width column so it inherits the parent gap: 28), headed by the existing SectionLabel with text Adaptations (D-07 — 5th and final block). Replicate the Ride log state machine per 13-UI-SPEC.md:
    - Loading: exactly 2 SkeletonRows (not 3).
    - Error: a button styled identically to the Ride log retry button (background none, border none, color var(--color-bad), fontSize 14, width 100%, textAlign center, padding 12) with onClick adaptationsQuery.refetch(); copy: Could not load adaptations. Tap to retry.
    - Empty (data loaded, length 0): a single centered p (fontSize 15, color var(--color-ink-2), textAlign center, lineHeight 1.5, paddingTop 24, margin 0) with the exact text: No adaptations yet. Your plan hasn't needed adjustment. (period, not an em dash — CLAUDE.md).
    - Data: map adaptations (already created_at desc from backend — no client sort, D-10) to a static div per row (NOT a button; rows are non-tappable, no expand) with borderBottom 1px solid var(--color-line), padding 12px 0; inside it a title div (fontSize 14, weight 600, var(--color-ink)) showing triggerLabel(a.trigger); a p (fontSize 13, var(--color-ink-2), margin 2px 0, lineHeight 1.5) showing a.explanation_text; a span (fontSize 12, var(--color-ink-3)) showing formatDate(a.created_at). No scope badge (resolves Open Question 1). Use optional-chaining/?? defensively on nullable fields so a partial row cannot crash the render (T-13-01), mirroring how the Ride log guards ride.compliance_pct.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ProgressScreen typechecks and renders the Adaptations section as the last block after Ride log
    - Uses queryKey ['adaptations'] + getAdaptations (D-08); no client-side re-sort (D-10)
    - Row markup matches 13-UI-SPEC.md: static rows, no scope badge, no brand-blue element, tokens as specified
  </acceptance_criteria>
  <done>The Adaptations section renders humanized trigger + explanation_text + formatted date rows, plus loading/error/empty states matching the UI-SPEC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: progress.test.tsx — empty state + populated rows + malformed-row safety</name>
  <files>frontend/src/tests/progress.test.tsx</files>
  <read_first>
    - frontend/src/tests/history.test.tsx (lines 1-42 — copy the vi.mock('../lib/api') + makeQueryClient + renderWithQuery scaffolding; lines 79-100 — the empty-state describe pattern; lines 106-123 — the local makeRide fixture pattern to mirror for makeAdaptation)
    - frontend/src/screens/ProgressScreen.tsx (component under test, after Task 1)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-PATTERNS.md (lines 317-351 — the fixture shape + test intent)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-UI-SPEC.md (the exact empty/error copy to assert)
  </read_first>
  <behavior>
    - getAdaptations() -> [] : the exact sentence "No adaptations yet. Your plan hasn't needed adjustment." renders
    - getAdaptations() -> [one realistic row] : the humanized trigger label ("Missed session"), the explanation_text, and a formatted date all render
    - getAdaptations() -> [row with signal_count/status/trigger_session_ids absent] : renders without throwing (T-13-01 robustness)
  </behavior>
  <action>
    Create frontend/src/tests/progress.test.tsx. Mock ../lib/api exposing getRides, getPmcHistory, getLatestPmc, getAdaptations as vi.fn(); in beforeEach resolve getRides/getPmcHistory/getLatestPmc with empty/null defaults so only the Adaptations section is under test. Reuse the makeQueryClient({ retry: false }) + renderWithQuery(QueryClientProvider) wrapper from history.test.tsx. Add a local makeAdaptation(overrides) fixture using the corrected real schema fields (id, trigger: 'missed', scope: 'micro', explanation_text: a realistic sentence, created_at: '2026-07-06T12:00:00Z') per 13-PATTERNS.md lines 341-350. Write three tests covering the three behavior cases: assert the exact empty sentence via screen.getByText; assert the humanized label + explanation text + a date fragment (e.g. /Jul/) render for a populated row; assert a row missing the optional/nullable fields still renders its trigger label without throwing. Wrap render in MemoryRouter if ProgressScreen or its children need router context (RideRow uses Link).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/progress.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Empty-state test asserts the exact D-11/UI-SPEC sentence (em-dash-free)
    - Populated-row test asserts humanized trigger + explanation_text + formatted date all render
    - Malformed-row test (nullable fields absent) renders without throwing (T-13-01)
  </acceptance_criteria>
  <done>progress.test.tsx green — the Adaptations section's empty, populated, and malformed-row paths are all covered.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 03)

- `frontend/src/screens/ProgressScreen.tsx` — new "Adaptations" section (TRANSP-03 UI consumer): useQuery(['adaptations']), 2-SkeletonRow loading, retry error, exact empty sentence, static reverse-chron rows (triggerLabel + explanation_text + formatDate)
- `frontend/src/tests/progress.test.tsx` — new test file (first coverage for ProgressScreen) with a local `makeAdaptation` fixture on the corrected schema
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend JSON -> ProgressScreen render | Adaptation rows (nullable columns) are rendered directly; malformed/partial rows must not crash the screen |
| authenticated user -> cross-account cache | The ['adaptations'] query cache must not leak a prior account's rows after a user switch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Information Disclosure / Robustness | Adaptations section rows (ProgressScreen) | medium | mitigate | Render nullable fields with optional-chaining/?? (mirroring the Ride log's ride.compliance_pct guard); triggerLabel already falls back to titleCase/'Adaptation' on an unexpected value. A malformed-row test asserts no crash. |
| T-13-SESS | Information Disclosure | ['adaptations'] React Query cache after account switch | low | mitigate | Already globally mitigated: router.tsx RootProvider clears the whole query cache on SIGNED_OUT/USER_UPDATED/SIGNED_IN, which covers the new ['adaptations'] key automatically; no per-screen code needed. |

`getAdaptations()` is a read-only JWT-scoped GET with no query params (V4 access control already enforced server-side, unchanged; V5 minimal). No new endpoint, no package install. ASVS level 1; no `high`-severity threat, so no blocking checkpoint.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/progress.test.tsx` green
- `cd frontend && npx tsc --noEmit` clean
- Wave merge: `cd frontend && npx vitest run` (full frontend suite) green
</verification>

<success_criteria>
- TRANSP-03 has a UI consumer: the Progress screen renders a readable Adaptations log
- Empty/loading/error/data states match 13-UI-SPEC.md exactly (em-dash-free copy)
- Rows render real corrected-schema fields (no undefined), robust to nullable fields
</success_criteria>

<output>
Create `.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-03-SUMMARY.md` when done
</output>
