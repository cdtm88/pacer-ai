---
phase: 07-deploy-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0007_fk_indexes.sql
autonomous: true
requirements: [DEPLOY-IDX-01, DEPLOY-BUCKET-01]
user_setup:
  - service: supabase
    why: "Apply the new index migration to the linked project"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (only needed if `supabase db push --linked --yes` prompts for interactive auth in this environment)"
must_haves:
  truths:
    - "Every user_id/FK column across all tables has a btree index"
    - "Migration 0007 applies cleanly and idempotently to the linked Supabase project"
    - "The fits storage bucket exists (provisioned by migration 0006) — verified present, not rebuilt"
  artifacts:
    - "supabase/migrations/0007_fk_indexes.sql"
  key_links:
    - "Migration adds CREATE INDEX statements only — no RLS/policy/data changes, so row-level security is unchanged"
    - "Indexes are pushed to the live linked DB (a missing index would not surface in any build or type check)"
---

<objective>
Add btree indexes on every user_id/FK column via a new idempotent migration and apply it to the linked Supabase project, then verify the fits storage bucket (already provisioned in migration 0006) is present. Today zero CREATE INDEX statements exist across all six migrations, so every list/detail query does a sequential scan on user_id/FK columns.

Purpose: Close a correctness-adjacent performance defect the ROADMAP requires ("indexes added on all user_id/FK columns") and confirm the "fits bucket provisioned as config" roadmap item is satisfied.
Output: supabase/migrations/0007_fk_indexes.sql applied to the linked DB; fits bucket presence confirmed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy-consolidation/07-RESEARCH.md
@.planning/phases/07-deploy-consolidation/07-PATTERNS.md
@supabase/migrations/0006_pmc_unique_and_fits_bucket.sql
</context>

<artifacts>
Artifacts this plan produces:
- CREATE `supabase/migrations/0007_fk_indexes.sql` — 12 `CREATE INDEX IF NOT EXISTS` statements (idempotent) covering: profiles.user_id, sessions.user_id, sessions.plan_id, rides.user_id, rides.session_id, conversations.user_id, messages.conversation_id, messages.user_id, capability_gaps.user_id, oauth_states.user_id, plans.user_id, adaptations.user_id. (pmc_history(user_id,date) already indexed via 0006's composite unique key — no additional index.)
No new application code or symbols.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0007_fk_indexes.sql</name>
  <read_first>supabase/migrations/0006_pmc_unique_and_fits_bucket.sql (header-comment + idempotency convention), 07-RESEARCH.md "Code Examples" (the exact CREATE INDEX statement list), 07-PATTERNS.md "Idempotent migration DDL"</read_first>
  <files>supabase/migrations/0007_fk_indexes.sql</files>
  <action>Create the new migration using the exact CREATE INDEX statement list from 07-RESEARCH.md's Code Examples section (12 statements). Follow the 0006 convention: a dated header comment block explaining why (no indexes existed on FK/user_id columns; every list/detail query was doing a sequential scan). Every statement uses the form `CREATE INDEX IF NOT EXISTS idx_<table>_<column> ON public.<table> (<column>)` so the migration is natively idempotent and safe to re-run. Do NOT include a pmc_history(user_id) index — 07-RESEARCH.md documents that 0006's composite unique key (pmc_history_user_id_date_key) already serves user_id lookups via leftmost-prefix matching; add a trailing comment noting that. Add no RLS, policy, GRANT, table, or column changes — CREATE INDEX only.</action>
  <verify>
    <automated>test -f supabase/migrations/0007_fk_indexes.sql && [ "$(grep -c 'CREATE INDEX IF NOT EXISTS' supabase/migrations/0007_fk_indexes.sql)" -eq 12 ] && ! grep -qiE 'DROP|ALTER TABLE|CREATE POLICY|GRANT|CREATE TABLE' supabase/migrations/0007_fk_indexes.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>Migration file exists with exactly 12 idempotent CREATE INDEX IF NOT EXISTS statements covering all listed FK/user_id columns, no pmc_history index, and no RLS/policy/table/data changes.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migration to linked Supabase and verify indexes + fits bucket</name>
  <read_first>07-RESEARCH.md (apply command: `supabase db push --linked --yes`; STATE.md established this non-interactive pattern), supabase/migrations/0006_pmc_unique_and_fits_bucket.sql (fits bucket provisioning to verify against)</read_first>
  <files>supabase/migrations/0007_fk_indexes.sql</files>
  <action>[BLOCKING — must run after Task 1 creates the migration and before phase verification; a missing index would not surface in any build or type check.] Apply the migration to the linked project with `supabase db push --linked --yes` (the project's established non-interactive pattern per STATE.md). If the push prompts for interactive auth that cannot be suppressed in this environment, set SUPABASE_ACCESS_TOKEN (from the Supabase Dashboard -> Account -> Access Tokens) and retry; if it still cannot run non-interactively, stop and surface it for manual application rather than skipping. After a successful push: (1) verify the new indexes exist by querying `SELECT indexname FROM pg_indexes WHERE schemaname='public' AND indexname LIKE 'idx_%'` (expect all 12 idx_* names present); (2) verify the fits storage bucket exists — it was provisioned by migration 0006, so this is a verification-only check, NOT a rebuild (query `storage.buckets` for a bucket named fits, or confirm via the Supabase Storage dashboard/CLI). Record both results in the SUMMARY.</action>
  <verify>
    <automated>supabase migration list --linked 2>/dev/null | grep -q '0007' && echo "0007 applied" || echo "MANUAL: confirm 0007 pushed + 12 idx_* indexes present + fits bucket exists"</automated>
  </verify>
  <acceptance_criteria>Migration 0007 is applied to the linked project; all 12 idx_* indexes are present in public schema; the fits storage bucket is confirmed to exist (already provisioned by 0006). If the push cannot run non-interactively, the task is surfaced for manual application rather than marked complete.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file -> linked production Postgres | DDL applied via `supabase db push` mutates the live schema; must be additive-only |
| Supabase RLS policies -> row access | Existing row-level security must remain unchanged by a pure indexing migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Tampering | 0007_fk_indexes.sql | high | mitigate | Restrict the migration to CREATE INDEX IF NOT EXISTS only; Task 1 verify greps to reject any DROP/ALTER TABLE/CREATE POLICY/GRANT/CREATE TABLE so RLS and data cannot be weakened |
| T-07-03-02 | Denial of Service | live db push | medium | mitigate | CREATE INDEX IF NOT EXISTS is idempotent and additive; on a small MVP dataset index builds are near-instant and non-locking in practice; push is gated as a [BLOCKING] step with explicit verification |
| T-07-03-03 | Information Disclosure | fits storage bucket | low | accept | Bucket is verified as private (already provisioned by 0006); this plan only verifies presence, it does not change bucket visibility or policies |
</threat_model>

<verification>
- `grep -c 'CREATE INDEX IF NOT EXISTS' supabase/migrations/0007_fk_indexes.sql` returns 12
- migration contains no DROP/ALTER TABLE/CREATE POLICY/GRANT/CREATE TABLE statements
- `supabase migration list --linked` shows 0007 applied
- `SELECT indexname FROM pg_indexes WHERE schemaname='public' AND indexname LIKE 'idx_%'` returns all 12 index names
- fits storage bucket confirmed present (verification-only; provisioned by 0006)
</verification>

<success_criteria>
Migration 0007 exists with 12 idempotent, additive-only index statements, is applied to the linked Supabase project, all 12 indexes are confirmed present, RLS/policies/data are unchanged, and the fits bucket is verified to exist.
</success_criteria>

<output>
Create `.planning/phases/07-deploy-consolidation/07-03-SUMMARY.md` when done
</output>
