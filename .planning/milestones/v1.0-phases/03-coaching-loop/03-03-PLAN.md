---
phase: 03-coaching-loop
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - agent/loop.py
  - api/routes/chat.py
  - api/routes/_sse.py
  - api/routes/onboarding.py
  - api/main.py
  - tests/api/test_onboarding.py
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04, PLAN-06, TRANSP-01]
must_haves:
  truths:
    - "POST /onboarding/start returns a text/event-stream SSE response"
    - "run_turn accepts an injected system prompt so onboarding and coaching use different prompts"
    - "A conversation row with context_type='onboarding' is created on start"
    - "GET /chat/stream loads the last 20 messages from the DB instead of an in-memory placeholder"
    - "New messages from each turn are persisted to the messages table"
    - "The onboarding system prompt instructs the agent to collect exactly 6 fields, gate on user approval before save_profile, and call progress_load + calculate_hr_zones before generate_plan"
  artifacts:
    - path: "api/routes/onboarding.py"
      provides: "POST /onboarding/start SSE endpoint with onboarding system prompt"
      contains: "ONBOARDING_SYSTEM_PROMPT"
      min_lines: 50
    - path: "api/routes/_sse.py"
      provides: "Shared sse_generator that accepts a system_prompt argument"
      contains: "def sse_generator"
      min_lines: 20
    - path: "agent/loop.py"
      provides: "run_turn with a system parameter (dynamic prompt injection, D-22)"
      contains: "system"
  key_links:
    - from: "api/routes/onboarding.py"
      to: "api/routes/_sse.py"
      via: "imports the shared sse_generator (no duplication of the SSE generator)"
      pattern: "from api.routes._sse import sse_generator"
    - from: "api/routes/onboarding.py"
      to: "agent/loop.run_turn"
      via: "sse_generator drives run_turn with ONBOARDING_SYSTEM_PROMPT"
      pattern: "ONBOARDING_SYSTEM_PROMPT"
    - from: "api/main.py"
      to: "api/routes/onboarding.py"
      via: "app.include_router(onboarding_router, prefix='/onboarding')"
      pattern: "onboarding_router"
---

<objective>
Build the onboarding interview entry point and upgrade the conversation layer to be DB-backed. Extract the SSE generator into a shared module that accepts a dynamic system prompt, add a `system` parameter to `run_turn` (D-22), create `POST /onboarding/start`, and replace the Phase 2 in-memory chat placeholder with real DB message load/save.

Purpose: ONBD-01 through ONBD-04 require a multi-turn interview that persists a profile and gates plan generation on explicit user approval. The agent already exists (Phase 2 run_turn); this plan wires it to a new endpoint with the onboarding prompt and makes conversations durable.
Output: api/routes/_sse.py (shared generator), api/routes/onboarding.py, modified agent/loop.py (system param), modified api/routes/chat.py (DB-backed), modified api/main.py (router mount), filled-in onboarding tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-coaching-loop/03-RESEARCH.md
@.planning/phases/03-coaching-loop/03-PATTERNS.md
@api/routes/chat.py
@agent/loop.py
@api/main.py
@sports_science/capability_gap.py
@tests/agent/test_sse.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared sse_generator with system_prompt param and add system parameter to run_turn</name>
  <files>api/routes/_sse.py, agent/loop.py, api/routes/chat.py</files>
  <read_first>
    - api/routes/chat.py (current sse_generator lines 58-80 and chat_stream lines 83-116 — extract the generator, keep the headers/StreamingResponse pattern)
    - agent/loop.py (run_turn signature lines 46-52 and the hardcoded `system=SYSTEM_PROMPT` at line 91 — change to accept an injected system string)
    - .planning/phases/03-coaching-loop/03-PATTERNS.md (onboarding.py section lines 27-76 — dynamic system prompt pattern, "change run_turn to accept system as a parameter")
    - .planning/phases/03-coaching-loop/03-RESEARCH.md (Dynamic System Prompt section lines 331-357 and 666-688)
  </read_first>
  <action>
    Create api/routes/_sse.py containing the shared async `sse_generator(messages: list[dict], model: str, system_prompt: str | None = None)` extracted from chat.py. It instantiates the per-request anthropic.AsyncAnthropic client, builds an audit_log list, and drives run_turn passing the system_prompt through. When system_prompt is None it falls back to the existing default behavior. Keep the existing exception-to-error-frame handling (the `except Exception` block that yields `event: error`). Keep the SSE frame format `event: {type}\ndata: {json}\n\n`.

    Modify agent/loop.py: change run_turn's signature to add a keyword parameter `system: str = SYSTEM_PROMPT` (default to the existing module-level SYSTEM_PROMPT constant so existing callers/tests are unaffected). Replace the hardcoded `system=SYSTEM_PROMPT` inside `client.messages.stream(...)` (line 91) with `system=system`. Do not change any other behavior, the retry loop, or the trust scan.

    Modify api/routes/chat.py: remove the now-duplicated sse_generator definition and import it from api.routes._sse instead (`from api.routes._sse import sse_generator`). The chat_stream endpoint keeps its existing StreamingResponse + headers but the message-loading change happens in Task 2. Keep `run_turn` importable at module scope (tests monkeypatch `chat_module.run_turn`) — re-export or keep the import so existing test_sse.py monkeypatch still resolves.
  </action>
  <verify>
    <automated>python -m pytest tests/agent/test_sse.py -x -q && python -c "from api.routes._sse import sse_generator; import inspect; assert 'system_prompt' in inspect.signature(sse_generator).parameters; from agent.loop import run_turn; assert 'system' in inspect.signature(run_turn).parameters; print('SSE_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - api/routes/_sse.py exists and defines sse_generator with a system_prompt parameter
    - run_turn signature includes a `system` parameter (`inspect.signature(run_turn).parameters` contains "system")
    - The string `system=system` appears in agent/loop.py inside the messages.stream call (grep returns >= 1)
    - tests/agent/test_sse.py still passes (existing SSE tests green; chat.py monkeypatch of run_turn still resolves)
    - chat.py no longer defines its own `async def sse_generator` (grep -c "async def sse_generator" api/routes/chat.py returns 0)
  </acceptance_criteria>
  <done>Shared sse_generator accepts a dynamic prompt; run_turn accepts a system param; existing SSE tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create POST /onboarding/start, conversation persistence helpers, and DB-backed chat load</name>
  <files>api/routes/onboarding.py, api/routes/chat.py, api/main.py</files>
  <read_first>
    - api/routes/chat.py (router/StreamingResponse pattern and the Phase 2 in-memory placeholder lines 96-106 to replace)
    - api/main.py (include_router pattern line 33 — add the onboarding router the same way)
    - sports_science/capability_gap.py (the _get_async_supabase singleton lines 28-50 — reuse this exact pattern for conversation/message DB ops)
    - .planning/phases/03-coaching-loop/03-PATTERNS.md (onboarding.py section lines 27-76: router mount, conversation creation, load_conversation/save_messages; adaptations.py router-registration lines 153-162)
    - .planning/phases/03-coaching-loop/03-RESEARCH.md (Onboarding Interview Implementation lines 315-399: endpoint shape, ONBOARDING_SYSTEM_PROMPT text, load_conversation/save_messages logic, 20-message cap)
    - .planning/phases/03-coaching-loop/03-CONTEXT.md (D-01, D-02, D-03, D-21, D-22 — interview drives via run_turn, exactly 6 fields, approval gate, context_type, dynamic prompt)
  </read_first>
  <action>
    Create api/routes/onboarding.py:
    - Define ONBOARDING_SYSTEM_PROMPT as a module string instructing the agent to collect EXACTLY these 6 fields through natural conversation (D-02): fitness_goals, weekly_hours, preferred_days, back_status (none/mild/moderate, ask about back issues and severity), equipment (Wahoo Kickr Core + Zwift, confirm with user), rpe_baseline (beginner/moderate/fit). After collecting all fields it must present a plain-text confirmation summary beginning "Here is what I have" and WAIT for explicit user approval before calling save_profile (D-03 / ONBD-04 gate). It must call save_profile only after approval, then call progress_load and calculate_hr_zones BEFORE generate_plan (D-08 order), and never emit a physiological number without a tool call (PLAN-06 / TRUST-04). If back_status is moderate, pass back_issues=true to progress_load (D-24). Do NOT write the trust-scanner negative-example numbers into this prompt as literals that the scanner would later flag.
    - Add a `_get_async_supabase()` singleton (reuse the capability_gap pattern) and two helpers: `async def create_conversation(user_id: str, context_type: str) -> str` (inserts a conversations row with context_type, returns the new id) and reuse load/save helpers from a shared location. Add `async def load_conversation(conversation_id: str, limit: int = 20) -> list[dict]` (SELECT from messages WHERE conversation_id ORDER BY created_at DESC LIMIT 20, reverse to chronological, map to {role, content}) and `async def save_messages(conversation_id: str, user_id: str, new_messages: list[dict])` (INSERT each new message). Add a TODO comment that the 20-message cap is a Phase 4 token-count-truncation deferral (RESEARCH Open Question 5).
    - Define `router = APIRouter()` and `@router.post("/start")` `async def onboarding_start(user_id: str = Body(...))`: create a conversation row with context_type='onboarding', seed messages with the opening user message "I'd like to start my training interview.", and return StreamingResponse(sse_generator(messages, model, system_prompt=ONBOARDING_SYSTEM_PROMPT), media_type="text/event-stream", headers={Cache-Control no-cache, Connection keep-alive, X-Accel-Buffering no}). Import sse_generator from api.routes._sse. Import run_turn at module scope so tests can monkeypatch onboarding_module.run_turn.

    Modify api/routes/chat.py: replace the in-memory placeholder messages (lines 96-106) with `messages = await load_conversation(conversation_id, limit=20)` (import load_conversation from onboarding or a shared helper module), and after the turn completes persist new messages via save_messages. If load_conversation returns empty (new conversation), fall back to an empty list and let run_turn drive the opening.

    Modify api/main.py: add `from api.routes.onboarding import router as onboarding_router` and `app.include_router(onboarding_router, prefix="/onboarding", tags=["onboarding"])`. Do NOT add the rides or adaptations routers here (those are owned by later plans).
  </action>
  <verify>
    <automated>python -c "from api.main import app; routes={r.path for r in app.routes}; assert '/onboarding/start' in routes, routes; import api.routes.onboarding as o; assert hasattr(o, 'ONBOARDING_SYSTEM_PROMPT'); assert callable(o.onboarding_start); print('ONBOARDING_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app.routes includes path "/onboarding/start"
    - api/routes/onboarding.py defines ONBOARDING_SYSTEM_PROMPT, create_conversation, load_conversation, save_messages, and onboarding_start
    - ONBOARDING_SYSTEM_PROMPT text names all 6 interview fields and the approval-before-save_profile gate (grep for "save_profile" and "Here is what I have" returns >= 1 each)
    - api/main.py registers onboarding_router with prefix "/onboarding"
    - chat.py calls load_conversation (grep -c "load_conversation" api/routes/chat.py returns >= 1)
    - api/main.py does NOT register a rides or adaptations router (grep -c "rides_router\|adaptations_router" api/main.py returns 0)
  </acceptance_criteria>
  <done>Onboarding endpoint live with the 6-field gated prompt; conversation create/load/save helpers wired; chat upgraded to DB-backed messages.</done>
</task>

<task type="auto">
  <name>Task 3: Fill in onboarding SSE and gate tests</name>
  <files>tests/api/test_onboarding.py</files>
  <read_first>
    - tests/api/test_onboarding.py (the Wave 0 stub from Plan 02 — fill in the skipped bodies)
    - tests/agent/test_sse.py (monkeypatch + ASGITransport pattern lines 110-129, parse_sse_frames lines 33-69)
    - api/routes/onboarding.py (the module to test — patch onboarding_module.run_turn and the DB helpers)
    - .planning/phases/03-coaching-loop/03-PATTERNS.md (test_onboarding.py section lines 367-409 — mock_interview_run_turn and the SSE assertion)
    - .planning/phases/03-coaching-loop/03-VALIDATION.md (ONBD test rows — exact function names)
  </read_first>
  <action>
    Replace the skipped onboarding test bodies with real tests, monkeypatching onboarding_module.run_turn and the DB helpers (create_conversation, save_messages, load_conversation) so no live Anthropic call and no live Supabase call occur:
    - test_onboarding_returns_sse: POST /onboarding/start with json {"user_id": TEST_USER_ID}; assert status 200 and content-type contains text/event-stream; parse frames and assert at least one token frame and a done frame appear.
    - test_confirmation_gate (ONBD-04): drive a mock run_turn whose sequence does NOT call save_profile until after a user-approval message; assert that in the emitted sequence no tool_start for save_profile appears before the approval token. Since the gate is enforced by the system prompt (LLM behavior), this test asserts the structural contract: the mock that approves-then-saves yields save_profile only after the approval marker, and a mock that saves-without-approval is documented as a manual-only verification (cross-reference 03-VALIDATION manual-only row). Implement the structural assertion on the mock sequence.
    - test_back_status_constraint (ONBD-02): call save_profile directly (from sports_science.profile) with back_status="moderate" under a mocked _get_async_supabase capturing the upsert payload; assert the captured constraints JSONB equals {"back_issues": True, "load_ramp_flag_threshold_pct": 10}.
    - test_profile_persisted (ONBD-03): call save_profile with mocked client; assert the upsert was invoked on table "profiles" with on_conflict user_id and result.value["saved"] is True.
    Keep mock_interview_run_turn and parse_sse_frames reusable. No @pytest.mark.asyncio needed (asyncio_mode=auto).
  </action>
  <verify>
    <automated>python -m pytest tests/api/test_onboarding.py -x -q && echo ONBOARDING_TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_onboarding.py has no remaining `pytest.mark.skip` on test_onboarding_returns_sse, test_back_status_constraint, test_profile_persisted, test_confirmation_gate
    - test_onboarding_returns_sse asserts 200 + text/event-stream and parses at least one token and one done frame
    - test_back_status_constraint asserts the moderate constraints JSONB is {"back_issues": True, "load_ramp_flag_threshold_pct": 10}
    - `python -m pytest tests/api/test_onboarding.py -x -q` passes
    - `python -m pytest tests/ -x -q` passes (no regression)
  </acceptance_criteria>
  <done>Onboarding endpoint, back-status persistence, profile persistence, and the approval-gate contract are covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /onboarding/start | Untrusted user_id and interview content cross into the agent + DB |
| LLM output → SSE stream | Assistant text must pass the trust scanner before reaching the client (TRUST-03, reused from Phase 2) |
| backend → Supabase (conversations/messages) | Writes use SERVICE_ROLE_KEY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | user_id on /onboarding/start | mitigate | user_id is taken as a Body param; auth middleware is deferred to Phase 4 per CONTEXT — documented as accepted gap; for Phase 3 the endpoint is backend/test-only and not exposed to untrusted clients |
| T-03-08 | Information Disclosure | unsourced physiological numbers in interview | mitigate | sse_generator passes scan_buffer as trust_scanner to run_turn; TRUST-03 enforcement is inherited unchanged from Phase 2 |
| T-03-09 | Tampering | conversation/message rows | mitigate | RLS user-owns-row on conversations and messages (from 0001); backend writes via SERVICE_ROLE_KEY; load_conversation filters by conversation_id |
| T-03-10 | Repudiation | onboarding profile changes | mitigate | save_profile upsert records inputs in ToolResult.inputs; messages persisted for audit trail |
| T-03-SC | Tampering | npm/pip installs | accept | No new package installs in this plan (fitdecode added in 03-02) |
</threat_model>

<verification>
- `python -m pytest tests/api/test_onboarding.py tests/agent/test_sse.py -x -q` passes
- `python -m pytest tests/ -x -q` passes (full regression)
- POST /onboarding/start present in app.routes; ONBOARDING_SYSTEM_PROMPT names the 6 fields and the approval gate
</verification>

<success_criteria>
- POST /onboarding/start returns SSE with the onboarding prompt
- run_turn accepts a dynamic system prompt (D-22)
- Conversations are DB-backed (create/load/save); chat.py no longer uses the in-memory placeholder
- ONBD-01..04 tests pass; existing suite green
</success_criteria>

<output>
Create `.planning/phases/03-coaching-loop/03-03-SUMMARY.md` when done
</output>
