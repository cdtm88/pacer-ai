---
phase: 13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/format.ts
  - frontend/src/components/history/RideRow.tsx
  - frontend/src/tests/format.test.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [ADAPT-04, TRANSP-03]
security_asvs_level: 1
security_block_on: high
must_haves:
  truths:
    - "The frontend `Adaptation` interface matches the real `adaptations` table columns (trigger, signal_count, scope, explanation_text, status, trigger_session_ids, created_at) so rows render real data instead of undefined (fixes Contract Mismatch)"
    - "triggerLabel('missed') returns 'Missed session', triggerLabel('underperformance') returns 'Underperformance', triggerLabel('overreaching') returns 'Overreaching', and any unrecognized value falls back to titleCase without throwing"
    - "formatDate(iso) returns a 'Mon, Jul 6'-shaped string (weekday short, month short, numeric day) and returns the raw input on an unparseable date"
    - "RideRow renders the identical date output after switching to the shared formatDate export (no visual regression)"
    - "checkAdaptations() POSTs to /api/adaptations/check via apiFetch and throws on a non-ok response"
  artifacts:
    - "frontend/src/lib/format.ts::triggerLabel"
    - "frontend/src/lib/format.ts::formatDate"
    - "frontend/src/lib/api.ts::checkAdaptations"
    - "frontend/src/lib/api.ts (corrected Adaptation interface)"
    - "frontend/src/tests/format.test.ts"
  key_links:
    - "ProgressScreen (13-03) imports triggerLabel + formatDate + the corrected Adaptation type from these files"
    - "useAdaptationCheck (13-02) imports checkAdaptations from api.ts"
---

<objective>
Lay the shared contract + formatting foundation both feature slices depend on. This plan fixes the pre-existing `Adaptation` interface Contract Mismatch (13-RESEARCH.md `## Contract Mismatch`), adds the `checkAdaptations()` POST wrapper (ADAPT-04 caller half), and adds the two pure display transforms the log UI needs: `triggerLabel` (humanizes the DB `trigger` per D-09) and a shared `formatDate` extracted from RideRow.

Purpose: The corrected interface MUST land before the Adaptations UI (13-03) so the UI is written against real field names; building against the stale interface silently renders `undefined` for every row (13-RESEARCH.md Pitfall 1). The format helpers and `checkAdaptations` are the shared symbols 13-02 and 13-03 import.
Output: corrected `Adaptation` type + `checkAdaptations()` in `api.ts`; `triggerLabel` + `formatDate` in `format.ts`; RideRow re-pointed to the shared `formatDate`; `format.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-RESEARCH.md
@.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add triggerLabel + shared formatDate to format.ts, re-point RideRow</name>
  <files>frontend/src/lib/format.ts, frontend/src/components/history/RideRow.tsx, frontend/src/tests/format.test.ts</files>
  <read_first>
    - frontend/src/lib/format.ts (target file — mirror the existing ZONE_LABELS + sessionTypeLabel Record-lookup + titleCase-fallback pattern)
    - frontend/src/components/history/RideRow.tsx (source of the private formatDate at lines 56-66; will import the shared one and delete the private copy)
    - frontend/src/tests/zones.test.ts (analog for a lib-level unit test file structure)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-PATTERNS.md (lines 268-313 — exact TRIGGER_LABELS map + formatDate body to extract)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-UI-SPEC.md (lines 113-124 — trigger humanization contract + date-format requirement)
  </read_first>
  <behavior>
    - triggerLabel('missed') === 'Missed session'
    - triggerLabel('underperformance') === 'Underperformance'
    - triggerLabel('overreaching') === 'Overreaching'
    - triggerLabel('some_new_value') === titleCase('some_new_value') (fallback, no throw)
    - triggerLabel(null) and triggerLabel(undefined) === 'Adaptation' (defensive default)
    - formatDate('2026-07-06T12:00:00Z') matches /Jul/ and /6/ and contains a short weekday (e.g. 'Mon, Jul 6')
    - formatDate('not-a-date') returns the raw input string (no throw)
  </behavior>
  <action>
    RED first: create frontend/src/tests/format.test.ts covering the seven behavior cases above, using the same describe/it/expect Vitest structure as zones.test.ts. Run it and confirm it fails (symbols not yet exported).

    GREEN: In frontend/src/lib/format.ts add a TRIGGER_LABELS Record with keys missed/underperformance/overreaching mapping to 'Missed session'/'Underperformance'/'Overreaching' (the D-09 humanization contract locked in 13-UI-SPEC.md lines 113-121), and export a triggerLabel(trigger: string | null | undefined) that returns 'Adaptation' when falsy, else TRIGGER_LABELS[trigger] ?? titleCase(trigger) — exactly mirroring the shape of the existing sessionTypeLabel. Also export a formatDate(isoDate: string) that returns new Date(isoDate).toLocaleDateString(undefined, { weekday: 'short', month: 'short', day: 'numeric' }) inside a try/catch that returns isoDate on failure — moved verbatim from RideRow.tsx so the output format is byte-identical (D-09 requires the Adaptations date to match the Ride log date exactly).

    Then update frontend/src/components/history/RideRow.tsx: add `import { formatDate } from '../../lib/format'`, and delete its now-duplicate private formatDate function (lines 56-66). RideRow's call site (formatDate(ride.ride_date)) is unchanged. Per this project's no-em-dashes rule, keep any comments/copy em-dash-free.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/format.test.ts src/tests/history.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - format.test.ts passes all seven behavior cases
    - history.test.tsx still passes (RideRow renders unchanged after the formatDate extraction — no visual regression)
    - RideRow.tsx has no local `function formatDate` remaining; it imports formatDate from ../../lib/format
  </acceptance_criteria>
  <done>triggerLabel + formatDate are exported from lib/format.ts, tested, and RideRow consumes the shared formatDate with identical output.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Adaptation interface to real schema + add checkAdaptations() in api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (target — Adaptation interface at lines 133-139; getAdaptations at 213-217 stays as-is; createConversation at 222-232 and markSessionMissed at 258-274 are the POST-wrapper analogs; apiFetch at 12-23)
    - supabase/migrations/0002_phase3_schema.sql (real adaptations table columns)
    - supabase/migrations/0005_phase6_persistence.sql (adaptations status + trigger_session_ids additions)
    - backend/routes/adaptations.py (lines 728-777 — list_adaptations does select("*"); check_adaptations returns {signals, scope, result} and takes no request body)
    - .planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-PATTERNS.md (lines 37-82 — corrected interface + checkAdaptations shape)
  </read_first>
  <action>
    Replace the stale Adaptation interface (currently fields id, session_id, adaptation_type, description, created_at — none of session_id/adaptation_type/description exist in the real table) with the corrected interface from 13-PATTERNS.md lines 39-49: id (string), trigger ('missed' | 'underperformance' | 'overreaching'), signal_count (optional number|null), scope ('micro' | 'macro'), explanation_text (string), status (optional 'applied' | 'proposed' | 'superseded' | null), trigger_session_ids (optional string[]|null), created_at (string). Mark signal_count/status/trigger_session_ids optional-and-nullable per the T-13-01 mitigation (schema allows these to be null/defaulted; the UI must not crash on a malformed row). Leave getAdaptations() unchanged (it already returns Promise<Adaptation[]>).

    Add checkAdaptations() following the createConversation POST-wrapper convention but simpler (check_adaptations takes no body): call apiFetch('/api/adaptations/check', { method: 'POST' }); if !res.ok throw new Error with the status; return res.json(). Return type Promise<unknown> is sufficient since the fire-and-forget caller (13-02) ignores the body (D-04); typing the {signals, scope, result} shape is optional discretion. Add a one-line comment noting this is the ADAPT-04 weekly-check caller.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with the corrected interface (no consumer references the removed fields)
    - Adaptation interface exposes trigger, scope, explanation_text, created_at as required and signal_count/status/trigger_session_ids as optional/nullable
    - checkAdaptations is exported, POSTs to /api/adaptations/check via apiFetch, and throws on a non-ok response
  </acceptance_criteria>
  <done>api.ts exposes a schema-accurate Adaptation type and a checkAdaptations() POST wrapper; the project typechecks clean.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 01)

- `frontend/src/lib/format.ts::triggerLabel` — humanizes DB `trigger` values (missed/underperformance/overreaching), titleCase fallback
- `frontend/src/lib/format.ts::formatDate` — shared 'Mon, Jul 6'-shaped date formatter, extracted from RideRow
- `frontend/src/lib/api.ts::checkAdaptations` — POST /api/adaptations/check wrapper (ADAPT-04 caller)
- `frontend/src/lib/api.ts` corrected `Adaptation` interface — fields: id, trigger, signal_count?, scope, explanation_text, status?, trigger_session_ids?, created_at
- `frontend/src/tests/format.test.ts` — new unit test for triggerLabel + formatDate
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend JSON -> frontend render | `GET /adaptations/` returns rows with nullable columns; the TS type governs whether the UI trusts every field to be present |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Information Disclosure / Robustness | Adaptation interface (api.ts) | medium | mitigate | Mark schema-nullable fields (signal_count, status, trigger_session_ids) optional/nullable in the corrected interface so a malformed/partial row cannot force the downstream UI (13-03) to render against a false "always present" contract; required fields (trigger, scope, explanation_text) match the DB NOT NULL / CHECK columns |

No new endpoints, auth paths, or package installs are introduced (13-RESEARCH.md `## Package Legitimacy Audit`: N/A). ASVS level 1; no `high`-severity threat, so no blocking checkpoint.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/format.test.ts src/tests/history.test.tsx` green
- `cd frontend && npx tsc --noEmit` clean
</verification>

<success_criteria>
- Corrected Adaptation interface + checkAdaptations() exist in api.ts and typecheck clean
- triggerLabel + formatDate exported from format.ts with passing unit tests
- RideRow consumes the shared formatDate with no visual/behavioral regression
</success_criteria>

<output>
Create `.planning/phases/13-close-gap-adapt-04-transp-03-wire-weekly-adaptation-check-ad/13-01-SUMMARY.md` when done
</output>
