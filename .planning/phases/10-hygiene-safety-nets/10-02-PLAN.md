---
phase: 10-hygiene-safety-nets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/full-uat.spec.ts
  - frontend/tests/e2e/phase4.spec.ts
autonomous: true
requirements: [ITEM-03]
must_haves:
  truths:
    - "Playwright ride fixtures use the real backend field names (duration_secs, avg_power), not the pre-Phase-9 names"
    - "The e2e suite passes against fixtures that faithfully mirror the real GET /rides/ response shape"
  artifacts:
    - frontend/tests/e2e/full-uat.spec.ts
    - frontend/tests/e2e/phase4.spec.ts
  key_links:
    - "fixtureRides field names match backend/routes/rides.py .select() columns and frontend api.ts Ride interface"
---

<objective>
Correct the Playwright e2e ride fixtures so they mirror the real backend response contract. Both `full-uat.spec.ts` and `phase4.spec.ts` still use the pre-Phase-9 field names `duration_seconds`/`avg_power_watts` and include phantom fields (`file_name`, `distance_m`) that the real `Ride` shape never had. The Phase 9 field-name fix landed in production code (`backend/routes/rides.py`, `frontend/src/lib/api.ts`) but was never propagated to these mocks, so the e2e tests have been asserting against a shape the API hasn't returned since Phase 9.

Purpose: A mock that lies about the API shape produces false-negative-safe tests (they pass only because the component under test never reads the wrong field). This is scope item 3 — align the mocks with the real contract so the e2e suite is a true guard.

Output: Two updated spec files whose `fixtureRides` faithfully mirror `GET /rides/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hygiene-safety-nets/10-CONTEXT.md
@.planning/phases/10-hygiene-safety-nets/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align fixtureRides in both e2e specs with the real Ride shape</name>
  <files>frontend/tests/e2e/full-uat.spec.ts, frontend/tests/e2e/phase4.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/full-uat.spec.ts (locate `fixtureRides` and every route mock that returns ride rows)
    - frontend/tests/e2e/phase4.spec.ts (same — its `fixtureRides` block)
    - backend/routes/rides.py (the `.select(...)` column list that is the source of truth for the response shape)
    - frontend/src/lib/api.ts (the `Ride` interface at lines 82-96: `id, user_id, session_id, ride_date, duration_secs, np_watts, tss, avg_power, intensity_factor, avg_hr, avg_cadence, ftp_used, compliance_pct`)
    - .planning/phases/10-hygiene-safety-nets/10-RESEARCH.md (Pitfall 3 — the exact rename/removal list, verified against both spec files this session)
  </read_first>
  <action>
    In BOTH spec files, update each ride fixture object so the field names match the real backend/`Ride`-interface contract:
    - RENAME `duration_seconds` → `duration_secs` and `avg_power_watts` → `avg_power`.
    - REMOVE the phantom fields `file_name` and `distance_m` (they do not exist in the real `Ride` shape).
    - Optionally ADD `intensity_factor`, `avg_hr`, `avg_cadence`, `ftp_used` with realistic values so the fixture stays a faithful mirror of the full real response (the UI does not read them today, but the fixture should not diverge from the API).
    Update any assertion in the same specs that referenced the old field names accordingly. Do not change production code (`backend/`, `frontend/src/`) — this task is fixtures/assertions only.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --list</automated>
    <human-check>Run `cd frontend && npx playwright test` locally (browser binaries required, not in CI this phase per D-05) and confirm the ride-history specs pass against the corrected fixtures.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -rc "duration_seconds\|avg_power_watts\|file_name\|distance_m" frontend/tests/e2e/full-uat.spec.ts frontend/tests/e2e/phase4.spec.ts` returns 0 for every stale/phantom token (source assertion: no pre-Phase-9 names remain in the ride fixtures).
    - `grep -rc "duration_secs\|avg_power" frontend/tests/e2e/full-uat.spec.ts frontend/tests/e2e/phase4.spec.ts` returns a non-zero count in each file (the real names are present).
    - `cd frontend && npx playwright test --list` succeeds (specs parse; no TypeScript/syntax error introduced).
    - No file under `backend/` or `frontend/src/` is modified (source assertion).
  </acceptance_criteria>
  <done>Both e2e ride fixtures use `duration_secs`/`avg_power`, drop `file_name`/`distance_m`, and the Playwright suite passes against the corrected shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → asserted behavior | e2e mocks stand in for the real API contract; a wrong shape yields false-green tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Tampering | e2e `fixtureRides` field names | low | mitigate | Align mock field names with the real `GET /rides/` contract so a component that reads `duration_secs`/`avg_power` is actually exercised, not silently passing on a never-read wrong key |
| T-10-02-02 | Repudiation | e2e suite as regression guard | low | accept | Playwright is deliberately out of CI this phase (D-05, browser-binary provisioning); run pre-merge manually. Accepted per D-05 scope |
</threat_model>

## Artifacts this phase produces

- `frontend/tests/e2e/full-uat.spec.ts` — corrected `fixtureRides` shape (no new symbols)
- `frontend/tests/e2e/phase4.spec.ts` — corrected `fixtureRides` shape (no new symbols)

<verification>
- `grep -rc "duration_seconds\|avg_power_watts\|file_name\|distance_m" frontend/tests/e2e/*.spec.ts` → 0 for all stale tokens
- `cd frontend && npx playwright test --list` → specs parse
- Manual: `cd frontend && npx playwright test` passes (pre-merge, human-check)
</verification>

<success_criteria>
- Both e2e ride fixtures mirror the real `Ride` contract exactly.
- No stale/phantom field names remain; Playwright suite passes.
</success_criteria>

<output>
Create `.planning/phases/10-hygiene-safety-nets/10-02-SUMMARY.md` when done.
</output>
