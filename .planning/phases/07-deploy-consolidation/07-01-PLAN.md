---
phase: 07-deploy-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - railway.toml
  - requirements.txt
  - README.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [DEPLOY-RAIL-01, DEPLOY-DOC-01]
must_haves:
  truths:
    - "Repository contains no Railway/Docker deploy artifacts (Dockerfile and railway.toml deleted)"
    - "requirements.txt no longer lists gunicorn; uvicorn retained for local dev"
    - "README documents Vercel as the sole deploy target with a corrected, complete backend env-var table"
    - ".claude/CLAUDE.md describes Vercel-only deployment (no Railway or Gunicorn as active targets)"
    - "README API endpoint list shows GET /chat/stream (not POST)"
  artifacts:
    - "README.md (corrected deploy, env-var, and endpoint sections)"
    - ".claude/CLAUDE.md (Railway references removed/corrected)"
    - "requirements.txt (gunicorn removed)"
  key_links:
    - "README backend env-var names exactly match the actual os.environ.get()/os.getenv() call sites in backend/ and api/"
    - "No real secret values appear in any committed doc — names and one-line purpose only"
---

<objective>
Remove all Railway/Docker deploy artifacts from git and correct the two stale deployment docs (README.md and the project's own .claude/CLAUDE.md) so they describe Vercel as the sole deploy target with an accurate, complete backend environment-variable table.

Purpose: Railway was abandoned 2026-07-03 (STATE.md decision); leaving Dockerfile/railway.toml/gunicorn and Railway-oriented docs in the repo is dead weight that misleads contributors and documents a deploy path that no longer exists.
Output: Deleted Dockerfile + railway.toml, gunicorn removed from requirements.txt, rewritten README deploy/env/endpoint sections, corrected .claude/CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy-consolidation/07-RESEARCH.md
@.planning/phases/07-deploy-consolidation/07-PATTERNS.md
</context>

<artifacts>
Artifacts this plan produces (files created / modified / deleted):
- DELETE `Dockerfile` (Railway/Docker-only build)
- DELETE `railway.toml` (Railway deploy config)
- MODIFY `requirements.txt` — remove line `gunicorn==22.*` (line 11); keep `uvicorn==0.30.*` (local dev)
- MODIFY `README.md` — rewrite `### Backend: Railway` deploy subsection, `### Backend (.env or Railway env vars)` env table, fix `POST /chat/stream` -> `GET /chat/stream`, fix `SUPABASE_SERVICE_KEY` -> `SUPABASE_SERVICE_ROLE_KEY`
- MODIFY `.claude/CLAUDE.md` — correct 5 Railway references (lines 14, 57, 101, 104, 149)
No new symbols or modules are created.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Delete Railway/Docker artifacts and remove gunicorn dependency</name>
  <read_first>Dockerfile, railway.toml, requirements.txt, .planning/phases/07-deploy-consolidation/07-RESEARCH.md (Runtime State Inventory + Open Question 3: keep uvicorn, remove only gunicorn)</read_first>
  <files>Dockerfile, railway.toml, requirements.txt</files>
  <action>Delete the files `Dockerfile` and `railway.toml` at repo root (Railway/Docker-only; unused by Vercel's Python runtime which invokes the ASGI app object directly). In `requirements.txt`, remove the `gunicorn==22.*` line only. Do NOT remove `uvicorn==0.30.*` — per RESEARCH.md Open Question 3, local dev still uses the `uvicorn api.main:app --reload` command documented in the README, so uvicorn must stay. No other requirements lines change.</action>
  <verify>
    <automated>test ! -e Dockerfile && test ! -e railway.toml && ! grep -q '^gunicorn' requirements.txt && grep -q '^uvicorn' requirements.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>Dockerfile and railway.toml no longer exist; requirements.txt has no gunicorn line but still has uvicorn.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite README deploy, env-var, and endpoint sections for Vercel-only</name>
  <read_first>README.md (lines 68-181 in particular), backend env-var call sites confirmed in RESEARCH.md/PATTERNS.md, backend/routes/chat.py (@router.get("/stream") at line ~69 confirms GET method)</read_first>
  <files>README.md</files>
  <action>Make four corrections, preserving the existing Markdown pipe-table format (RESEARCH.md Security Domain: document variable NAMES and one-line purpose only, never paste real .env values):
1. Replace the entire `### Backend: Railway` deployment subsection (README lines ~147-158, including the Dockerfile/gunicorn/railway.toml prose) with a Vercel-only backend deployment description. Describe it at the architecture level so it stays accurate regardless of the vercel.json routing outcome decided in plan 07-04: the FastAPI app runs as a Vercel Python Function (entrypoint api/index.py), the frontend builds to a static bundle, and request routing between them is configured in the root vercel.json. Do NOT hard-code the word "services" here (plan 07-04 may fall back to the single-function model); keep it model-agnostic.
2. In the API Endpoints list (README line ~72), change `POST /chat/stream` to `GET /chat/stream` and add a short note that it is GET because the browser EventSource API can only issue GET requests (RESEARCH.md Pitfall 4).
3. Rename the env-var table heading `### Backend (.env or Railway env vars)` (line ~169) to drop "Railway" (e.g. `### Backend (.env or Vercel env vars)`). Fix the row `SUPABASE_SERVICE_KEY` -> `SUPABASE_SERVICE_ROLE_KEY`. Remove the `PORT` row (Gunicorn/Railway-only). Add rows for the four undocumented backend vars actually read by the code: `SUPABASE_JWT_SECRET`, `CALENDAR_FERNET_KEY`, `BACKEND_BASE_URL`, `ANTHROPIC_MODEL` (each with a one-line purpose). Keep existing valid rows: ANTHROPIC_API_KEY, SUPABASE_URL, FRONTEND_URL, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET.
4. In the `### Backend` Local Development block, fix the `cp .env.example .env` comment (line ~113) that lists `SUPABASE_SERVICE_KEY` -> `SUPABASE_SERVICE_ROLE_KEY`.
Add a one-line note under the env table that these vars must be set in Vercel Project Settings -> Environment Variables (per environment) for production.</action>
  <verify>
    <automated>grep -q 'SUPABASE_SERVICE_ROLE_KEY' README.md && grep -q 'SUPABASE_JWT_SECRET' README.md && grep -q 'CALENDAR_FERNET_KEY' README.md && grep -q 'BACKEND_BASE_URL' README.md && grep -q 'ANTHROPIC_MODEL' README.md && grep -q 'GET /chat/stream' README.md && ! grep -q 'SUPABASE_SERVICE_KEY ' README.md && ! grep -qi 'Backend: Railway' README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>README deployment section describes Vercel-only; env table lists all five previously-missing/renamed vars, drops PORT, and uses SUPABASE_SERVICE_ROLE_KEY; endpoint list shows GET /chat/stream; no "Backend: Railway" heading remains; no real secret values present.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Correct Railway references in .claude/CLAUDE.md</name>
  <read_first>.claude/CLAUDE.md (lines 14, 57, 101, 104, 149 identified in 07-PATTERNS.md)</read_first>
  <files>.claude/CLAUDE.md</files>
  <action>Correct the 5 stale Railway references enumerated in 07-PATTERNS.md, preserving surrounding table/prose structure:
- Line 14 (Constraints > Tech Stack): change `Vercel (frontend) + Railway (API/DB)` to `Vercel (frontend + backend)`.
- Line 57 (Uvicorn row rationale): remove the "pair with Gunicorn for Railway" clause; state Uvicorn is used for local dev (Vercel's Python runtime invokes the ASGI app directly, no process manager).
- Line 101 (Dev/Deploy table Railway row): remove the `Railway | FastAPI + Uvicorn hosting ...` row (or replace with a Vercel Python Runtime row if one is not already present in that table).
- Line 104 (PostgreSQL row): change `Railway or Supabase` to `Supabase`.
- Line 149 (Confidence Notes Railway row): remove the `Railway for FastAPI` confidence row (no longer a live decision).
Also scan the "What NOT to Use" and Dev/Deploy tables for any remaining `Gunicorn + Uvicorn workers` / Railway rows that now describe a dead path and correct them to reflect the Vercel Python runtime. Do not otherwise restructure the file.</action>
  <verify>
    <automated>! grep -qi 'railway' .claude/CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>No occurrence of "Railway" (case-insensitive) remains in .claude/CLAUDE.md; the Tech Stack constraint reads "Vercel (frontend + backend)"; PostgreSQL host is Supabase only.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> public git history | Committed README.md/CLAUDE.md are readable by anyone with repo access; must never contain real secret values |
| documented env-var names -> production config | Env-var names in docs are the contract operators follow when configuring Vercel; a wrong/missing name causes a production misconfiguration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | README.md / .claude/CLAUDE.md env-var tables | high | mitigate | Document variable NAMES and one-line purpose only; Task 2 verify greps for the required names, and the executor must not paste any real `.env`/`.env.local` value (RESEARCH.md Security Domain) |
| T-07-01-02 | Tampering | requirements.txt | low | mitigate | Remove only the `gunicorn` line; verify uvicorn and all other deps remain (Task 1 automated check) so no runtime dependency is accidentally dropped |
| T-07-01-03 | Information Disclosure | stale docs implying Railway still holds SUPABASE_SERVICE_ROLE_KEY | medium | transfer | Actual live-service decommission is owned by plan 07-04's cutover checkpoint; this plan only removes the misleading in-repo references |
</threat_model>

<verification>
- `test ! -e Dockerfile && test ! -e railway.toml` — Railway artifacts gone
- `grep -c '^gunicorn' requirements.txt` returns 0; `grep -c '^uvicorn' requirements.txt` returns 1
- README env table contains SUPABASE_SERVICE_ROLE_KEY, SUPABASE_JWT_SECRET, CALENDAR_FERNET_KEY, BACKEND_BASE_URL, ANTHROPIC_MODEL and no PORT row
- `grep -i railway .claude/CLAUDE.md` returns nothing
- `pytest tests/ -q` still green (docs/config-only change must not break the suite)
</verification>

<success_criteria>
Railway/Docker artifacts deleted, gunicorn removed, README and .claude/CLAUDE.md describe Vercel-only deployment with an accurate backend env-var table (correct key names, no PORT, five previously-missing vars added, no real secret values), and the API endpoint list shows GET /chat/stream.
</success_criteria>

<output>
Create `.planning/phases/07-deploy-consolidation/07-01-SUMMARY.md` when done
</output>
