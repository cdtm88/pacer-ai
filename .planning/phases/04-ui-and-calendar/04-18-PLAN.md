---
phase: 04-ui-and-calendar
plan: 18
type: execute
wave: 8
depends_on: [04-15]
files_modified:
  - frontend/src/components/session/SessionCard.tsx
autonomous: true
gap_closure: true
requirements: [UI-02]
must_haves:
  truths:
    - "Clicking Mark Missed opens a real alert dialog (role=alertdialog) titled 'Mark this session as missed?' with the re-plan body copy"
    - "Confirming via 'Yes, mark missed' triggers the re-plan (markSessionMissed) and closes the dialog; 'Keep it' cancels without firing the re-plan"
    - "No em dashes appear anywhere in the dialog copy"
  artifacts:
    - path: "frontend/src/components/session/SessionCard.tsx"
      provides: "Mark Missed confirmation rendered with the shadcn AlertDialog component instead of an inline conditional block"
      contains: "AlertDialog"
  key_links:
    - from: "frontend/src/components/session/SessionCard.tsx"
      to: "frontend/src/components/ui/alert-dialog.tsx"
      via: "imports AlertDialog primitives and renders the missed-confirmation as a real alertdialog"
      pattern: "alert-dialog"
    - from: "frontend/src/components/session/SessionCard.tsx"
      to: "api/routes/adaptations.py"
      via: "AlertDialogAction onClick -> handleMarkMissed -> markSessionMissed POST /adaptations/sessions/{id}/missed"
      pattern: "handleMarkMissed"
---

<objective>
Close UAT GAP 2 (Test 8: "mark missed popup is just broken"). SessionCard currently renders the mark-missed confirmation as an inline state-toggled block (missedOpen ? confirmation : buttons), which produces no role=alertdialog in the DOM and reads as broken to the user. The shadcn AlertDialog component already exists at frontend/src/components/ui/alert-dialog.tsx but is never imported here.

Replace the inline confirmation with a real AlertDialog bound to the existing missedOpen state. The dialog copy, button labels, and handlers stay identical so 04-15's query-key invalidation and the existing Playwright assertions keep passing.

Purpose: UI-02 (Today session card actions). Mark Missed must present a proper confirmation dialog before triggering the re-plan.

Output: SessionCard imports AlertDialog primitives and renders the missed-confirmation as a controlled AlertDialog (open bound to missedOpen, onOpenChange bound to setMissedOpen), with the confirm action calling handleMarkMissed and the cancel action calling setMissedOpen(false). The four primary action buttons always render; the Mark missed button opens the dialog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UAT.md
@frontend/src/components/session/SessionCard.tsx
@frontend/src/components/ui/alert-dialog.tsx
@frontend/tests/e2e/full-uat.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the inline missed-confirmation block with a controlled AlertDialog</name>
  <files>frontend/src/components/session/SessionCard.tsx</files>
  <read_first>
    - frontend/src/components/session/SessionCard.tsx lines 62-65: existing state — missedOpen / setMissedOpen, isMissedLoading. Keep these; AlertDialog will be controlled by missedOpen.
    - frontend/src/components/session/SessionCard.tsx lines 88-101: handleMarkMissed — calls markSessionMissed, then setMissedOpen(false) on success, invalidates the today and upcoming query keys, toasts on error, resets isMissedLoading in finally. Keep this handler unchanged.
    - frontend/src/components/session/SessionCard.tsx lines 181-252: the action row. Currently renders the inline confirmation when missedOpen is true, else the Start / Export / Mark done / Mark missed buttons. The inline confirmation (lines 183-207) must become an AlertDialog; the four-button block (lines 208-251) must ALWAYS render, with Mark missed opening the dialog.
    - frontend/src/components/ui/alert-dialog.tsx lines 183-197: exported primitives — AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger (plus Overlay/Portal/Media). AlertDialogContent renders Radix Content with role of alertdialog. AlertDialogAction and AlertDialogCancel render Buttons.
    - frontend/tests/e2e/full-uat.spec.ts lines 411-453: the two assertions that MUST keep passing — (a) click button named /Mark missed/i, expect the text "Mark this session as missed?" visible, click button named exactly "Yes, mark missed", then expect that text NOT visible and the missed POST fired; (b) read the alertdialog innerText and assert no em dash. These dictate the required labels and the alertdialog role.
  </read_first>
  <action>
    Convert the missed-confirmation to a controlled shadcn AlertDialog while preserving every label, handler, and the existing missedOpen state.

    1. Import the needed primitives at the top of SessionCard.tsx from '@/components/ui/alert-dialog': AlertDialog, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel.

    2. Remove the missedOpen ternary in the action column. The four primary action buttons (Start session, Export to Zwift, Mark done, Mark missed) must ALWAYS render. Keep their existing styles, icons, onClick handlers, and disabled bindings. The Mark missed ghost button keeps onClick calling setMissedOpen(true) to open the dialog.

    3. Render a controlled AlertDialog inside the component's returned tree: AlertDialog with open bound to missedOpen and onOpenChange bound to setMissedOpen, containing an AlertDialogContent with:
       - AlertDialogHeader containing AlertDialogTitle with EXACT text: Mark this session as missed?
       - AlertDialogHeader containing AlertDialogDescription with EXACT text: This will trigger a re-plan. Your coach will adjust upcoming sessions.
       - AlertDialogFooter containing the confirm and cancel controls (see step 4).
       Because the dialog is controlled by missedOpen via onOpenChange, do NOT use AlertDialogTrigger to open it — the Mark missed button drives missedOpen. Radix still calls onOpenChange(false) on overlay/escape, which closes via setMissedOpen.

    4. Confirm and cancel controls with retry-on-failure semantics:
       - Cancel: render AlertDialogCancel with EXACT label text "Keep it"; its onClick calls setMissedOpen(false).
       - Confirm: the label is EXACTLY "Yes, mark missed", styled destructive (background var(--color-bad), white text), disabled bound to isMissedLoading, onClick calls handleMarkMissed. handleMarkMissed already calls setMissedOpen(false) ONLY after a successful markSessionMissed (kept from 04-15), so a failed re-plan must leave the dialog OPEN for retry. Radix AlertDialogAction auto-closes the dialog on click, which would defeat retry-on-failure. To keep the dialog open on failure, render the confirm control as a plain Button (imported from '@/components/ui/button') inside the AlertDialogFooter rather than AlertDialogAction, so closing is owned solely by handleMarkMissed's success path. (If you instead use AlertDialogAction, you must call e.preventDefault() in its onClick to suppress the auto-close; the plain-Button approach is preferred for reliability.) Document the chosen approach in a one-line code comment.

    5. Em dash discipline: the dialog copy uses re-plan with a hyphen and ordinary sentences — no em dashes. The rendered AlertDialog text must contain no em-dash character (the spec asserts this against the alertdialog element).

    Do not change handleMarkDone, the Start/Export buttons, the ZwoExportModal, or the query-key invalidation. Do not add new dependencies — radix-ui already backs alert-dialog.tsx.
  </action>
  <verify>
    <automated>cd frontend && grep -q "alert-dialog" src/components/session/SessionCard.tsx && grep -q "Mark this session as missed?" src/components/session/SessionCard.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SessionCard imports AlertDialog primitives from '@/components/ui/alert-dialog'
    - The four primary action buttons always render; Mark missed opens the dialog via setMissedOpen(true)
    - Clicking Mark missed shows an element with the alertdialog role whose title is exactly "Mark this session as missed?" and body is exactly "This will trigger a re-plan. Your coach will adjust upcoming sessions."
    - The confirm control is labeled exactly "Yes, mark missed", calls handleMarkMissed, and closes the dialog only on success (stays open on failure for retry); the cancel control is labeled exactly "Keep it" and closes without firing the re-plan
    - No em dash character appears in the dialog copy
    - tsc --noEmit passes and npm run build succeeds
  </acceptance_criteria>
  <done>Mark Missed renders a real shadcn AlertDialog with the correct title, body, and buttons; confirming fires the re-plan and closes on success, cancel/failure keeps state intact, and the existing Playwright Mark Missed flow passes.</done>
</task>

</tasks>

<verification>
- SessionCard imports and renders AlertDialog (grep confirms "alert-dialog"); title text present
- Confirm closes on success only; cancel closes without firing; no em dashes in dialog
- Frontend type-checks and builds
- Run the Mark Missed e2e: cd frontend && npx playwright test full-uat --grep "Mark Missed" — both cases pass
</verification>

<success_criteria>
- Mark Missed shows a proper alert dialog and confirming triggers the re-plan (UAT GAP 2 closed)
- The existing Playwright Mark Missed confirm-flow and no-em-dash assertions pass
</success_criteria>

<artifacts_produced>
This gap-closure plan produces no new files. It modifies:
- frontend/src/components/session/SessionCard.tsx (inline confirmation replaced by a controlled AlertDialog)
No new exported symbols.
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-18-SUMMARY.md` when done
</output>
