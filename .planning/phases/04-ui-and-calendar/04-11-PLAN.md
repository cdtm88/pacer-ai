---
phase: 04-ui-and-calendar
plan: 11
type: execute
wave: gap_closure
depends_on: ["04-10"]
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - CAL-03
user_setup:
  - service: google-cloud-console
    why: "CAL-03 requires the OAuth consent screen to be published (In production), not in Testing mode. Testing mode limits authorization to whitelisted test accounts only, blocking any real user from connecting Google Calendar."
    dashboard_config:
      - task: "Confirm OAuth consent screen is published as In production"
        location: "Google Cloud Console -> APIs & Services -> OAuth consent screen"

must_haves:
  truths:
    - "Google Cloud OAuth consent screen shows 'In production' status for the PacerAI app"
    - "The calendar.events scope is listed and approved on the consent screen"
    - "Real users outside the test-user whitelist can complete the Google Calendar OAuth flow"
  artifacts:
    - path: "api/routes/calendar.py"
      provides: "OAuth flow (GET /calendar/auth, GET /calendar/callback) — already implemented; this plan confirms the cloud-side configuration matches"
  key_links:
    - from: "frontend/src/screens/SettingsScreen.tsx"
      to: "api/routes/calendar.py"
      via: "GET /calendar/auth redirect to Google OAuth"
      pattern: "calendar/auth"
---

<objective>
Confirm that the Google Cloud OAuth consent screen for PacerAI is published as "In production" (not "Testing"), satisfying CAL-03.

This is a human-only verification step. No code changes are required — the OAuth implementation in calendar.py, the Fernet token encryption, and the /calendar/auth redirect flow are all correct and verified in prior plans. The sole outstanding item is the Google Cloud Console publication status, which cannot be confirmed from code.

Purpose: Until the app is published, only whitelisted test accounts can authorize the calendar connection. Any real user clicking "Connect Google Calendar" in Settings will see a blocked or unverified-app page.

Output: Human confirms "In production" status; CAL-03 is marked verified; Phase 4 is complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ui-and-calendar/04-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm Google Cloud OAuth consent screen is published (CAL-03)</name>
  <files>api/routes/calendar.py</files>
  <what-built>
    The Google Calendar OAuth integration (calendar.py) is fully implemented: GET /calendar/auth builds the authorization URL with the calendar.events scope, GET /calendar/callback stores Fernet-encrypted tokens in the DB, and SettingsScreen displays the connected/disconnected state. The only unverified item is whether the Google Cloud project's OAuth consent screen has been published.
  </what-built>
  <action>
    No code change required. Human must confirm publication status in Google Cloud Console (see how-to-verify).

    If status is "Testing", click "Publish app" and confirm the transition to "In production" before resuming.
  </action>
  <how-to-verify>
    1. Open https://console.cloud.google.com
    2. Select the PacerAI project (the one that owns the GOOGLE_CLIENT_ID in Railway env vars)
    3. Navigate to: APIs & Services -> OAuth consent screen
    4. Check the "Publishing status" field:
       - PASS: Shows "In production"
       - FAIL: Shows "Testing" — any non-whitelisted user will see "Access blocked: This app is not verified"

    If status is "Testing":
    - Click "Publish app" to move to production
    - If a verification warning appears for sensitive scopes, click "Confirm" — calendar.events is sensitive but Google does not block production publishing for it; verification is only required for restricted scopes (e.g. gmail.readonly)
    - Confirm the status has changed to "In production" before resuming

    5. Also confirm:
       - "calendar.events" (or "https://www.googleapis.com/auth/calendar.events") is listed under Scopes
       - The app name shown is "PacerAI"
  </how-to-verify>
  <verify>
    <human-check>Google Cloud Console OAuth consent screen shows "In production" and calendar.events scope is listed.</human-check>
  </verify>
  <done>Publishing status is "In production"; calendar.events scope is listed; any real user can complete the Google Calendar OAuth flow without being blocked.</done>
  <resume-signal>
    Type "confirmed — in production" once the consent screen shows "In production" and the calendar.events scope is listed.
    If you needed to publish the app, type "published — now in production".
    If the project cannot be found or you do not have access, type "blocked — [reason]" and we will investigate.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google OAuth -> PacerAI API | Authorization code exchanged at /calendar/callback; token stored Fernet-encrypted in DB, never sent to browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Information Disclosure | OAuth tokens in DB | mitigate | Fernet encryption confirmed in calendar.py; tokens never returned to frontend |
| T-04-11-02 | Elevation of Privilege | calendar.events scope | accept | Scope is per-user; tokens are user_id-scoped in DB; no cross-user access possible |
| T-04-11-03 | Spoofing | OAuth state parameter (CSRF) | mitigate | CSRF state param generated and verified in calendar.py GET /calendar/callback |
</threat_model>

<verification>
Human verification only for this plan — no automated assertions can confirm Google Cloud Console publication status.

After human confirmation:
- Phase 4 CAL-03 is verified
- All five gaps from 04-VERIFICATION.md are resolved (gaps 1-4 fixed in prior commits WR-005 through WR-009; gap 5 confirmed here)
- Phase 4 is complete
</verification>

<success_criteria>
- Google Cloud Console OAuth consent screen shows "In production" for the PacerAI app
- calendar.events scope is listed on the consent screen
- Human has confirmed the above via resume signal
</success_criteria>

<output>
Create `.planning/phases/04-ui-and-calendar/04-11-SUMMARY.md` when done
</output>
