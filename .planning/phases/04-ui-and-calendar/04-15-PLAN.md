---
phase: 04-ui-and-calendar
plan: 15
type: execute
wave: 7
depends_on: [04-05, 04-09, 04-14]
files_modified:
  - frontend/src/components/session/SessionCard.tsx
  - frontend/src/lib/api.ts
autonomous: true
gap_closure: true
requirements: [UI-02]
must_haves:
  truths:
    - "Clicking Mark Done marks the session completed and the card updates without a silent no-op"
    - "Clicking Mark Missed shows the confirmation UI, and confirming triggers the re-plan and updates the card"
    - "When a session action fails, the user sees an error toast rather than nothing happening"
  artifacts:
    - path: "frontend/src/components/session/SessionCard.tsx"
      provides: "Mark Done and Mark Missed handlers that invalidate the correct query keys and surface failures"
      contains: "invalidateQueries"
    - path: "frontend/src/lib/api.ts"
      provides: "markSessionDone and markSessionMissed that throw informative errors on non-ok responses"
      contains: "markSessionDone"
  key_links:
    - from: "frontend/src/components/session/SessionCard.tsx"
      to: "frontend/src/screens/TodayScreen.tsx"
      via: "queryClient.invalidateQueries keys match the keys TodayScreen queries with"
      pattern: "invalidateQueries"
    - from: "frontend/src/components/session/SessionCard.tsx"
      to: "api/routes/sessions.py"
      via: "markSessionDone -> PATCH /sessions/{id}; markSessionMissed -> POST /adaptations/sessions/{id}/missed"
      pattern: "markSessionDone|markSessionMissed"
---

<objective>
Close UAT GAP 2 (Mark Session Missed popup broken, Test 8) and UAT GAP 3 (Mark Session Done does nothing, Test 9). The backend routes both exist (PATCH /sessions/{id} from 04-09; POST /adaptations/sessions/{id}/missed in adaptations.py) and SessionCard already wires markSessionDone / markSessionMissed. The most likely root cause of "does nothing" / "broken popup" is that these requests previously failed auth (no persisted session) — that underlying session bug is fixed in 04-14, which this plan depends on. This plan verifies the actual runtime failure mode against a running stack and hardens the two action flows: correct query-key invalidation so the card visibly updates, and visible error feedback so a failure is never a silent no-op.

Purpose: UI-02 requires the Today session card's Mark Done and Mark Missed actions to work. A button that does nothing (Mark Done) or shows a broken interaction (Mark Missed) makes the primary daily action unusable.

Output: SessionCard invalidates the exact query keys TodayScreen uses so the card refreshes after an action; both api.ts helpers throw informative errors; the handlers surface those errors as toasts. Any concrete defect found during the diagnosis step (e.g. mismatched query keys, missing refetch, a real backend error) is corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UAT.md
@frontend/src/screens/TodayScreen.tsx
@api/routes/adaptations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose the real Mark Done / Mark Missed failure mode against a running stack</name>
  <files>frontend/src/components/session/SessionCard.tsx</files>
  <read_first>
    - frontend/src/components/session/SessionCard.tsx lines 74-99 (handleMarkDone and handleMarkMissed: both call the api helper, invalidate queryKeys ['session','today'] and ['sessions','upcoming'], catch errors into a toast.error; handleMarkMissed also closes the inline confirmation via setMissedOpen(false))
    - frontend/src/components/session/SessionCard.tsx lines 179-250 (the action row: when missedOpen is true it renders an inline confirmation block — heading "Mark this session as missed?", body, "Yes, mark missed" destructive button, "Keep it" cancel button; when false it renders Start/Export/Mark done/Mark missed buttons. This inline block IS the "popup" the UAT calls broken)
    - frontend/src/screens/TodayScreen.tsx (find the exact queryKey arrays used by useQuery for today's session and the upcoming strip — the invalidation in SessionCard must match these key arrays exactly or the card will not refetch and the action will appear to "do nothing")
    - frontend/src/lib/api.ts lines 195-211 (markSessionMissed posts to /adaptations/sessions/{id}/missed; markSessionDone PATCHes /sessions/{id}; both throw on non-ok)
    - api/routes/adaptations.py lines 647-712 (mark_session_missed: verifies ownership, updates status to "missed", re-runs detection, returns marked_missed True — confirm the path is /adaptations/sessions/{session_id}/missed under the adaptations router prefix)
  </read_first>
  <action>
    First DIAGNOSE, then fix. Start the backend and frontend locally (uvicorn api.main:app and cd frontend && npm run dev) with a signed-in session, open the Today screen with a planned session, and observe the network requests for Mark Done and Mark Missed. Capture the actual status codes and response bodies. Record the observed failure mode in the SUMMARY.

    Then resolve the concrete defect you observe. The two most likely defects, in priority order, are:

    1. Query-key mismatch (manifests as "does nothing"): Compare the queryKey arrays in TodayScreen's useQuery calls against the arrays passed to queryClient.invalidateQueries in SessionCard (currently ['session','today'] and ['sessions','upcoming']). If they do not match exactly (same array shape and string values), the card never refetches and a successful action looks like a no-op. Fix by aligning the invalidation keys in SessionCard to the EXACT keys TodayScreen queries with. Cite the TodayScreen key you matched against in a code comment.

    2. A genuine backend error (manifests as a thrown error / "broken"): If a request returns a non-2xx (e.g. 404 because the session id is wrong, or 422/500), trace it to the responsible route and fix the mismatch (path, body, or ownership filter). Do NOT weaken the ownership filters in sessions.py or adaptations.py.

    If, after 04-14's session fix, both actions already function and the card updates, the remaining defect is the silent failure path — proceed to Task 2 to make failures visible, and record in the SUMMARY that the action itself was fixed by the 04-14 session-persistence change.

    Keep the inline confirmation UI structure (heading, body, "Yes, mark missed", "Keep it") — it matches the UAT expected behavior (Test 8). Do not convert it to a portal/modal unless the diagnosis shows the inline block fails to render; if it does fail to render, fix the rendering rather than redesigning.
  </action>
  <verify>
    <automated>cd frontend && grep -q "invalidateQueries" src/components/session/SessionCard.tsx && grep -q "setMissedOpen(false)" src/components/session/SessionCard.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the observed status code and response body for both Mark Done and Mark Missed against a running stack
    - SessionCard's invalidateQueries key arrays exactly match the queryKey arrays used by TodayScreen's useQuery calls (verified by reading both files)
    - Any genuine backend error found is traced to its route and corrected without weakening ownership filters
    - The inline confirmation block (heading "Mark this session as missed?", "Yes, mark missed", "Keep it") still renders when Mark Missed is clicked
    - npm run build succeeds
  </acceptance_criteria>
  <done>The real cause of Mark Done "does nothing" and Mark Missed "broken popup" is identified against a running stack and corrected; the card refetches after a successful action.</done>
</task>

<task type="auto">
  <name>Task 2: Make session-action failures visible and not silent</name>
  <files>frontend/src/components/session/SessionCard.tsx, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts lines 195-211 (markSessionMissed and markSessionDone currently throw new Error(`...failed: ${res.status}`) on non-ok; they do not parse the structured backend error detail {error, detail})
    - frontend/src/components/session/SessionCard.tsx lines 74-99 (handlers catch errors and call toast.error with a generic message; a 401 or 404 currently produces only that generic toast — acceptable, but confirm the catch actually fires and the loading state always resets)
    - frontend/src/lib/api.ts lines 9-20 (apiFetch wrapper injects the Authorization header from the Supabase session)
  </read_first>
  <action>
    Ensure a failed session action always surfaces to the user and never leaves the card in a stuck state.

    In api.ts, for markSessionDone and markSessionMissed: when the response is not ok, attempt to read the structured error detail (the backend returns {detail: {error, detail}} or {detail: "..."}). Parse the JSON best-effort (wrap in try/catch returning an empty object) and include the backend's human-readable detail in the thrown Error message when available, falling back to the status code. This gives the toast a meaningful message instead of only a number.

    In SessionCard, confirm both handlers: (a) always reset their loading flag in the finally block (already present — verify it remains); (b) call toast.error in the catch with the error message; and (c) for Mark Missed, keep the confirmation open on failure so the user can retry (do not call setMissedOpen(false) until the action succeeds — move setMissedOpen(false) to run only after a successful markSessionMissed, i.e. before the invalidation but inside the try after the await resolves, not in a path that also runs on error).

    Do not change the success behavior (invalidate the matched query keys from Task 1). Do not add new dependencies.

    Resolves the silent-failure aspect of UAT GAP 2 and GAP 3: a user who clicks an action that fails now sees a toast and can retry, instead of the button appearing to do nothing.
  </action>
  <verify>
    <automated>cd frontend && grep -q "toast.error" src/components/session/SessionCard.tsx && grep -qE "markSessionDone|markSessionMissed" src/lib/api.ts && npm run build && npm test -- --run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - markSessionDone and markSessionMissed parse the backend error detail best-effort and include it in the thrown Error message, falling back to the status code
    - Both SessionCard handlers reset their loading flag in finally
    - Both handlers call toast.error on failure
    - Mark Missed keeps the confirmation open on failure and only closes it after a successful action
    - npm run build succeeds and the vitest suite shows no new failures
  </acceptance_criteria>
  <done>A failed Mark Done or Mark Missed shows an informative error toast and the user can retry; a successful action closes the confirmation and refreshes the card.</done>
</task>

</tasks>

<verification>
- The real failure mode for both actions is diagnosed against a running stack and recorded in the SUMMARY
- SessionCard invalidation keys match TodayScreen query keys
- api.ts helpers surface backend error detail; handlers always reset loading and toast on failure
- Frontend builds and vitest shows no new failures
- Manual smoke (human): Mark Done updates the card; Mark Missed shows the confirmation, confirming triggers the re-plan and updates the card
</verification>

<success_criteria>
- Mark Done marks the session completed and the card updates (UAT GAP 3 closed)
- Mark Missed shows the confirmation and completes the re-plan (UAT GAP 2 closed)
- Failures are visible via toast, never silent (UI-02)
</success_criteria>

<artifacts_produced>
This gap-closure plan produces no new files. It modifies:
- frontend/src/components/session/SessionCard.tsx (aligned query-key invalidation, retry-safe confirmation, visible failures)
- frontend/src/lib/api.ts (markSessionDone/markSessionMissed surface backend error detail)
No new exported symbols.
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-15-SUMMARY.md` when done
</output>
