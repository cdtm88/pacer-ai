---
phase: 04-ui-and-calendar
plan: 16
type: execute
wave: 6
depends_on: [04-06]
files_modified:
  - api/routes/rides.py
  - frontend/src/lib/api.ts
autonomous: true
gap_closure: true
requirements: [UI-04]
must_haves:
  truths:
    - "Uploading a valid real .FIT ride file succeeds and the ride appears in the History list"
    - "When a file is genuinely invalid or too short, the user sees a specific reason, not a bare 422"
    - "The upload response shape matches what the frontend expects so a success is not misread as a failure"
  artifacts:
    - path: "api/routes/rides.py"
      provides: "parse_fit_file / upload_fit handling that accepts valid real FIT files and returns precise error detail"
      contains: "fit_parse_failed"
    - path: "frontend/src/lib/api.ts"
      provides: "uploadRide that reads the {ride_id, status} response and surfaces the backend error detail on failure"
      contains: "uploadRide"
  key_links:
    - from: "frontend/src/components/history/FitUploadZone.tsx"
      to: "api/routes/rides.py"
      via: "uploadRide -> POST /rides/upload multipart"
      pattern: "rides/upload"
    - from: "api/routes/rides.py"
      to: "rides table"
      via: "upload_fit inserts a ride row after a successful parse"
      pattern: "table\\(\"rides\"\\)"
---

<objective>
Close UAT GAP 4: FIT file upload fails with "uploadRide failed: 422" (Tests 12 and 13, major). The 422 is raised by upload_fit in rides.py when parse_fit_file returns None (unreadable) or the parsed duration is under the 10-minute minimum. The diagnosis must run against a REAL .FIT file (the repo's test-ride.fit is a 66-byte stub that correctly produces 422 — it is not a valid test case). The real-world failure is most likely either (a) parse_fit_file returning None / zero samples for a valid Zwift FIT due to field-name handling, or (b) the duration computation undercounting. Secondary: uploadRide in api.ts types the response as Ride but the endpoint returns {ride_id, status}, and it throws a bare "422" with no reason.

Purpose: UI-04 (History with FIT upload) is unusable — every upload reports failure. FIT ingestion is the core data input that drives PMC, compliance, and adaptation.

Output: A valid real .FIT file uploads successfully and appears in History. parse_fit_file robustly counts samples/duration for real files. The 422 detail carries a specific, accurate reason. uploadRide reads the actual {ride_id, status} response and surfaces the backend error detail on failure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UAT.md
@.planning/phases/04-ui-and-calendar/04-PATTERNS.md
@frontend/src/components/history/FitUploadZone.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose the 422 against a real .FIT file and fix the parse/duration handling</name>
  <files>api/routes/rides.py</files>
  <read_first>
    - api/routes/rides.py lines 65-168 (parse_fit_file: iterates fitdecode frames, appends power per record frame, returns None on any open exception; duration_secs is computed as len(power_samples) — i.e. one sample per record frame, assuming 1 Hz recording)
    - api/routes/rides.py lines 426-473 (upload_fit: reads up to MAX_UPLOAD_BYTES, raises 422 fit_parse_failed when len > cap; calls parse_fit_file via asyncio.to_thread; raises 422 when parsed is None OR parsed["duration_secs"] < MIN_RIDE_DURATION_SECS which is 600)
    - api/routes/rides.py lines 54-56 (MAX_UPLOAD_BYTES 25MB, MIN_RIDE_DURATION_SECS 600, COLD_START_FTP 150.0)
    - tests/api/test_rides.py (find any existing FIT fixture used by the test suite — a valid multi-minute .FIT fixture is the correct artifact to diagnose against; the repo-root test-ride.fit is only 66 bytes and is NOT valid)
  </read_first>
  <action>
    DIAGNOSE FIRST. Obtain a valid real .FIT ride file (use the fixture referenced by tests/api/test_rides.py if one exists; otherwise the developer can drop a real export at a known path — record which file you used). Run parse_fit_file on its bytes directly via a small python -c or pytest-style harness and inspect: did it return None? how many power_samples? what is duration_secs? what were the first record frame's available field names (parse_fit_file already logs them)? Record these findings in the SUMMARY.

    Then fix the concrete defect the diagnosis reveals. Likely fixes, in priority order:

    1. Duration undercount: duration_secs = len(power_samples) assumes exactly one record frame per second. If a valid file legitimately has fewer record frames than seconds (e.g. smart recording, or non-1Hz sampling), prefer computing duration from the record timestamps when available — use (last_record_ts - first_record_ts) total seconds when both timestamps exist, falling back to len(power_samples) only when timestamps are absent. Capture the last record timestamp alongside the existing first_record_ts. This makes the 10-minute gate accurate for real files.

    2. parse_fit_file returns None for a valid file: if fitdecode opens the file but the record-frame filter never matches (field-name or message-name differences), adjust the frame handling so valid power/record data is captured. Do NOT broaden the except to hide real corruption — keep returning None only when fitdecode genuinely cannot open the file.

    3. If the diagnosis shows the real file IS under 10 minutes and the 422 is therefore correct, the defect is purely the opaque error message — proceed to Task 2 to surface the specific reason, and record in the SUMMARY that the upload path is functioning correctly for valid-length files.

    Keep all security guards intact: the 25MB size cap (T-03-11), the duration minimum gate (T-03-12) — only make its duration computation accurate, the filename sanitization (T-03-13), and user-scoped inserts (T-03-14). Do not remove the MIN_RIDE_DURATION_SECS gate; make it correct.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && .venv/bin/python -c "import ast; ast.parse(open('api/routes/rides.py').read()); print('parse ok')" && .venv/bin/pytest tests/api/test_rides.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the diagnosis: which real .FIT file was used, whether parse_fit_file returned None, the sample count, the computed duration, and the first-record field names
    - If a duration undercount is found, duration is computed from record timestamps (last minus first) when both are present, falling back to sample count otherwise
    - If parse_fit_file wrongly returned None for a valid file, the frame handling is corrected to capture valid record data
    - The 25MB cap, the duration-minimum gate, filename sanitization, and user-scoped inserts all remain in place
    - api/routes/rides.py parses without syntax error and existing test_rides.py tests still pass
  </acceptance_criteria>
  <done>A valid real .FIT file parses to an accurate duration and is accepted; the 422 only fires for genuinely invalid or too-short files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the uploadRide response shape and surface the backend error reason</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts lines 251-269 (uploadRide: posts multipart to /rides/upload with the JWT, then on non-ok throws new Error(`uploadRide failed: ${res.status}`) with no reason, and on success returns res.json() typed as Promise<Ride>)
    - api/routes/rides.py lines 438-541 (upload_fit returns {"ride_id": str, "status": "processing"} — NOT a full Ride object; errors return {detail: {error: "fit_parse_failed", detail: "File too short or unreadable..."}})
    - frontend/src/components/history/FitUploadZone.tsx lines 20-35 (handleUpload awaits uploadRide, shows a success toast, invalidates ['rides']; on error shows toast.error(`Upload failed. ${message}. Try again.`) — so the thrown Error message is what the user sees)
  </read_first>
  <action>
    Make uploadRide match the real response contract and give the user the real failure reason.

    Concretely in api.ts uploadRide:
    - On a non-ok response, parse the JSON body best-effort (try/catch returning {}), read the structured backend detail. The backend error shape is {detail: {error, detail}} — extract the human-readable detail string (e.g. "File too short or unreadable (minimum 10 minutes of data required)") and throw new Error with that message; fall back to `uploadRide failed: ${res.status}` only when no detail is present. This means FitUploadZone's toast shows the actual reason instead of a bare 422.
    - Fix the return type and parsing: upload_fit returns {ride_id, status}, not a Ride. Change uploadRide's return type to a small interface (e.g. { ride_id: string; status: string }) and return that parsed shape. Update the function signature accordingly. FitUploadZone does not consume the return value beyond awaiting it, so this is safe; confirm no other caller depends on a Ride return.

    Do not change the multipart construction (no Content-Type header, file field only, no user_id — JWT supplies identity).

    Resolves the frontend half of UAT GAP 4: the user sees the precise reason on failure, and a successful upload's {ride_id, status} response is no longer mis-typed.
  </action>
  <verify>
    <automated>cd frontend && grep -q "ride_id" src/lib/api.ts && grep -qE "uploadRide" src/lib/api.ts && npm run build && npm test -- --run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - uploadRide parses the backend error detail on non-ok and throws an Error carrying that human-readable message, falling back to the status code
    - uploadRide returns the actual {ride_id, status} shape with a matching return type, not Promise<Ride>
    - The multipart request still sends only the file field with the Authorization header and no explicit Content-Type
    - No other caller breaks from the return-type change (verified by grep/build)
    - npm run build succeeds and the vitest suite shows no new failures
  </acceptance_criteria>
  <done>A failed upload shows the specific backend reason in the toast, and a successful upload reads the real {ride_id, status} response so it is not misreported as a failure.</done>
</task>

</tasks>

<verification>
- parse_fit_file / upload_fit accept a valid real .FIT file with an accurate duration computation; the diagnosis is recorded in the SUMMARY
- The 422 detail is specific and accurate
- uploadRide matches the {ride_id, status} contract and surfaces backend error detail
- Backend suite passes: .venv/bin/pytest tests/api/test_rides.py -q
- Frontend builds and vitest shows no new failures
- Manual smoke (human): uploading a real ride file succeeds and the ride appears in History
</verification>

<success_criteria>
- A valid real .FIT file uploads and appears in History (UAT GAP 4 closed)
- Invalid/too-short files report a precise reason (UI-04)
- The upload response shape is correct
</success_criteria>

<artifacts_produced>
This gap-closure plan produces no new files. It modifies:
- api/routes/rides.py (accurate duration computation / robust parse; precise 422 detail)
- frontend/src/lib/api.ts (uploadRide reads {ride_id, status} and surfaces backend error detail)
New symbols: a small {ride_id, status} return interface for uploadRide.
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-16-SUMMARY.md` when done
</output>
