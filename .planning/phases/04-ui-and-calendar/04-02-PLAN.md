---
phase: 04-ui-and-calendar
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - api/routes/chat.py
  - api/routes/onboarding.py
  - api/routes/rides.py
  - api/routes/adaptations.py
  - tests/api/test_auth.py
autonomous: true
requirements: [UI-01, UI-06]
must_haves:
  truths:
    - "Every existing route extracts user_id from a verified JWT via Depends(get_current_user), not from a query/body/form param"
    - "An unauthenticated request to any protected route returns 401"
    - "The SSE endpoints (chat/stream, onboarding/start) accept the JWT via ?token= query param because EventSource cannot send headers"
    - "A request with a JWT for user A cannot read user B's data"
  artifacts:
    - path: "tests/api/test_auth.py"
      provides: "JWT middleware behavior tests"
    - path: "api/routes/chat.py"
      provides: "JWT-protected SSE chat stream"
      contains: "get_current_user"
  key_links:
    - from: "api/routes/chat.py"
      to: "api/auth.py"
      via: "Depends(get_current_user)"
      pattern: "Depends\\(get_current_user\\)"
    - from: "api/routes/adaptations.py"
      to: "api/auth.py"
      via: "Depends(get_current_user)"
      pattern: "Depends\\(get_current_user\\)"
---

<objective>
Replace every `# SECURITY TODO (Phase 4)` insecure user_id pattern across the four existing route modules with the JWT dependency from api/auth.py (created in 04-01). This is the security gate for the entire API: until this lands, any caller can supply an arbitrary user_id to read or mutate another user's data.

Purpose: D-03 mandates Supabase JWT verification on every API request, with the Supabase user_id flowing through as the authenticated identity. This is the highest-severity security work in the phase.
Output: chat.py, onboarding.py, rides.py, adaptations.py all using `Depends(get_current_user)`; SSE endpoints accepting `?token=`; `tests/api/test_auth.py` proving 401 on unauthenticated access and cross-user isolation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-PATTERNS.md
@api/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate chat.py and onboarding.py SSE endpoints to JWT (header or ?token=)</name>
  <read_first>
    - api/auth.py (get_current_user signature: accepts HTTPBearer header OR ?token= query param)
    - api/routes/chat.py (full file; chat_stream signature line 66-105; user_id query param line 69; SECURITY TODO line 80)
    - api/routes/onboarding.py (onboarding_start lines 214-258; OnboardingStartRequest model lines 53-55; SECURITY TODO lines 224-229; create_conversation, load_conversation use user_id)
    - .planning/phases/04-ui-and-calendar/04-PATTERNS.md ("JWT Auth Replacement Pattern" shared pattern; Pitfall 1 EventSource cannot send headers)
  </read_first>
  <files>api/routes/chat.py, api/routes/onboarding.py</files>
  <action>
    In api/routes/chat.py: import get_current_user from api.auth at module scope. Change `chat_stream` so it no longer takes `user_id: str = Query(...)`; add `current_user: dict = Depends(get_current_user)` and set `user_id = current_user["user_id"]`. Keep `conversation_id: str = Query(...)`. Because chat_stream is consumed by EventSource (which cannot send an Authorization header), get_current_user already reads `?token=` as a fallback, so the frontend will call `GET /chat/stream?conversation_id=...&token=<jwt>`. Remove the SECURITY TODO comment block now that auth is enforced; replace it with a one-line note that the JWT is accepted via header or ?token= for SSE.

    In api/routes/onboarding.py: import get_current_user. Change `onboarding_start(request: OnboardingStartRequest)` to `onboarding_start(current_user: dict = Depends(get_current_user))` and set `user_id = current_user["user_id"]`. Remove the now-unused OnboardingStartRequest model and the request.user_id read, and the SECURITY TODO block. Onboarding/start is a POST consumed via EventSource-or-fetch; the frontend passes the JWT as `?token=` for the SSE case, so get_current_user's query-param path covers it. Leave create_conversation/load_conversation/save_messages helper signatures unchanged (they already take user_id as an argument).
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && python -c "import ast; ast.parse(open('api/routes/chat.py').read()); ast.parse(open('api/routes/onboarding.py').read()); print('ok')" && grep -c "Depends(get_current_user)" api/routes/chat.py api/routes/onboarding.py</automated>
  </verify>
  <acceptance_criteria>
    - chat.py: `chat_stream` uses `Depends(get_current_user)` and no longer has `user_id: str = Query`
    - onboarding.py: `onboarding_start` uses `Depends(get_current_user)`; OnboardingStartRequest and request.user_id are removed
    - Neither file contains the string `SECURITY TODO`
    - `grep -v '^#' api/routes/chat.py | grep -c "user_id: str = Query"` returns 0
    - Both files parse via ast
  </acceptance_criteria>
  <done>Both SSE endpoints authenticate via JWT (header or ?token=); the insecure user_id query/body params are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate rides.py and adaptations.py endpoints to JWT</name>
  <read_first>
    - api/auth.py (get_current_user)
    - api/routes/rides.py (upload_fit lines 434-544; user_id Form param line 438; SECURITY TODO lines 453-459; validate_uuid usage; the new list_rides added in 04-01 already uses get_current_user)
    - api/routes/adaptations.py (list_adaptations line 618-643; check_adaptations line 644-674; mark_session_missed line 675+; all user_id query/body params; "JWT Auth Replacement Pattern" in 04-PATTERNS.md)
  </read_first>
  <files>api/routes/rides.py, api/routes/adaptations.py</files>
  <action>
    In api/routes/rides.py: change `upload_fit` to drop the `user_id: str = Form(...)` param and instead take `current_user: dict = Depends(get_current_user)`, setting `user_id = current_user["user_id"]`. Remove the `validate_uuid(user_id, "user_id")` call (the JWT sub is already a valid UUID from Supabase) or keep it as defence-in-depth against malformed tokens. Remove the SECURITY TODO block. The frontend will send only the `file` field as multipart and the JWT in the Authorization header (FitUploadZone does not append user_id).

    In api/routes/adaptations.py: import get_current_user. For each of `list_adaptations`, `check_adaptations`, and `mark_session_missed`, replace the `user_id` query/body param with `current_user: dict = Depends(get_current_user)` and set `user_id = current_user["user_id"]`. Preserve the `session_id` path param on mark_session_missed and any request-body models for non-identity fields. Remove every `# TODO(phase-4-auth)` and SECURITY TODO comment in this file. Do not change the internal helper functions (detect_signals, apply_micro_adjustment, apply_macro_replan) which already take user_id as an argument.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && python -c "import ast; ast.parse(open('api/routes/rides.py').read()); ast.parse(open('api/routes/adaptations.py').read()); print('ok')" && grep -rn "SECURITY TODO\|phase-4-auth" api/routes/ | grep -vc "^$"</automated>
  </verify>
  <acceptance_criteria>
    - rides.py: upload_fit uses `Depends(get_current_user)` and has no `user_id: str = Form`
    - adaptations.py: list_adaptations, check_adaptations, mark_session_missed all use `Depends(get_current_user)`
    - `grep -rn "SECURITY TODO" api/routes/` returns no matches
    - `grep -rn "phase-4-auth" api/routes/` returns no matches
    - Both files parse via ast
  </acceptance_criteria>
  <done>All four mutation/read endpoints in rides.py and adaptations.py authenticate via JWT; no insecure user_id params or SECURITY TODOs remain anywhere in api/routes/.</done>
</task>

<task type="auto">
  <name>Task 3: Write JWT middleware tests</name>
  <read_first>
    - api/auth.py (get_current_user)
    - tests/api/conftest.py (TEST_USER_ID, mock_supabase_factory, AsyncClient usage)
    - tests/api/test_rides.py (AsyncClient request pattern, monkeypatch of _get_async_supabase)
  </read_first>
  <files>tests/api/test_auth.py</files>
  <action>
    Create tests/api/test_auth.py covering get_current_user behavior. Generate valid and invalid test JWTs using PyJWT: set SUPABASE_JWT_SECRET in the test env (monkeypatch.setenv), then `jwt.encode({"sub": TEST_USER_ID, "email": "t@example.com", "aud": "authenticated"}, secret, algorithm="HS256")`. Tests: (1) a request to a protected endpoint with no Authorization header and no ?token= returns 401; (2) a request with a malformed/garbage bearer token returns 401; (3) a request with a token signed by the WRONG secret returns 401; (4) a request with a valid token resolves user_id == TEST_USER_ID (assert via a small endpoint or by calling get_current_user directly with a fabricated credentials object); (5) a valid token passed as `?token=` query param (SSE path) also authenticates. Use direct unit calls to get_current_user where constructing a full request is heavy; use the AsyncClient against the FastAPI app for the HTTP-level 401 tests. Mock Supabase where the endpoint queries the DB.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && python -m pytest tests/api/test_auth.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_auth.py exists
    - Tests cover: missing token -> 401, garbage token -> 401, wrong-secret token -> 401, valid token -> user_id resolves, ?token= path authenticates
    - `pytest tests/api/test_auth.py -x -q` passes
  </acceptance_criteria>
  <done>test_auth.py proves the JWT dependency rejects unauthenticated/forged requests and accepts valid header and ?token= credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → FastAPI | All four existing route modules cross here; identity must derive from a verified JWT |
| EventSource → SSE endpoints | EventSource cannot send Authorization headers; JWT travels in ?token= query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | all existing routes (user_id from request) | mitigate | Replace every user_id query/body/form param with Depends(get_current_user); identity comes only from the JWT sub claim |
| T-04-05 | Information Disclosure | EventSource JWT in ?token= | accept | Documented tradeoff (Open Question 1): query-param token is simplest for EventSource; access tokens are short-lived (Supabase ~1hr) and refreshed by @supabase/supabase-js; suppress `token` from server logs in deployment config |
| T-04-06 | Elevation of Privilege | rides upload, adaptations mutate | mitigate | user_id sourced from JWT so a caller cannot upload a ride or trigger a replan as another user |
| T-04-07 | Repudiation | mark_session_missed | mitigate | Authenticated identity is logged in adaptations audit trail; no anonymous mutations |
</threat_model>

<verification>
- `python -m pytest tests/api/test_auth.py -x -q` passes
- `grep -rn "SECURITY TODO\|phase-4-auth" api/routes/` returns no matches
- All four route files and main.py parse via ast
- Existing suites still pass: `python -m pytest tests/api/ -x -q`
</verification>

<success_criteria>
- Every existing endpoint authenticates via Depends(get_current_user)
- No insecure user_id query/body/form params remain in api/routes/
- SSE endpoints accept the JWT via ?token=
- test_auth.py passes proving 401 on unauthenticated and forged requests
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 04-02)

- **Modified endpoints (now JWT-protected):** `GET /chat/stream`, `POST /onboarding/start`, `POST /rides/upload`, `GET /adaptations/`, `POST /adaptations/check`, `POST /adaptations/sessions/{id}/missed`
- **Removed:** `OnboardingStartRequest` model; all `# SECURITY TODO (Phase 4)` comments; all insecure user_id query/body/form params
- **Tests:** `tests/api/test_auth.py`
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-02-SUMMARY.md` when done
</output>
