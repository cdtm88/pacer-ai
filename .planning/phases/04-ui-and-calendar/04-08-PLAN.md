---
phase: 04-ui-and-calendar
plan: 08
type: execute
wave: 5
depends_on: ["04-04", "04-05"]
files_modified:
  - frontend/src/components/session/SessionStepList.tsx
  - frontend/src/screens/DuringSessionScreen.tsx
  - frontend/src/components/pwa/IOSInstallBanner.tsx
  - frontend/public/apple-touch-icon.png
  - frontend/public/pwa-192x192.png
  - frontend/public/pwa-512x512.png
  - frontend/vercel.json
  - frontend/.env.example
  - frontend/src/tests/pwa.test.tsx
autonomous: true
requirements: [UI-05, UI-09, UI-10]
must_haves:
  truths:
    - "The During-Session screen renders the static step hierarchy: large current step, next step below, later steps smaller (UI-05, D-09)"
    - "The During-Session screen shows a static placeholder timer with a 'Timer activates in next phase' note"
    - "Start Session on Today navigates to /session"
    - "The iOS install banner appears only on iOS, not in standalone mode, and not after dismissal (UI-09)"
    - "The PWA manifest references apple-touch-icon (180x180) plus 192 and 512 icons"
    - "The app is installable as a PWA and the service worker precaches the app shell"
    - "Light mode only with no pure black anywhere (UI-10)"
  artifacts:
    - path: "frontend/src/screens/DuringSessionScreen.tsx"
      provides: "Static during-session stepper"
    - path: "frontend/src/components/pwa/IOSInstallBanner.tsx"
      provides: "iOS install instructional banner"
      contains: "ontouchstart"
    - path: "frontend/public/apple-touch-icon.png"
      provides: "180x180 iOS home-screen icon"
    - path: "frontend/vercel.json"
      provides: "Vercel SPA rewrite + deploy config"
  key_links:
    - from: "frontend/src/screens/DuringSessionScreen.tsx"
      to: "frontend/src/components/session/SessionStepList.tsx"
      via: "renders the step hierarchy"
      pattern: "SessionStepList"
    - from: "frontend/src/components/pwa/IOSInstallBanner.tsx"
      to: "localStorage ios-banner-dismissed"
      via: "dismiss persists to localStorage"
      pattern: "ios-banner-dismissed"
---

<objective>
Ship the During-Session static screen (D-09: full visual layout, non-ticking placeholder timer; Phase 5 wires the real timer), the iOS PWA install banner (UI-09), the PWA icon assets, and the Vercel deploy configuration. This closes the remaining UI requirements and makes the app installable and deployable.

Purpose: UI-05 (during-session stepper, static in Phase 4 per D-09/D-10), UI-09 (PWA installable + offline during-session + iOS banner), UI-10 (light mode, no pure black). Vercel config makes the SPA deployable with correct routing.
Output: SessionStepList, DuringSessionScreen, IOSInstallBanner, the three PWA icon files, vercel.json, .env.example, and a PWA/banner test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UI-SPEC.md
@.planning/phases/04-ui-and-calendar/04-RESEARCH.md
@.planning/phases/04-ui-and-calendar/04-PATTERNS.md
@frontend/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionStepList and the static During-Session screen</name>
  <read_first>
    - .planning/phases/04-ui-and-calendar/04-UI-SPEC.md (During-Session screen lines 286-301: step hierarchy sizes, zone color strip, static timer + "Timer activates in next phase" note, End session button; Typography Display role; Copywriting Contract during-session timer placeholder)
    - .planning/phases/04-ui-and-calendar/04-PATTERNS.md (SessionStepList / DuringSessionScreen no-analog notes: static Phase 4, no behavior)
    - frontend/src/components/session/ZoneChip.tsx (zone color tokens for the strip)
    - frontend/src/components/session/SessionCard.tsx (Start session navigates to /session — confirm the link target)
  </read_first>
  <files>frontend/src/components/session/SessionStepList.tsx, frontend/src/screens/DuringSessionScreen.tsx</files>
  <action>
    Create SessionStepList.tsx: takes an array of steps (target + duration) and renders the static hierarchy per UI-SPEC: current step in Display size (40px bold --color-ink, "Zone 2, 20 min" style format, no em dash), a 4px zone-color horizontal strip below it (using the zone token), the next step in Heading size (20px semibold --color-ink-2, "Next: Cool-down 5 min"), and remaining steps in Body size (16px --color-ink-3, stacked, max 3 visible). No ticking and no auto-advance (Phase 5 wires behavior). Layout must be pixel-correct so Phase 5 can add behavior without layout changes.

    Create DuringSessionScreen.tsx: full-screen, no nav chrome, --color-bg-2 background. Renders SessionStepList for the current session's steps (derive steps from the session structure; if no live session is selected, render a representative/placeholder set so the layout is verifiable). Shows a static timer display "00:00" in 32px with tabular-nums, plus the caption "Timer activates in next phase" (--color-ink-3) shown while static. An "End session" Button variant outline, bottom-right, --color-bad text, navigates back to "/". No em dashes. Confirm the Today SessionCard "Start session" button already navigates to /session (it does, from 04-05); if not, this task does not modify SessionCard.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx tsc --noEmit 2>&1 | tail -15 && grep -q "Timer activates in next phase" src/screens/DuringSessionScreen.tsx && grep -q "SessionStepList" src/screens/DuringSessionScreen.tsx && ! grep -q "setInterval\|Date.now" src/screens/DuringSessionScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SessionStepList renders current (Display 40px) / next (Heading) / remaining (Body) hierarchy with a zone-color strip
    - DuringSessionScreen shows the static "00:00" timer and the "Timer activates in next phase" note
    - No setInterval and no Date.now timer logic appears (static only, D-09; Phase 5 owns behavior)
    - DuringSessionScreen has an "End session" button navigating to "/"
    - No em dashes; `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>The During-Session screen is a pixel-correct static layout with no timer behavior, ready for Phase 5 to wire the real timer.</done>
</task>

<task type="auto">
  <name>Task 2: iOS install banner and PWA icon assets</name>
  <read_first>
    - .planning/phases/04-ui-and-calendar/04-UI-SPEC.md (PWA Install Banner lines 388-400: trigger conditions, position, copy "Install PacerAI: tap the Share button, then 'Add to Home Screen'.", dismiss "Got it"; PWA Manifest Contract lines 540-561 icon files)
    - .planning/phases/04-ui-and-calendar/04-RESEARCH.md (Pattern 6 iOS banner; Pitfall 5 isIOS + ontouchstart to exclude Mac Safari)
    - frontend/src/stores/uiStore.ts (iOSBannerDismissed)
    - frontend/vite.config.ts (manifest icons already configured in 04-03)
  </read_first>
  <files>frontend/src/components/pwa/IOSInstallBanner.tsx, frontend/public/apple-touch-icon.png, frontend/public/pwa-192x192.png, frontend/public/pwa-512x512.png</files>
  <action>
    Create IOSInstallBanner.tsx: shows only when isIOS() (UA `/iphone|ipad|ipod/i`) AND `'ontouchstart' in window` (excludes Mac Safari per Pitfall 5) AND not in standalone (`!window.matchMedia('(display-mode: standalone)').matches`) AND not previously dismissed (`localStorage.getItem('ios-banner-dismissed')` is null). Position: fixed, `bottom: calc(56px + env(safe-area-inset-bottom))`, inset-x lg, --color-surface bg, 1px --color-line border, 12px radius, lg padding, shadow. Copy exactly: "Install PacerAI: tap the Share button, then 'Add to Home Screen'." with a "Got it" dismiss button that writes localStorage 'ios-banner-dismissed' = '1' and hides the banner (also reflect via uiStore). No em dashes. Render the banner from AppLayout or the app root so it appears on first visit on iOS.

    Create the three PWA icon PNGs in frontend/public/: apple-touch-icon.png (180x180), pwa-192x192.png (192x192), pwa-512x512.png (512x512), each a white background with the PacerAI mark in blue-6 (#228BE6). Generate them programmatically (e.g. a small Pillow/node-canvas script, or an SVG rasterized to the three sizes) so the bytes are valid PNGs of the exact dimensions; the mark can be a simple "P" or pace glyph. These must exist as real files (vite-plugin-pwa includeAssets references apple-touch-icon.png).
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx tsc --noEmit 2>&1 | tail -10 && grep -q "ontouchstart" src/components/pwa/IOSInstallBanner.tsx && grep -q "ios-banner-dismissed" src/components/pwa/IOSInstallBanner.tsx && test -f public/apple-touch-icon.png && test -f public/pwa-192x192.png && test -f public/pwa-512x512.png && file public/apple-touch-icon.png | grep -qi "PNG"</automated>
  </verify>
  <acceptance_criteria>
    - IOSInstallBanner gates on isIOS AND 'ontouchstart' in window AND not standalone AND not dismissed (Pitfall 5)
    - Banner copy is exactly "Install PacerAI: tap the Share button, then 'Add to Home Screen'." with a "Got it" dismiss writing localStorage 'ios-banner-dismissed'
    - apple-touch-icon.png (180x180), pwa-192x192.png, and pwa-512x512.png exist as valid PNG files
    - No em dashes; `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>The iOS install banner shows only on real iOS first visits and dismisses persistently; the three PWA icons exist as valid PNGs.</done>
</task>

<task type="auto">
  <name>Task 3: Vercel deploy config, env example, and PWA/banner test</name>
  <read_first>
    - frontend/vite.config.ts (VitePWA config, navigateFallback '/index.html')
    - frontend/src/vite-env.d.ts (VITE_ env vars)
    - frontend/src/components/pwa/IOSInstallBanner.tsx (Task 2)
    - .planning/phases/04-ui-and-calendar/04-RESEARCH.md (Environment Availability: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_URL; Vercel hosting notes)
  </read_first>
  <files>frontend/vercel.json, frontend/.env.example, frontend/src/tests/pwa.test.tsx</files>
  <action>
    Create frontend/vercel.json configuring the Vite SPA deploy: a rewrite sending all non-asset paths to /index.html (SPA client routing fallback), the build command (`npm run build`) and output directory (`dist`) if not auto-detected, and HTTPS is automatic on Vercel. Do not hard-code secrets; env vars are set in the Vercel dashboard.

    Create frontend/.env.example listing the required client env vars with placeholder values: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_URL. Add a comment that the service-role key and JWT secret must NEVER be placed here (they are server-only).

    Create frontend/src/tests/pwa.test.tsx (Vitest + RTL): (1) IOSInstallBanner does NOT render when navigator.userAgent has no iOS token (simulate a desktop UA); (2) IOSInstallBanner renders the install copy when iOS conditions are met (mock userAgent to include 'iPhone', define window.ontouchstart, mock matchMedia standalone false, clear localStorage); (3) clicking "Got it" sets localStorage 'ios-banner-dismissed' and hides the banner. Use vi.stubGlobal / Object.defineProperty to control navigator and matchMedia.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npm test -- --run 2>&1 | tail -20 && test -f vercel.json && test -f .env.example && grep -q "VITE_SUPABASE_URL" .env.example && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json exists with an SPA rewrite to /index.html and the Vite build/output settings
    - .env.example lists VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_URL with a note that server-only secrets must not be added
    - pwa.test.tsx passes: banner hidden on desktop UA, shown on iOS conditions, dismiss persists to localStorage
    - `npm run build` exits 0 (the full app including all screens still builds)
    - `npm test -- --run` passes
  </acceptance_criteria>
  <done>The frontend is deployable to Vercel with correct SPA routing and documented env vars; the iOS banner gating is covered by passing tests and the full app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → Vercel | The deployed bundle carries only public VITE_ env vars; secrets stay server-side |
| service worker → cache | The during-session route is cached for offline use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Information Disclosure | .env.example / Vercel env | mitigate | .env.example documents only public VITE_ vars with an explicit note that the service-role key and JWT secret must never appear client-side |
| T-04-27 | Tampering | service worker cache | accept | Workbox precache + NetworkFirst for session route; cached data is the user's own session plan, no cross-user exposure |
| T-04-28 | Denial of Service | iOS banner render loop | mitigate | Banner gated on a localStorage flag so it shows at most once until dismissed; no re-render loop |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` passes
- `cd frontend && npm run build` exits 0
- DuringSessionScreen has the static timer note and no setInterval/Date.now
- IOSInstallBanner gates on ontouchstart + ios-banner-dismissed; three PWA PNGs exist
- vercel.json and .env.example exist
</verification>

<success_criteria>
- During-Session static stepper renders the step hierarchy with a placeholder timer (UI-05, D-09)
- iOS install banner shows only on real iOS first visits and dismisses persistently (UI-09)
- PWA icons exist; app is installable; light mode only, no pure black (UI-10)
- Vercel deploy config with SPA routing; env documented
- PWA/banner gating covered by passing tests; full app builds
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 04-08)

- **Session components:** `frontend/src/components/session/SessionStepList.tsx`
- **Screen:** `frontend/src/screens/DuringSessionScreen.tsx` (static, no timer behavior)
- **PWA:** `frontend/src/components/pwa/IOSInstallBanner.tsx`, `frontend/public/apple-touch-icon.png` (180x180), `pwa-192x192.png`, `pwa-512x512.png`
- **Deploy:** `frontend/vercel.json` (SPA rewrite), `frontend/.env.example` (VITE_ vars)
- **Tests:** `frontend/src/tests/pwa.test.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-08-SUMMARY.md` when done
</output>
