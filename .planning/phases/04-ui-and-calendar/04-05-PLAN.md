---
phase: 04-ui-and-calendar
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - frontend/src/components/AppLayout.tsx
  - frontend/src/components/nav/BottomTabBar.tsx
  - frontend/src/components/nav/DesktopSidebar.tsx
  - frontend/src/components/session/ZoneChip.tsx
  - frontend/src/components/session/TsbChip.tsx
  - frontend/src/components/session/SessionCard.tsx
  - frontend/src/screens/TodayScreen.tsx
  - frontend/src/screens/AgendaScreen.tsx
  - frontend/src/tests/today.test.tsx
autonomous: true
requirements: [UI-02, UI-03, UI-07, UI-08]
must_haves:
  truths:
    - "The mobile bottom tab bar shows Today / Agenda / History / Chat and a Settings gear; the desktop left sidebar shows the same destinations"
    - "Active navigation state is highlighted per UI-SPEC (blue-7 + indicator)"
    - "The Today screen renders today's session card with objective, structure, targets, duration, and the four action buttons (Start session, Export to Zwift disabled, Mark done, Mark missed)"
    - "The TSB form chip appears ONLY when pmc_history.tss_display_ready is true (D-14)"
    - "Export to Zwift is visually disabled with a 'Coming soon' tooltip (D-10)"
    - "The Agenda screen lists sessions grouped by week with zone-color dots matching the PRD zone tokens"
  artifacts:
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "Responsive nav shell (bottom bar mobile, sidebar desktop) + Outlet"
    - path: "frontend/src/components/session/SessionCard.tsx"
      provides: "Today session card + action row"
    - path: "frontend/src/screens/TodayScreen.tsx"
      provides: "Today screen data-connected"
    - path: "frontend/src/screens/AgendaScreen.tsx"
      provides: "Agenda screen grouped by week"
  key_links:
    - from: "frontend/src/screens/TodayScreen.tsx"
      to: "frontend/src/lib/api.ts"
      via: "useQuery(getSessionToday) and getLatestPmc"
      pattern: "getSessionToday"
    - from: "frontend/src/components/session/TsbChip.tsx"
      to: "pmc_history.tss_display_ready"
      via: "render only when tss_display_ready is true"
      pattern: "tss_display_ready"
    - from: "frontend/src/screens/AgendaScreen.tsx"
      to: "frontend/src/lib/api.ts"
      via: "useQuery(getUpcomingSessions)"
      pattern: "getUpcomingSessions"
---

<objective>
Build the navigation shell and the two primary planning screens: Today (session card with actions, TSB chip, next-few-days strip) and Agenda (week-grouped session list with zone colors). These are the screens a returning user sees most.

Purpose: UI-07 (mobile bottom tab + desktop sidebar), UI-02 (Today card + actions + gated TSB chip + D-10 disabled Zwift export), UI-03 (Agenda grouped by week with PRD zone colors). All gated behaviors (D-14 TSB chip) and design-system rules (UI-08) are enforced here.
Output: AppLayout, BottomTabBar, DesktopSidebar, ZoneChip, TsbChip, SessionCard, TodayScreen, AgendaScreen, plus a test for the TSB-gate and Mark-missed flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UI-SPEC.md
@.planning/phases/04-ui-and-calendar/04-PATTERNS.md
@frontend/src/lib/api.ts
@frontend/src/router.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppLayout, BottomTabBar, DesktopSidebar with responsive nav</name>
  <read_first>
    - frontend/src/router.tsx (AppLayout placeholder slot under the gates)
    - frontend/src/stores/uiStore.ts (activeTab)
    - .planning/phases/04-ui-and-calendar/04-UI-SPEC.md (Navigation section lines 356-385: tab labels/icons table, mobile bottom bar 56px + safe-area, desktop sidebar 240px, active/inactive states, breakpoint 768px; Settings via gear not 5th tab D-15)
  </read_first>
  <files>frontend/src/components/AppLayout.tsx, frontend/src/components/nav/BottomTabBar.tsx, frontend/src/components/nav/DesktopSidebar.tsx</files>
  <action>
    Create BottomTabBar.tsx: a fixed-bottom bar (height 56px plus env(safe-area-inset-bottom)), --color-surface background, 1px --color-line top border, rendered only below the 768px breakpoint. Four tabs Today/Agenda/History/Chat using react-router NavLink to /, /agenda, /history, /chat with lucide icons Home/Calendar/Clock/MessageCircle (24px) above 10px labels. Active state: icon and label in blue-7 (#1B73C0) with a 4px blue-6 indicator dot; inactive in --color-ink-3.

    Create DesktopSidebar.tsx (D-16: desktop left sidebar with the same 4 destinations): a fixed 240px left sidebar shown at >=768px, --color-surface bg, 1px --color-line right border, PacerAI logotype at top (16px semibold --color-ink), nav items for the same 4 destinations plus Settings at the bottom (Settings icon). 44px item height; active item blue-0 background plus blue-7 text plus 3px blue-6 left border; inactive --color-ink-2 text with --color-bg hover. 20px lucide icons.

    Create AppLayout.tsx: renders DesktopSidebar (>=768px) OR BottomTabBar (<768px) using Tailwind responsive classes (md: breakpoint, single breakpoint only), a screen header containing the Settings gear icon (top-right, navigates to /settings, the mobile path for Settings since it is not a tab per D-15), and an `<Outlet />` for the active screen. Account for bottom-bar height as bottom padding on mobile content so the tab bar never overlaps scrollable content. Call the useAuth() hook here (or confirm it runs at root) so onAuthStateChange stays active. Wire AppLayout into router.tsx as the layout component wrapping the four tab screens plus /session plus /settings.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx tsc --noEmit 2>&1 | tail -15 && grep -q "BottomTabBar" src/components/AppLayout.tsx && grep -q "DesktopSidebar" src/components/AppLayout.tsx && grep -q "Outlet" src/components/AppLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BottomTabBar renders NavLinks to /, /agenda, /history, /chat with the four lucide icons; active state uses blue-7
    - DesktopSidebar renders the same 4 destinations plus Settings, with the 3px blue-6 active left border
    - AppLayout swaps nav by the 768px breakpoint and renders an Outlet plus a Settings gear (D-15)
    - router.tsx uses AppLayout as the layout wrapping the tab screens, /session, and /settings
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>Responsive navigation shell works: bottom tab bar on mobile, left sidebar on desktop, Settings reachable via gear (not a 5th tab).</done>
</task>

<task type="auto">
  <name>Task 2: ZoneChip, TsbChip, and SessionCard</name>
  <read_first>
    - frontend/src/index.css (zone color tokens --color-zone-*)
    - .planning/phases/04-ui-and-calendar/04-UI-SPEC.md (Today Session Card lines 188-216: card style, targets chip row, TSB chip rule, action row 4 buttons with exact variants and copy; Interaction States disabled state; Copywriting Contract)
    - .planning/phases/04-ui-and-calendar/04-PATTERNS.md (sessions table shape interface lines 538-559; TSB gate lines 561-572; Mark missed endpoint)
    - frontend/src/lib/api.ts (markSessionMissed, markSessionDone helpers)
  </read_first>
  <files>frontend/src/components/session/ZoneChip.tsx, frontend/src/components/session/TsbChip.tsx, frontend/src/components/session/SessionCard.tsx</files>
  <action>
    Create ZoneChip.tsx: takes a zone/type string (recovery, endurance, tempo, threshold, vo2) and renders a shadcn Badge whose background is the matching --color-zone-* token at roughly 15% opacity and text the zone color darkened for contrast. Map type via a single lookup keyed to the CSS custom property names (single source of truth, no inline hex per the Don't-Hand-Roll zone-color guidance).

    Create TsbChip.tsx: takes the latest pmc row; returns null unless `tss_display_ready === true` (D-14). When ready, derive fresh/balanced/fatigued from tsb (fresh when tsb is clearly positive, fatigued when clearly negative, balanced otherwise; use simple thresholds documented inline) and render a pill: fresh = good-green tint, balanced = blue-0 tint, fatigued = amber tint. No placeholder and no reserved space when not shown.

    Create SessionCard.tsx implementing the UI-SPEC Today session card: --color-surface card, 1px --color-line border, 12px radius, lg padding; top line weekday and date (label, --color-ink-2); objective (heading 20px semibold --color-ink); structure as plain prose (body, --color-ink-2); a targets chip row composed of RPE/HR/power-zone ZoneChips as applicable; duration with a lucide Clock icon; the TsbChip (passed the pmc row). Below the card render the 4-button action row: "Start session" (Button variant default blue-6, lucide Play, navigates to /session), "Export to Zwift" (Button variant outline, DISABLED with a shadcn Tooltip "Coming in the next update", lucide Download, per D-10), "Mark done" (Button variant outline, --color-good, lucide CheckCircle, calls markSessionDone then invalidates the today query), "Mark missed" (Button variant ghost, --color-bad, lucide XCircle, opens a shadcn AlertDialog "Mark this session as missed?" with the exact UI-SPEC body and CTA copy, and on confirm calls markSessionMissed then invalidates queries). All copy verbatim from the Copywriting Contract; no em dashes anywhere.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx tsc --noEmit 2>&1 | tail -15 && grep -q "tss_display_ready" src/components/session/TsbChip.tsx && grep -q "Coming in the next update" src/components/session/SessionCard.tsx && grep -q "Start session" src/components/session/SessionCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ZoneChip maps each of the five zone types to its --color-zone-* token via a single lookup (no inline hex)
    - TsbChip returns null unless tss_display_ready is true (D-14) and renders fresh/balanced/fatigued when ready
    - SessionCard renders all four action buttons with exact copy; Export to Zwift is disabled with the "Coming in the next update" tooltip (D-10)
    - Mark missed opens an AlertDialog with the exact UI-SPEC confirmation copy and calls markSessionMissed on confirm
    - No em dash characters in SessionCard.tsx
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>SessionCard, gated TsbChip, and token-driven ZoneChip render per the UI contract with verbatim copy and the disabled Zwift export.</done>
</task>

<task type="auto">
  <name>Task 3: TodayScreen, AgendaScreen, and gate/flow test</name>
  <read_first>
    - frontend/src/components/session/SessionCard.tsx (Task 2)
    - frontend/src/lib/api.ts (getSessionToday, getUpcomingSessions, getLatestPmc)
    - .planning/phases/04-ui-and-calendar/04-UI-SPEC.md (Today screen layout mobile/desktop + next-few-days strip + empty/error states lines 182-216; Agenda screen lines 218-246: week grouping, session row, zone dots, empty/error states; Copywriting Contract empty-state rows)
    - .planning/phases/04-ui-and-calendar/04-PATTERNS.md (sessions table shape)
  </read_first>
  <files>frontend/src/screens/TodayScreen.tsx, frontend/src/screens/AgendaScreen.tsx, frontend/src/tests/today.test.tsx</files>
  <action>
    Create TodayScreen.tsx: useQuery(['session','today'], getSessionToday) and useQuery(['pmc','latest'], getLatestPmc). On success render SessionCard with the session and pmc row. Layout: single column mobile, two-column desktop (left = card + next-few-days, right empty in Phase 4). Render a next-few-days strip (from getUpcomingSessions, limited to a few rows) showing date label + session type + zone color dot + duration, tap navigates to /agenda. Empty state (no session today): heading "No session today", body "Your next ride is {day}. Rest up." with no CTA. Error state: inline card "Could not load today's session. Pull down to refresh." with a Retry that refetches. Loading: shadcn Skeleton.

    Create AgendaScreen.tsx: useQuery(['sessions','upcoming'], getUpcomingSessions). Group sessions by ISO week with a sticky week header ("Week of Jun 23" format). Each row: date column, type label + 1-line truncated objective preview, a zone color dot (12px using the zone token) + duration, and a status indicator (CheckCircle --color-good if completed, XCircle --color-bad if missed/skipped, nothing if planned). Tap to expand into a shadcn Accordion revealing objective + structure prose + targets. Empty state: heading "No sessions planned yet", body "Complete the interview in Chat to generate your plan.", CTA "Go to Chat" (text button blue-7 to /chat). Error state: inline "Could not load your plan. Tap to retry."

    Create frontend/src/tests/today.test.tsx (Vitest + RTL): (1) when getLatestPmc returns a row with tss_display_ready false, the TSB chip is NOT in the document; when true, the fresh/balanced/fatigued pill IS rendered; (2) Export to Zwift button is disabled; (3) clicking Mark missed opens the confirmation dialog with the exact title text. Mock the api helpers and wrap in QueryClientProvider + a memory router as needed.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TodayScreen fetches today's session and latest PMC via TanStack Query and renders SessionCard plus a next-few-days strip
    - TodayScreen empty state shows "No session today" with no CTA
    - AgendaScreen groups sessions by week with sticky headers and zone-color dots matching the five zone tokens
    - AgendaScreen empty state shows "No sessions planned yet" with a "Go to Chat" CTA
    - today.test.tsx passes: TSB chip hidden when tss_display_ready false and shown when true; Export disabled; Mark missed dialog opens
    - `npm test -- --run` passes
  </acceptance_criteria>
  <done>Today and Agenda screens are data-connected with all empty/error states; the TSB-gate and Mark-missed flows are covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → FastAPI | Today/Agenda read endpoints carry the JWT; server enforces user scoping (04-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | session/PMC data render | mitigate | Data fetched via apiFetch with JWT; server returns only the authenticated user's rows |
| T-04-15 | Tampering | Mark missed/done mutations | mitigate | Mutations go through JWT-protected endpoints; AlertDialog confirmation prevents accidental destructive action |
| T-04-16 | Information Disclosure | TSB chip premature display | accept | Gated on tss_display_ready; no sensitive data, but D-14 correctness enforced to avoid misleading the user |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` passes
- `npx tsc --noEmit` reports no errors
- SessionCard contains "Coming in the next update" and "Start session"; TsbChip references tss_display_ready
- No em dashes in SessionCard.tsx, TodayScreen.tsx, AgendaScreen.tsx
</verification>

<success_criteria>
- Responsive nav (bottom bar + sidebar) with correct active states
- Today screen: card, 4 actions, gated TSB chip, next-few-days strip, empty/error states
- Agenda screen: week-grouped list with PRD zone colors, accordion expand, empty/error states
- TSB-gate and Mark-missed flows covered by passing tests
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 04-05)

- **Layout:** `frontend/src/components/AppLayout.tsx`
- **Nav:** `frontend/src/components/nav/BottomTabBar.tsx`, `frontend/src/components/nav/DesktopSidebar.tsx`
- **Session components:** `frontend/src/components/session/ZoneChip.tsx`, `TsbChip.tsx`, `SessionCard.tsx`
- **Screens:** `frontend/src/screens/TodayScreen.tsx`, `frontend/src/screens/AgendaScreen.tsx`
- **Tests:** `frontend/src/tests/today.test.tsx`
- **shadcn components used:** badge, button, tooltip, alert-dialog, accordion, skeleton, separator
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-05-SUMMARY.md` when done
</output>
