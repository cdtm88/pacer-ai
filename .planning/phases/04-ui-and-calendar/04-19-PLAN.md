---
phase: 04-ui-and-calendar
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - api/routes/sessions.py
autonomous: true
gap_closure: true
requirements: [UI-02, ONBD-01]
must_haves:
  truths:
    - "PATCH /sessions/{id} returns the updated session row on a successful status change, so Mark Done succeeds instead of raising a false 404"
    - "Marking another user's session (or a non-existent id) still returns 404 — the ownership filter and the not-found path are preserved"
    - "Onboarding multi-turn persistence is confirmed: save_messages runs after each streamed turn, so the agent advances instead of looping its opening question"
  artifacts:
    - path: "api/routes/sessions.py"
      provides: "update_session PATCH handler that selects the updated row so result.data is populated on success"
      contains: "select(_SESSION_COLUMNS)"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "api/routes/sessions.py"
      via: "markSessionDone PATCH /sessions/{id} now receives a 200 with the updated row instead of a false 404"
      pattern: "update_session"
---

<objective>
Close UAT GAP 3 (Test 9: "mark as done does nothing") and confirm UAT GAP 6 (Test 15: onboarding loop) is closed.

GAP 3 root cause: api/routes/sessions.py update_session calls update(...).eq().eq().execute() WITHOUT a select(). The supabase-py client returns data=[] for an update that has no select(), so the handler's "if not result.data: raise 404" fires on every successful update. The frontend catches the 404 and re-enables the button, so Mark Done appears to do nothing even though the row was updated. Fix: add select(_SESSION_COLUMNS) before execute() — the exact pattern already used by the GET handlers in this file (lines 69, 104, 306).

GAP 6: the prior 04-12 fix wired save_messages into the onboarding stream. The current onboarding.py already calls save_messages after the stream completes (lines 278-291). This plan does NOT re-implement it — it VERIFIES the call path is intact and records the confirmation, so the gap closes with evidence rather than re-opening blindly.

Purpose: UI-02 (Mark Done must mark the session completed) and ONBD-01 (onboarding interview must advance without looping).

Output: update_session selects the updated row so a successful PATCH returns 200 with the row; the onboarding save_messages path is verified present and exercised by the existing test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-calendar/04-UAT.md
@api/routes/sessions.py
@api/routes/onboarding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add select(_SESSION_COLUMNS) to the update_session PATCH handler</name>
  <files>api/routes/sessions.py</files>
  <read_first>
    - api/routes/sessions.py line 37: _SESSION_COLUMNS is the canonical column list used by the GET handlers.
    - api/routes/sessions.py lines 69, 104, 306: the GET/export handlers already call select(_SESSION_COLUMNS) — copy this exact pattern.
    - api/routes/sessions.py lines 245-262: update_session — currently chains table("sessions").update({"status": "completed"}).eq("id", session_id).eq("user_id", user_id).execute() with no select(); then "if not result.data: raise 404". Because supabase-py returns an empty data list for an update without select(), result.data is [] on success and the 404 always fires.
  </read_first>
  <action>
    In update_session (lines 245-251), insert a select(_SESSION_COLUMNS) call into the query chain immediately before execute(), after the two eq() filters. The resulting chain is:
      supabase.table("sessions").update({"status": "completed"}).eq("id", session_id).eq("user_id", user_id).select(_SESSION_COLUMNS).execute()

    With select() present, supabase-py returns the updated row(s) in result.data, so:
    - On a successful update of an owned session, result.data is non-empty and the handler returns result.data[0] (the updated row) — the frontend markSessionDone resolves and the card refetches.
    - When no row matches (non-existent id OR a session owned by another user), result.data is still [] and the existing "if not result.data: raise HTTPException(404, ...)" correctly fires. Do NOT change the 404 block or the eq("user_id", user_id) ownership filter — IDOR protection (T-04-03) must remain.

    Do not change any other handler, the validate_uuid call, or the return shape. This is a one-line insertion mirroring the GET handlers.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && python -m pytest tests/api/test_sessions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The update_session chain calls update(...).eq("id", ...).eq("user_id", ...).select(_SESSION_COLUMNS).execute()
    - The eq("user_id", user_id) ownership filter and the "if not result.data: raise 404" block are unchanged
    - sessions.py imports/parses cleanly and the sessions test module passes
    - A successful PATCH now returns the updated row; a non-matching id/user still returns 404
  </acceptance_criteria>
  <done>PATCH /sessions/{id} returns 200 with the updated row on a successful status change, so Mark Done marks the session completed instead of silently failing with a false 404; ownership and not-found behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the onboarding save_messages persistence path is intact (GAP 6 confirmation)</name>
  <files>api/routes/onboarding.py</files>
  <read_first>
    - api/routes/onboarding.py lines 148-173: save_messages — INSERTs {conversation_id, user_id, role, content} rows; returns early on empty input.
    - api/routes/onboarding.py lines 263-291: _stream_with_metadata — captures the assistant reply into assistant_sink, then after the stream completes builds new_turns (the incoming user message when present plus the assistant reply when the sink is non-empty) and calls save_messages(conversation_id, user_id, new_turns) inside a best-effort try/except.
    - api/routes/onboarding.py lines 250-261: prior-turn loading — load_conversation is called and the incoming body.message is appended before the agent runs, so persisted turns are what make the interview advance.
  </read_first>
  <action>
    This is a verification-and-evidence task; do NOT re-implement save_messages.

    Confirm, by reading the current onboarding.py, that ALL of the following hold:
    1. save_messages is DEFINED (lines ~148-173) and INSERTs rows into the messages table.
    2. save_messages is CALLED after the stream completes in _stream_with_metadata (lines ~278-291), passing both the incoming user message (when body.message is non-empty) and the captured assistant reply (when assistant_sink holds non-empty text).
    3. load_conversation is called before the agent run so persisted turns are reloaded on the next request, which is what stops the opening-question loop.

    If all three hold, the GAP 6 loop is already fixed (04-12). Record this in the SUMMARY with the exact line numbers you confirmed, and run the onboarding test suite to prove it (verify below). Do NOT modify onboarding.py.

    If — and only if — any of the three are NOT present in the current file (e.g. a regression dropped the save_messages call), add the missing piece: after the sse_generator loop completes in _stream_with_metadata, build new_turns from the incoming user message and the assistant_sink reply and call save_messages(conversation_id, user_id, new_turns) inside a try/except that swallows persistence errors (a persistence failure must never break the already-streamed response). Match the existing best-effort pattern exactly. If you make this change, note in the SUMMARY that GAP 6 had regressed and was re-fixed.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && python -m pytest tests/api/test_onboarding.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the confirmed line numbers for the save_messages definition, the save_messages call in _stream_with_metadata, and the load_conversation call
    - save_messages is called with both the user message and the assistant reply after the stream completes
    - tests/api/test_onboarding.py passes
    - onboarding.py is unchanged unless a regression was found (documented in the SUMMARY if so)
  </acceptance_criteria>
  <done>Onboarding multi-turn persistence is confirmed (save_messages runs each turn and prior turns are reloaded), so the interview advances instead of repeating the opening question; GAP 6 is closed with evidence.</done>
</task>

</tasks>

<verification>
- update_session selects the updated row; sessions tests pass; ownership and 404 behavior preserved
- onboarding save_messages call path confirmed present and exercised; onboarding tests pass
- Manual smoke (human): on the Today card click Mark Done — the session marks completed and the card updates; start an onboarding interview, answer the first question, confirm the agent advances rather than re-asking
</verification>

<success_criteria>
- Mark Done marks the session completed (UAT GAP 3 closed)
- Onboarding advances without looping (UAT GAP 6 confirmed closed)
</success_criteria>

<artifacts_produced>
This gap-closure plan modifies:
- api/routes/sessions.py (update_session selects the updated row so success returns 200, not a false 404)
It verifies (and only re-fixes if regressed):
- api/routes/onboarding.py (save_messages persistence path)
No new files; no new exported symbols.
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-and-calendar/04-19-SUMMARY.md` when done
</output>
