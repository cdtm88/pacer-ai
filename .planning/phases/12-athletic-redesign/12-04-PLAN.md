---
phase: 12-athletic-redesign
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - frontend/src/screens/DuringSessionScreen.tsx
autonomous: true
requirements: [D-1, D-2, D-3, D-4, D-7, D-11]
must_haves:
  truths:
    - "The active-session screen renders on a near-black cockpit surface (no pure black), not the old light zone-tinted wash"
    - "The watt target is the arm's-length hero; the timer is demoted to large-but-secondary"
    - "When ftp is null, an effort-word + RPE string renders at the same hero scale and position as the watt target"
    - "A session profile rail at the bottom shows the whole workout as proportional zone-colored bars with the current block lit and elapsed dimmed"
    - "The session-complete CTA uses the single sanctioned --color-achieve orange"
    - "Timer/persistence logic and iOS behavior are unchanged (render-layer-only rebuild)"
  artifacts:
    - "frontend/src/screens/DuringSessionScreen.tsx cockpit render tree (lines 389+) using --cockpit-* tokens and lib/zones map"
  key_links:
    - "SessionRunner reads existing state (displaySecs, zoneColor, target, currentStep, ...) unchanged; only JSX/styles change"
    - "rpe_target threaded from the data-loader into SessionRunner as a new session-level prop for the no-FTP hero"
    - "cockpit rail reuses WorkoutProfileChart's flexBasis/flexGrow geometry over SessionStep[]"
---

<objective>
Slice B (biggest win): rebuild `DuringSessionScreen`'s render layer into a dark cockpit (D-1), invert the hierarchy so watts are the hero and the timer is secondary (D-2), add the no-FTP effort-word+RPE hero fallback (D-4), add the session profile rail (D-3), keep zone colors as the identity accents (D-7), and switch the session-complete CTA to `--color-achieve` (D-11). This is a strictly render-layer rebuild: the timer, persistence, pause, fast-forward, and iOS plumbing are frozen.

Purpose: The on-bike view is the product's signature surface; this is where "SaaS dashboard" becomes "sports product." It is isolated to one file with a hard frozen boundary to protect iOS kill/reopen persistence.
Output: A rebuilt cockpit JSX tree in `DuringSessionScreen.tsx`; `session.test.tsx` stays green (proving persistence untouched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-CONTEXT.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
@.planning/phases/12-athletic-redesign/12-RESEARCH.md
@frontend/src/components/session/WorkoutProfileChart.tsx
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- New `rpe_target` prop on the `SessionRunner` component in `DuringSessionScreen.tsx`
- New cockpit profile-rail JSX sub-region inside `SessionRunner` (no new exported symbol)
- Local `ZONE_META` in `DuringSessionScreen.tsx` is removed in favor of the `@/lib/zones` import
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Cockpit surface + hero inversion + zone-map swap + session-complete CTA</name>
  <files>frontend/src/screens/DuringSessionScreen.tsx</files>
  <read_first>
    - frontend/src/screens/DuringSessionScreen.tsx (FULL — must see the frozen persistence boundary before editing JSX)
    - frontend/src/lib/zones.ts (the consolidated map to import; pctHigh nullable)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen B; Hero numeral scale table; Color section cockpit exception)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (DuringSessionScreen JSX regions + frozen boundary, lines 270-286)
    - .planning/phases/12-athletic-redesign/12-RESEARCH.md (DuringSessionScreen inventory + Pitfall 2, exact line anchors)
  </read_first>
  <action>
    FROZEN BOUNDARY (do not edit, do not reorder, do not change hook call order): everything above line ~389 — `computeRestoredState`/`fastForwardSteps` (125-161), `restoredRef` (190), state/refs (198-203), `useSessionTimer` (208), pause logic (218-236), `buildPayload`/`goNext` (243-270), the three persistence `useEffect`s (272-295), the live-resume effect (305-322), `finishSession` (181-188). Treat these as read-only inputs. The timer-formatting helpers `powerTarget()`/`formatTimer()`/`formatClock()`/`validZone()` stay local (they are not zone-map duplication).

    Replace the local `ZONE_META` object (lines 29-35) with an import of `ZONE_META`, `zoneColor`, `zoneLabel`, and `ZoneKey` from `@/lib/zones`; alias the local `ZoneType` to `ZoneKey`. `powerTarget()` continues to read `ZONE_META[zone].pctLow/pctHigh` from the imported map (pctHigh is `number | null`, so its existing `pctHigh ? ... : ...` branch is preserved unchanged).

    Cockpit surface (D-1): change the main content background at line ~397 from the light zone-mix wash to `var(--color-cockpit-bg)`. Recolor body/secondary text and dividers in the main SessionRunner branch to `--color-cockpit-ink` / `--color-cockpit-ink-2` / `--color-cockpit-line`. No pure black anywhere.

    Hero inversion (D-2), in the hero block (lines ~505-549): render the watt target (the `target` string that `powerTarget()` already produces) as the hero at `clamp(96px, 18vw, 160px)`, Barlow Condensed 700 via `var(--font-family-display)`, color `--color-cockpit-ink`, line-height 1. Demote the timer (`formatTimer(displaySecs)`) to `clamp(48px, 10vw, 72px)`, Barlow Condensed 600, color `--color-cockpit-ink-2`, line-height 1, positioned below the watts. Keep reading the SAME state variables (`displaySecs`, `target`, `zoneColor`, `zoneLabel`/`zoneName`, `currentStep`) — do not recompute them differently. Recolor the zone chip for the dark surface: border + text in the zone color, no background tint (the light-mode `color-mix(...15%)` fill is not used here).

    Pause/Resume/Skip/End controls (lines ~579-632): keep them as custom `<button>` elements (Foundation Fixes 3 explicitly exempts this screen from the `<Button>` migration) — only re-theme borders from `--color-line` to `--color-cockpit-line` and text from `--color-ink-2` to `--color-cockpit-ink-2`. Keep `minHeight: 48` (>=44px touch target).

    Session-complete CTA (D-11): in the session-complete early-return block (lines ~326-371), the ONLY change is the CTA fill color at line ~356 from `var(--color-blue-6)` to `var(--color-achieve)`. The session-complete surface stays LIGHT (it is not part of the cockpit dark scope) — do not darken it.

    iOS preservation: keep `minHeight: '100dvh'` on all four return branches (lines ~334, 391, 690, 723) and both `env(safe-area-inset-*)` padding patterns (341-342 and 463-464) exactly as-is.
  </action>
  <verify>
    <automated>cd frontend && grep -q "@/lib/zones" src/screens/DuringSessionScreen.tsx && grep -q "var(--color-cockpit-bg)" src/screens/DuringSessionScreen.tsx && grep -q "var(--color-achieve)" src/screens/DuringSessionScreen.tsx && test $(grep -c "100dvh" src/screens/DuringSessionScreen.tsx) -eq 4 && npx vitest run src/tests/session.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `DuringSessionScreen.tsx` imports the zone map from `@/lib/zones`; no local `ZONE_META` object literal remains in the file.
    - The main SessionRunner content background uses `var(--color-cockpit-bg)`; the old light `color-mix(... var(--color-surface))` wash is gone from that branch.
    - The watt target renders at `clamp(96px, 18vw, 160px)` with `var(--font-family-display)`; the timer renders at `clamp(48px, 10vw, 72px)` (watts max size exceeds timer max size).
    - The session-complete CTA fill is `var(--color-achieve)`; no `var(--color-blue-6)` remains on that CTA.
    - `grep -c "100dvh"` returns exactly 4; `env(safe-area-inset` still present.
    - No `#000` / `#000000` introduced.
    - `npx vitest run src/tests/session.test.tsx` is green (persistence/fast-forward behavior unchanged).
  </acceptance_criteria>
  <done>Dark cockpit surface, watts-hero/timer-secondary inversion, zone-map consolidated, achieve-orange complete CTA; persistence tests green.</done>
</task>

<task type="auto">
  <name>Task 2: No-FTP effort-word + RPE hero fallback (D-4)</name>
  <files>frontend/src/screens/DuringSessionScreen.tsx</files>
  <read_first>
    - frontend/src/screens/DuringSessionScreen.tsx (SessionRunner props signature lines 167-177; hero block; data-loader lines 643-735 where props are assembled; ftp derived at line 708; session has rpe_target)
    - frontend/src/components/session/SessionCard.tsx (SessionData shape — confirms session.rpe_target is a session-level number|null)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Copywriting Contract: effort-word vocabulary per zone; Hero numeral scale)
    - .planning/phases/12-athletic-redesign/12-RESEARCH.md (Open Question 3: plumb rpe_target as a SessionRunner prop, not into SessionStep)
  </read_first>
  <action>
    Add a session-level `rpe_target: number | null` prop to `SessionRunner` (extend its props type alongside the existing `ftp`). In the data-loader `DuringSessionScreen` (the `<SessionRunner .../>` render at line ~734), pass `rpe_target={session?.rpe_target ?? null}` (for free-ride sessions with no `session`, pass `null`). Do not thread rpe into `SessionStep` — it is session-level, not per-step.

    In the hero block, when `ftp` is null/absent (the same condition `powerTarget()` already detects), render the no-FTP fallback at the SAME hero scale and position as the watt target (`clamp(96px, 18vw, 160px)`, Barlow Condensed 700, `--color-cockpit-ink`, line-height 1): an effort word derived from the current step's zone plus the RPE and a short cue, matching the pattern effort-word, RPE/10, short cue (for example "HARD, 8/10, push"). Map effort words per zone consistent with the recovery->vo2 ordering from the Copywriting Contract: recovery = "EASY", endurance = "STEADY", tempo = "BRISK", threshold = "HARD", vo2 = "MAX EFFORT". Use `rpe_target` for the RPE number when present; if `rpe_target` is null, omit the "/10" segment and show effort word + cue only. The numeric RPE portion may use `.stat-num-hero`; the effort word uses the same display-face treatment. No em dashes in any of these strings (use commas).

    When `ftp` is present, the watt-target hero (Task 1) renders instead — the no-FTP block and the watt block are mutually exclusive on the same hero slot. Do not touch any persistence/timer state.
  </action>
  <verify>
    <automated>cd frontend && grep -q "rpe_target" src/screens/DuringSessionScreen.tsx && npx vitest run src/tests/session.test.tsx 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `SessionRunner`'s props include `rpe_target: number | null`; the data-loader passes `session?.rpe_target ?? null`.
    - When `ftp` is null, the hero slot renders an effort word from the zone->word map (recovery EASY / endurance STEADY / tempo BRISK / threshold HARD / vo2 MAX EFFORT) at `clamp(96px, 18vw, 160px)`, plus the RPE (when present) and a short cue, with no em dash.
    - When `ftp` is present, the watt target renders (no effort-word block).
    - `npx vitest run src/tests/session.test.tsx` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>No-FTP rides show a hero-scale effort-word+RPE cue; rpe_target plumbed session-level; tests/types green.</done>
</task>

<task type="auto">
  <name>Task 3: Session profile rail (D-3)</name>
  <files>frontend/src/screens/DuringSessionScreen.tsx</files>
  <read_first>
    - frontend/src/components/session/WorkoutProfileChart.tsx (FULL — toSegments() lines 34-51, flexBasis/flexGrow render lines 69-114; the geometry to reuse, NOT import as-is)
    - frontend/src/screens/DuringSessionScreen.tsx (steps: SessionStep[], currentIndex, completedDurationSecs; the top progress bar block lines 415-454 to keep)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Screen-by-Screen B: session profile rail spec)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (WorkoutProfileChart geometry donor, lines 289-301)
  </read_first>
  <action>
    Add a new session profile rail element at the BOTTOM of the cockpit content column (D-3), keeping the existing top overall-progress bar (lines 415-454) intact. Build the rail inline in `DuringSessionScreen.tsx` using `WorkoutProfileChart`'s proportional-sizing approach (a flex row of `div`s sized by `flexBasis`/`flexGrow` from each step's `duration`) but over the full `SessionStep[]` array, one segment per step (free-ride sessions can have more granular steps than warmup/main/cooldown, so do not reuse `WorkoutProfileChart`'s fixed 3-segment prop shape — reuse only the layout math). Do NOT import `WorkoutProfileChart` as a component here.

    Each segment's base fill is its zone color via `zoneColor(step.zone)` from `@/lib/zones`. Three visual states keyed off `currentIndex`: the current step (index === currentIndex) is "lit" (full zone-color opacity plus a subtle border/glow in the zone color); already-elapsed steps (index < currentIndex) are "dimmed" via `color-mix(in srgb, <zoneColor> 35%, var(--color-cockpit-bg))`; upcoming steps (index > currentIndex) at standard zone-color opacity. The rail panel sits on `--color-cockpit-surface` with `--color-cockpit-line` separators as needed. Read `currentIndex` and `steps` from existing state only; do not add or mutate any persistence state.
  </action>
  <verify>
    <automated>cd frontend && grep -Eq "flexBasis|flexGrow" src/screens/DuringSessionScreen.tsx && grep -q "color-mix(in srgb" src/screens/DuringSessionScreen.tsx && npx vitest run src/tests/session.test.tsx 2>&1 | tail -6 && npm test -- --run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - A new bottom rail renders one proportional segment per `SessionStep` using `flexBasis`/`flexGrow`, distinct from the retained top overall-progress bar.
    - Segment fills come from `zoneColor(step.zone)`; the current step is visually lit, elapsed steps are dimmed via `color-mix(... 35%, var(--color-cockpit-bg))`, upcoming steps at standard opacity.
    - `WorkoutProfileChart` is not imported into `DuringSessionScreen.tsx` (geometry reused, not the component).
    - `npx vitest run src/tests/session.test.tsx` green; `npm test -- --run` full suite green.
  </acceptance_criteria>
  <done>Bottom session profile rail renders per-step zone bars with lit/dimmed/upcoming states; persistence + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Render-layer rebuild of an already-authenticated screen; no new input, auth flow, persistence, or endpoint. The frozen timer/persistence boundary means even client-side session state is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | DuringSessionScreen persistence logic | medium | mitigate | Frozen-boundary rule (lines <389 read-only) + `session.test.tsx` gate on every task prevents accidental regression of iOS kill/reopen persistence. This is a correctness guard, not a security-surface change. |
| T-12-04b | Information Disclosure | cockpit presentation | low | accept | Presentation-layer-only; no new inputs/auth/persistence/endpoints. Carries forward RESEARCH.md Security Domain conclusion (ASVS L1, no new attack surface); no LLM/tool-call surface in this file. |

No high/critical threats (security_block_on: high). No package installs — no supply-chain gate.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/session.test.tsx` green after every task (persistence untouched).
- `cd frontend && npm test -- --run` full suite green; `npx tsc --noEmit` clean.
- Manual (not gating, tracked in 12-VALIDATION.md): browser visual check of dark cockpit (no pure black, watt hero legible at arm's length) + outstanding physical iOS Safari re-test.
</verification>

<success_criteria>
Dark cockpit surface with watts-as-hero, demoted timer, no-FTP effort-word hero fallback, bottom session profile rail, and achieve-orange complete CTA; zone map consolidated to `@/lib/zones`; persistence/iOS plumbing provably unchanged (session.test.tsx green); no pure black.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-04-SUMMARY.md` when done.
</output>
