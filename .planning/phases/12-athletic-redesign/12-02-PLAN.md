---
phase: 12-athletic-redesign
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/zones.ts
  - frontend/src/lib/format.ts
  - frontend/src/tests/zones.test.ts
autonomous: true
requirements: [D-8, D-7]
must_haves:
  truths:
    - "A single canonical zone map (color + label + percent range) exists in one module"
    - "Existing import sites that pull ZONE_META/zoneColor/ZoneKey from lib/format.ts keep working unchanged"
    - "A smoke test guards the zone map against silent drift (the exact defect this phase retires)"
  artifacts:
    - "frontend/src/lib/zones.ts exporting ZoneKey, ZoneMeta, ZONE_META, zoneColor(), zoneLabel()"
    - "frontend/src/lib/format.ts re-export shim for ZONE_META, zoneColor, ZoneKey"
    - "frontend/src/tests/zones.test.ts drift-guard smoke test"
  key_links:
    - "lib/format.ts re-exports from ./zones so SessionCard.tsx / WorkoutProfileChart.tsx imports are unbroken"
    - "zones.ts pctHigh is number|null so DuringSessionScreen.powerTarget()'s nullable branch is preserved"
---

<objective>
Foundation Slice A, part 2: create the single source of truth for zone metadata (D-8 unification, D-7 identity system). Extract the canonical `ZoneKey`/`ZONE_META`/`zoneColor()` shape (already correct in `lib/format.ts`) into a new `lib/zones.ts`, add `zoneLabel()`, reconcile `pctHigh` to `number | null`, and re-export from `lib/format.ts` so existing import sites are untouched. Lock it with a drift-guard smoke test (Wave 0 gap from 12-VALIDATION.md).

Purpose: This module is the import target every downstream slice (B-E) repoints its local duplicate zone map at. Creating it first (with a re-export shim) lets B-E delete their local copies without breaking any current `lib/format.ts` consumer.
Output: `lib/zones.ts`, `lib/format.ts` re-export shim, `tests/zones.test.ts`. This plan does NOT delete the per-screen duplicate maps — each downstream slice removes its own local copy when it restyles that screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- New file `frontend/src/lib/zones.ts` exporting: type `ZoneKey`, interface `ZoneMeta`, const `ZONE_META`, function `zoneColor(type: string | null): string`, function `zoneLabel(type: string | null): string`
- New file `frontend/src/tests/zones.test.ts`
- Re-export statement in `frontend/src/lib/format.ts`: `export { ZONE_META, zoneColor, zoneLabel, type ZoneKey } from './zones'`
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED - drift-guard smoke test for the consolidated zone map</name>
  <files>frontend/src/tests/zones.test.ts</files>
  <read_first>
    - frontend/src/lib/format.ts (current canonical ZONE_META / zoneColor / ZoneKey, lines 32-54)
    - frontend/src/components/session/ZoneChip.tsx (local ZONE_VAR + ZONE_LABEL to be superseded)
    - frontend/src/screens/DuringSessionScreen.tsx (local ZONE_META with pctHigh: number|null, lines 29-35 — the nullable variant to preserve)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (zone hex table, Foundation Fixes 5)
    - .planning/phases/12-athletic-redesign/12-VALIDATION.md (Wave 0 requirement: zone drift smoke test)
  </read_first>
  <behavior>
    - `ZONE_META` from `@/lib/zones` has exactly the 5 keys: recovery, endurance, tempo, threshold, vo2.
    - Each entry's `color` equals the matching CSS token string: recovery `var(--color-zone-recovery)`, endurance `var(--color-zone-endurance)`, tempo `var(--color-zone-tempo)`, threshold `var(--color-zone-threshold)`, vo2 `var(--color-zone-vo2)`.
    - Each entry's `label` equals: Recovery, Endurance, Tempo, Threshold, VO2 Max (canonical label carried over from lib/format.ts to avoid regressing SessionCard/WorkoutProfileChart consumers).
    - Percent ranges: recovery 0-55, endurance 56-75, tempo 76-90, threshold 91-105, vo2 pctLow 106 with `pctHigh` strictly `null` (open-ended upper bound, preserving DuringSessionScreen.powerTarget()'s nullable branch).
    - `zoneColor('threshold')` returns `var(--color-zone-threshold)`; `zoneColor(null)` and `zoneColor('bogus')` return `var(--color-ink-3)`.
    - `zoneLabel('vo2')` returns `VO2 Max`; `zoneLabel(null)` returns an empty string.
  </behavior>
  <action>
    Create `frontend/src/tests/zones.test.ts` asserting the behavior above by importing from `@/lib/zones`. Run it and confirm it FAILS (module does not exist yet) — this is the RED step. Commit with message `test(12-02): add failing zone-map drift guard`.

    Do not implement `lib/zones.ts` in this task. The test encodes the exact contract Task 2 must satisfy, including the `pctHigh: null` for vo2 and the `VO2 Max` canonical label.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/zones.test.ts 2>&1 | grep -qiE "fail|error|cannot find" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/tests/zones.test.ts` exists and imports from `@/lib/zones`.
    - Running the test fails because `@/lib/zones` does not resolve yet (RED confirmed).
    - Test asserts all 5 keys, the 5 color token strings, the 5 labels (vo2 = `VO2 Max`), the percent ranges (vo2 pctHigh null), and `zoneColor`/`zoneLabel` fallbacks.
  </acceptance_criteria>
  <done>A failing drift-guard test encodes the zone-map contract before implementation.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN - create lib/zones.ts and re-export from lib/format.ts</name>
  <files>frontend/src/lib/zones.ts, frontend/src/lib/format.ts</files>
  <read_first>
    - frontend/src/lib/format.ts (extraction source + the shim insertion point; note it also has ZONE_LABELS/sessionTypeLabel which stay in format.ts)
    - frontend/src/tests/zones.test.ts (the contract from Task 1)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (extraction target shape + re-export shim, lines 32-66)
  </read_first>
  <action>
    Create `frontend/src/lib/zones.ts` exporting: `type ZoneKey = 'recovery' | 'endurance' | 'tempo' | 'threshold' | 'vo2'`; `interface ZoneMeta { color: string; label: string; pctLow: number; pctHigh: number | null }` (note `pctHigh` is nullable, the reconciliation point); `const ZONE_META: Record<ZoneKey, ZoneMeta>` with the 5 entries exactly as the test requires (vo2 `pctHigh: null`, vo2 label `VO2 Max`); `function zoneColor(type: string | null): string` returning `ZONE_META[type].color` for a known key else `var(--color-ink-3)`; and a NEW `function zoneLabel(type: string | null): string` returning `ZONE_META[type].label` for a known key, empty string for null/unknown.

    In `frontend/src/lib/format.ts`: remove the now-duplicated `ZoneKey`, `ZoneMeta`, `ZONE_META`, and `zoneColor` definitions and replace them with a re-export shim `export { ZONE_META, zoneColor, zoneLabel, type ZoneKey } from './zones'` (and `export type { ZoneMeta } from './zones'` if ZoneMeta is referenced elsewhere). Keep `titleCase`, `ZONE_LABELS`, `sessionTypeLabel`, `classifyTsb`, `TsbClass` in `format.ts` unchanged — those are not zone-map duplication. Verify existing `lib/format.ts` importers (SessionCard.tsx, WorkoutProfileChart.tsx) still type-check via the re-export.

    Run the zones smoke test — it must now PASS (GREEN). Commit with message `feat(12-02): consolidate zone map into lib/zones.ts`.

    Zone hex values are NOT changed anywhere; `ZONE_META` stores `var(--color-zone-*)` token references, and the PRD hexes remain defined only in `index.css` (D-7: unchanged).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/zones.test.ts && npx tsc --noEmit 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/zones.ts` exists and exports `ZoneKey`, `ZoneMeta`, `ZONE_META`, `zoneColor`, `zoneLabel`.
    - `frontend/src/lib/format.ts` no longer defines a local `ZONE_META`/`zoneColor`; it re-exports them (and `zoneLabel`, `ZoneKey`) from `./zones`.
    - `npx vitest run src/tests/zones.test.ts` passes (GREEN).
    - `npx tsc --noEmit` reports no new type errors (SessionCard.tsx / WorkoutProfileChart.tsx still resolve their imports).
    - `npm test -- --run` full suite is green.
  </acceptance_criteria>
  <done>Single canonical zone map exists, format.ts re-exports it, drift guard passes, no type regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure client-side utility module refactor; no data, input, or trust boundary is introduced or moved |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02 | Tampering | lib/zones.ts zone map | low | mitigate | Drift-guard smoke test (Task 1) asserts the 5 zone token mappings + labels + ranges, preventing silent divergence. Carries forward RESEARCH.md Security Domain conclusion (ASVS L1, no new attack surface). |

No high/critical threats (security_block_on: high). No package installs — no supply-chain gate required.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/zones.test.ts` passes.
- `cd frontend && npx tsc --noEmit` clean; `cd frontend && npm test -- --run` fully green.
</verification>

<success_criteria>
`lib/zones.ts` is the single canonical zone map (nullable pctHigh, `zoneLabel` added); `lib/format.ts` re-exports it with zero break to existing importers; the drift-guard test passes; full suite green.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-02-SUMMARY.md` when done.
</output>
