---
phase: 12-athletic-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/components/ui/StatTile.tsx
autonomous: true
requirements: [D-5, D-8, D-1]
must_haves:
  truths:
    - "Barlow Condensed 600/700 loads from the Google Fonts CDN alongside Inter"
    - "Every shadcn <Button> variant resolves against the real brand palette (no undefined-token / browser-default rendering)"
    - "Hero-scale numeric readouts render in the loaded display face, not a synthesized Inter 800"
    - "The --cockpit-* scoped dark tokens exist for the Slice B cockpit to consume"
  artifacts:
    - "frontend/index.html font <link> including Barlow+Condensed:wght@600;700"
    - "frontend/src/index.css @theme button-token block, --font-family-display, --cockpit-* tokens"
    - "frontend/src/index.css .stat-num (Inter 700) + .stat-num-hero (Barlow Condensed 700) classes"
  key_links:
    - "button.tsx bg-primary/text-primary-foreground/... -> new --color-primary/... @theme entries"
    - "StatTile value span -> .stat-num-hero -> --font-family-display"
---

<objective>
Foundation Slice A, part 1: fix the two latent typography/token defects and add the scoped dark-cockpit tokens that every downstream slice (B-E) depends on. This maps shadcn's button tokens to the real palette (D-8), loads the Barlow Condensed display face and splits `.stat-num` into inline vs hero classes (D-5), and defines the `--cockpit-*` token set (D-1) without applying it anywhere yet.

Purpose: Unblock all of Slices B-E. Without the `@theme` button-token block, every `<Button>` renders on undefined tokens; without `--font-family-display`/`.stat-num-hero`, hero numerals stay synthesized-bold; without `--cockpit-*`, the cockpit rebuild has no dark surface tokens.
Output: Updated `index.html`, `index.css`, and `StatTile.tsx`; no visual regression to existing screens; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-athletic-redesign/12-CONTEXT.md
@.planning/phases/12-athletic-redesign/12-UI-SPEC.md
@.planning/phases/12-athletic-redesign/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/artifacts created by THIS plan (exclude from drift verification):
- CSS custom properties in `index.css` `@theme`: `--color-primary`, `--color-primary-foreground`, `--color-destructive`, `--color-secondary`, `--color-secondary-foreground`, `--color-accent`, `--color-accent-foreground`, `--color-background`, `--color-foreground`, `--color-border`, `--color-input`, `--color-ring`, `--font-family-display`, `--color-cockpit-bg`, `--color-cockpit-surface`, `--color-cockpit-ink`, `--color-cockpit-ink-2`, `--color-cockpit-line`
- CSS class `.stat-num-hero` in `index.css`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Load Barlow Condensed and add the @theme token blocks</name>
  <files>frontend/index.html, frontend/src/index.css</files>
  <read_first>
    - frontend/index.html (current single Inter-only font link)
    - frontend/src/index.css (current @theme block, lines 3-50; .stat-num at 81-85)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Foundation Fixes sections 1, 3, 7 — the exact token values)
    - .planning/phases/12-athletic-redesign/12-PATTERNS.md (index.css diff target, lines 152-173)
  </read_first>
  <action>
    In `frontend/index.html`, replace the existing single Inter `<link>` (loads `Inter:wght@400;500;600;700`) with a combined Google Fonts stylesheet link that adds `family=Barlow+Condensed:wght@600;700` and keeps the existing Inter weights, retaining `&display=swap`. The combined href is `https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Barlow+Condensed:wght@600;700&display=swap`.

    In `frontend/src/index.css`, inside the existing `@theme` block, append the shadcn button-token mapping exactly as specified in UI-SPEC Foundation Fixes 3, mapping to already-existing palette tokens (all right-hand-side tokens confirmed present in index.css): `--color-primary: var(--color-brand)`, `--color-primary-foreground: #FFFFFF`, `--color-destructive: var(--color-bad)`, `--color-secondary: var(--color-bg-2)`, `--color-secondary-foreground: var(--color-ink)`, `--color-accent: var(--color-blue-0)`, `--color-accent-foreground: var(--color-brand)`, `--color-background: var(--color-surface)`, `--color-foreground: var(--color-ink)`, `--color-border: var(--color-line)`, `--color-input: var(--color-line)`, `--color-ring: var(--color-brand)`. Also add `--font-family-display: "Barlow Condensed", "Inter", ui-sans-serif, sans-serif`.

    Add the scoped dark-cockpit token set to the same `@theme` block (D-1, values from UI-SPEC Foundation Fixes 7): `--color-cockpit-bg: #14171D`, `--color-cockpit-surface: #1D2129`, `--color-cockpit-ink: #F2F3F5`, `--color-cockpit-ink-2: #9AA0AC`, `--color-cockpit-line: #2A2F3A`. Do NOT apply these tokens to any screen in this plan; they are consumed by Slice B (12-04). No pure black (#000) is introduced.

    Do not invent any new hex colors: every button-token entry references an existing palette token; only the five `--color-cockpit-*` hexes are new, and they come verbatim from UI-SPEC.
  </action>
  <verify>
    <automated>cd frontend && grep -q "Barlow+Condensed:wght@600;700" index.html && grep -q -- "--color-primary:" src/index.css && grep -q -- "--font-family-display:" src/index.css && grep -q -- "--color-cockpit-bg:" src/index.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` contains the substring `family=Barlow+Condensed:wght@600;700` and still contains `Inter:wght@400;500;600;700` in one combined link with `display=swap`.
    - `frontend/src/index.css` `@theme` defines all 12 button tokens (`--color-primary`, `--color-primary-foreground`, `--color-destructive`, `--color-secondary`, `--color-secondary-foreground`, `--color-accent`, `--color-accent-foreground`, `--color-background`, `--color-foreground`, `--color-border`, `--color-input`, `--color-ring`) and `--font-family-display`.
    - `frontend/src/index.css` `@theme` defines `--color-cockpit-bg: #14171D`, `--color-cockpit-surface: #1D2129`, `--color-cockpit-ink: #F2F3F5`, `--color-cockpit-ink-2: #9AA0AC`, `--color-cockpit-line: #2A2F3A`.
    - No occurrence of `#000000` or `#000;` is introduced anywhere in the diff.
  </acceptance_criteria>
  <done>Barlow Condensed loads; all button tokens and cockpit tokens defined; no new pure-black.</done>
</task>

<task type="auto">
  <name>Task 2: Split .stat-num and migrate hero-scale usage in StatTile</name>
  <files>frontend/src/index.css, frontend/src/components/ui/StatTile.tsx</files>
  <read_first>
    - frontend/src/index.css (current single .stat-num rule, weight 800 — lines 81-85)
    - frontend/src/components/ui/StatTile.tsx (value span uses .stat-num at clamp(34px,8vw,52px), hero scale)
    - .planning/phases/12-athletic-redesign/12-UI-SPEC.md (Foundation Fixes 2 + Typography "Today-hub StatTile value" row)
  </read_first>
  <action>
    In `frontend/src/index.css`, replace the single `.stat-num` rule (currently `font-weight: 800`, an unloaded/synthesized weight) with TWO classes per UI-SPEC Foundation Fixes 2: (a) `.stat-num` keeps `font-variant-numeric: tabular-nums; letter-spacing: -0.02em;` but corrects `font-weight` to `700` (loaded); (b) new `.stat-num-hero` with `font-family: var(--font-family-display); font-variant-numeric: tabular-nums; letter-spacing: -0.01em; font-weight: 700;`.

    In `frontend/src/components/ui/StatTile.tsx`, change the value span's class from `stat-num` to `stat-num-hero` (StatTile renders at hero scale `clamp(34px, 8vw, 52px)`, so per the Typography table its value moves to the display face). Do not change StatTile's size, layout, props, or any other class.

    Before finishing, grep the frontend for other `stat-num` consumers (`grep -rn "stat-num" frontend/src`). Leave all small/inline tabular usages (agenda date column, ride-row stats, week totals at 12-13px) on `.stat-num` — only StatTile migrates to `.stat-num-hero`. Do not migrate any other usage in this plan.
  </action>
  <verify>
    <automated>cd frontend && grep -q "stat-num-hero" src/index.css && grep -q "stat-num-hero" src/components/ui/StatTile.tsx && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `index.css` defines both `.stat-num` (with `font-weight: 700`) and `.stat-num-hero` (with `font-family: var(--font-family-display)` and `font-weight: 700`); no rule with `font-weight: 800` remains.
    - `StatTile.tsx` value span uses class `stat-num-hero`; no `stat-num` (non-hero) class remains in StatTile.
    - `cd frontend && npm test -- --run` reports the full suite green (0 failures) — confirms no regression to ProgressScreen or any StatTile consumer.
  </acceptance_criteria>
  <done>`.stat-num`/`.stat-num-hero` split live; StatTile on display face; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Fonts CDN -> browser | Static webfont fetch over HTTPS; no user data crosses; identical exposure to the existing Inter fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | index.css @theme / index.html font link | low | accept | Presentation-layer-only change: no new inputs, auth flows, persistence, or endpoints. Carries forward RESEARCH.md Security Domain conclusion (ASVS L1, no new attack surface). Font CDN exposure is pre-existing and unchanged. |

No high/critical threats (security_block_on: high). No package-manager installs in this plan — no supply-chain (T-*-SC) gate required.
</threat_model>

<verification>
- `cd frontend && npm test -- --run` is fully green after Task 2.
- Manual (not gating): open the app, confirm cards/text unchanged; Barlow Condensed available in devtools for `.stat-num-hero` elements.
</verification>

<success_criteria>
Barlow Condensed loads; the full shadcn button token set + `--cockpit-*` + `--font-family-display` exist in `@theme`; `.stat-num`/`.stat-num-hero` split live with StatTile on the display face; full suite green; no pure black introduced.
</success_criteria>

<output>
Create `.planning/phases/12-athletic-redesign/12-01-SUMMARY.md` when done.
</output>
