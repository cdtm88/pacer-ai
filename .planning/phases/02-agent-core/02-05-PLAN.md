---
phase: 02-agent-core
plan: 05
type: execute
wave: 5
depends_on: ["02-02", "02-04"]
files_modified:
  - tests/agent/test_sdk_contract.py
autonomous: true
requirements: [AGENT-01, AGENT-02]
must_haves:
  truths:
    - "The installed anthropic SDK exposes AsyncAnthropic with a messages.stream context manager, proving the loop's streaming entrypoint is real not assumed"
    - "The SDK stream object provides an awaitable get_final_message, matching what run_turn calls after iterating deltas"
    - "The SDK's tool-use streaming event types the loop and mocks depend on (RawContentBlockDeltaEvent text delta path, message stop_reason, ToolUseBlock name/input/id) exist on the real installed types"
    - "The mock-fixture attribute surface in tests/agent/conftest.py matches the real anthropic type surface, so green compliance tests cannot mask a real SDK shape mismatch"
    - "No test in this plan makes a network call to api.anthropic.com"
  artifacts:
    - path: "tests/agent/test_sdk_contract.py"
      provides: "Offline contract-conformance tests asserting the real anthropic SDK surface matches run_turn's assumptions (A1/A2/Open Question 1)"
      contains: "AsyncAnthropic"
  key_links:
    - from: "tests/agent/test_sdk_contract.py"
      to: "anthropic"
      via: "imports the real installed anthropic SDK and introspects its streaming/tool-use type surface"
      pattern: "import anthropic"
    - from: "tests/agent/test_sdk_contract.py"
      to: "agent/loop.py"
      via: "asserts run_turn's required SDK attributes exist on the real SDK (stream, get_final_message, stop_reason)"
      pattern: "from agent"
---

<objective>
Close the highest-risk verification gap in Phase 2: the agent loop (`agent/loop.py`, Plan 02) and the compliance mocks (`tests/agent/conftest.py`, Plan 04) are built entirely on the ASSUMED anthropic SDK streaming/tool-use event shape (RESEARCH.md A1, A2, Assumption A3, Open Question 1 — rated LOW/ASSUMED because the SDK was not installed at research time). Every existing test passes against hand-authored mocks, so a mismatch between the assumed shape and the real installed SDK would leave all four plans green while the agent is broken in production (AGENT-01, AGENT-02). This plan adds `tests/agent/test_sdk_contract.py`: an offline, no-network contract test that imports the REAL installed anthropic SDK and asserts the exact attributes `run_turn` calls (`AsyncAnthropic`, `messages.stream` async context manager, `get_final_message`, the `stop_reason`/`tool_use` event surface) actually exist, and that the conftest mock surface matches the real type surface.

Purpose: Convert RESEARCH.md A1/A2/Open Question 1 from a silent assumption into a hard, offline-verifiable gate. This is the only test in the phase that touches the real anthropic package surface; without it, "compliance suite green" does not imply "loop works against the real SDK."
Output: `tests/agent/test_sdk_contract.py` (offline SDK contract-conformance tests; no live API call).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-core/02-CONTEXT.md
@.planning/phases/02-agent-core/02-RESEARCH.md
@.planning/phases/02-agent-core/02-02-SUMMARY.md
@.planning/phases/02-agent-core/02-04-SUMMARY.md
@agent/loop.py
@tests/agent/conftest.py
@pytest.ini
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline anthropic SDK contract-conformance tests (tests/agent/test_sdk_contract.py)</name>
  <read_first>
    - agent/loop.py (the exact SDK attributes run_turn calls: client.messages.stream(...), async-for over stream events, event.delta.text on content_block_delta, await stream.get_final_message(), final_msg.stop_reason, tool_use block .name/.input/.id)
    - tests/agent/conftest.py (the mock stream surface authored in Plan 04: __aenter__/__aexit__, __aiter__, get_final_message, msg.stop_reason, content blocks with type/name/input/id — these encode the ASSUMED shape this test must validate against the real SDK)
    - .planning/phases/02-agent-core/02-RESEARCH.md (Assumptions A1, A2, A3; Open Question 1 "Anthropic SDK streaming delta event shape"; Pattern 1 streaming loop; Pattern 6 mock fixtures)
    - pytest.ini (asyncio_mode = auto — async tests need no marker)
  </read_first>
  <files>tests/agent/test_sdk_contract.py</files>
  <!-- planner-discipline-allow: api.anthropic.com -->
  <behavior>
    - test_async_anthropic_exists: `anthropic.AsyncAnthropic` exists and is a class; constructing it with a dummy api_key="sk-ant-test-not-real" does not require a network call (construction is offline)
    - test_messages_stream_is_context_manager: an AsyncAnthropic instance exposes `.messages` with a callable `stream` attribute; the object returned by `messages.stream(...)` (without entering it / without a network call) exposes `__aenter__` and `__aexit__` (it is an async context manager), matching `async with client.messages.stream(...)` in run_turn
    - test_stream_get_final_message_attr: the stream manager type (or its entered stream type) declares a `get_final_message` attribute, matching `await stream.get_final_message()` in run_turn
    - test_stop_reason_literal_supported: the anthropic types expose a Message type whose stop_reason includes the literal "tool_use" and "end_turn" the loop branches on (assert via the SDK's typing module or by constructing/inspecting the Message model fields); if the literal set is not introspectable, assert the ToolUseBlock type exists and exposes name/input/id fields
    - test_content_block_delta_text_path: the anthropic types expose the streaming text-delta event type the loop reads `event.delta.text` from (e.g. RawContentBlockDeltaEvent with a TextDelta carrying `.text`); assert the attribute path the loop uses is present on the real type
    - test_conftest_mock_matches_real_surface: every SDK attribute the conftest mock simulates (stop_reason, content block .type/.name/.input/.id, get_final_message) corresponds to a real attribute name on the installed anthropic types — so a green mocked suite cannot diverge from the real SDK
    - test_no_network_call: importing anthropic and running this module performs zero HTTP requests to api.anthropic.com (the test references no live endpoint and never calls .create()/.stream().__aenter__() against the network)
  </behavior>
  <action>
    Create `tests/agent/test_sdk_contract.py`. Import the real installed SDK with `import anthropic` and `from anthropic import AsyncAnthropic` (Plan 01 installed anthropic==0.67.*; if the import fails the test must fail loudly, not skip — this is the gate). Implement the tests described in <behavior> using introspection only, never a network call: construct `AsyncAnthropic(api_key="sk-ant-test-not-real")` (the SDK reads the key lazily and does not validate it at construction, so this is offline), then assert `hasattr(client.messages, "stream")` and that `callable(client.messages.stream)`. For the stream context-manager check, call `client.messages.stream(model="claude-sonnet-4-5", max_tokens=16, messages=[{"role":"user","content":"hi"}])` to obtain the manager object WITHOUT `async with` (do not enter it — entering would open a connection) and assert it has `__aenter__` and `__aexit__`; wrap in try/finally and, if a manager must be closed, close it without awaiting any network I/O. For the type-surface checks, import the concrete types the loop relies on from the `anthropic.types` namespace (for example `from anthropic.types import Message, ToolUseBlock` and the raw streaming delta event type) and assert each required attribute/field exists: `Message` has a `stop_reason` field; `ToolUseBlock` has `name`, `input`, and `id` fields (use `model_fields` for pydantic v2 models or `hasattr`/`__annotations__` as a fallback). For the text-delta path, locate the streaming delta event type used by the SDK (e.g. `RawContentBlockDeltaEvent` / `TextDelta`) and assert the `event.delta.text` access path the loop uses resolves on the real type; if the precise type name differs in 0.67.x, assert via `anthropic.types` module membership that a text-delta type exposing a `.text` attribute exists, and record the resolved type name in the test as a comment for traceability. For test_conftest_mock_matches_real_surface, import nothing from conftest at module scope that triggers fixtures; instead assert the literal attribute NAMES the mock uses ("stop_reason", "type", "name", "input", "id", "get_final_message") each map to a real attribute/field on the corresponding anthropic type, failing if the SDK renamed any of them. Do NOT call `client.messages.create(...)` or enter the stream against the network; do NOT reference api.anthropic.com; do NOT add an ANTHROPIC_API_KEY requirement (the dummy key is inert). Keep every assertion exact and the whole module runnable with `pytest tests/agent/test_sdk_contract.py -x -q` offline.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import anthropic; from anthropic import AsyncAnthropic; assert isinstance(AsyncAnthropic, type)"` exits 0
    - `pytest tests/agent/test_sdk_contract.py -x -q` passes
    - `grep -c 'import anthropic' tests/agent/test_sdk_contract.py` returns at least 1
    - `grep -c 'get_final_message' tests/agent/test_sdk_contract.py` returns at least 1
    - `grep -c 'stop_reason' tests/agent/test_sdk_contract.py` returns at least 1
    - `grep -ci 'api.anthropic.com' tests/agent/test_sdk_contract.py` returns 0
    - `grep -c '.messages.create' tests/agent/test_sdk_contract.py` returns 0 (no live completion call)
    - `pytest tests/agent/ -x -q` still passes (this plan adds tests; it does not break the existing compliance suite)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/agent/test_sdk_contract.py -x -q && test "$(grep -ci 'api.anthropic.com' tests/agent/test_sdk_contract.py)" -eq 0</automated>
  </verify>
  <done>tests/agent/test_sdk_contract.py imports the real anthropic SDK and offline-asserts that AsyncAnthropic, messages.stream (async context manager), get_final_message, the stop_reason tool_use/end_turn branch, the ToolUseBlock name/input/id fields, and the text-delta path all exist on the installed package and match the conftest mock surface — with zero network calls. The existing agent compliance suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assumed SDK shape → real installed SDK | The loop and mocks encode an ASSUMED event shape; a divergence from the real package surface must be caught before it reaches production |
| test harness → external network | The contract test must introspect types only and never open a connection to api.anthropic.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | tests/agent/conftest.py mock surface | mitigate | test_conftest_mock_matches_real_surface asserts the mock's attribute names map to real anthropic type fields, so a green mocked suite cannot diverge from the SDK (closes A1/A2/Open Question 1) |
| T-02-15 | Spoofing | tests/agent/test_sdk_contract.py | mitigate | Construct AsyncAnthropic with an inert dummy key and introspect types only; never call .create()/.stream() over the network, so no real api.anthropic.com request and no live key is exercised |
| T-02-16 | Denial of Service | anthropic SDK upgrade | accept | A future anthropic version bump that renames the streaming surface will fail this contract test loudly rather than silently breaking the loop; pinning is handled by requirements.txt in Plan 01 |
</threat_model>

<verification>
- `pytest tests/agent/test_sdk_contract.py -x -q` passes offline
- `pytest tests/agent/ -x -q` passes (whole agent suite still green with the new contract test added)
- `grep -ci 'api.anthropic.com' tests/agent/test_sdk_contract.py` returns 0
- `grep -c '.messages.create' tests/agent/test_sdk_contract.py` returns 0
</verification>

<success_criteria>
The agent loop's anthropic SDK assumptions (A1/A2/Open Question 1) are now backed by an offline contract test that imports the real installed SDK and asserts every streaming/tool-use attribute run_turn depends on exists and matches the conftest mock surface, with zero network calls — so "compliance suite green" provably implies "the loop targets the real SDK shape," not just the assumed one (AGENT-01, AGENT-02).
</success_criteria>

<output>
Create `.planning/phases/02-agent-core/02-05-SUMMARY.md` when done
</output>
