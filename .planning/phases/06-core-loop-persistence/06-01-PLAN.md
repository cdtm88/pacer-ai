---
phase: 06-core-loop-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_phase6_persistence.sql
autonomous: true
requirements: [FIT-04, PLAN-04, ADAPT-01, ADAPT-04, TRUST-04]
must_haves:
  truths:
    - "Migration 0005 is applied to the linked Supabase project"
    - "sessions.status CHECK constraint accepts 'missed'"
    - "pmc_history has tss and days_of_data columns"
    - "profiles has ftp and lthr columns"
    - "rides has content_hash with a UNIQUE(user_id, content_hash) constraint"
    - "adaptations has trigger_session_ids and status columns"
  artifacts:
    - supabase/migrations/0005_phase6_persistence.sql
  key_links:
    - "Migration applied via supabase db push --linked --yes BEFORE any dependent plan verifies against the live DB"
---

<objective>
Create and apply migration `0005_phase6_persistence.sql`, the schema foundation the rest of Phase 6 depends on. Every other plan in this phase writes to or reads from columns/constraints created here, and three of these changes fix silent production failures already occurring today (pmc_history missing tss/days_of_data columns the code already writes; sessions.status rejecting 'missed'; profiles.ftp not existing while sessions.py:328 selects it).

Phase 6 has no new requirement IDs (ROADMAP lists Requirements: TBD); the IDs above are the existing requirements this migration re-enables by removing schema drift.

Purpose: Remove schema drift that makes the core loop silently fail against a real database. Mocked tests cannot catch missing-column or CHECK-constraint errors, so this must land and be pushed first.
Output: `supabase/migrations/0005_phase6_persistence.sql` applied to the linked project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-core-loop-persistence/06-RESEARCH.md
@.planning/phases/06-core-loop-persistence/06-PATTERNS.md
@supabase/migrations/0001_initial_schema.sql
@supabase/migrations/0002_phase3_schema.sql
@supabase/migrations/0003_phase4_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0005_phase6_persistence.sql</name>
  <files>supabase/migrations/0005_phase6_persistence.sql</files>
  <read_first>
    - supabase/migrations/0003_phase4_schema.sql (exact header + section style to copy: Migration/Applied-via/Purpose header block, numbered `-- ====` sections, ADD COLUMN IF NOT EXISTS throughout)
    - supabase/migrations/0001_initial_schema.sql (sessions.status inline CHECK is `CHECK (status IN ('planned','completed','skipped','partial'))`; the auto-generated constraint name for a single-column inline CHECK is `sessions_status_check`)
    - supabase/migrations/0002_phase3_schema.sql (confirms plans/adaptations/rides/profiles current columns so you do not re-add existing ones)
  </read_first>
  <action>
    Create the new migration file mirroring the 0003 header/section style. Include these DDL changes, all idempotent:
    1. pmc_history: `ADD COLUMN IF NOT EXISTS tss numeric` and `ADD COLUMN IF NOT EXISTS days_of_data int NOT NULL DEFAULT 0` (Pitfall 1: the code at rides.py already writes these keys; upserts silently no-op today because the columns do not exist).
    2. sessions.status CHECK: `ALTER TABLE public.sessions DROP CONSTRAINT IF EXISTS sessions_status_check;` then `ALTER TABLE public.sessions ADD CONSTRAINT sessions_status_check CHECK (status IN ('planned','completed','skipped','partial','missed'));` (drop-then-add is the idempotent form; keeps all existing values plus 'missed' per Pitfall 2).
    3. profiles: `ADD COLUMN IF NOT EXISTS ftp numeric` and `ADD COLUMN IF NOT EXISTS lthr numeric` (both nullable; ftp is write-back-populated by 06-05, lthr by save_profile in 06-02; keeps existing lthr_estimate column untouched).
    4. rides: `ADD COLUMN IF NOT EXISTS content_hash text`, then `ALTER TABLE public.rides DROP CONSTRAINT IF EXISTS rides_user_content_hash_unique;` then `ALTER TABLE public.rides ADD CONSTRAINT rides_user_content_hash_unique UNIQUE (user_id, content_hash);` (composite key is user-scoped so one user's hash never blocks another user's upload; existing rows have content_hash NULL and multiple NULLs remain distinct under a UNIQUE constraint, so no violation on existing data — this is the authoritative dedup guard and mitigates T-06-01).
    5. adaptations: `ADD COLUMN IF NOT EXISTS trigger_session_ids uuid[]` and `ADD COLUMN IF NOT EXISTS status text NOT NULL DEFAULT 'applied'` (existing rows backfill to 'applied'; lifecycle values used by code are 'applied' | 'proposed' | 'superseded'; do NOT add a CHECK constraint here — values are enforced in the application layer to keep the migration idempotent).
    Do NOT add any index beyond the UNIQUE constraint (broader index work is Phase 7). Do NOT modify plans, conversations, or any other table.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0005_phase6_persistence.sql | grep -c "content_hash" | (read n; test "$n" -ge 1)</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0005_phase6_persistence.sql` exists
    - `grep -v '^--' supabase/migrations/0005_phase6_persistence.sql | grep -c "ADD COLUMN IF NOT EXISTS tss"` returns >= 1
    - `grep -v '^--' supabase/migrations/0005_phase6_persistence.sql | grep -c "days_of_data"` returns >= 1
    - File contains `CHECK (status IN ('planned','completed','skipped','partial','missed'))`
    - File contains `ADD COLUMN IF NOT EXISTS ftp numeric` and `ADD COLUMN IF NOT EXISTS lthr numeric`
    - File contains `UNIQUE (user_id, content_hash)`
    - File contains `trigger_session_ids uuid[]` and `status text NOT NULL DEFAULT 'applied'`
    - No `CREATE INDEX` statement appears in the file
  </acceptance_criteria>
  <done>All six schema changes are present in an idempotent migration file styled after 0003.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration to the linked Supabase project</name>
  <files>supabase/migrations/0005_phase6_persistence.sql</files>
  <read_first>
    - supabase/migrations/0004_auto_provision_users.sql (most recent applied migration; confirm 0005 is the next unapplied one)
    - .planning/STATE.md (decision: "Migration applied via supabase db push --linked (non-interactive with --yes flag)")
  </read_first>
  <action>
    This is the mandatory [BLOCKING] schema-push task. Apply the migration to the linked project with `supabase db push --linked --yes` (the non-interactive pattern already used in this repo). This must complete successfully before any dependent plan (06-02..06-05) is verified against live-DB behavior. After the push, run the same command a second time to prove idempotency (a clean re-run must not error). If the push reports auth/link errors, surface the exact error rather than marking the phase verifiable — code-level tests mock the DB and will pass even when the live schema is wrong, so this push is the only guard against schema drift.
  </action>
  <verify>
    <automated>supabase db push --linked --yes</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked --yes` exits 0
    - A second immediate `supabase db push --linked --yes` exits 0 and reports no pending changes (idempotency proof)
    - Live confirmation that `pmc_history.tss`, `pmc_history.days_of_data`, `profiles.ftp`, `profiles.lthr`, `rides.content_hash`, `adaptations.trigger_session_ids`, and `adaptations.status` exist (via Supabase table introspection / list_tables), and that an UPDATE of a sessions row to status='missed' does not raise a check violation
  </acceptance_criteria>
  <done>Migration 0005 is applied to the linked project; a re-run is a no-op; the seven new columns exist and sessions.status accepts 'missed' against the real database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Uploaded FIT bytes and request params cross here; content_hash is server-computed downstream |
| API→DB | All persistence crosses here; RLS + service-role writes already established |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | rides dedup UNIQUE constraint | high | mitigate | Composite `UNIQUE (user_id, content_hash)` keeps dedup user-scoped so one user's hash can never block or collide with another user's upload |
| T-06-02 | Tampering | adaptations.status default | low | accept | New column defaults existing rows to 'applied'; no client input path to this column in this plan |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs zero new packages (RESEARCH: Package Legitimacy Audit not applicable); no supply-chain surface introduced |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-01)

Migration file:
- `supabase/migrations/0005_phase6_persistence.sql`

Columns/constraints created:
- `pmc_history.tss numeric`
- `pmc_history.days_of_data int NOT NULL DEFAULT 0`
- `sessions_status_check` recreated to include `'missed'`
- `profiles.ftp numeric`
- `profiles.lthr numeric`
- `rides.content_hash text`
- `rides_user_content_hash_unique UNIQUE (user_id, content_hash)`
- `adaptations.trigger_session_ids uuid[]`
- `adaptations.status text NOT NULL DEFAULT 'applied'`
</artifacts_produced>

<verification>
- Migration file present with all six schema changes (Task 1 acceptance criteria).
- `supabase db push --linked --yes` exits 0 and is idempotent on re-run (Task 2).
- Live introspection confirms the seven new columns and the 'missed' status value.
</verification>

<success_criteria>
Migration 0005 is authored, idempotent, and applied to the linked Supabase project; downstream plans can now be verified against a schema that matches the code.
</success_criteria>

<output>
Create `.planning/phases/06-core-loop-persistence/06-01-SUMMARY.md` when done.
</output>
