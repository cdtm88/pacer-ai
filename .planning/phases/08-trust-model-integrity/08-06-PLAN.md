---
phase: 08-trust-model-integrity
plan: 06
type: execute
wave: 3
depends_on: [08-04, 08-05]
files_modified:
  - backend/agent/tools.py
  - tests/agent/test_tools_phase3.py
autonomous: true
requirements: [TRUST-07, PLAN-07, PLAN-06]
must_haves:
  truths:
    - "For generate_plan, dispatch_tool discards ANY LLM-supplied current_ctl / ftp_watts / ftp_confidence / load_targets / preferred_days and injects server-verified values instead — the same trust boundary already applied to user_id (D-02/D-07)"
    - "current_ctl comes from the latest pmc_history row (durable), preferred_days from the profiles row (durable); ftp_watts/ftp_confidence/load_targets come from THIS turn's in-memory audit_log entries for estimate_ftp_from_rides/progress_load (D-08 order guarantee)"
    - "When no same-turn estimate_ftp_from_rides/progress_load entry exists, generate_plan receives the cold-start-safe fallback (ftp_confidence='insufficient_data', ftp_watts=None, load_targets={'recommended_ctl_target': current_ctl}) — never the LLM's values (Pitfall 4)"
    - "generate_plan's LLM-facing TOOL_SCHEMA no longer declares the five server-injected keys, so the model is not even invited to supply them"
  artifacts:
    - backend/agent/tools.py
    - tests/agent/test_tools_phase3.py
  key_links:
    - "the injection block is gated to name == 'generate_plan' and runs AFTER the existing user_id allowlist block (which already guarantees user_id is not None for generate_plan)"
    - "current_ctl is queried directly from pmc_history (correct 0.0 for cold-start onboarding), not read from the in-memory audit_log (Open Question 1 resolution)"
    - "TRUST-02 invariant still holds: only the schema's properties/required shrink, the tool name stays in both registry and schemas"
---

<objective>
Close the laundering vector (CONTEXT.md defect #2 / D-02): today the LLM can hand a fabricated `current_ctl`/`ftp_watts`/`load_targets` straight into `generate_plan`; the tool echoes it back and the scanner sees the number in the tool result and calls it "attributed." Extend the exact server-side-injection choke point that 260702-wev built for `user_id` so that, for `generate_plan` only, these five trust-sensitive keys are always sourced from verified server state and any LLM-supplied values are discarded. This also delivers the injection half of D-07 (preferred_days from the profile) that Plan 04's computation consumes.

Purpose: TRUST-07 (D-02 + D-07 injection); completes PLAN-06/PLAN-07 end-to-end.
Output: an injection block + schema tightening in tools.py, and injection tests proving LLM values are discarded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-trust-model-integrity/08-CONTEXT.md
@.planning/phases/08-trust-model-integrity/08-RESEARCH.md
@.planning/phases/08-trust-model-integrity/08-PATTERNS.md
@backend/agent/tools.py
@backend/routes/adaptations.py
@tests/agent/test_tools_phase3.py
@tests/agent/test_loop.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject server-verified generate_plan inputs in dispatch_tool</name>
  <files>backend/agent/tools.py</files>
  <read_first>
    - backend/agent/tools.py (dispatch_tool 559-652 — the existing user_id allowlist block 577-597 to extend AFTER; the get_async_supabase import at line 40 as _get_async_supabase; the audit_log list shape written by Plan 05, entries {tool_use_id, name, result} where result is ToolResult.model_dump())
    - backend/routes/adaptations.py lines 550-558 (the proven pmc_history latest-CTL query shape to reuse)
    - .planning/phases/08-trust-model-integrity/08-RESEARCH.md ("Pattern 2: Server-side value injection" Code Example; Open Question 1 = query pmc_history directly for current_ctl; Pitfall 4 = cold-start-safe fallback for ftp/load)
    - .planning/phases/08-trust-model-integrity/08-PATTERNS.md (dispatch_tool Pattern Assignments — the injection block placement)
  </read_first>
  <action>
    In `backend/agent/tools.py`, add a helper `_last_audit_result(audit_log: list, tool_name: str) -> dict | None` returning the `result` dict of the most recent in-memory audit_log entry whose `name == tool_name` and that carries a `result` (skip error entries). Then, immediately after the existing `if name in {"save_profile", "generate_plan"}:` user_id block, add a block gated to `name == "generate_plan"` that, using `supabase = await _get_async_supabase()`:
    - queries the latest pmc_history row for user_id (select ctl, order by date desc, limit 1) and derives `current_ctl = float((rows[0].get('ctl') or 0) if rows else 0)` using the adaptations.py query shape;
    - queries the profiles row for user_id (select preferred_days) and derives `preferred_days = rows[0].get('preferred_days') or []`;
    - reads `ftp_entry = _last_audit_result(audit_log, 'estimate_ftp_from_rides')` and `load_entry = _last_audit_result(audit_log, 'progress_load')` from THIS turn's in-memory audit_log;
    - rebuilds `inputs` by discarding any LLM-supplied current_ctl/ftp_watts/ftp_confidence/load_targets/preferred_days and setting: current_ctl (from pmc), ftp_watts = ftp_entry value's ftp_watts or None, ftp_confidence = ftp_entry value's confidence or 'insufficient_data', load_targets = load_entry value or {'recommended_ctl_target': current_ctl}, preferred_days (from profiles). Follow RESEARCH Pattern 2 for the exact key paths into the ToolResult-dump dicts. Never fall back to trusting the LLM's supplied values for these five keys (Pitfall 4). This block runs while user_id is guaranteed non-None (the preceding allowlist block already returned an error otherwise).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/agent/test_tools_phase3.py -k generate_plan_injection -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q '_last_audit_result' backend/agent/tools.py` and the injection block references pmc_history, profiles, and both estimate_ftp_from_rides and progress_load.
    - Behavior: the injection test (Task 2) proves bogus LLM-supplied current_ctl/ftp_watts/preferred_days are replaced by the server-sourced values.
    - Behavior: with no same-turn ftp/load audit entry, generate_plan receives ftp_confidence='insufficient_data', ftp_watts=None, load_targets={'recommended_ctl_target': current_ctl}.
  </acceptance_criteria>
  <done>generate_plan's five trust-sensitive inputs are server-injected; LLM-supplied values are discarded with cold-start-safe fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the generate_plan schema and add injection tests</name>
  <files>backend/agent/tools.py, tests/agent/test_tools_phase3.py</files>
  <read_first>
    - backend/agent/tools.py (generate_plan schema 313-364 — properties + required to shrink; the TRUST-02 invariant assertion 372-378)
    - tests/agent/test_loop.py (test_user_id_injected_server_side_through_run_turn ~345 — the patch.dict TOOL_REGISTRY + call_args.kwargs assertion template to mirror; build_fake_client / conftest stream helpers)
    - tests/agent/test_tools_phase3.py (existing structure + naming; add the new generate_plan_injection test here)
    - backend/sports_science/types.py (ToolResult so a fake generate_plan can return a valid result)
  </read_first>
  <action>
    In `backend/agent/tools.py`, edit the `generate_plan` schema: remove `current_ctl`, `load_targets`, `ftp_confidence`, and `ftp_watts` from `input_schema.properties`, and remove `current_ctl`, `load_targets`, `ftp_confidence` from `required` (leaving required = [weekly_hours, back_status, hr_zones]). Update the tool description to note these values are supplied by the server from prior tool results and the profile, so the model must not attempt to provide them. Keep the tool name in both TOOL_REGISTRY and TOOL_SCHEMAS (do not break the TRUST-02 invariant).
    In `tests/agent/test_tools_phase3.py`, add a test named to match the `-k generate_plan_injection` filter (for example `test_generate_plan_injection_discards_llm_values`). Mirror the user_id-injection test: patch `TOOL_REGISTRY['generate_plan']` with a MagicMock returning a valid ToolResult; patch `backend.agent.tools._get_async_supabase` (AsyncMock) to return a chain-mock whose pmc_history query yields a known ctl (e.g. 12.0) and whose profiles query yields a known preferred_days (e.g. ['Monday','Wednesday']); drive run_turn with a fake generate_plan tool_use block whose input supplies BOGUS current_ctl=999, ftp_watts=999, preferred_days=['Xday']; assert the MagicMock's call_args.kwargs has current_ctl==12.0, preferred_days==['Monday','Wednesday'], ftp_confidence=='insufficient_data', ftp_watts is None (no same-turn ftp entry), and load_targets=={'recommended_ctl_target':12.0}; assert the bogus 999 values are absent.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/agent/test_tools_phase3.py -x -q && .venv/bin/python -c "from backend.agent.tools import TOOL_SCHEMAS; gp=[s for s in TOOL_SCHEMAS if s['name']=='generate_plan'][0]; p=gp['input_schema']['properties']; assert 'current_ctl' not in p and 'load_targets' not in p and 'ftp_watts' not in p and 'ftp_confidence' not in p; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: generate_plan's input_schema.properties no longer contains current_ctl/load_targets/ftp_watts/ftp_confidence (the inline python check prints ok); required == [weekly_hours, back_status, hr_zones].
    - Source: importing backend.agent.tools does not raise the TRUST-02 invariant RuntimeError.
    - Test command: `.venv/bin/python -m pytest tests/agent/test_tools_phase3.py -x` exits 0, including the generate_plan_injection case.
  </acceptance_criteria>
  <done>generate_plan schema strips the server-injected keys; injection test proves LLM values are discarded.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `backend/agent/tools.py`: helper `_last_audit_result(audit_log, tool_name) -> dict | None`; generate_plan server-injection block in `dispatch_tool` (current_ctl, ftp_watts, ftp_confidence, load_targets, preferred_days); tightened generate_plan `TOOL_SCHEMA` (properties + required shrunk; description updated)
- `tests/agent/test_tools_phase3.py`: `generate_plan_injection` test proving LLM-supplied values are discarded
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool-call arguments → generate_plan | physiological facts the server already knows must never be transcribed by the LLM |
| ephemeral tool outputs (ftp/load) → generate_plan | when the D-08 order is not honoured, the fallback must be cold-start-safe, not LLM-trusting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | generate_plan input laundering (LLM invents number → tool input → echoed → "attributed") | high | mitigate | Server-inject current_ctl/ftp_watts/ftp_confidence/load_targets/preferred_days from pmc_history/profiles/same-turn audit_log; discard all LLM-supplied values for these keys; strip them from the LLM-facing schema |
| T-08-08 | Tampering (stale data) | generate_plan called out of D-08 order → missing same-turn ftp/load | medium | mitigate | Cold-start-safe fallback (insufficient_data / None / recommended_ctl_target=current_ctl); never fall back to LLM values (Pitfall 4) |
| T-08-SC | Tampering | package installs | low | accept | No new packages; reuses _get_async_supabase + existing query patterns |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/agent/test_tools_phase3.py -x -q` green (generate_plan_injection included).
- generate_plan schema no longer exposes the five server-injected keys; TRUST-02 invariant holds.
- Full suite shows no NEW failures beyond the 9 documented pre-existing baseline failures.
</verification>

<success_criteria>
- LLM-supplied generate_plan trust-sensitive inputs are discarded and server-injected.
- Cold-start fallback proven; schema tightened; TRUST-02 intact.
- No new full-suite regressions.
</success_criteria>

<output>
Create `.planning/phases/08-trust-model-integrity/08-06-SUMMARY.md` when done.
</output>
