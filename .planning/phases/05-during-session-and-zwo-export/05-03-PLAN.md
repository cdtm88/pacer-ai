---
phase: 05-during-session-and-zwo-export
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - frontend/src/stores/uiStore.ts
  - frontend/src/screens/DuringSessionScreen.tsx
  - frontend/src/components/session/DurationPickerModal.tsx
  - frontend/src/screens/TodayScreen.tsx
  - frontend/src/tests/session.test.tsx
autonomous: true
requirements: [IOS-01, IOS-02]
must_haves:
  truths:
    - "DuringSessionScreen shows the current step label (40px bold) and a live MM:SS countdown that ticks down (IOS-02)"
    - "When the timer reaches stepDuration-3s, a 'Starting [next step] in 3...' warning counts 3, 2, 1 below the timer (D-01)"
    - "When the timer hits 0, the screen auto-advances to the next step; tapping 'Skip step' advances immediately without countdown (D-01, D-02)"
    - "When the last step completes, a full-screen 'Session complete' overlay shows total time, '{N} steps completed', and a 'Back to today' button that navigates to / (D-03)"
    - "DuringSessionScreen calls useWakeLock on mount so the screen stays lit (IOS-01)"
    - "DuringSessionScreen loads today's session via useQuery(['session','today']); when freeRideDurationMins is set in the store, it generates 3 generic steps (10%/80%/10%) instead (D-10, D-11, D-12)"
    - "Today's rest-day empty state shows a 'Ride anyway' button that opens a duration picker (30/45/60 + custom 10-180), and selecting a duration stores it and navigates to /session (D-11)"
  artifacts:
    - path: "frontend/src/screens/DuringSessionScreen.tsx"
      provides: "Live during-session stepper with timer, auto-advance, wake lock, complete overlay"
      contains: "useSessionTimer"
      min_lines: 90
    - path: "frontend/src/components/session/DurationPickerModal.tsx"
      provides: "Rest-day Ride-anyway duration picker modal"
      contains: "freeRideDurationMins"
    - path: "frontend/src/stores/uiStore.ts"
      provides: "freeRideDurationMins slice"
      contains: "freeRideDurationMins"
    - path: "frontend/src/tests/session.test.tsx"
      provides: "Timer auto-advance + Session complete overlay tests"
      contains: "Session complete"
  key_links:
    - from: "frontend/src/screens/DuringSessionScreen.tsx"
      to: "frontend/src/hooks/useSessionTimer.ts"
      via: "import { useSessionTimer }"
      pattern: "useSessionTimer"
    - from: "frontend/src/screens/DuringSessionScreen.tsx"
      to: "frontend/src/hooks/useWakeLock.ts"
      via: "import { useWakeLock }"
      pattern: "useWakeLock"
    - from: "frontend/src/components/session/DurationPickerModal.tsx"
      to: "frontend/src/stores/uiStore.ts"
      via: "setFreeRideDurationMins then navigate('/session')"
      pattern: "freeRideDurationMins"
  prohibitions:
    - statement: "No em dash appears in any copy string (Session complete overlay, countdown warning, duration picker)"
      status: resolved
      verification: "grep for the em dash character returns nothing in the modified files"
---

<objective>
Rewrite DuringSessionScreen from the Phase 4 static stub into the live stepper: parse the session structure into 3 steps, drive useSessionTimer + useWakeLock, render the auto-advance countdown warning and Skip control, and show the Session complete overlay. Add the freeRideDurationMins store slice, the rest-day DurationPickerModal, and wire the "Ride anyway" entry point into TodayScreen.

Purpose: this is the user-facing during-session experience (IOS-01, IOS-02 behavior in context). It composes the plan-02 hooks; depends on them existing.
Output: rewritten DuringSessionScreen.tsx, new DurationPickerModal.tsx, uiStore slice, TodayScreen "Ride anyway" wiring, and session.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-during-session-and-zwo-export/05-CONTEXT.md
@.planning/phases/05-during-session-and-zwo-export/05-RESEARCH.md
@.planning/phases/05-during-session-and-zwo-export/05-PATTERNS.md
@.planning/phases/05-during-session-and-zwo-export/05-UI-SPEC.md
</context>

<artifacts_produced>
This plan creates/modifies the following symbols:
- Store slice: `freeRideDurationMins: number | null` and `setFreeRideDurationMins(mins)` added to `UiState` in `frontend/src/stores/uiStore.ts`
- Component: `DurationPickerModal` in `frontend/src/components/session/DurationPickerModal.tsx`
- Rewritten: `DuringSessionScreen` in `frontend/src/screens/DuringSessionScreen.tsx` (now uses useSessionTimer, useWakeLock, parses structure, renders countdown + Skip + Session complete overlay)
- Local helpers in DuringSessionScreen: `formatTimer(secondsLeft)`, `parseSteps(structure, type)`, `generateFreeRideSteps(totalMins)`
- TodayScreen: "Ride anyway" button added to the rest-day empty state
- New test file: `frontend/src/tests/session.test.tsx`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: freeRideDurationMins store slice + DurationPickerModal</name>
  <files>frontend/src/stores/uiStore.ts, frontend/src/components/session/DurationPickerModal.tsx</files>
  <read_first>
    - frontend/src/stores/uiStore.ts (full file: UiState interface lines 5-10 and create() body lines 20-29 - the exact slice pattern to extend)
    - frontend/src/components/session/SessionCard.tsx lines 1-17 and 226-245 (AlertDialog import block and dialog open/close structure to mirror)
    - frontend/src/components/settings/CalendarStatus.tsx (async-action + dialog analog)
    - .planning/phases/05-during-session-and-zwo-export/05-UI-SPEC.md sections "Ride Anyway Rest-Day Flow" and "Duration Picker Modal" (exact copy strings and validation rules)
    - .planning/phases/05-during-session-and-zwo-export/05-PATTERNS.md sections "frontend/src/stores/uiStore.ts" and "frontend/src/components/session/DurationPickerModal.tsx"
  </read_first>
  <action>
    Extend frontend/src/stores/uiStore.ts: add to the UiState interface `freeRideDurationMins: number | null` and `setFreeRideDurationMins: (mins: number | null) => void`; add to the create() body `freeRideDurationMins: null` and `setFreeRideDurationMins: (mins) => useUiStore.setState({ freeRideDurationMins: mins })`. Do not persist this slice to localStorage (unlike iOSBannerDismissed); it is ephemeral per-navigation handoff.

    Create frontend/src/components/session/DurationPickerModal.tsx. Use the shadcn AlertDialog primitive set imported the same way as SessionCard.tsx. Props: `{ open: boolean; onOpenChange: (open: boolean) => void }`. Internal state: const PRESETS = [30, 45, 60]; selected preset (number | null) and custom string input. Title copy: "How long will you ride?" (UI-SPEC). Render three preset buttons labeled "30 min", "45 min", "60 min" - mutually exclusive: selecting one highlights it (use --color-blue-6 accent for the selected fill) and clears the custom input. Render a Custom input labeled "Custom" with placeholder "e.g. 75" and a trailing "min" suffix (14px --color-ink-2). Validate custom as an integer between 10 and 180 inclusive; if out of range show inline error "Enter a time between 10 and 180 minutes" (UI-SPEC, --color-bad). The "Start session" button is disabled when no preset is selected and the custom input is empty or invalid. The cancel button is labeled "Never mind". On "Start session": resolve chosen = selected preset ?? parseInt(custom, 10); call useUiStore.getState().setFreeRideDurationMins(chosen); call onOpenChange(false); then navigate('/session') (useNavigate from react-router). Use design tokens only (--color-surface background, --color-ink/--color-ink-2 text, --color-line borders, --color-blue-6 accent). No em dashes in any copy.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx tsc --noEmit && grep -c "freeRideDurationMins" src/stores/uiStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/stores/uiStore.ts contains `freeRideDurationMins: number | null` and `setFreeRideDurationMins`
    - frontend/src/components/session/DurationPickerModal.tsx exists and exports `DurationPickerModal`
    - DurationPickerModal contains the literal preset labels `30 min`, `45 min`, `60 min` and the title `How long will you ride?`
    - DurationPickerModal contains the validation copy `Enter a time between 10 and 180 minutes`
    - `npx tsc --noEmit` exits 0
    - No em dash character appears in either file
  </acceptance_criteria>
  <done>The store carries an ephemeral freeRideDurationMins slice; DurationPickerModal renders presets + custom input with 10-180 validation, writes the chosen duration to the store, and navigates to /session. tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DuringSessionScreen (timer, auto-advance, wake lock, complete overlay) + Wave 0 test</name>
  <files>frontend/src/screens/DuringSessionScreen.tsx, frontend/src/tests/session.test.tsx</files>
  <read_first>
    - frontend/src/screens/DuringSessionScreen.tsx (full current stub: layout shell lines 19-64, timer typography lines 30-50, End session button to keep)
    - frontend/src/components/session/SessionStepList.tsx (SessionStep type and currentIndex prop contract - reuse unchanged)
    - frontend/src/screens/TodayScreen.tsx lines 32-40 (useQuery(['session','today']) pattern to copy)
    - frontend/src/hooks/useSessionTimer.ts and frontend/src/hooks/useWakeLock.ts (the plan-02 hooks this screen composes - signatures and return shapes)
    - frontend/src/stores/uiStore.ts (freeRideDurationMins slice added in Task 1)
    - frontend/src/lib/api.ts (getSessionToday return type; Session.structure shape - note: api.ts structure type is extended in plan 04, so read the cached query value defensively)
    - frontend/src/tests/today.test.tsx (Vitest + RTL + QueryClient render harness to mirror for session.test.tsx)
    - .planning/phases/05-during-session-and-zwo-export/05-UI-SPEC.md sections "DuringSessionScreen - Timer & Steps", "Session Complete Overlay", "Timer Auto-Advance", "Wake Lock" (exact copy + interaction contract)
    - .planning/phases/05-during-session-and-zwo-export/05-RESEARCH.md Code Examples (formatTimer, parseSteps) and Open Question 3 (zone assignment: endurance for main, recovery for warmup/cooldown)
  </read_first>
  <action>
    First write the Wave 0 test frontend/src/tests/session.test.tsx using Vitest fake timers and the QueryClientProvider render harness from today.test.tsx, with a MemoryRouter. Mock getSessionToday to return a session with a 3-segment structure ({warmup:{duration_minutes, description}, main_set:{...}, cooldown:{...}}). Mock useWakeLock to a no-op (vi.mock the hook module) so the test does not touch navigator. Tests, failing until the rewrite lands:
    - "renders the first step label and a MM:SS timer": render the screen; assert the warmup description text appears and a timer in MM:SS form is present.
    - "auto-advances when the timer hits 0": advance system time past the first step duration; assert the main_set description becomes the current step.
    - "shows Session complete overlay after the last step": advance time past all 3 steps; assert "Session complete" and "3 steps completed" text appear and a "Back to today" button is present.
    - "Skip step advances immediately": click "Skip step"; assert current step advances without waiting for the timer.

    Then rewrite frontend/src/screens/DuringSessionScreen.tsx. Keep the existing layout shell (min-h-screen flex column, backgroundColor var(--color-bg-2), the px-6 pt-12 pb-6 content area, and the bottom-right "End session" outline button styled with --color-bad). Imports: useEffect/useState/useCallback from react, useNavigate from react-router, useQuery from @tanstack/react-query, Button, SessionStepList + SessionStep type, getSessionToday from api, useSessionTimer + useWakeLock hooks, useUiStore.

    Call useWakeLock() at the top of the component (IOS-01). Read freeRideDurationMins from the store. Load session via useQuery(['session','today'], getSessionToday). Compute steps: if freeRideDurationMins is set, generateFreeRideSteps(totalMins) builds 3 steps - Warm-up 10%, Free ride 80%, Cool-down 10% of total, rounded to nearest minute, minimum 3 min per segment (UI-SPEC; zones: warmup/cooldown 'recovery', main 'endurance'); otherwise parseSteps(session.structure, session.type) maps warmup/main_set/cooldown to SessionStep[] (label = segment description, duration = duration_minutes; zone: warmup/cooldown 'recovery', main_set the session type if a valid zone else 'endurance' per RESEARCH Open Question 3). Guard for missing/empty structure (render nothing or a minimal empty state - do not crash).

    Maintain currentIndex state (start 0). currentStep = steps[currentIndex]. Call useSessionTimer(currentStep ? currentStep.duration * 60 : 0). On each step change, call the hook's advance() (via key remount or effect) so the timer resets to the new step's duration. Implement an effect that, when secondsLeft hits 0, advances currentIndex (auto-advance, D-01). Implement the 3-second countdown warning: when secondsLeft <= 3 and there IS a next step, render below the timer "Starting [next step label] in {secondsLeft}..." in --color-warn at 14px (replaces the static sublabel). A "Skip step" button (min 44px height, trailing/right on the current step card) advances currentIndex immediately with no countdown (D-02). Render SessionStepList with steps and currentIndex; render the timer using formatTimer(secondsLeft) in the Display 40px/700 tabular-nums style.

    When currentIndex >= steps.length, render the Session complete overlay INSTEAD of the step list: a full-screen container (same --color-bg-2 background, not a new route) with heading "Session complete" (40px/700 --color-ink), subtext "Total time: {H}h {M}m" if total >= 60 min else "Total time: {M}m {S}s", a line "{N} steps completed", and a primary "Back to today" Button (--color-blue-6 accent) that calls navigate('/'). Track total elapsed as the sum of completed step durations.

    Add local helpers formatTimer(secondsLeft) -> "MM:SS" (two digits each, per RESEARCH), parseSteps, and generateFreeRideSteps. No em dashes in any copy string (use colons/commas as in UI-SPEC). Reset freeRideDurationMins to null when leaving the screen (cleanup) so a stale free-ride duration does not leak into a later real session.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx vitest run src/tests/session.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/screens/DuringSessionScreen.tsx contains `useSessionTimer` and `useWakeLock`
    - DuringSessionScreen contains the copy literals `Skip step`, `Session complete`, `Back to today`, and a `Starting ` countdown prefix
    - DuringSessionScreen no longer renders the Phase 4 placeholder text "Timer activates in next phase"
    - `npx vitest run src/tests/session.test.tsx` exits 0 (renders first step, auto-advances, shows complete overlay, Skip advances)
    - `npx tsc --noEmit` exits 0
    - No em dash character appears in the screen or test file
  </acceptance_criteria>
  <done>DuringSessionScreen drives a live Date.now() timer with 3-second countdown warning, auto-advance and Skip, holds a wake lock, and shows the Session complete overlay with total time and step count; free-ride sessions generate 3 proportional steps. All session tests pass and tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire "Ride anyway" into TodayScreen rest-day empty state</name>
  <files>frontend/src/screens/TodayScreen.tsx</files>
  <read_first>
    - frontend/src/screens/TodayScreen.tsx lines 92-111 (the existing "No session today" rest-day empty state to extend)
    - frontend/src/components/session/DurationPickerModal.tsx (the modal component created in Task 1 - its open/onOpenChange props)
    - .planning/phases/05-during-session-and-zwo-export/05-UI-SPEC.md "Ride Anyway Rest-Day Flow" (the "Ride anyway" secondary button copy)
  </read_first>
  <action>
    In frontend/src/screens/TodayScreen.tsx, add a useState `pickerOpen` (boolean, default false) at the top of the component. In the rest-day empty state block (the `if (!session)` branch rendering "No session today"), add a secondary button labeled "Ride anyway" below the rest-day message that sets pickerOpen to true. Render the DurationPickerModal with open={pickerOpen} and onOpenChange={setPickerOpen} (import DurationPickerModal from '@/components/session/DurationPickerModal'). Style the "Ride anyway" button as a secondary/outline action using design tokens (--color-blue-6 text or --color-line border, not a primary fill) so it reads as secondary to the rest message. Do not alter the session-present branch. No em dashes in copy.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && npx tsc --noEmit && grep -c "Ride anyway\|DurationPickerModal" src/screens/TodayScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/screens/TodayScreen.tsx imports and renders `DurationPickerModal`
    - TodayScreen contains the literal copy `Ride anyway`
    - The "Ride anyway" button appears only inside the `!session` rest-day branch
    - `npx tsc --noEmit` exits 0
    - Existing today.test.tsx still passes: `npx vitest run src/tests/today.test.tsx` exits 0
  </acceptance_criteria>
  <done>The rest-day empty state offers a "Ride anyway" secondary button that opens the duration picker; existing Today tests still pass and tsc is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> duration | Custom duration input is user-controllable free text |
| store -> screen | freeRideDurationMins flows from the picker into the session screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | custom duration input | mitigate | Validate integer 10-180 inclusive before enabling Start; reject out-of-range with inline error (UI-SPEC) |
| T-05-09 | Integrity | stale freeRideDurationMins leaking into a real session | mitigate | Reset slice to null on DuringSessionScreen unmount; prefer real session structure when present |
| T-05-10 | Information disclosure | during-session screen | accept | No PII beyond the user's own session; client-only state; no new network surface |
</threat_model>

<verification>
- `cd /Users/christianmoore/ai/pacer-ai/frontend && npx vitest run src/tests/session.test.tsx src/tests/today.test.tsx` is green
- `cd frontend && npx tsc --noEmit` is clean
- Manual: starting a session shows a ticking timer; the 3-second warning appears; the complete overlay shows after the last step
</verification>

<success_criteria>
- DuringSessionScreen is a live stepper: Date.now() timer, 3-second countdown warning, auto-advance, Skip, wake lock, Session complete overlay (IOS-01/IOS-02 in-context behavior; D-01/D-02/D-03/D-10/D-12)
- "Ride anyway" rest-day flow stores a validated duration and generates 3 proportional steps (D-11)
- All Phase 5 frontend behavior unit-verified; IOS-03 physical-device verification is checkpoint-gated in plan 05
</success_criteria>

<output>
Create `.planning/phases/05-during-session-and-zwo-export/05-03-SUMMARY.md` when done
</output>
