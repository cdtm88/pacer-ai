---
phase: 03-coaching-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - sports_science/profile.py
  - sports_science/plan.py
  - agent/tools.py
  - tests/api/__init__.py
  - tests/api/conftest.py
  - tests/api/test_onboarding.py
  - tests/api/test_rides.py
  - tests/api/test_adaptations.py
  - tests/agent/test_tools_phase3.py
  - tests/fixtures/sample_zwift.fit
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, ONBD-02, ONBD-03, FIT-02, FIT-06]
must_haves:
  truths:
    - "fitdecode==0.11.0 is in requirements.txt and importable"
    - "save_profile (async) and generate_plan (sync) exist and return ToolResult"
    - "TOOL_REGISTRY and TOOL_SCHEMAS each contain exactly 10 entries and the same names"
    - "generate_plan produces a 4-week mesocycle with no power_targets when ftp_confidence is insufficient_data"
    - "generate_plan applies back-protective caps when back_status is moderate"
    - "All Wave 0 test stub files exist and collect without import errors"
    - "A real Zwift .FIT fixture exists at tests/fixtures/sample_zwift.fit"
  artifacts:
    - path: "sports_science/plan.py"
      provides: "generate_plan() pure-computation 4-week mesocycle builder"
      contains: "def generate_plan"
      min_lines: 60
    - path: "sports_science/profile.py"
      provides: "save_profile() async Supabase upsert into profiles"
      contains: "async def save_profile"
      min_lines: 40
    - path: "agent/tools.py"
      provides: "TOOL_REGISTRY and TOOL_SCHEMAS extended to 10 tools atomically"
      contains: "generate_plan"
    - path: "tests/agent/test_tools_phase3.py"
      provides: "TRUST-02 count check + generate_plan + save_profile unit tests"
      contains: "def test_trust02"
      min_lines: 40
    - path: "tests/fixtures/sample_zwift.fit"
      provides: "Real Zwift .FIT fixture for FIT-06 acceptance test"
  key_links:
    - from: "agent/tools.py"
      to: "sports_science/profile.py"
      via: "import save_profile and register in TOOL_REGISTRY + TOOL_SCHEMAS atomically"
      pattern: "from sports_science.profile import save_profile"
    - from: "agent/tools.py"
      to: "sports_science/plan.py"
      via: "import generate_plan and register in TOOL_REGISTRY + TOOL_SCHEMAS atomically"
      pattern: "from sports_science.plan import generate_plan"
---

<objective>
Lay the Wave 1 foundation for all of Phase 3: add the fitdecode dependency, create the two new sports-science tool modules (`save_profile` and `generate_plan`), register them atomically in the tool registry (TRUST-02), and scaffold every Wave 0 test stub plus the real Zwift .FIT fixture. This unblocks the onboarding, FIT, and adaptation plans in later waves.

Purpose: Downstream plans import these tools and write tests against these stubs. The TRUST-02 import-time assertion means the two tool additions must be one atomic edit or the entire test suite breaks. The Zwift fixture is required for the FIT-06 acceptance test.
Output: fitdecode dependency, sports_science/profile.py, sports_science/plan.py, extended agent/tools.py (10 tools), tests/api/ package with conftest + 3 stub files, tests/agent/test_tools_phase3.py, tests/fixtures/sample_zwift.fit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-coaching-loop/03-RESEARCH.md
@.planning/phases/03-coaching-loop/03-PATTERNS.md
@agent/tools.py
@sports_science/load.py
@sports_science/capability_gap.py
@sports_science/types.py
@tests/agent/test_sse.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fitdecode dependency and create the generate_plan + save_profile tool modules</name>
  <files>requirements.txt, sports_science/plan.py, sports_science/profile.py</files>
  <read_first>
    - requirements.txt (current pinned versions — add the new line in the same style)
    - sports_science/load.py (exact analog for sports_science/plan.py: sync function, no DB, returns ToolResult(value, unit, methodology, inputs))
    - sports_science/capability_gap.py (exact analog for sports_science/profile.py: async _get_async_supabase singleton + ToolResult return)
    - sports_science/types.py (ToolResult is frozen pydantic BaseModel with value/unit/methodology/inputs)
    - .planning/phases/03-coaching-loop/03-RESEARCH.md (New Tools Required section, lines 147-228 — exact signatures and value shapes for both tools; Plan Generation Implementation section, lines 403-449 — cold-start, FTP upgrade path, back-protective constraints)
    - .planning/phases/03-coaching-loop/03-PATTERNS.md (sports_science/plan.py and sports_science/profile.py sections, lines 166-281 — copy the singleton and ToolResult shapes verbatim)
  </read_first>
  <action>
    Add `fitdecode==0.11.0` to requirements.txt (CLAUDE.md mandates fitdecode over fitparse; this is the project's authoritative package decision per the Package Legitimacy Audit in RESEARCH.md — already approved, no human checkpoint needed).

    Create sports_science/plan.py with a SYNC function `generate_plan(user_id: str, weekly_hours: float, back_status: str, current_ctl: float, load_targets: dict, hr_zones: list[dict], ftp_confidence: str, ftp_watts: float | None) -> ToolResult`. It performs pure computation only — NO DB calls, NO imports of other sports_science tools (trust model: the agent calls tools in sequence, the tool never chains). Build a 4-week mesocycle (D-09):
    - Session count from weekly_hours: <=1h -> 2 sessions/week, 2-3h -> 3 sessions/week, 4h+ -> 4 sessions/week.
    - Each session object has keys: week (1-4), day (assigned from preferred_days order, fall back to Tuesday/Thursday/Saturday/Sunday), type (endurance/recovery/strength/interval), objective (text), duration_minutes (int), structure (dict with warmup/main_set/cooldown sub-objects each having duration_minutes + description), zone_targets (dict from the passed hr_zones, choose zone 2 for endurance), power_targets (None when ftp_confidence in {"insufficient_data","low"}; otherwise a dict built from ftp_watts), rpe_target (int 1-10).
    - Week 1 policy (hardcoded, NOT LLM judgment): type endurance only, zone 2 aerobic, duration_minutes capped at 45, rpe_target <= 3, power_targets None, no interval/strength type.
    - Week 4 policy: multiply every session duration_minutes by 0.6 (40% volume reduction).
    - back_status == "moderate" policy: cap weeks 1-2 session duration_minutes at 30; no type == "strength" until week 3; rpe_target <= 6 in week 1; include "back_protective" in constraints_applied.
    - Return ToolResult(value={"plan_id": None, "mesocycle_weeks": 4, "sessions": [...], "week4_volume_reduction_pct": 40, "constraints_applied": [...], "methodology": "4-week base mesocycle; Week 1 conservative; Week 4 -40% recovery"}, unit="", methodology="mesocycle_plan_generation", inputs={"user_id": user_id, "weekly_hours": weekly_hours, "back_status": back_status, "ftp_confidence": ftp_confidence}).

    Create sports_science/profile.py with the module-level `_supabase_client` singleton and `_get_async_supabase()` copied from capability_gap.py, plus an ASYNC function `save_profile(user_id: str, fitness_goals: str, weekly_hours: float, preferred_days: list[str], back_status: str, equipment: dict, rpe_baseline: str, lthr_estimate: float | None = None) -> ToolResult`. It maps back_status to a constraints JSONB: moderate -> {"back_issues": True, "load_ramp_flag_threshold_pct": 10}, mild -> {"back_issues": True}, none -> {"back_issues": False}. It upserts into the profiles table on_conflict="user_id" with all interview fields (fitness_goals, weekly_hours, preferred_days, back_status, equipment, rpe_baseline, lthr_estimate, constraints). Returns ToolResult(value={"profile_id": <row id>, "saved": True}, unit="", methodology="profile_persistence", inputs={"user_id": user_id, "back_status": back_status, "weekly_hours": weekly_hours}). Do NOT add @async to generate_plan and do NOT add DB calls to plan.py.
  </action>
  <verify>
    <automated>python -c "from sports_science.plan import generate_plan; r = generate_plan(user_id='u1', weekly_hours=3.0, back_status='none', current_ctl=0.0, load_targets={'recommended_ctl_target': 8.0}, hr_zones=[{'zone': 2, 'lower_bpm': 120, 'upper_bpm': 145}], ftp_confidence='insufficient_data', ftp_watts=None); assert r.value['mesocycle_weeks'] == 4; assert all(s['power_targets'] is None for s in r.value['sessions']); import sports_science.profile; assert callable(sports_science.profile.save_profile); print('TOOLS_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fitdecode==0.11.0" requirements.txt` returns 1
    - generate_plan returns value with mesocycle_weeks == 4
    - Every session in a cold-start (ftp_confidence="insufficient_data") plan has power_targets is None
    - generate_plan is a plain `def` (not `async def`): `grep -c "^async def generate_plan" sports_science/plan.py` returns 0 and `grep -c "^def generate_plan" sports_science/plan.py` returns 1
    - save_profile is `async def`: `grep -c "async def save_profile" sports_science/profile.py` returns 1
    - sports_science/plan.py contains no `import` of other sports_science tool modules (grep for `from sports_science.load`, `from sports_science.zones`, etc. returns 0)
  </acceptance_criteria>
  <done>fitdecode added; both tool modules created with correct sync/async types and ToolResult shapes; cold-start produces no power targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomically register save_profile and generate_plan in the tool registry (TRUST-02)</name>
  <files>agent/tools.py</files>
  <behavior>
    - TOOL_REGISTRY has exactly 10 keys; TOOL_SCHEMAS has exactly 10 entries; their name sets are equal (TRUST-02 import-time assertion does not raise)
    - The module imports without RuntimeError
    - "save_profile" and "generate_plan" are present in both TOOL_REGISTRY and TOOL_SCHEMAS
  </behavior>
  <read_first>
    - agent/tools.py (the self analog — existing import block lines 26-36, TOOL_REGISTRY lines 42-51, TOOL_SCHEMAS lines 57-256, and the TRUST-02 assertion lines 263-269)
    - .planning/phases/03-coaching-loop/03-PATTERNS.md (agent/tools.py MODIFY section, lines 284-329 — exact import lines, registry additions, and schema dict shape)
    - .planning/phases/03-coaching-loop/03-RESEARCH.md (New Tools Required section — full input_schema fields for both tools)
  </read_first>
  <action>
    In a SINGLE atomic edit (TRUST-02 fires at import if the two dicts diverge), make ALL of the following changes to agent/tools.py:
    1. Add imports after the existing sports_science import block: `from sports_science.profile import save_profile` and `from sports_science.plan import generate_plan`.
    2. Add to TOOL_REGISTRY: "save_profile": save_profile and "generate_plan": generate_plan.
    3. Append two schema dicts to TOOL_SCHEMAS. The save_profile schema: name "save_profile", description noting it persists onboarding data and MUST be called only after explicit user approval of the summary (D-03 gate), input_schema with properties user_id (string), fitness_goals (string), weekly_hours (number), preferred_days (array of string), back_status (string enum none/mild/moderate), equipment (object), rpe_baseline (string), lthr_estimate (number), required = [user_id, fitness_goals, weekly_hours, preferred_days, back_status, equipment, rpe_baseline]. The generate_plan schema: name "generate_plan", description noting it returns a structured 4-week plan and must be called only after progress_load and calculate_hr_zones (D-08 order), input_schema with properties user_id (string), weekly_hours (number), back_status (string), current_ctl (number), load_targets (object), hr_zones (array of object), ftp_confidence (string), ftp_watts (number), required = [user_id, weekly_hours, back_status, current_ctl, load_targets, hr_zones, ftp_confidence].

    Do NOT change the existing 8 schema entries or the existing TRUST-02 assertion block. After the edit, len(TOOL_REGISTRY) == 10 and len(TOOL_SCHEMAS) == 10. save_profile is async — dispatch_tool already routes async via the iscoroutinefunction branch (no change to dispatch_tool needed). generate_plan is sync — dispatch_tool runs it via asyncio.to_thread (no change needed).

    Do NOT write the literal TRUST-02 violation message strings or schema-name literals into any head comment as a negative example.
  </action>
  <verify>
    <automated>python -c "from agent.tools import TOOL_REGISTRY, TOOL_SCHEMAS; sn={s['name'] for s in TOOL_SCHEMAS}; rn=set(TOOL_REGISTRY); assert sn==rn, (sn,rn); assert len(TOOL_REGISTRY)==10; assert 'save_profile' in rn and 'generate_plan' in rn; print('TRUST02_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import agent.tools"` exits 0 (TRUST-02 assertion does not raise)
    - len(TOOL_REGISTRY) == 10 and len(TOOL_SCHEMAS) == 10
    - The name set of TOOL_SCHEMAS equals the key set of TOOL_REGISTRY
    - Both "save_profile" and "generate_plan" appear in TOOL_REGISTRY and TOOL_SCHEMAS
    - The existing 8 tool names are still present (no regression)
  </acceptance_criteria>
  <done>Both new tools registered atomically; TRUST-02 invariant holds at import; registry and schemas both at 10.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold Wave 0 test stubs, shared fixtures, and the real Zwift .FIT fixture</name>
  <files>tests/api/__init__.py, tests/api/conftest.py, tests/api/test_onboarding.py, tests/api/test_rides.py, tests/api/test_adaptations.py, tests/agent/test_tools_phase3.py, tests/fixtures/sample_zwift.fit</files>
  <read_first>
    - tests/agent/test_sse.py (exact analog: parse_sse_frames helper lines 33-69, mock run_turn patterns lines 77-99, monkeypatch + ASGITransport pattern lines 110-129 — copy these verbatim into the new test files)
    - .planning/phases/03-coaching-loop/03-PATTERNS.md (all tests/api/* sections lines 367-528 — exact stub shapes, mock run_turn for onboarding, fixture path pattern, Supabase mock pattern, TRUST-02 test)
    - .planning/phases/03-coaching-loop/03-VALIDATION.md (Per-Task Verification Map — the exact test function names each later wave will fill in)
    - .planning/phases/03-coaching-loop/03-RESEARCH.md (Testing Strategy section lines 708-789 — test file table and acceptance-test shape)
  </read_first>
  <action>
    Create tests/api/__init__.py (empty package marker).

    Create tests/api/conftest.py with shared fixtures: a TEST_USER_ID constant (a fixed UUID string), a `mock_supabase_factory(return_rows)` helper that returns an async-callable producing a mock client whose `.table(...).select/insert/upsert/eq/order/execute` chain resolves to an object with `.data = return_rows`, and a `parse_sse_frames` import or re-export. Follow the monkeypatch idioms from test_sse.py.

    Create stub test files with the function names from 03-VALIDATION.md, each defined but skipped via `pytest.mark.skip(reason="Wave N implements")` OR written as a Nyquist scaffold that imports the target module and asserts the symbol exists (so collection passes and later waves fill in the body):
    - tests/agent/test_tools_phase3.py: test_trust02_still_passes_after_new_tools (IMPLEMENT NOW — asserts schema_names == registry_names and len == 10), test_generate_plan, test_cold_start_hr_only, test_power_targets_cold_start, test_session_schema, test_back_constraints. Implement test_generate_plan, test_cold_start_hr_only, test_power_targets_cold_start, test_session_schema, and test_back_constraints NOW since generate_plan exists (call the sync function directly per the PATTERNS unit-test examples). test_back_constraints asserts that with back_status="moderate", weeks 1-2 sessions have duration_minutes <= 30 and no week-1 session has type=="strength".
    - tests/api/test_onboarding.py: test_onboarding_returns_sse (skip — needs Wave 2 router), test_back_status_constraint, test_profile_persisted, test_confirmation_gate (skip — Wave 2). Include a mock_interview_run_turn generator stub and parse_sse_frames so Wave 2 reuses them.
    - tests/api/test_rides.py: test_upload_returns_200, test_fit_parse_warn, test_missing_fields, test_tss_computed, test_session_compliance, test_fit_upload_integration (all skip — Wave 3 router). Add FIXTURE_PATH = pathlib.Path(__file__).parents[1] / "fixtures" / "sample_zwift.fit" and a non-skipped test_fixture_exists that asserts FIXTURE_PATH.exists().
    - tests/api/test_adaptations.py: test_missed_detection, test_micro_macro_branch, test_shift_limit, test_weekly_check, test_intensity_from_tools, test_log_persisted, test_get_adaptations (all skip — Wave 4).

    Acquire the real Zwift .FIT fixture at tests/fixtures/sample_zwift.fit. The fixture MUST be a genuinely parseable .FIT file with a power data stream (FIT-06 acceptance test asserts TSS > 0). Acquisition options in priority order: (1) check whether a sample exists in the repo or developer Downloads; (2) download a public-domain sample indoor/Zwift .FIT from the Garmin FIT SDK examples or a public fixtures repository; (3) generate a synthetic but spec-valid .FIT with a `record` message stream containing `power`, `heart_rate`, `cadence`, and `timestamp` fields at 1 Hz for >= 600 seconds using fitdecode's writer or the Garmin FIT SDK. If none can be obtained autonomously, write tests/fixtures/sample_zwift.fit.README noting the file is required and mark test_fit_upload_integration with an explicit skip reason "real Zwift .FIT fixture not yet acquired" so the gap is visible — do not fabricate a zero-byte file that would silently pass.
  </action>
  <verify>
    <automated>python -m pytest tests/agent/test_tools_phase3.py -x -q && python -m pytest tests/api/ --collect-only -q && echo STUBS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/api/ --collect-only -q` exits 0 (all stub files import and collect)
    - tests/agent/test_tools_phase3.py passes fully (TRUST-02 + generate_plan unit tests are real, not skipped)
    - test_back_constraints asserts moderate back_status caps weeks 1-2 at 30 minutes and no strength in week 1
    - tests/api/__init__.py exists (package marker)
    - tests/fixtures/sample_zwift.fit exists OR test_fit_upload_integration carries an explicit skip reason naming the missing fixture (no silent zero-byte pass)
    - `python -m pytest tests/ -x -q` exits 0 (existing suite still green; new stubs skipped not failing)
  </acceptance_criteria>
  <done>All Wave 0 stubs collect cleanly, generate_plan/TRUST-02 tests pass, and the Zwift fixture is acquired or its absence is explicitly surfaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → tool registry | Claude can only invoke physiological logic via TOOL_REGISTRY; ad-hoc tools forbidden (TRUST-02) |
| save_profile → Supabase | Profile write crosses into DB; uses SERVICE_ROLE_KEY |
| package install → environment | fitdecode added to requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Spoofing | tool registration | mitigate | TRUST-02 import-time assertion guarantees only registered sports_science functions map to schemas; no ad-hoc tool can be injected |
| T-03-05 | Tampering | generate_plan trust model | mitigate | generate_plan does pure computation and imports no other tools; LLM cannot bypass tool sequencing (TRUST-04) |
| T-03-06 | Information Disclosure | save_profile inputs | mitigate | ToolResult.inputs records only user_id/back_status/weekly_hours, never secrets; SERVICE_ROLE_KEY read from env, never logged |
| T-03-SC | Tampering | fitdecode==0.11.0 install | accept | fitdecode is the CLAUDE.md-mandated FIT library (over abandoned fitparse); Package Legitimacy Audit in RESEARCH.md approved it; SUS verdict is a download-data false positive, not a slop signal — no human checkpoint required per audit |
</threat_model>

<verification>
- `python -c "import agent.tools"` exits 0 (TRUST-02 holds)
- `python -m pytest tests/agent/test_tools_phase3.py -x -q` passes
- `python -m pytest tests/ -x -q` passes (no regression; new stubs skip cleanly)
- `grep -c fitdecode requirements.txt` returns 1
</verification>

<success_criteria>
- fitdecode==0.11.0 in requirements.txt
- save_profile (async) and generate_plan (sync) implemented per RESEARCH signatures
- TOOL_REGISTRY and TOOL_SCHEMAS atomically extended to 10 each; TRUST-02 holds
- All Wave 0 test stubs collect; generate_plan + TRUST-02 tests pass
- Real Zwift .FIT fixture present or its absence explicitly flagged
</success_criteria>

<output>
Create `.planning/phases/03-coaching-loop/03-02-SUMMARY.md` when done
</output>
