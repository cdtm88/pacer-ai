---
phase: 11-ride-analysis-dashboard
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/sports_science/zones.py
  - tests/sports_science/test_zones.py
autonomous: true
requirements: [RIDE-04, RIDE-12]
must_haves:
  truths:
    - "time_in_hr_zones returns a ToolResult of 5 per-zone rows with seconds and pct, one row per HR zone"
    - "Zone boundaries come from calculate_hr_zones, never re-derived in this function (D-11-02, TRUST-01)"
    - "Percentages sum to 100 (within rounding) and seconds sum to len(hr_array) for a non-empty array"
    - "A known hand-checked HR array maps to the expected per-zone seconds"
  artifacts:
    - "backend/sports_science/zones.py::time_in_hr_zones"
    - "tests/sports_science/test_zones.py (appended time_in_hr_zones tests)"
  key_links:
    - "time_in_hr_zones calls calculate_hr_zones(lthr).value and applies the exclusive-upper-bound membership convention (>= lower AND < upper, top zone >= lower only)"
    - "Returns the standard ToolResult(value, unit, methodology, inputs) contract consumed by 11-03's route"
---

<objective>
Add the `time_in_hr_zones(hr_array, lthr)` sports-science tool test-first (RIDE-04). It returns a `ToolResult` of per-zone seconds and percentages, reusing `calculate_hr_zones` for boundaries so there is exactly one definition of an HR zone in the codebase (D-11-02, TRUST-01).

Purpose: TRUST-01 is non-negotiable — every physiological number the frontend renders (zone boundaries, time-in-zone percentages) must originate from a deterministic `sports_science/` function returning a methodology-tagged `ToolResult`. No zone maths may live in TypeScript.
Output: `time_in_hr_zones` in `backend/sports_science/zones.py`; appended tests in `tests/sports_science/test_zones.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ride-analysis-dashboard/11-CONTEXT.md
@.planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md
@.planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `backend/sports_science/zones.py::time_in_hr_zones(hr_array: list[float], lthr: float) -> ToolResult` — new pure function beside `calculate_hr_zones`. `ToolResult.value` is a list of `{"zone": int, "name": str, "seconds": int, "pct": float}` rows; `unit="seconds"`; `methodology="Coggan/Allen time-in-zone from LTHR-derived HR-zone boundaries"`; `inputs={"lthr": lthr, "total_seconds": total}`.
- Appended tests in `tests/sports_science/test_zones.py` (`time_in_hr_zones` keyword selectable via `-k time_in_hr_zones`).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing hand-checked tests for time_in_hr_zones</name>
  <files>tests/sports_science/test_zones.py</files>
  <read_first>
    - tests/sports_science/test_zones.py (existing calculate_hr_zones / calculate_power_zones test style — plain asserts, imports from backend.sports_science.zones)
    - backend/sports_science/zones.py (calculate_hr_zones boundary shape: rows with zone, name, lower_bpm, upper_bpm; top zone upper_bpm is None)
    - backend/sports_science/constants.py (HR_ZONE_BOUNDARIES: 5 zones, ratios 0.68/0.83/0.94/1.05, top open-ended)
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Pattern 2 full reference implementation)
  </read_first>
  <behavior>
    - For lthr=160, calculate_hr_zones(160) yields boundaries lower_bpm = round(ratio*160): Z1 [0,109), Z2 [109,133), Z3 [133,150), Z4 [150,168), Z5 [168,None). Construct a hand-checked hr_array and assert the exact per-zone `seconds` counts and that `pct` = round(100*seconds/total,1).
    - Membership is exclusive-upper: a sample exactly equal to a zone's upper_bpm counts in the NEXT zone up, never both (mirrors calculate_hr_zones convention). Include at least one boundary-value sample to lock this.
    - The top zone (upper_bpm None) counts any sample >= its lower_bpm.
    - Result is a ToolResult; `.value` has length 5; `.unit == "seconds"`; `.methodology` contains "time-in-zone"; `.inputs["lthr"] == 160` and `.inputs["total_seconds"] == len(hr_array)`.
    - sum of seconds across zones == len(hr_array); sum of pct == 100.0 within +/- 0.2 rounding tolerance.
    - Empty hr_array: every zone seconds == 0 and pct == 0.0 (no ZeroDivisionError).
  </behavior>
  <action>
    Append test functions to `tests/sports_science/test_zones.py` importing `time_in_hr_zones` from `backend.sports_science.zones` (add it to the existing import line). Name them with the `time_in_hr_zones` keyword: `test_time_in_hr_zones_hand_checked`, `test_time_in_hr_zones_boundary_exclusive_upper`, `test_time_in_hr_zones_toolresult_contract`, `test_time_in_hr_zones_empty_array`. Compute the expected boundaries yourself from lthr=160 and pick an hr_array whose per-zone counts are trivially hand-verifiable (e.g. a fixed list with a known number of samples in each band plus one sample exactly on a boundary). Assert exact `seconds` and `pct`. Do NOT implement `time_in_hr_zones` in this task — the import MUST fail (RED proof).
  </action>
  <verify>
    <automated>python -m pytest tests/sports_science/test_zones.py -k time_in_hr_zones 2>&1 | grep -Eiq "error|failed|cannot import" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - The four named `time_in_hr_zones` tests exist in `tests/sports_science/test_zones.py`.
    - Running `-k time_in_hr_zones` fails at import or on assertions (RED); command prints `RED-CONFIRMED`.
    - Expected boundaries are derived from `calculate_hr_zones(160)`, not hardcoded independent of it.
  </acceptance_criteria>
  <done>Hand-checked time-in-zone tests written and failing because the function does not yet exist.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement time_in_hr_zones reusing calculate_hr_zones</name>
  <files>backend/sports_science/zones.py</files>
  <read_first>
    - backend/sports_science/zones.py (calculate_hr_zones — reuse its output; ToolResult import already present)
    - backend/sports_science/types.py (ToolResult contract: value, unit, methodology, inputs; frozen)
    - tests/sports_science/test_zones.py (the tests written in Task 1)
    - .planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md (time_in_hr_zones ready-to-paste reference, Shared Patterns / TRUST-01)
  </read_first>
  <action>
    Add `time_in_hr_zones(hr_array: list[float], lthr: float) -> ToolResult` beside `calculate_hr_zones`. Get `zones = calculate_hr_zones(lthr).value` (D-11-02 — do not re-derive boundaries). Set `total = len(hr_array)`. For each hr sample, classify into the first zone where `hr >= lower_bpm and (upper_bpm is None or hr < upper_bpm)` and increment that zone's count, breaking after the first match (exclusive-upper, top zone open-ended — mirrors calculate_hr_zones). Build `value` rows `{"zone": z["zone"], "name": z["name"], "seconds": counts[i], "pct": round(100 * counts[i] / total, 1) if total else 0.0}`. Return `ToolResult(value=rows, unit="seconds", methodology="Coggan/Allen time-in-zone from LTHR-derived HR-zone boundaries", inputs={"lthr": lthr, "total_seconds": total})`. Pure function — no DB, no Anthropic import, no numpy required (preserves the sports_science import boundary).
  </action>
  <verify>
    <automated>python -m pytest tests/sports_science/test_zones.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `time_in_hr_zones` exists in `backend/sports_science/zones.py` and calls `calculate_hr_zones(lthr).value` for boundaries.
    - `python -m pytest tests/sports_science/test_zones.py -x` passes all tests including the pre-existing zone tests (GREEN, no regression).
    - `python -m pytest tests/sports_science/test_import_boundary.py -x` still passes (no Anthropic/DB import leaked into sports_science).
  </acceptance_criteria>
  <done>time_in_hr_zones returns the correct ToolResult; all zone tests green; import boundary intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed HR array → zone maths | Operates on an already-parsed in-memory HR array; no untrusted input, no I/O, no new boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-TRUST | Tampering | time_in_hr_zones | low | mitigate | TRUST-01: all zone boundaries and percentages originate here in a ToolResult with a named methodology; no zone maths is ever computed in TypeScript. Reusing calculate_hr_zones prevents a second, driftable definition of a zone (D-11-02). |

No untrusted input crosses a new boundary; endpoint/Storage threats (T-11-01/02/03) are handled in 11-03.
</threat_model>

<verification>
- `python -m pytest tests/sports_science/test_zones.py -x` — all zone tests green.
- `python -m pytest tests/sports_science/test_import_boundary.py -x` — sports_science import boundary intact.
- `ruff check backend/sports_science/zones.py` — clean.
</verification>

<success_criteria>
- time_in_hr_zones returns methodology-tagged per-zone seconds/pct sourced from calculate_hr_zones; hand-checked values pass; TRUST-01 preserved.
</success_criteria>

<output>
Create `.planning/phases/11-ride-analysis-dashboard/11-02-SUMMARY.md` when done.
</output>
