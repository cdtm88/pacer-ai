---
phase: 11-ride-analysis-dashboard
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/routes/_stream_utils.py
  - backend/routes/rides.py
  - tests/api/test_rides_stream.py
autonomous: true
requirements: [RIDE-01, RIDE-02, RIDE-03, RIDE-12]
must_haves:
  truths:
    - "parse_fit_stream returns index-aligned per-second channel arrays (power, heart_rate, cadence, speed, altitude, distance) where every array has the same length"
    - "A channel with 0 or 1 distinct non-null value is reported absent; a channel with 2+ distinct non-null values is reported present"
    - "The Zwift fixture reports altitude absent; the hilly fixture reports altitude present"
    - "lap_bounds contains exactly 6 elapsed-seconds boundaries for both fixtures"
    - "downsample never returns more than 4000 points and preserves the first record"
  artifacts:
    - backend/routes/_stream_utils.py
    - "backend/routes/rides.py::parse_fit_stream"
    - tests/api/test_rides_stream.py
  key_links:
    - "parse_fit_stream builds one row per FIT record keyed to elapsed seconds from the same start_time used for lap_bounds (Pitfall 1 + 2)"
    - "detect_presence and downsample operate on the aligned rows produced by parse_fit_stream"
---

<objective>
Build the backend data layer for ride time-series: a genuinely new sibling parser `parse_fit_stream` plus a dependency-free `_stream_utils.py` (`detect_presence`, `downsample`), driven test-first against the two placed fixtures (RIDE-01, RIDE-02, RIDE-03).

Purpose: Produce index-aligned per-second channel arrays that every chart can share on one X axis (D-11-06 syncId), detect channel presence (D-11-03) so indoor Zwift rides hide elevation, and cap payload size (D-11-04) so mobile Safari Recharts stays performant. This is the foundation the stream endpoint (11-03) serialises.
Output: `backend/routes/_stream_utils.py` (new), `parse_fit_stream` added to `backend/routes/rides.py`, backend parser/util tests in `tests/api/test_rides_stream.py` (new file; the stream-endpoint tests are appended by 11-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ride-analysis-dashboard/11-CONTEXT.md
@.planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md
@.planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `backend/routes/_stream_utils.py` — new module (stdlib-only), functions:
  - `detect_presence(channel_values: list[float | None]) -> bool`
  - `downsample(series: list[dict], target_interval_secs: int = 3, max_points: int = 4000) -> list[dict]`
- `backend/routes/rides.py::parse_fit_stream(file_bytes: bytes) -> Optional[dict]` — new sibling function beside the existing `parse_fit_file`; returns a dict with keys `series` (list of aligned per-record row dicts), `lap_bounds` (list[int] elapsed seconds), `start_time`.
- `tests/api/test_rides_stream.py` — new test file (parser + util coverage; endpoint tests appended later by 11-03).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing tests for _stream_utils + parse_fit_stream against both fixtures</name>
  <files>tests/api/test_rides_stream.py</files>
  <read_first>
    - tests/api/test_rides.py (fixture-path + FIXTURE_PATH pattern, how a .fit is loaded from tests/fixtures/)
    - tests/api/conftest.py (TEST_USER_ID, auth_headers, asyncio_mode=auto — no marker needed)
    - tests/sports_science/test_zones.py (plain-assert unit-test style used in this repo)
    - backend/routes/rides.py (existing parse_fit_file for the field-name/fitdecode conventions the new parser mirrors)
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Pitfall 1, 2, 4, 5; Code Examples for detect_presence/downsample)
  </read_first>
  <behavior>
    - detect_presence([]) is False; detect_presence([5.0, 5.0, 5.0]) is False (1 distinct); detect_presence([None, None]) is False; detect_presence([1.0, 2.0]) is True; detect_presence([None, 3.0, 4.0]) is True.
    - downsample of a 10000-row list returns <= 4000 rows; the first element of the input is the first element of the output (stride sampling preserves index 0); downsample([]) returns [].
    - downsample of an 1800-row list at default target_interval_secs=3 returns ~600 rows (stride 3), still <= 4000.
    - parse_fit_stream(zwift_ride_30min.fit bytes) returns a dict whose `series` rows all contain the same keys, every channel array (derived by reading one key across all rows) has identical length equal to len(series); `lap_bounds` has length 6; detect_presence over the altitude column is False; detect_presence over the power column is True.
    - parse_fit_stream(hilly_ride_30min.fit bytes) returns detect_presence over the altitude column is True; `lap_bounds` length 6.
    - parse_fit_stream(b"not a fit file") returns None (total parse failure contract mirrors parse_fit_file).
  </behavior>
  <action>
    Create `tests/api/test_rides_stream.py`. Import `parse_fit_stream` from `backend.routes.rides` and `detect_presence`, `downsample` from `backend.routes._stream_utils`. Define two `pathlib.Path` fixture constants pointing at `tests/fixtures/zwift_ride_30min.fit` and `tests/fixtures/hilly_ride_30min.fit` using the `pathlib.Path(__file__).parents[1] / "fixtures" / ...` pattern from test_rides.py. Write plain pytest functions (no asyncio marker — these are sync unit tests) named with a `parse`, `channels`, or `downsample` keyword so 11-03 and RESEARCH's `-k` filters select them: e.g. `test_downsample_caps_at_4000`, `test_downsample_preserves_first_and_stride`, `test_detect_presence_rules`, `test_parse_stream_channels_aligned`, `test_parse_stream_zwift_altitude_absent`, `test_parse_stream_hilly_altitude_present`, `test_parse_stream_lap_bounds_six`, `test_parse_stream_corrupt_returns_none`. Assert the exact behaviors in the behavior block. For channel-column extraction in tests, read a key across `[row[k] for row in parsed["series"]]`. Use `len(parsed["lap_bounds"]) == 6` (RESEARCH Pitfall 4 — six laps, never seven). Do NOT implement the parser or utils in this task — the imports MUST fail (ModuleNotFoundError for `_stream_utils`, ImportError/AttributeError for `parse_fit_stream`), which is the RED proof.
  </action>
  <verify>
    <automated>python -m pytest tests/api/test_rides_stream.py -x 2>&1 | grep -Eiq "error|failed|no module named|cannot import" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/test_rides_stream.py` exists and contains the eight named test functions above.
    - Running the file fails at import/collection or on assertions (RED), proving the tests exercise not-yet-written code; command prints `RED-CONFIRMED`.
    - Every lap-count assertion uses `== 6`, never `== 7`.
  </acceptance_criteria>
  <done>Backend parser/util tests are written and fail because parse_fit_stream / _stream_utils do not yet exist.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement _stream_utils.py and parse_fit_stream to pass</name>
  <files>backend/routes/_stream_utils.py, backend/routes/rides.py</files>
  <read_first>
    - backend/routes/rides.py (parse_fit_file lines 66-196 — copy the fitdecode/ErrorHandling.WARN/get_value(...,fallback=None)/session-start_time structure; do NOT copy its append-only-when-present alignment logic)
    - tests/api/test_rides_stream.py (the tests written in Task 1 — implement to satisfy them)
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Pitfall 1 alignment, Pitfall 2 elapsed-seconds, Pitfall 5 enhanced_* fields; Code Examples)
    - .planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md (parse_fit_stream skeleton, presence+downsample verbatim)
  </read_first>
  <action>
    Create `backend/routes/_stream_utils.py` as a stdlib-only module (no imports beyond typing) exposing `detect_presence(channel_values)` returning `len({v for v in channel_values if v is not None}) > 1` (D-11-03), and `downsample(series, target_interval_secs=3, max_points=4000)` returning `series` unchanged when empty else `series[::interval]` where `interval = max(target_interval_secs, -(-len(series) // max_points))` (ceil division; D-11-04). Add a module docstring noting NP/TSS/zone maths are computed from the full array before downsampling so downsample fidelity never affects a physiological number.

    Add `parse_fit_stream(file_bytes: bytes) -> Optional[dict]` to `backend/routes/rides.py` beside `parse_fit_file`. Open with `fitdecode.FitReader(io.BytesIO(file_bytes), error_handling=fitdecode.ErrorHandling.WARN)`; wrap the whole loop in `try/except Exception: logger.warning(...); return None` (same total-failure contract as parse_fit_file so 11-03's 422 path applies). Resolve `start_time` from the `session` frame's `start_time` value, falling back to the first `record` frame's `timestamp` (same resolution parse_fit_file uses). For each `record` frame build ONE row dict and append it to a `series` list — never skip an index (Pitfall 1). Each row has keys: `t` = `int((record.timestamp - start_time).total_seconds())` computed from real timestamps not array index (Pitfall 2); `power`, `heart_rate`, `cadence` via `frame.get_value(name, fallback=None)`; `altitude` = `frame.get_value("enhanced_altitude", fallback=None) or frame.get_value("altitude", fallback=None)`; `speed` = `frame.get_value("enhanced_speed", fallback=None) or frame.get_value("speed", fallback=None)`; `distance` via `frame.get_value("distance", fallback=None)`. Insert `None` (not 0.0) for any missing channel so presence detection stays honest. For each `lap` frame, append `int((lap.get_value("start_time", fallback=record_or_session_start) - start_time).total_seconds())` — actually capture the lap boundary as elapsed seconds from the SAME `start_time` used for `t` (Pitfall 2); collect these into `lap_bounds`. Return `{"series": series, "lap_bounds": lap_bounds, "start_time": start_time}`. Keep this additive: do NOT modify `parse_fit_file`, the upload path, or any existing import.
  </action>
  <verify>
    <automated>python -m pytest tests/api/test_rides_stream.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/routes/_stream_utils.py` exists, imports only stdlib/typing, and both functions match the D-11-03 / D-11-04 rules exactly.
    - `parse_fit_stream` exists in `backend/routes/rides.py`; `parse_fit_file` and the upload path are byte-for-byte unchanged (verify by diff scope).
    - `python -m pytest tests/api/test_rides_stream.py -x` passes all eight tests (GREEN).
    - `python -m pytest tests/api/test_rides.py -x` still passes (no regression to the upload path).
  </acceptance_criteria>
  <done>parse_fit_stream returns aligned rows + 6 lap bounds; presence and downsample behave per D-11-03/D-11-04; all Task 1 tests pass and existing ride tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored .fit bytes → parser | Already-accepted file bytes (passed the 25 MB upload cap) are re-read; this plan only parses them into arrays, it does not fetch them or cross the HTTP boundary (that is 11-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Denial of Service | parse_fit_stream | low | mitigate | Corrupt/malformed bytes are contained by `fitdecode.ErrorHandling.WARN` + a top-level `try/except Exception: return None`, mirroring parse_fit_file. The route (11-03) converts `None` to a 422; the parser itself never raises out. |

No new untrusted input crosses a new boundary in this plan. IDOR (T-11-01) and Storage-object handling (T-11-02) are mitigated in 11-03 where the endpoint and Storage download live.
</threat_model>

<verification>
- `python -m pytest tests/api/test_rides_stream.py -x` — all parser/util tests green.
- `python -m pytest tests/api/test_rides.py tests/sports_science -x` — no regression.
- `ruff check backend/routes/_stream_utils.py backend/routes/rides.py` — clean.
</verification>

<success_criteria>
- Aligned per-second series, correct presence on both fixtures, 6 lap bounds, 4000-point cap all verified by passing tests.
- parse_fit_file and the upload pipeline untouched.
</success_criteria>

<output>
Create `.planning/phases/11-ride-analysis-dashboard/11-01-SUMMARY.md` when done.
</output>
