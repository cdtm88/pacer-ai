---
phase: 11-ride-analysis-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - frontend/src/lib/api.ts
autonomous: true
requirements: [RIDE-06]
must_haves:
  truths:
    - "A typed getRideStream(rideId) fetcher exists and returns a RideStream matching the /rides/{id}/stream response shape"
    - "RideStream types mirror the backend payload exactly (series points, channels map, laps, hr_zone_distribution nullable)"
  artifacts:
    - "frontend/src/lib/api.ts::getRideStream"
    - "frontend/src/lib/api.ts RideStream / RideStreamPoint / RideZoneDistribution interfaces"
  key_links:
    - "getRideStream uses the existing apiFetch wrapper (injects the Supabase JWT) — no new auth code"
    - "Response shape mirrors 11-03's get_ride_stream return keys exactly (series, channels, laps, hr_zone_distribution)"
---

<objective>
Add the frontend contract for the stream endpoint (RIDE-06): the `RideStream` / `RideStreamPoint` / `RideZoneDistribution` interfaces and a `getRideStream(rideId)` fetcher in `frontend/src/lib/api.ts`, mirroring the exact JSON shape 11-03 returns.

Purpose: This is the typed seam the chart (11-05) and screen (11-06) consume. It mirrors the existing `getRides()` / `Ride` conventions so the rest of the frontend stays uniform.
Output: three interfaces + one async fetcher in `api.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md
@.planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates in `frontend/src/lib/api.ts`:
- `interface RideStreamPoint` — `{ t: number; power: number | null; heart_rate: number | null; cadence: number | null; speed: number | null; altitude: number | null; distance: number | null }`
- `interface RideZoneDistribution` — `{ zone: number; name: string; seconds: number; pct: number }`
- `interface RideStream` — `{ series: RideStreamPoint[]; channels: Record<'power'|'heart_rate'|'cadence'|'speed'|'altitude'|'distance', boolean>; laps: number[]; hr_zone_distribution: RideZoneDistribution[] | null }`
- `async function getRideStream(rideId: string): Promise<RideStream>`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add RideStream types + getRideStream fetcher</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (apiFetch wrapper lines 12-23; getRides lines 154-159; Ride interface lines 84-98 — match this exact shape/placement)
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Frontend fetcher code example under Code Examples — matches getRides shape)
    - .planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md (RideStream interface + getRideStream ready-to-copy block)
    - .planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md (confirms distance is readout-only, not its own chart — the type still carries distance)
  </read_first>
  <action>
    In `frontend/src/lib/api.ts`, add the three interfaces (`RideStreamPoint`, `RideZoneDistribution`, `RideStream`) near the other exported interfaces (after `Ride`), with the exact field names and nullability listed in Artifacts above — these must byte-match 11-03's response keys (`series`, `channels`, `laps`, `hr_zone_distribution`) so no adapter is needed. Add `export async function getRideStream(rideId: string): Promise<RideStream>` modeled on `getRides`: call `apiFetch(\`/api/rides/${rideId}/stream\`)`, throw `new Error(\`getRideStream failed: ${res.status}\`)` when `!res.ok`, and `return res.json() as Promise<RideStream>`. Reuse the existing `apiFetch` wrapper (it injects the Supabase JWT) — do not add new auth handling. Do not modify any existing export.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -Eiq "error" && echo TYPECHECK-FAIL || echo TYPECHECK-OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` exports `RideStream`, `RideStreamPoint`, `RideZoneDistribution`, and `getRideStream`.
    - `RideStream.channels` is typed as `Record<'power'|'heart_rate'|'cadence'|'speed'|'altitude'|'distance', boolean>` and `hr_zone_distribution` is `RideZoneDistribution[] | null`.
    - `getRideStream` calls `apiFetch('/api/rides/<id>/stream')` and returns the parsed JSON typed as `RideStream`.
    - `npx tsc --noEmit` reports no errors (command prints `TYPECHECK-OK`).
  </acceptance_criteria>
  <done>The typed getRideStream fetcher and RideStream types exist and the project typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | The fetcher calls the already-secured `/api/rides/{id}/stream`; auth is carried by the existing apiFetch JWT injection. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | getRideStream | low | mitigate | Access control is enforced server-side (11-03 dual-filter IDOR). The fetcher adds no client-side authorization logic and cannot widen access; it only forwards the JWT via apiFetch. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — no type errors.
- `cd frontend && npx eslint src/lib/api.ts` — clean.
</verification>

<success_criteria>
- Typed fetcher + interfaces exist, mirror the backend contract exactly, and typecheck.
</success_criteria>

<output>
Create `.planning/phases/11-ride-analysis-dashboard/11-04-SUMMARY.md` when done.
</output>
