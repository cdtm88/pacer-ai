---
phase: 11-ride-analysis-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["11-04"]
files_modified:
  - frontend/src/components/rides/RideChart.tsx
  - frontend/src/tests/rideChart.test.tsx
autonomous: true
requirements: [RIDE-07, RIDE-08, RIDE-09, RIDE-12]
must_haves:
  truths:
    - "RideChart renders exactly one chart card per present channel; an absent channel renders no card at all"
    - "When channels.altitude is false, no elevation chart is in the DOM"
    - "All channel charts share syncId='ride' so hovering one moves the readout across all"
    - "A synced readout row shows time as 'Mm SSs', a lap chip, and each present channel's value"
    - "The time-in-zone section renders only when hr_zone_distribution is not null; otherwise nothing"
  artifacts:
    - "frontend/src/components/rides/RideChart.tsx"
    - "frontend/src/tests/rideChart.test.tsx"
  key_links:
    - "RideChart imports RideStream/RideZoneDistribution types from lib/api and ZoneChip/ZoneType from session/ZoneChip (no hand-rolled zone map)"
    - "Recharts ReferenceLine per lap from stream.laps, no isFront/alwaysShow (Recharts v3), isAnimationActive=false"
    - "Channel presence gates each chart card; hr_zone_distribution null gates the whole zone section"
---

<objective>
Build `RideChart.tsx` (RIDE-07/08/09): one Recharts line chart per present channel with a shared `syncId` for cross-chart hover sync (D-11-06), lap `ReferenceLine`s, a synced readout row (time / lap / per-channel values), and a time-in-zone bar + rows from the backend distribution. Test-first for the absent-channel and null-distribution behaviors (RIDE-12 frontend half).

Purpose: This is the deep-dive visualization. Absent channels (D-11-03) render no card so indoor Zwift rides show 4 charts not 5; the zone-model selector and smoothing toggle are OUT of scope (D-11-07). All zone maths come pre-computed from the backend — any `if (hr > X)` classification in TS would be a TRUST-01 defect.
Output: `frontend/src/components/rides/RideChart.tsx` (new; creates the `components/rides/` directory), `frontend/src/tests/rideChart.test.tsx` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md
@.planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md
@.planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `frontend/src/components/rides/RideChart.tsx` — exports `RideChart({ stream }: { stream: RideStream })`. Internal: a `CHART_CONFIG` array (one entry per chartable channel: power, heart_rate, cadence, speed, altitude — NOT distance), a synced readout row, and a time-in-zone section. Reuses `ZoneChip`/`ZoneType` and `--color-zone-*` tokens.
- `frontend/src/tests/rideChart.test.tsx` — Vitest + @testing-library/react component tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing component tests for absent-channel and null-distribution behavior</name>
  <files>frontend/src/tests/rideChart.test.tsx</files>
  <read_first>
    - frontend/src/tests/history.test.tsx (Vitest + render() pattern, how a component is mounted and queried)
    - frontend/src/tests/AppLayout.test.tsx (render + role/text queries in this repo)
    - frontend/src/lib/api.ts (RideStream / RideStreamPoint / RideZoneDistribution shapes to build fixtures)
    - .planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md (Screen & Component Contract: card per present channel, chart title text "Power"/"Heart rate"/"Cadence"/"Speed"/"Elevation", "Time in zone" heading)
  </read_first>
  <behavior>
    - Given a RideStream with channels.altitude=false (and power/heart_rate true), RideChart renders no element containing the elevation chart title "Elevation"; it DOES render titles "Power" and "Heart rate".
    - Given channels with power=true, heart_rate=true, cadence=false, speed=false, altitude=false, exactly two chart card titles render (Power, Heart rate).
    - Given hr_zone_distribution=null, no "Time in zone" heading is in the DOM.
    - Given a non-null hr_zone_distribution (5 rows), the "Time in zone" heading renders and each zone label (Recovery, Endurance, Tempo, Threshold, VO2max) appears.
    - The synced readout row renders a time formatted as "Mm SSs" for the first series point (e.g. t=0 → "0m 0s").
  </behavior>
  <action>
    Create `frontend/src/tests/rideChart.test.tsx`. Import `RideChart` from `../components/rides/RideChart` and `render`, `screen` from `@testing-library/react`. Build small in-file `RideStream` fixtures (2-3 series points is enough) for each case. Note Recharts uses `ResponsiveContainer` which needs a width; if charts do not measure in jsdom, assert on the presence/absence of the chart card TITLE text (e.g. `screen.queryByText('Elevation')`) and the "Time in zone" heading rather than SVG internals — titles and the zone section are plain DOM and render regardless of chart measurement. Write tests: `renders no elevation card when altitude absent`, `renders one card per present channel`, `hides time-in-zone section when distribution null`, `shows time-in-zone rows when distribution present`, `formats readout time as Mm SSs`. Do NOT implement RideChart yet — the import fails (RED).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/rideChart.test.tsx 2>&1 | grep -Eiq "fail|error|cannot find module" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/tests/rideChart.test.tsx` exists with the five named tests.
    - Running it fails because `RideChart` does not exist yet (RED); command prints `RED-CONFIRMED`.
    - Tests assert on chart-title text and the "Time in zone" heading (DOM), not on Recharts SVG measurement.
  </acceptance_criteria>
  <done>Frontend component tests written and failing because RideChart is not implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement RideChart.tsx per the UI-SPEC</name>
  <files>frontend/src/components/rides/RideChart.tsx</files>
  <read_first>
    - frontend/src/components/progress/PmcChart.tsx (chart chrome: AXIS_TICK const, card-elev wrapper, ResponsiveContainer→LineChart, CartesianGrid stroke, XAxis/YAxis props, ReferenceLine dashed, isAnimationActive=false, PmcTooltip payload-driven row pattern)
    - frontend/src/components/session/ZoneChip.tsx (ZoneType, ZONE_VAR, ZONE_LABEL — reuse directly, do not hand-roll)
    - frontend/src/lib/api.ts (RideStream, RideStreamPoint, RideZoneDistribution types)
    - frontend/src/tests/rideChart.test.tsx (the tests to satisfy)
    - .planning/phases/11-ride-analysis-dashboard/11-UI-SPEC.md (full Screen & Component Contract for RideChart: sizes, colors, copy, formatter)
    - .planning/phases/11-ride-analysis-dashboard/11-RESEARCH.md (Pattern 3 Recharts styling; Anti-Pattern: no isFront/alwaysShow; no zone maths in TS)
    - .planning/phases/11-ride-analysis-dashboard/11-PATTERNS.md (RideChart analog blocks)
  </read_first>
  <action>
    Create `frontend/src/components/rides/RideChart.tsx` exporting `RideChart({ stream }: { stream: RideStream })`. Import Recharts primitives (`LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ReferenceLine, ResponsiveContainer`) and `import { ZoneChip, type ZoneType } from '../session/ZoneChip'`; import the `RideStream` types from `../../lib/api`. Reuse the `AXIS_TICK` constant shape from PmcChart (`{ fontSize: 11, fill: 'var(--color-ink-3)', fontVariantNumeric: 'tabular-nums' }`).

    Define a `CHART_CONFIG` array of chartable channels in order: power (title "Power", stroke `var(--color-zone-endurance)`), heart_rate ("Heart rate", `var(--color-warm)`), cadence ("Cadence", `var(--color-ink-3)`), speed ("Speed", `var(--color-blue-4)`), altitude ("Elevation", `var(--color-good)`). `distance` is NOT charted (readout-only per UI-SPEC / D-11-07 spirit). Render one `card-elev` panel (padding `16px 12px 8px`) per config entry WHERE `stream.channels[key]` is true — an absent channel renders no card (no placeholder). Each panel: a Label-role title (13px/600) top-left; a `height: 200` chart area; `<ResponsiveContainer><LineChart data={stream.series} syncId="ride" margin={{ top: 6, right: 8, bottom: 0, left: 0 }}>`; `<CartesianGrid stroke="var(--color-line-2)" vertical={false} />`; `<XAxis dataKey="t" tick={AXIS_TICK} tickLine={false} axisLine={{ stroke: 'var(--color-line)' }} tickFormatter={(s) => \`${Math.round(s/60)}m\`} />`; `<YAxis tick={AXIS_TICK} tickLine={false} axisLine={false} width={38} />`; a `<Tooltip>` whose content drives the shared readout (see below); one `<ReferenceLine>` per `stream.laps` value with `x={t} stroke="var(--color-line)" strokeDasharray="3 3"` and NO `isFront`/`alwaysShow` (removed in Recharts v3); one `<Line type="monotone" dataKey={key} stroke={color} strokeWidth={1.5} dot={false} isAnimationActive={false} />`.

    Add a NEW `formatRideTime(seconds)` helper returning `"Mm SSs"` (e.g. `0m 0s`, `5m 32s`) — deliberately distinct from DuringSessionScreen's `MM:SS` colon timer (not a reuse case, RIDE-08). Render a synced readout row as the FIRST element in the scroll flow (a `card-elev` bar, not sticky): a lap chip pill (`background: color-mix(in srgb, var(--color-brand) 10%, transparent)`, text "Lap N", 13px/600, `--color-brand`, `border-radius: 999px`, padding `2px 10px`), the time via `formatRideTime`, and one small label/value pair per PRESENT channel (uppercase Micro label in the channel color, Body-role value in `--color-ink`). Drive the hovered point from Recharts' synced Tooltip payload (mirror PmcChart's payload-reading pattern); before any hover, show the values at `stream.series[0]` (t=0) as the rest state (RIDE-08). Distance appears in this row only (label "DIST") when `channels.distance` is true.

    Time-in-zone section (RIDE-09): render ONLY when `stream.hr_zone_distribution != null` (this covers both no-HR and no-LTHR — no disabled variant). Heading "Time in zone" (Heading role 18px/600, `lg`=24px gap above, `md`=16px below). A single horizontal stacked bar (`height: 12px`, `border-radius: 6px`, segments touching) with 5 segments in zone order, each width = that zone's `pct`, fill = the zone's `--color-zone-*` token. Map `hr_zone_distribution` rows (zone 1..5) to `ZONE_ORDER: ZoneType[] = ['recovery','endurance','tempo','threshold','vo2']` by index. Below the bar, one row per zone using `<ZoneChip zone={...} />` + seconds via `formatRideTime` + `pct` as `"NN%"` (Body role, `stat-num`, right-aligned); render zero-second zones too. Do NOT compute any zone boundary or percentage in TS — render the backend values verbatim (TRUST-01). Do NOT hand-roll a zone-color map — use ZoneChip / ZONE_VAR.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/rideChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/rides/RideChart.tsx` exists and exports `RideChart`.
    - `npx vitest run src/tests/rideChart.test.tsx` passes all five tests (GREEN).
    - Grep confirms no zone-classification arithmetic in the component: `grep -nE "lower_bpm|upper_bpm|0\.68|0\.83|0\.94|1\.05" frontend/src/components/rides/RideChart.tsx` returns nothing (all zone maths came from the backend).
    - `ReferenceLine` usages contain no `isFront` or `alwaysShow` prop.
    - The component imports `ZoneChip` from `../session/ZoneChip` and does not define its own zone-color hex map.
    - `cd frontend && npx tsc --noEmit` reports no errors.
  </acceptance_criteria>
  <done>RideChart renders present-channel charts with synced hover, lap lines, a Mm SSs readout, and a backend-sourced time-in-zone section; all component tests pass and no zone maths lives in TS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend payload → chart render | RideChart renders already-computed values (including all zone maths). No new boundary; the only integrity concern is that zone classification stays server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-TRUST | Tampering | RideChart zone rendering | medium | mitigate | TRUST-01: the component renders `hr_zone_distribution` verbatim and computes no zone boundary or percentage in TypeScript; enforced by an acceptance-criteria grep. Zone colors/labels come from the single ZoneChip source of truth, so a future backend zone correction cannot silently drift out of sync. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/tests/rideChart.test.tsx` — component tests green.
- `cd frontend && npx tsc --noEmit` — no type errors.
- `grep -nE "lower_bpm|upper_bpm|0\.68|0\.83|0\.94|1\.05" frontend/src/components/rides/RideChart.tsx` — empty (no TS zone maths).
</verification>

<success_criteria>
- Present-channel charts, synced readout, lap lines, and a backend-sourced time-in-zone section all render; absent channels and null distribution render nothing; TRUST-01 preserved.
</success_criteria>

<output>
Create `.planning/phases/11-ride-analysis-dashboard/11-05-SUMMARY.md` when done.
</output>
