---
phase: 01-sports-science-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/sports_science/test_import_boundary.py
  - backend/sports_science/ftp.py
  - tests/sports_science/test_ftp.py
  - backend/sports_science/capability_gap.py
  - tests/sports_science/conftest.py
  - tests/sports_science/test_capability_gap.py
requirements: [TRUST-01, TOOL-03, TOOL-06, TOOL-08, TOOL-10, GAP-01]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "The TRUST-01 import-boundary test greps backend/sports_science/ (the real package path) and passes only on grep's clean no-match exit code 1; a grep tooling error (exit 2) and a real match (exit 0) both fail loudly, so an actual anthropic/fastapi import would now be caught."
    - "estimate_ftp_from_rides returns a numeric FTP for a deconditioned-beginner effort set (140-149W) given >=4 quality efforts; the _is_quality_effort ratio branch is reachable (called with a non-None best_ftp_estimate) rather than permanently dead."
    - "log_capability_gap logs DB-insert failures via logger.exception (no longer silently swallowed), and its DB-failure regression test genuinely exercises the raising client (the raising execute mock is awaited and an ERROR record is emitted)."
  artifacts:
    - path: "tests/sports_science/test_import_boundary.py"
      provides: "Non-vacuous TRUST-01 enforcement: correct grep target + strict exit-code assertion + a meta-test proving a seeded violation is detected"
      contains: "returncode == 1"
    - path: "backend/sports_science/ftp.py"
      provides: "Two-pass quality-effort filter (loose duration pass -> rough CP estimate -> ratio re-filter) so FTP estimation is reachable for beginners"
      contains: "_rough_ftp_estimate"
    - path: "backend/sports_science/capability_gap.py"
      provides: "logger.exception on DB failure + _reset_client_for_tests() test seam"
      contains: "_reset_client_for_tests"
    - path: "tests/sports_science/conftest.py"
      provides: "autouse fixture resetting the module-level Supabase client cache before each test"
      contains: "_reset_client_for_tests"
  key_links:
    - from: "backend/sports_science/ftp.py::estimate_ftp_from_rides"
      to: "backend/sports_science/ftp.py::_is_quality_effort"
      via: "second-pass call passes a non-None best_ftp_estimate (rough CP estimate), activating the 85% ratio branch"
      pattern: "best_ftp_estimate="
    - from: "tests/sports_science/conftest.py"
      to: "backend/sports_science/capability_gap.py::_reset_client_for_tests"
      via: "autouse fixture invokes the reset hook so each test starts with _supabase_client = None"
      pattern: "_reset_client_for_tests"
    - from: "tests/sports_science/test_import_boundary.py"
      to: "backend/sports_science/"
      via: "grep target path (the real package location)"
      pattern: "backend/sports_science/"
---

<objective>
Close the three Phase 1 verification gaps (VERIFICATION.md, status gaps_found, 2/5 must-haves). These repair artifacts already built in plans 01-04 (ftp.py) and 01-05 (capability_gap.py, boundary test) — do NOT re-do those plans; only fix the three specific defects:

1. TRUST-01 boundary test is vacuous — it greps a nonexistent root path and treats grep's error exit (2) as a pass, so it enforces nothing (SC3, TRUST-01).
2. FTP estimation is dead-coded — the two-pass ratio filter never runs, so a deconditioned beginner (the project's explicit target persona) can never receive an FTP estimate (SC2, TOOL-03/TOOL-06/TOOL-10).
3. capability_gap DB-failure path is untested and silently swallowed — a module-level client cache makes the failure test order-dependent (its own mocks never run) and `except Exception: pass` hides real production failures with zero logging (SC4, GAP-01/TOOL-08).

Purpose: The phase goal is "every physiological function is verified correct before any other layer depends on it." A vacuous test, an unreachable code path, and a silently-swallowed failure all mean the invariant holds by author discipline rather than by enforcement. Phase 2-8 already build on this foundation, so the enforcement must be real.
Output: A genuinely-enforcing TRUST-01 test, a reachable FTP two-pass filter with a beginner regression test, and an observable capability-gap failure path with a test that actually exercises it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-sports-science-foundation/01-VERIFICATION.md
@backend/sports_science/ftp.py
@backend/sports_science/capability_gap.py
@backend/sports_science/constants.py
@backend/sports_science/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the TRUST-01 import-boundary test genuinely enforcing (Gap 1)</name>
  <files>tests/sports_science/test_import_boundary.py</files>
  <read_first>
    - tests/sports_science/test_import_boundary.py (current vacuous tests — wrong grep path, `returncode != 0` assertion)
    - .planning/phases/01-sports-science-foundation/01-VERIFICATION.md (Gap 1 detail: grep exits 2 on the nonexistent root path; `!= 0` conflates error exit with no-match)
    - .planning/phases/01-sports-science-foundation/01-REVIEW.md (CR-01 fix — the corrected assertion `returncode == 1`)
  </read_first>
  <action>
    Fix both existing tests (test_sports_science_has_zero_anthropic_imports, test_sports_science_has_zero_fastapi_imports) so the grep target is the real package path `backend/sports_science/` instead of the nonexistent root-level `sports_science/`.

    Tighten each assertion from `result.returncode != 0` to the strict `result.returncode == 1`. Rationale to encode in the assertion message: grep exit 1 means "ran cleanly, found no match" (the real pass signal); exit 0 means "found a forbidden import" (a real violation, must fail); exit 2 means "path/usage error" (tooling problem, must fail loudly rather than be mistaken for a pass). `!= 0` accepts both 1 and 2, which is exactly the vacuity. Use `== 1` so a tooling error and a real violation are both caught.

    Add a third meta-test, test_import_boundary_check_detects_violations(tmp_path), that proves the mechanism is non-vacuous: create a subdirectory under tmp_path containing a throwaway .py file whose text is a forbidden import, run the same grep against that temp directory, and assert grep returns exit code 0 (a violation IS detectable). This locks in that the check can actually catch a real import, directly answering the verifier's "a test that always passes is not verification" finding. Use `str(<tmp subdir>)` as the grep target so the literal package path is not duplicated by this meta-test.

    Do not touch backend/sports_science/ source — the underlying invariant already holds (zero anthropic/fastapi imports today); this task fixes only the enforcement test.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && .venv/bin/pytest tests/sports_science/test_import_boundary.py -q && [ "$(grep -c 'backend/sports_science/' tests/sports_science/test_import_boundary.py)" -ge 2 ] && [ "$(grep -c 'returncode == 1' tests/sports_science/test_import_boundary.py)" -ge 2 ] && [ "$(grep -c 'returncode == 0' tests/sports_science/test_import_boundary.py)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/sports_science/test_import_boundary.py -q` exits 0 (all tests pass — backend/sports_science/ genuinely has no anthropic/fastapi imports today).
    - Both boundary tests grep the target path `backend/sports_science/` (line count for that literal in the file is >= 2).
    - Both boundary tests assert `result.returncode == 1` (line count for `returncode == 1` in the file is >= 2); neither still uses the loose `!= 0` form.
    - The new meta-test test_import_boundary_check_detects_violations asserts grep exit code 0 on a seeded violation (line count for `returncode == 0` is >= 1), proving the check detects a real import.
  </acceptance_criteria>
  <done>The TRUST-01 boundary test greps backend/sports_science/, fails on any exit code other than 1, and is accompanied by a meta-test proving a seeded forbidden import is detected. A future `import anthropic` under the tool library would now fail the suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make FTP estimation reachable for the beginner persona via the two-pass filter (Gap 2)</name>
  <files>backend/sports_science/ftp.py, tests/sports_science/test_ftp.py</files>
  <read_first>
    - backend/sports_science/ftp.py (current single-pass: line 46 calls _is_quality_effort with best_ftp_estimate=None unconditionally, so the ratio branch is dead and the flat 150W floor always applies)
    - backend/sports_science/constants.py (QUALITY_EFFORT_MIN_DURATION_SECS=180, QUALITY_EFFORT_MIN_POWER_RATIO=0.85, QUALITY_EFFORT_FALLBACK_WATTS=150, MIN_QUALITY_EFFORTS=4)
    - tests/sports_science/test_ftp.py (existing fixtures and assertions the two-pass change must not regress; sample_quality_efforts fixture lives in conftest.py)
    - tests/sports_science/conftest.py (sample_quality_efforts fixture)
    - .planning/phases/01-sports-science-foundation/01-VERIFICATION.md (Gap 2 detail: beginner 140-149W efforts currently yield value=None forever)
  </read_first>
  <behavior>
    RED first — add these two tests to test_ftp.py and confirm they FAIL against the current single-pass code before implementing:
    - test_deconditioned_beginner_gets_estimate: for the exact persona effort set [(1200s,140W),(600s,145W),(300s,148W),(180s,149W)] (4 efforts, all below the current 150W flat floor), estimate_ftp_from_rides(rides).value is not None, result.value["ftp"] is within [50, 500], result.value["confidence"] == "low" (4 quality efforts), and result.inputs["quality_efforts"] == 4. This currently returns value=None with confidence="insufficient_data" — it must return a real estimate after the fix.
    - test_quality_effort_ratio_branch_is_live: call the private _is_quality_effort directly with a non-None best_ftp_estimate to prove the ratio branch is reachable and correct: _is_quality_effort({"duration_secs":300,"mean_power_watts":180}, best_ftp_estimate=200) is truthy (180 >= 0.85*200=170), and _is_quality_effort({"duration_secs":300,"mean_power_watts":100}, best_ftp_estimate=200) is falsy (100 < 170). This exercises the branch that was permanently dead.
  </behavior>
  <action>
    Implement the intended two-pass quality-effort design in estimate_ftp_from_rides so the ratio branch of _is_quality_effort is actually reached (D-03). Keep _is_quality_effort's signature and its ratio-vs-fallback logic unchanged; the fix is that the second pass now passes a live estimate.

    Add a helper _rough_ftp_estimate(efforts: list[dict]) -> float | None that fits the existing _cp_model to the given efforts with the same curve_fit call, p0, bounds, and maxfev already used in estimate_ftp_from_rides, and returns the fitted cp as a float; on RuntimeError/ValueError it returns None.

    Rewrite the effort-selection portion of estimate_ftp_from_rides as:
      1. First pass (loose, duration-only): loose_efforts = efforts whose duration_secs >= QUALITY_EFFORT_MIN_DURATION_SECS. If len(loose_efforts) < MIN_QUALITY_EFFORTS, return the existing insufficient_data ToolResult (value=None, confidence="insufficient_data", quality_efforts count = len(loose_efforts)).
      2. Rough estimate: rough = _rough_ftp_estimate(loose_efforts).
      3. Second pass (ratio re-filter): if rough is not None, quality_efforts = [e for e in loose_efforts if _is_quality_effort(e, best_ftp_estimate=rough)] — this activates the 85% ratio threshold anchored to the rider's own power level. If rough is None (rough fit failed), fall back to quality_efforts = loose_efforts (duration-qualified efforts stand) so estimation degrades gracefully rather than returning null.
      4. Keep the existing >= MIN_QUALITY_EFFORTS gate on the final quality_efforts set (SC2 truth: reject any estimate from fewer than 4 quality efforts must still hold), then run the existing final CP fit, confidence assignment, and ToolResult construction unchanged.

    Do NOT reintroduce a flat-150W-only path and do NOT lower QUALITY_EFFORT_FALLBACK_WATTS; the fallback constant stays as the no-estimate default inside _is_quality_effort, but the second pass now supplies a real estimate so the 85% ratio (scale-invariant, works for a 140W beginner and a 300W rider alike) is the operative threshold. This preserves the >=4-quality-effort rigor while making the tool usable for the target persona.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && .venv/bin/pytest tests/sports_science/test_ftp.py -q && .venv/bin/python -c "from backend.sports_science.ftp import estimate_ftp_from_rides as e; r=e([{'duration_secs':1200,'mean_power_watts':140},{'duration_secs':600,'mean_power_watts':145},{'duration_secs':300,'mean_power_watts':148},{'duration_secs':180,'mean_power_watts':149}]); assert r.value is not None and 50<=r.value['ftp']<=500 and r.value['confidence']=='low', r.value" && .venv/bin/python -c "from backend.sports_science.ftp import _is_quality_effort as q; assert q({'duration_secs':300,'mean_power_watts':180}, best_ftp_estimate=200); assert not q({'duration_secs':300,'mean_power_watts':100}, best_ftp_estimate=200)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/sports_science/test_ftp.py -q` exits 0 (all existing tests plus the two new tests pass — no regression in insufficient/short/sample/medium/high confidence cases).
    - estimate_ftp_from_rides on the beginner set [(1200,140),(600,145),(300,148),(180,149)] returns value is not None, value["ftp"] in [50,500], value["confidence"] == "low", inputs["quality_efforts"] == 4.
    - _is_quality_effort called with best_ftp_estimate=200 returns truthy for a 180W/300s effort and falsy for a 100W/300s effort (ratio branch reachable and correct).
    - backend/sports_science/ftp.py defines _rough_ftp_estimate and estimate_ftp_from_rides calls _is_quality_effort with a non-None best_ftp_estimate in its second pass (line count for `best_ftp_estimate=rough` is >= 1).
    - The >= MIN_QUALITY_EFFORTS (4) gate is still enforced on the final quality-effort set (test_insufficient_efforts_returns_none and test_short_effort_not_quality still pass).
  </acceptance_criteria>
  <done>A deconditioned-beginner effort set produces a real FTP estimate; the _is_quality_effort ratio branch is live and scale-invariant; the 4-quality-effort gate still holds; all existing FTP tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the capability-gap DB-failure path observable and genuinely tested (Gap 3)</name>
  <files>backend/sports_science/capability_gap.py, tests/sports_science/conftest.py, tests/sports_science/test_capability_gap.py</files>
  <read_first>
    - backend/sports_science/capability_gap.py (module-level _supabase_client cache at lines 25/37-50; `except Exception: pass` at lines 81-82)
    - tests/sports_science/test_capability_gap.py (test_db_error_returns_fallback_tool_result at lines 120-139 — currently passes without invoking its own raising mock)
    - tests/sports_science/conftest.py (where the autouse reset fixture will be added)
    - .planning/phases/01-sports-science-foundation/01-VERIFICATION.md (Gap 3 detail: cached client makes the failure test order-dependent; silent swallow defeats GAP-01 telemetry)
    - backend/sports_science/types.py (ToolResult contract)
  </read_first>
  <behavior>
    RED first — strengthen test_db_error_returns_fallback_tool_result so it FAILS against the current code (the raising mock is never invoked today because of the cached client), then confirm it PASSES after the reset fixture + logging are added:
    - The test keeps a handle to its raising execute mock and, after calling log_capability_gap, asserts that mock was actually awaited (execute mock's await_count == 1) — proving the DB path ran instead of being short-circuited by a cached client.
    - The test uses the pytest caplog fixture and asserts at least one log record at ERROR level was emitted (proving the failure is logged, not swallowed).
    - The test still asserts the returned ToolResult has value["status"] == "logged" (the fallback is still returned despite the DB failure — GAP-02).
  </behavior>
  <action>
    In backend/sports_science/capability_gap.py:
      - Add module logging: import logging and create `logger = logging.getLogger(__name__)`.
      - Replace the bare `except Exception: pass` in log_capability_gap with `except Exception: logger.exception(...)` before falling through to build and return the fallback ToolResult. The log message should include the method_name (backend telemetry only; method_name in backend logs is fine and is the point of GAP-01 — it must still NOT appear in the user-facing value["message"], which stays generic per GAP-03). Keep the best-effort semantics: gap logging must never re-raise or block the fallback response.
      - Add a test seam _reset_client_for_tests() that sets the module-level `_supabase_client = None` (use `global _supabase_client`). This lets tests start each case with a fresh, uncached client so a patched raising client is actually used instead of a client cached by an earlier test.

    In tests/sports_science/conftest.py:
      - Add an autouse fixture that imports backend.sports_science.capability_gap and calls _reset_client_for_tests() before (and after) each test, so the module-level client cache never leaks between tests. This restores test isolation for the whole tests/sports_science/ suite.

    In tests/sports_science/test_capability_gap.py:
      - Strengthen test_db_error_returns_fallback_tool_result per the behavior block: assert the raising execute mock was awaited exactly once, assert an ERROR-level log record was captured via caplog, and keep the existing ToolResult/status assertions.

    Do not remove the module-level client cache itself (WR-04 keeps it to avoid connection-pool leaks); the fix is the reset seam plus surfacing the exception. Do not change log_capability_gap's public signature or the generic user-facing message.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && .venv/bin/pytest tests/sports_science/test_capability_gap.py -q && [ "$(grep -c 'logger.exception' backend/sports_science/capability_gap.py)" -ge 1 ] && [ "$(grep -c '_reset_client_for_tests' backend/sports_science/capability_gap.py)" -ge 1 ] && [ "$(grep -c '_reset_client_for_tests' tests/sports_science/conftest.py)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/sports_science/test_capability_gap.py -q` exits 0 with all tests passing, including the strengthened DB-error test.
    - test_db_error_returns_fallback_tool_result asserts its raising execute mock was awaited exactly once (the DB failure path genuinely runs) and that an ERROR-level record was captured by caplog.
    - backend/sports_science/capability_gap.py contains `logger.exception` inside the DB-insert except block (line count >= 1) — failures are logged, not silently swallowed.
    - backend/sports_science/capability_gap.py defines _reset_client_for_tests() and tests/sports_science/conftest.py invokes it from an autouse fixture (line count for `_reset_client_for_tests` is >= 1 in each file).
    - The user-facing value["message"] still does not contain the internal method name (test_user_message_no_method_name still passes — GAP-03).
  </acceptance_criteria>
  <done>DB-insert failures in log_capability_gap are logged via logger.exception; the DB-failure regression test actually exercises the raising client (asserted awaited + logged); the module client cache is reset between tests via an autouse fixture, ending the order-dependence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/agent layer -> backend/sports_science/ | The tool library must contain zero anthropic/fastapi imports (TRUST-01). This is the enforced boundary guaranteeing the LLM never emits physiological numbers directly; the tool library is the only authoritative source. |
| backend -> Supabase capability_gaps table | Service-role writes; insert failures must be observable telemetry (GAP-01), not silently swallowed. |
| tool library -> target-persona rider data | FTP estimation must produce correct results for real deconditioned-beginner power data, not silently return null for the persona the product is built for. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Elevation | backend/sports_science/ import boundary + tests/sports_science/test_import_boundary.py | high | mitigate | Fix grep target to backend/sports_science/ and assert `returncode == 1`; add a meta-test asserting a seeded forbidden import yields grep exit 0. Restores real TRUST-01 enforcement so an `import anthropic`/`import fastapi` added under the tool library is detected, preventing LLM-influenced numbers from laundering across the trust boundary (Task 1). |
| T-01-02 | Denial of Service (capability) | backend/sports_science/ftp.py::estimate_ftp_from_rides | medium | mitigate | Two-pass filter (loose duration pass -> rough CP estimate -> 85% ratio re-filter) makes the quality-effort threshold reachable for sub-150W beginner efforts; regression test locks the persona in. Prevents the target persona from being permanently denied an FTP-based plan (Task 2). |
| T-01-03 | Repudiation | backend/sports_science/capability_gap.py::log_capability_gap | medium | mitigate | Replace silent `except Exception: pass` with `logger.exception`; GAP-01 telemetry failures become operator-visible. Reset seam + autouse fixture make the DB-failure test genuinely exercise the raising client, so the failure path is regression-covered (Task 3). |
| T-01-SC | Tampering | pip package installs | low | accept | No new package installs in this gap-closure plan; all fixes use existing pinned deps (numpy, scipy, supabase) and the Python stdlib logging module. No [ASSUMED]/[SUS] packages introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/sports_science/test_import_boundary.py -q` passes; boundary tests grep backend/sports_science/ and assert `returncode == 1`; meta-test proves a seeded violation is detected (Gap 1).
- `.venv/bin/pytest tests/sports_science/test_ftp.py -q` passes; the beginner effort set (140-149W, 4 efforts) yields a real FTP estimate with confidence "low"; the _is_quality_effort ratio branch is live (Gap 2).
- `.venv/bin/pytest tests/sports_science/test_capability_gap.py -q` passes; the DB-failure test awaits its raising mock and captures an ERROR log; capability_gap.py logs via logger.exception; conftest resets the client cache (Gap 3).
- Full suite regression: `.venv/bin/pytest tests/sports_science/ -q` passes.
</verification>

<success_criteria>
- SC3 / TRUST-01: The import-boundary test genuinely enforces the zero-SDK-import invariant (correct path, strict exit code, violation-detection meta-test) rather than passing unconditionally.
- SC2 / TOOL-03, TOOL-06, TOOL-10: estimate_ftp_from_rides returns an estimate for a deconditioned beginner given >=4 quality efforts; the two-pass ratio filter is reachable; the 4-quality-effort rejection gate still holds; a sub-150W persona case is now covered by test.
- SC4 / GAP-01, TOOL-08: log_capability_gap logs DB failures observably; the DB-failure regression test actually exercises the failure path; the fallback ToolResult is still returned and hides the internal method name (GAP-03 preserved).
- No regression: the full tests/sports_science/ suite passes.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (Plan 06, gap closure)

- Modified `tests/sports_science/test_import_boundary.py`: correct grep target `backend/sports_science/`; strict `returncode == 1` assertion in both boundary tests; new meta-test test_import_boundary_check_detects_violations(tmp_path).
- Modified `backend/sports_science/ftp.py`: new helper _rough_ftp_estimate(efforts) -> float | None; estimate_ftp_from_rides rewritten to a two-pass filter that calls _is_quality_effort with a non-None best_ftp_estimate.
- Modified `tests/sports_science/test_ftp.py`: new test_deconditioned_beginner_gets_estimate; new test_quality_effort_ratio_branch_is_live.
- Modified `backend/sports_science/capability_gap.py`: module logger; logger.exception on DB-insert failure; new test seam _reset_client_for_tests() -> None.
- Modified `tests/sports_science/conftest.py`: autouse fixture calling capability_gap._reset_client_for_tests() before/after each test.
- Modified `tests/sports_science/test_capability_gap.py`: strengthened test_db_error_returns_fallback_tool_result (asserts raising execute mock awaited once + ERROR log captured via caplog).
- New symbols: `backend.sports_science.ftp._rough_ftp_estimate`, `backend.sports_science.capability_gap._reset_client_for_tests`.
</artifacts_produced>

<output>
Create `.planning/phases/01-sports-science-foundation/01-06-SUMMARY.md` when done
</output>
