---
phase: 01-sports-science-foundation
plan: 04
type: tdd
wave: 1
depends_on: [01-01]
files_modified:
  - sports_science/ftp.py
  - sports_science/load.py
  - sports_science/compliance.py
  - tests/sports_science/test_ftp.py
  - tests/sports_science/test_load.py
  - tests/sports_science/test_compliance.py
requirements: [TOOL-03, TOOL-06, TOOL-07, TOOL-09, TOOL-10, TRUST-01]

autonomous: true

must_haves:
  truths:
    - "estimate_ftp_from_rides returns value=None with confidence insufficient_data when fewer than 4 quality efforts are provided"
    - "A quality effort requires duration > 3 minutes and mean power above the threshold (85% of best estimate, or 150W fallback)"
    - "estimate_ftp_from_rides returns an FTP estimate and confidence level (low/medium/high) when 4+ quality efforts are present"
    - "progress_load applies back-protective caps from the constraints dict when back_issues is true"
    - "validate_session_vs_actual returns compliance percentage, TSS delta, and flags"
  artifacts:
    - path: "sports_science/ftp.py"
      provides: "estimate_ftp_from_rides (CP model), _is_quality_effort, _cp_model"
      exports: ["estimate_ftp_from_rides"]
    - path: "sports_science/load.py"
      provides: "progress_load with back-protective constraints"
      exports: ["progress_load"]
    - path: "sports_science/compliance.py"
      provides: "validate_session_vs_actual"
      exports: ["validate_session_vs_actual"]
  key_links:
    - from: "sports_science/ftp.py"
      to: "sports_science/constants.py"
      via: "imports QUALITY_EFFORT_* and MIN_QUALITY_EFFORTS"
      pattern: "MIN_QUALITY_EFFORTS"
    - from: "sports_science/ftp.py"
      to: "scipy.optimize"
      via: "curve_fit for 2-parameter CP model"
      pattern: "curve_fit"
    - from: "sports_science/load.py"
      to: "profiles.constraints JSONB schema"
      via: "reads back_issues, max_initial_weekly_hours, load_ramp_flag_threshold_pct"
      pattern: "back_issues"
---

<objective>
Implement the three remaining pure-Python tool functions: `sports_science/ftp.py` (`estimate_ftp_from_rides`, TOOL-03 — the 2-parameter Critical Power model with the 4-quality-effort guard, D-03/D-04), `sports_science/load.py` (`progress_load`, TOOL-06 — safe weekly ramp with back-protective caps, D-09), and `sports_science/compliance.py` (`validate_session_vs_actual`, TOOL-07).

This is a TDD plan. The most important test encodes the trust-model rule: never fabricate an FTP from sparse data — return None below 4 quality efforts.

Purpose: FTP confidence gates whether the plan uses power targets at all (cold-start beginners stay on RPE/HR). progress_load enforces back-protective safety. compliance feeds the adaptive loop. All three must return ToolResult and source constants only.
Output: Tested `estimate_ftp_from_rides`, `progress_load`, `validate_session_vs_actual`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-sports-science-foundation/01-CONTEXT.md
@.planning/phases/01-sports-science-foundation/01-PATTERNS.md
@sports_science/types.py
@sports_science/constants.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: FTP estimation, load progression, and session compliance (RED then GREEN)</name>
  <files>sports_science/ftp.py, sports_science/load.py, sports_science/compliance.py, tests/sports_science/test_ftp.py, tests/sports_science/test_load.py, tests/sports_science/test_compliance.py</files>
  <read_first>
    - sports_science/constants.py (QUALITY_EFFORT_MIN_DURATION_SECS, QUALITY_EFFORT_MIN_POWER_RATIO, QUALITY_EFFORT_FALLBACK_WATTS, MIN_QUALITY_EFFORTS)
    - sports_science/types.py (ToolResult contract)
    - .planning/phases/01-sports-science-foundation/01-PATTERNS.md (ftp.py quality effort + CP model patterns and confidence levels; load.py full pattern with CTL_RAMP_CEILING_PER_WEEK; compliance.py full pattern)
    - .planning/phases/01-sports-science-foundation/01-CONTEXT.md (D-03 confidence thresholds, D-04 sparse-data contract, D-09 back-protective constraints JSONB schema)
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Pattern 4 CP model, Pitfall 2 CP nonsense on sparse data, Don't Hand-Roll, Assumption A4 bounds syntax)
    - tests/sports_science/conftest.py (sample_quality_efforts fixture)
  </read_first>
  <behavior>
    RED first — write these tests and confirm they fail before implementing.

    test_ftp.py (TOOL-03, TOOL-10):
    - test_insufficient_efforts_returns_none: estimate_ftp_from_rides with 3 quality efforts returns ToolResult value=None and confidence/inputs indicating insufficient_data; never a fabricated number
    - test_short_effort_not_quality: a 120-second effort (under 3 min) is rejected by the quality-effort filter even with high power
    - test_sufficient_efforts_returns_estimate: estimate_ftp_from_rides with the sample_quality_efforts fixture (4 efforts) returns a numeric FTP estimate within physiological bounds [50, 500] and confidence "low" (4-6 efforts)
    - test_confidence_levels: 7 efforts -> "medium"; 12+ -> "high" (per D-03 thresholds)
    - test_returns_tool_result: returns ToolResult

    test_load.py (TOOL-06):
    - test_back_constraints_cap: progress_load(current_ctl=40, target_ctl=80, constraints={"back_issues": True, "max_initial_weekly_hours": 3.5, "load_ramp_flag_threshold_pct": 10}) caps max_weekly_increase below the default ceiling and reports back_constraints_applied=True
    - test_no_back_issues_default: constraints={"back_issues": False} applies the standard ramp ceiling (8 pts/week) and back_constraints_applied=False
    - test_returns_tool_result: returns ToolResult

    test_compliance.py (TOOL-07):
    - test_compliance_pct: validate_session_vs_actual(planned={"tss":100}, actual={"tss":80}) returns compliance_pct=80.0 and delta_tss=-20.0
    - test_under_performed_flag: compliance_pct < 70 produces an "under_performed" flag; > 130 produces "over_performed"
    - test_zero_planned_tss: planned tss of 0 returns compliance_pct=None without dividing by zero
    - test_returns_tool_result: returns ToolResult
  </behavior>
  <action>
    GREEN — after tests fail, implement.

    `sports_science/ftp.py`:
    - `_is_quality_effort(effort, best_ftp_estimate) -> bool`: duration = effort["duration_secs"]; mean_power = effort["mean_power_watts"]; threshold = best_ftp_estimate*QUALITY_EFFORT_MIN_POWER_RATIO if best_ftp_estimate else QUALITY_EFFORT_FALLBACK_WATTS; return duration >= QUALITY_EFFORT_MIN_DURATION_SECS and mean_power >= threshold
    - `_cp_model(t, cp, wprime) -> ndarray`: return cp + wprime/t
    - `estimate_ftp_from_rides(rides) -> ToolResult`: extract candidate efforts; filter via _is_quality_effort; if count < MIN_QUALITY_EFFORTS return ToolResult(value=None, unit="watts", methodology="2-parameter Critical Power model (Morton 1996)", inputs={"quality_efforts": count, "required": MIN_QUALITY_EFFORTS, "confidence": "insufficient_data"}) (D-04 — never fabricate); else fit via scipy.optimize.curve_fit(_cp_model, durations, mean_powers, p0=[200.0, 20000.0], bounds=([50.0,1000.0],[500.0,100000.0]), maxfev=5000); FTP ~= CP (or a documented fraction of CP); assign confidence by effort count (4-6 low, 7-12 medium, 12+ high per D-03); return ToolResult(value={ftp, cp, wprime, confidence}, unit="watts", methodology="2-parameter Critical Power model (Morton 1996)", inputs=...). Verify the bounds list-of-lists syntax works in scipy 1.17.x (Assumption A4) on first run.

    `sports_science/load.py`:
    - module constant CTL_RAMP_CEILING_PER_WEEK = 8.0
    - `progress_load(current_ctl, target_ctl, constraints) -> ToolResult`: start max_ctl_increase = CTL_RAMP_CEILING_PER_WEEK; if constraints.get("back_issues"): ramp_threshold = constraints.get("load_ramp_flag_threshold_pct", 10)/100; max_ctl_increase = min(max_ctl_increase, current_ctl*ramp_threshold); recommended_ctl = min(current_ctl + max_ctl_increase, target_ctl); return ToolResult(value={recommended_ctl_target, max_weekly_increase, back_constraints_applied}, unit="CTL", methodology="CTL ramp ceiling 8pts/week; back-protective JSONB constraints applied", inputs=...).

    `sports_science/compliance.py`:
    - `validate_session_vs_actual(planned, actual) -> ToolResult`: planned_tss/actual_tss via .get with 0 default; compliance_pct = actual_tss/planned_tss*100 if planned_tss else None; delta_tss = actual_tss - planned_tss; flags: append "under_performed" if pct < 70, "over_performed" if pct > 130; return ToolResult(value={compliance_pct, delta_tss, flags}, unit="%", methodology="Session compliance: actual TSS / planned TSS * 100", inputs=...).

    No anthropic imports in any file (TRUST-01).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sports_science/test_ftp.py tests/sports_science/test_load.py tests/sports_science/test_compliance.py -x -q && .venv/bin/pytest tests/sports_science/test_import_boundary.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `sports_science/ftp.py` defines `estimate_ftp_from_rides(rides) -> ToolResult` plus private `_is_quality_effort` and `_cp_model`
    - `sports_science/load.py` defines `progress_load(current_ctl, target_ctl, constraints) -> ToolResult` with module-level CTL_RAMP_CEILING_PER_WEEK
    - `sports_science/compliance.py` defines `validate_session_vs_actual(planned, actual) -> ToolResult`
    - estimate_ftp_from_rides returns value=None with confidence insufficient_data for fewer than 4 quality efforts; returns a numeric estimate within [50,500] for 4+
    - confidence is "low" at 4-6 efforts, "medium" at 7-12, "high" at 12+
    - progress_load applies a back-protective cap when back_issues is true and reports back_constraints_applied
    - validate_session_vs_actual returns compliance_pct=80.0 / delta_tss=-20.0 for planned 100 / actual 80; returns None compliance_pct (no zero-division) for planned 0
    - `pytest tests/sports_science/test_ftp.py tests/sports_science/test_load.py tests/sports_science/test_compliance.py -x -q` exits 0
    - `pytest tests/sports_science/test_import_boundary.py -x` exits 0 (TRUST-01)
  </acceptance_criteria>
  <done>estimate_ftp_from_rides (CP model with 4-effort guard), progress_load (back-protective caps), and validate_session_vs_actual implemented test-first; all return ToolResult; sparse-data returns None; import boundary clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sports_science/ -> anthropic SDK | No import from ftp.py/load.py/compliance.py to the LLM SDK (TRUST-01) |
| sparse ride data -> FTP estimate | Insufficient data must not produce a fabricated physiological number (trust model) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | ftp/load/compliance import boundary | mitigate | test_import_boundary.py greps for anthropic; runs in this plan's verify (TRUST-01) |
| T-01-05 | Tampering | Fabricated FTP from < 4 efforts | mitigate | test_insufficient_efforts_returns_none asserts value=None below MIN_QUALITY_EFFORTS (D-04, Pitfall 2) |
| T-01-09 | Tampering | curve_fit converges on physiologically absurd CP | mitigate | bounds=([50,1000],[500,100000]) caps the fit; test asserts estimate within [50,500] (Pitfall 2) |
| T-01-07 | Denial of Service | Division by zero on planned_tss=0 | mitigate | test_zero_planned_tss asserts None return, no exception |
</threat_model>

<verification>
- `pytest tests/sports_science/test_ftp.py tests/sports_science/test_load.py tests/sports_science/test_compliance.py -x -q` exits 0
- `pytest tests/sports_science/test_import_boundary.py -x -q` exits 0 (TRUST-01)
- estimate_ftp_from_rides returns None below 4 quality efforts; valid estimate within bounds above
- progress_load applies back-protective cap; compliance handles zero planned TSS
</verification>

<success_criteria>
- TOOL-03: CP model with 4-quality-effort guard and confidence levels
- TOOL-06: progress_load with back-protective caps
- TOOL-07: validate_session_vs_actual with compliance %, delta, flags
- TOOL-09: all return ToolResult
- TOOL-10: sparse data, short effort, zero planned TSS edge cases covered
- TRUST-01: zero anthropic imports
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- New files: `sports_science/ftp.py`, `sports_science/load.py`, `sports_science/compliance.py`
- Symbols: `estimate_ftp_from_rides(rides) -> ToolResult`, `_is_quality_effort(effort, best_ftp_estimate) -> bool` (private), `_cp_model(t, cp, wprime) -> ndarray` (private), `progress_load(current_ctl, target_ctl, constraints) -> ToolResult`, `validate_session_vs_actual(planned, actual) -> ToolResult`
- Module-level: `CTL_RAMP_CEILING_PER_WEEK` in load.py
- Tests: test_insufficient_efforts_returns_none, test_short_effort_not_quality, test_sufficient_efforts_returns_estimate, test_confidence_levels (test_ftp.py); test_back_constraints_cap, test_no_back_issues_default (test_load.py); test_compliance_pct, test_under_performed_flag, test_zero_planned_tss (test_compliance.py)
</artifacts_produced>

<output>
Create `.planning/phases/01-sports-science-foundation/01-04-SUMMARY.md` when done
</output>
