---
phase: 01-sports-science-foundation
plan: 05
type: execute
wave: 2
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - supabase/migrations/0001_initial_schema.sql
  - supabase/config.toml
  - sports_science/capability_gap.py
  - sports_science/__init__.py
  - tests/sports_science/test_capability_gap.py
  - tests/sports_science/test_db_schema.py
requirements: [TOOL-08, GAP-01, GAP-02, GAP-03, TRUST-02, TOOL-09]
autonomous: false
user_setup:
  - service: supabase
    why: "Schema migration (supabase db push) applies to the cloud project created in plan 01; capability_gap inserts need a live capability_gaps table"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Data API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API Keys -> service_role secret"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (needed for non-interactive `supabase db push`)"
    dashboard_config:
      - task: "Run `supabase login` (or set SUPABASE_ACCESS_TOKEN) and `supabase link --project-ref <ref>` once before db push"
        location: "Local terminal + Supabase Dashboard project ref"

must_haves:
  truths:
    - "The 8-table schema (users, profiles, sessions, rides, pmc_history, conversations, messages, capability_gaps) is applied to the cloud Supabase project"
    - "RLS is enabled on all 8 tables with a user-owns-row policy"
    - "profiles.constraints is a JSONB column defaulting to {\"back_issues\": false}"
    - "log_capability_gap inserts a structured row and returns a user-safe message that does not contain the internal method name"
    - "Only registered sports_science functions are exported from the package (__init__.py) for the Phase 2 tool registry to wrap"
  artifacts:
    - path: "supabase/migrations/0001_initial_schema.sql"
      provides: "8-table schema with RLS and profiles.constraints JSONB"
      contains: "create table"
    - path: "sports_science/capability_gap.py"
      provides: "log_capability_gap (Supabase insert + user-safe fallback)"
      exports: ["log_capability_gap"]
    - path: "sports_science/__init__.py"
      provides: "Public API surface — the registry-eligible function set (TRUST-02)"
      contains: "__all__"
  key_links:
    - from: "sports_science/capability_gap.py"
      to: "capability_gaps table"
      via: "supabase service-role client insert (bypasses RLS, Pitfall 6)"
      pattern: "table\\(.capability_gaps.\\)"
    - from: "sports_science/__init__.py"
      to: "all 8 tool functions"
      via: "__all__ export list defines the Phase 2 tool-registry surface (TRUST-02)"
      pattern: "__all__"
---

<objective>
Apply the 8-table Supabase schema with RLS (D-07, D-09), implement `log_capability_gap` (TOOL-08, GAP-01/02/03) against the live `capability_gaps` table, and finalize the package public API surface (`__init__.py` `__all__`) that the Phase 2 tool registry will wrap (TRUST-02 readiness). This is the final wave — it depends on all tool modules existing (plans 02-04) so the export list is complete and the import-boundary test still holds.

Purpose: The schema is the persistence backbone for every later phase; capability-gap logging is the trust-model escape hatch (the agent logs gaps instead of improvising numbers); the `__all__` list is the contract that constrains what Phase 2 can expose as Anthropic tools. The schema must be applied via `supabase db push` (not the dashboard) so migration history is preserved (Pitfall 5).
Output: Applied cloud schema, tested `log_capability_gap`, finalized package exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-sports-science-foundation/01-CONTEXT.md
@.planning/phases/01-sports-science-foundation/01-PATTERNS.md
@.planning/phases/01-sports-science-foundation/01-RESEARCH.md
@sports_science/types.py
@sports_science/constants.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 8-table Supabase schema migration with RLS</name>
  <files>supabase/migrations/0001_initial_schema.sql, supabase/config.toml</files>
  <read_first>
    - .planning/phases/01-sports-science-foundation/01-PATTERNS.md (supabase migration pattern, RLS template, profiles.constraints JSONB schema, capability_gaps special case, table list)
    - .planning/phases/01-sports-science-foundation/01-CONTEXT.md (D-07 RLS on all tables, D-09 constraints JSONB schema, specifics: capability_gaps columns)
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Pattern 5 schema+RLS, Pitfall 6 service-role insert, Security Domain RLS controls)
  </read_first>
  <action>
    Initialize the Supabase project config if not present: run `supabase init` (creates `supabase/config.toml` and the `supabase/migrations/` directory). Do not start the local Docker stack — D-07 is cloud-first.

    Write `supabase/migrations/0001_initial_schema.sql` creating all 8 tables in the `public` schema:
      - `users`: id uuid PK references auth.users on delete cascade, email text not null, created_at timestamptz default now(), google_tokens jsonb (encrypted at app layer)
      - `profiles`: id uuid PK, user_id uuid references public.users on delete cascade, constraints jsonb default '{"back_issues": false}'::jsonb (D-09), baseline fitness / equipment / goals columns as jsonb or text, created_at timestamptz default now()
      - `sessions`: id uuid PK, user_id uuid FK, planned detail (objective, structure, targets, duration) as columns/jsonb, status text, scheduled_date date, created_at
      - `rides`: id uuid PK, user_id uuid FK, tss numeric, np_watts numeric, intensity_factor numeric, duration_secs int, raw_fit_path text, created_at
      - `pmc_history`: id uuid PK, user_id uuid FK, date date, ctl numeric, atl numeric, tsb numeric, tss_display_ready boolean, created_at
      - `conversations`: id uuid PK, user_id uuid FK, created_at
      - `messages`: id uuid PK, conversation_id uuid FK references public.conversations, user_id uuid FK, role text, content text, created_at
      - `capability_gaps`: id uuid PK, user_id uuid (nullable), method_name text not null, description text, context jsonb, conversation_id uuid (nullable), created_at timestamptz default now()

    Enable RLS on every table: `alter table public.<t> enable row level security;` and add a user-owns-row policy. For tables with a direct user_id, use `using (user_id = auth.uid())`. For `users`, use `using (id = auth.uid())`. For `messages`, scope via the owning user_id column. For `capability_gaps`, add the same read policy AND a comment noting backend inserts use the service-role key to bypass RLS (Pitfall 6).

    Use lowercase `create table public.<name>`. Confirm the file contains all 8 table names and an `enable row level security` statement for each.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0001_initial_schema.sql && for t in users profiles sessions rides pmc_history conversations messages capability_gaps; do grep -qi "create table public.$t" supabase/migrations/0001_initial_schema.sql || { echo "MISSING TABLE $t"; exit 1; }; done && [ "$(grep -ci 'enable row level security' supabase/migrations/0001_initial_schema.sql)" -ge 8 ] && grep -qi "back_issues" supabase/migrations/0001_initial_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0001_initial_schema.sql` contains `create table public.<t>` for all of: users, profiles, sessions, rides, pmc_history, conversations, messages, capability_gaps
    - The file contains at least 8 `enable row level security` statements (one per table)
    - `profiles` has a `constraints jsonb` column whose default is the JSON object with back_issues false
    - `capability_gaps` has `method_name text not null`, `description`, `context jsonb`, nullable `user_id` and `conversation_id`
    - `supabase/config.toml` exists (from `supabase init`)
    - Each table has a user-owns-row RLS policy (`auth.uid()` referenced at least 8 times)
  </acceptance_criteria>
  <done>Migration file creates all 8 tables with RLS enabled and a user-owns-row policy each; profiles.constraints JSONB defaults to back_issues false; capability_gaps columns match the log_capability_gap insert contract; config.toml present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement log_capability_gap and finalize package exports</name>
  <files>sports_science/capability_gap.py, sports_science/__init__.py, tests/sports_science/test_capability_gap.py</files>
  <read_first>
    - sports_science/types.py (ToolResult contract)
    - .planning/phases/01-sports-science-foundation/01-PATTERNS.md (capability_gap.py full pattern, __init__.py export pattern, service-role key shared pattern)
    - .planning/phases/01-sports-science-foundation/01-CONTEXT.md (GAP-01/02/03, TOOL-08, D-10 module list, specifics: capability_gaps columns)
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Code Examples Supabase capability_gap insert, Pitfall 6, Anti-Patterns: do not expose internal method name)
    - sports_science/zones.py, sports_science/metrics.py, sports_science/pmc.py, sports_science/ftp.py, sports_science/load.py, sports_science/compliance.py (the modules whose public functions __init__.py must export)
  </read_first>
  <behavior>
    RED first — write tests in test_capability_gap.py and confirm they fail before implementing.
    - test_user_message_no_method_name: log_capability_gap("estimate_vo2max", {"foo": "bar"}) returns a ToolResult whose value["message"] is a non-empty user-facing string that does NOT contain the substring "estimate_vo2max" (GAP-03). The Supabase client is mocked/patched so no network call happens in the unit test.
    - test_returns_tool_result: returns a ToolResult with keys value, unit, methodology, inputs (TOOL-09)
    - test_inputs_no_secret: the returned ToolResult.inputs does not contain a service-role key or full context values — only context_keys (the key names)
  </behavior>
  <action>
    Implement `sports_science/capability_gap.py` per the PATTERNS.md pattern:
      - `_get_supabase() -> Client`: read SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from os.environ; return create_client(url, key). Service-role key bypasses RLS for backend writes (Pitfall 6); never the anon key; never expose the key to any frontend.
      - `log_capability_gap(method_name, context, user_id=None) -> ToolResult`: insert a row into the `capability_gaps` table with user_id, method_name, description f"Missing tool: {method_name}", and context (GAP-01). Return ToolResult(value={"status": "logged", "message": <generic user-facing string>}, unit="", methodology="capability_gap_log", inputs={"context_keys": list(context.keys())}). The user-facing message is generic and must NOT contain method_name (GAP-03). The function is a logging sentinel — it never computes or returns a physiological number (GAP-02).

    The unit test must patch `_get_supabase` (or the supabase client) so test_user_message_no_method_name runs without network/env. Live DB insert is verified in the human checkpoint (Task 4), not in this unit test.

    Finalize `sports_science/__init__.py` (overwrite the empty stub from plan 01) to export ONLY the 8 public tool functions plus ToolResult, with an explicit `__all__` list: calculate_power_zones, calculate_hr_zones (zones), compute_tss (metrics), update_pmc (pmc), estimate_ftp_from_rides (ftp), progress_load (load), validate_session_vs_actual (compliance), log_capability_gap (capability_gap), ToolResult (types). Do NOT export private helpers (_compute_np, _cp_model, _is_quality_effort, _get_supabase). This `__all__` is the TRUST-02 contract: the Phase 2 tool registry maps only this set to Anthropic tool schemas.

    No anthropic imports anywhere (TRUST-01).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/sports_science/test_capability_gap.py -x -q && .venv/bin/python -c "import sports_science as s; assert set(s.__all__) >= {'calculate_power_zones','calculate_hr_zones','compute_tss','update_pmc','estimate_ftp_from_rides','progress_load','validate_session_vs_actual','log_capability_gap','ToolResult'}; assert not any(n.startswith('_') for n in s.__all__)" && .venv/bin/pytest tests/sports_science/test_import_boundary.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `sports_science/capability_gap.py` defines `log_capability_gap(method_name, context, user_id=None) -> ToolResult` and a private `_get_supabase`
    - `pytest tests/sports_science/test_capability_gap.py -x -q` passes including test_user_message_no_method_name (returned message does not contain the internal method name — GAP-03)
    - `sports_science/__init__.py` defines `__all__` containing exactly the 8 public functions plus ToolResult and no private (underscore-prefixed) names
    - `python -c "import sports_science"` imports cleanly with all tool modules present
    - `pytest tests/sports_science/test_import_boundary.py -x` exits 0 (TRUST-01 still holds with all modules written)
    - log_capability_gap returns ToolResult.inputs containing only context key names, not values or secrets
  </acceptance_criteria>
  <done>log_capability_gap logs a structured gap and returns a generic user-safe message that hides the internal method name; package __init__ exports exactly the registry-eligible public surface; import boundary still clean.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Apply schema to cloud via supabase db push</name>
  <files>supabase/migrations/0001_initial_schema.sql</files>
  <read_first>
    - supabase/migrations/0001_initial_schema.sql (the migration written in Task 1)
    - .planning/phases/01-sports-science-foundation/01-RESEARCH.md (Pitfall 5 CLI missing; Open Questions 1 — project must exist; schema-gate non-TTY workaround)
    - .planning/phases/01-sports-science-foundation/01-CONTEXT.md (D-07 cloud-first, supabase db push)
  </read_first>
  <action>
    This is the mandatory schema-push task injected by the schema-gate. It runs AFTER the migration file is authored (Task 1) and BEFORE phase verification. The phase cannot pass verification without it.

    Link the local project to the cloud Supabase project: `supabase link --project-ref <ref>` (the ref comes from the project URL set in plan 01). For non-interactive execution, set `SUPABASE_ACCESS_TOKEN` in the environment (schema-gate non-TTY workaround) so `supabase link` and `supabase db push` do not prompt.

    Apply the migration: `supabase db push`. This applies `0001_initial_schema.sql` to the cloud Postgres, preserving migration history (do NOT paste SQL into the dashboard — Pitfall 5).

    If `supabase db push` requires an interactive confirmation prompt that cannot be suppressed even with SUPABASE_ACCESS_TOKEN, stop and surface the exact command to the developer for manual run (this plan is already autonomous: false to allow that).
  </action>
  <verify>
    <automated>supabase migration list 2>&1 | grep -q "0001" && echo "MIGRATION APPLIED"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list` shows `0001_initial_schema` as applied to the remote (Remote column populated)
    - All 8 tables are queryable in the cloud project (verified in the human checkpoint Task 4)
  </acceptance_criteria>
  <done>The 0001 migration is applied to the cloud Supabase project via `supabase db push`; migration history is preserved; all 8 tables exist remotely.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint, verify cloud schema, RLS enforcement, and live capability-gap insert</name>
  <action>Human verification gate. The executor pauses here. The developer verifies the 8-table cloud schema, RLS enforcement, and a live capability-gap insert per the how-to-verify steps below before the phase is considered complete.</action>
  <what-built>8-table cloud schema with RLS applied via supabase db push; log_capability_gap implemented against the live capability_gaps table; package __all__ finalized as the Phase 2 tool-registry contract.</what-built>
  <how-to-verify>
    1. In the Supabase Dashboard -> Table Editor, confirm all 8 tables exist: users, profiles, sessions, rides, pmc_history, conversations, messages, capability_gaps. Confirm profiles has a `constraints` jsonb column.
    2. Confirm RLS is enabled on each table (the shield icon / "RLS enabled" badge in the Table Editor).
    3. RLS smoke test (Information Disclosure / Elevation of Privilege): in the SQL editor or via the anon key, attempt to select from a table without an auth context and confirm zero rows / blocked — RLS is enforced in the cloud, not just locally.
    4. Live capability-gap insert: with SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in `.env`, run `.venv/bin/python -c "from sports_science import log_capability_gap; print(log_capability_gap('test_method', {'k':'v'}).value)"`. Confirm a new row appears in capability_gaps with method_name=test_method, and the printed message does NOT contain "test_method".
    5. Confirm no service-role key is committed: `git grep -n SUPABASE_SERVICE_ROLE_KEY -- ':!*.example'` returns nothing in tracked source.
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 tables exist with RLS enforced and the live capability-gap insert succeeded, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend -> capability_gaps table | Service-role write bypasses RLS; key must stay backend-only (Pitfall 6) |
| anon client -> any table | RLS must block cross-user reads in the cloud, not just locally |
| sports_science/ -> anthropic SDK | capability_gap.py and __init__.py must not import anthropic (TRUST-01) |
| internal method_name -> user-facing chat | GAP-03: internal method names must never surface to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | capability_gap.py / __init__.py import boundary | mitigate | test_import_boundary.py greps for anthropic; runs in Task 2 verify (TRUST-01) |
| T-01-02 | Spoofing/EoP | Registry exposes non-sanctioned function | mitigate | `__all__` restricts the public surface; Task 2 asserts it contains only the 8 functions + ToolResult, no private names (TRUST-02 readiness) |
| T-01-03 | Information Disclosure | RLS not enforced in cloud | mitigate | Human checkpoint step 3 attempts an unauthorized read and confirms it is blocked (GAP-02 persistence accessible only under RLS) |
| T-01-04 | Information Disclosure | Internal method name leaks to user | mitigate | test_user_message_no_method_name asserts the user message omits method_name (GAP-03) |
| T-01-08 | Information Disclosure | Service-role key committed or exposed to frontend | mitigate | Checkpoint step 5 git-greps for the key; .gitignore (plan 01) excludes .env; key read from env only |
| T-01-SC | Tampering | supabase CLI / supabase pip package | accept | RESEARCH.md Package Legitimacy Audit approved supabase 2.31.0; CLI is the official Supabase tool; no [ASSUMED]/[SUS] blocking checkpoint required |
</threat_model>

<verification>
- Migration file creates all 8 tables with RLS + user-owns-row policies; profiles.constraints JSONB present
- `supabase db push` applied the migration to cloud (migration list shows 0001)
- `pytest tests/sports_science/test_capability_gap.py -x -q` passes; user message hides method_name (GAP-03)
- `pytest tests/sports_science/test_import_boundary.py -x` exits 0 (TRUST-01)
- `import sports_science` exposes exactly the 8 public functions + ToolResult via __all__ (TRUST-02)
- Human checkpoint: all 8 tables exist in cloud with RLS enforced; live capability-gap insert works; no service-role key committed
</verification>

<success_criteria>
- TOOL-08: log_capability_gap writes a structured row and returns a user-safe fallback message
- GAP-01: structured entry appended to capability_gaps (method_name, description, context, timestamp)
- GAP-02: capability_gaps is a runtime log only; log_capability_gap never computes a number
- GAP-03: user-facing message is brief and does not expose internal method names
- TRUST-02: package __all__ defines the registry-eligible function set; no ad-hoc surface
- 8-table schema migrated and accessible in the cloud with RLS enforced
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

- New files: `supabase/config.toml`, `supabase/migrations/0001_initial_schema.sql`, `sports_science/capability_gap.py`, `tests/sports_science/test_db_schema.py`
- Modified: `sports_science/__init__.py` (finalized `__all__` export list)
- Symbols: `log_capability_gap(method_name, context, user_id=None) -> ToolResult`, `_get_supabase() -> Client` (private)
- Database: 8 tables (users, profiles, sessions, rides, pmc_history, conversations, messages, capability_gaps) with RLS; profiles.constraints JSONB column
- Package public API: `__all__` = [calculate_power_zones, calculate_hr_zones, compute_tss, update_pmc, estimate_ftp_from_rides, progress_load, validate_session_vs_actual, log_capability_gap, ToolResult]
- Tests: test_user_message_no_method_name, test_returns_tool_result, test_inputs_no_secret (test_capability_gap.py); test_db_schema.py (schema accessibility, gated behind live env)
</artifacts_produced>

<output>
Create `.planning/phases/01-sports-science-foundation/01-05-SUMMARY.md` when done
</output>
