---
phase: quick-260709-qw0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/onboarding.py
  - tests/api/test_adaptations.py
autonomous: true
requirements: [CI-RUFF-CLEAN]

must_haves:
  truths:
    - "`ruff check .` exits 0 with no F401 unused-import errors"
    - "onboarding.py no longer imports get_current_user"
    - "test_adaptations.py no longer imports inspect"
  artifacts:
    - backend/routes/onboarding.py
    - tests/api/test_adaptations.py
  key_links:
    - "Import removals must not break existing symbol references (both confirmed unused: onboarding.py:266 is docstring prose only; inspect has no other reference)"
---

<objective>
Remove two unused imports flagged by `ruff check .` in CI, both orphaned by the Google Calendar removal commit (a174e47):
- `backend.auth.get_current_user` in backend/routes/onboarding.py:41
- `inspect` in tests/api/test_adaptations.py:20

Purpose: Restore a green CI lint stage. `ruff check .` currently fails on two F401 unused-import violations.
Output: Two surgical import-line deletions; `ruff check .` passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@backend/routes/onboarding.py
@tests/api/test_adaptations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused get_current_user import from onboarding.py</name>
  <files>backend/routes/onboarding.py</files>
  <action>Delete the entire import line `from backend.auth import get_current_user` at line 41. Confirmed unused: the only other occurrence of the name (line 266) is inside a docstring describing that this endpoint uses rate_limited_user instead of get_current_user, not executable code. Leave the surrounding imports (run_turn on line 40, get_async_supabase on line 42) untouched. Do not modify the docstring reference.</action>
  <verify>
    <automated>! grep -n '^from backend.auth import get_current_user' backend/routes/onboarding.py</automated>
  </verify>
  <done>Import line deleted; no top-level `import` statement for get_current_user remains in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused inspect import from test_adaptations.py</name>
  <files>tests/api/test_adaptations.py</files>
  <action>Delete the entire import line `import inspect` at line 20. Confirmed unused: `inspect` appears only in the import statement itself with no other reference in the file. Leave the adjacent imports (`import datetime` on line 19, `from unittest.mock import ...` on line 21) untouched.</action>
  <verify>
    <automated>! grep -nE '^import inspect$' tests/api/test_adaptations.py</automated>
  </verify>
  <done>Import line deleted; no `import inspect` statement remains in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Verify ruff passes and affected tests still import cleanly</name>
  <files>backend/routes/onboarding.py, tests/api/test_adaptations.py</files>
  <action>Run `ruff check .` from the repository root to confirm zero lint errors (this is the exact CI gate). Then run the affected test file to confirm no import was removed that the module actually needed. If ruff is invoked via a virtualenv or `python -m ruff`, use whatever invocation the project's CI uses.</action>
  <verify>
    <automated>ruff check . && python -m pytest tests/api/test_adaptations.py -q</automated>
  </verify>
  <done>`ruff check .` exits 0 with no F401 errors; test_adaptations.py collects and runs without ImportError.</done>
</task>

</tasks>

<verification>
- `ruff check .` exits 0 (CI lint gate green)
- No F401 unused-import errors reported for either file
- tests/api/test_adaptations.py collects and runs (no ImportError from the removed `inspect`)
- git diff shows exactly two deleted lines and no other changes
</verification>

<success_criteria>
- backend/routes/onboarding.py no longer imports get_current_user
- tests/api/test_adaptations.py no longer imports inspect
- `ruff check .` passes locally
- No unrelated code touched (diff limited to the two import lines)
</success_criteria>

<output>
Create `.planning/quick/260709-qw0-fix-ruff-unused-imports-ci-failure/260709-qw0-SUMMARY.md` when done
</output>
