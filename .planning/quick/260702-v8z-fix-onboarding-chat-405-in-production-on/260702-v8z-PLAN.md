---
phase: quick-260702-v8z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/screens/OnboardingScreen.tsx
autonomous: true
requirements: [QUICK-260702-v8z]

must_haves:
  truths:
    - "Both onboarding SSE POST calls target the same-origin path /api/onboarding/start"
    - "No reference to import.meta.env.VITE_API_URL remains in OnboardingScreen.tsx"
    - "The deployed production JS bundle contains /api/onboarding/start and not undefined/onboarding/start"
    - "POST https://www.pacer.moorelabs.uk/api/onboarding/start (unauthenticated) returns an auth/validation status (401/403/422), not 404/405"
  artifacts:
    - frontend/src/screens/OnboardingScreen.tsx
  key_links:
    - "OnboardingScreen fetch URL -> same-origin /api/* convention (matches frontend/src/lib/api.ts BASE='')"
---

<objective>
Fix the onboarding chat 405 error in production. `OnboardingScreen.tsx` builds its
SSE POST URL from the undefined `VITE_API_URL` env var, which resolves to the literal
`undefined/onboarding/start` in the production build and hits the SPA catch-all (405 on POST).
Change both call sites to the same-origin literal `/api/onboarding/start`, matching the
`/api/*` convention used by every other backend call (`frontend/src/lib/api.ts`, `BASE=''`).

Purpose: Restore the onboarding interview flow for new users in production.
Output: Corrected OnboardingScreen.tsx, pushed to main, verified against the live deployment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/screens/OnboardingScreen.tsx
@frontend/src/lib/api.ts

# Convention reference: frontend/src/lib/api.ts uses `const BASE = ''` (same-origin) and
# paths that already include the `/api` prefix, e.g. apiFetch('/api/profiles/me').
# The two OnboardingScreen call sites use raw fetch() with a manual SSE stream reader
# (EventSource cannot POST), so they must NOT be refactored to apiFetch() — only the
# URL string changes to match the convention.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point both onboarding SSE POSTs at /api/onboarding/start</name>
  <files>frontend/src/screens/OnboardingScreen.tsx</files>
  <action>
    Replace both fetch URL template literals that reference the undefined VITE_API_URL
    env var with the same-origin literal string path '/api/onboarding/start'.

    There are exactly two occurrences, both of the form
    fetch(`${import.meta.env.VITE_API_URL}/onboarding/start`, { ... }):
      1. In the runStream useCallback (initial + subsequent interview turns), ~line 143.
      2. In the handleConfirm useCallback (post-confirmation save/plan turn), ~line 306.

    After the change, both call sites read fetch('/api/onboarding/start', { ... }) with all
    other fetch options (method, signal, headers, body) unchanged.

    Do NOT refactor these call sites to use apiFetch() — that helper does not support the raw
    SSE ReadableStream reader pattern these two sites rely on. Change ONLY the URL string.
    Do NOT touch api.ts or any other file. Do NOT alter the SSE parsing/reader logic.
    The literal string import.meta.env.VITE_API_URL must not remain anywhere in this file.
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai/frontend && grep -c "VITE_API_URL" src/screens/OnboardingScreen.tsx | grep -qx 0 && grep -c "'/api/onboarding/start'" src/screens/OnboardingScreen.tsx | grep -qx 2 && npx tsc --noEmit -p tsconfig.app.json 2>/dev/null; echo "exit=$?"</automated>
  </verify>
  <done>
    Both fetch calls use '/api/onboarding/start'; zero references to VITE_API_URL remain;
    exactly two occurrences of '/api/onboarding/start'; TypeScript compiles cleanly (exit=0).
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the fix and push to main, then wait for deploy</name>
  <files>frontend/src/screens/OnboardingScreen.tsx</files>
  <action>
    Stage ONLY frontend/src/screens/OnboardingScreen.tsx (do not use `git add -A` / `git add .`).
    The working tree has unrelated pre-existing changes (.gitignore, .planning/PROJECT.md,
    node_modules/, test-ride.fit, docs/, .planning/ui-reviews/) — none of these may be staged.

    Commit with message:
      fix(onboarding): use same-origin /api/onboarding/start for SSE POST (405 fix)

      VITE_API_URL is not configured in Vercel, so the production build resolved
      the onboarding SSE URL to the literal undefined/onboarding/start and hit the
      SPA catch-all (405 on POST). Both call sites now use the /api/* convention.

      Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

    Push directly to main (pre-approved for this quick task, consistent with 260702-tth
    and 260702-ulq). After pushing, allow time for the Vercel production deploy to complete
    before verifying in Task 3 (poll the live bundle in Task 3 rather than assuming instant deploy).
  </action>
  <verify>
    <automated>cd /Users/christianmoore/ai/pacer-ai && git log --oneline -1 --name-only | grep -q "frontend/src/screens/OnboardingScreen.tsx" && git status --porcelain | grep -q "OnboardingScreen.tsx" && echo "STILL_DIRTY" || echo "COMMITTED_CLEAN"</automated>
  </verify>
  <done>
    HEAD commit is the fix and touches OnboardingScreen.tsx; that file is no longer in the
    working-tree diff (COMMITTED_CLEAN); commit pushed to origin/main; no unrelated files staged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify the live deployment serves the fix</name>
  <files>frontend/src/screens/OnboardingScreen.tsx</files>
  <action>
    Verify the deployed production app against https://www.pacer.moorelabs.uk. Do NOT run a
    full Playwright/auth/SSE browser test — verify with lightweight curl checks only:

    (1) Bundle check: curl the served index.html at https://www.pacer.moorelabs.uk, extract the
        hashed JS asset URL(s) from the <script type="module" src="..."> tag(s) (assets live under
        /assets/*.js). curl the referenced bundle(s) and confirm the built JS contains the string
        "/api/onboarding/start" and does NOT contain "undefined/onboarding/start". Because the
        bundle is hashed and Vercel deploys are not instant, poll: re-fetch index.html + bundle
        every ~20s (up to ~5 min) until the new bundle contains "/api/onboarding/start"; the
        onboarding code may be code-split, so if the main entry chunk lacks the string, also fetch
        any additional /assets/*.js chunks referenced (modulepreload links / dynamic import chunks).

    (2) Route sanity check: POST to https://www.pacer.moorelabs.uk/api/onboarding/start with no
        auth header and an empty JSON body. Expect an auth/validation rejection (401, 403, or 422).
        A 404 or 405 means routing is still broken — fail the task in that case.

    Report the observed statuses. Do not attempt an authenticated SSE stream read here; that live
    confirmation is done separately via browser automation after this quick task.
  </action>
  <verify>
    <automated>set -e; BASE=https://www.pacer.moorelabs.uk; POST=$(curl -s -o /dev/null -w "%{http_code}" -X POST "$BASE/api/onboarding/start" -H "Content-Type: application/json" -d '{}'); echo "POST_STATUS=$POST"; case "$POST" in 401|403|422) echo "ROUTE_OK";; 404|405) echo "ROUTE_BROKEN"; exit 1;; *) echo "ROUTE_UNEXPECTED_$POST"; exit 1;; esac; for i in $(seq 1 15); do HTML=$(curl -s "$BASE/"); ASSET=$(printf '%s' "$HTML" | grep -oE '/assets/[^"]+\.js' | head -1); if [ -n "$ASSET" ]; then JS=$(curl -s "$BASE$ASSET"); if printf '%s' "$JS" | grep -q "/api/onboarding/start"; then echo "BUNDLE_OK=$ASSET"; printf '%s' "$JS" | grep -q "undefined/onboarding/start" && { echo "BUNDLE_HAS_BROKEN_URL"; exit 1; }; exit 0; fi; fi; sleep 20; done; echo "BUNDLE_NOT_UPDATED_YET"; exit 1</automated>
  </verify>
  <done>
    Unauthenticated POST to /api/onboarding/start returns 401/403/422 (ROUTE_OK, not 404/405);
    the served production JS bundle contains "/api/onboarding/start" (BUNDLE_OK) and does not
    contain "undefined/onboarding/start".
  </done>
</task>

</tasks>

<verification>
- OnboardingScreen.tsx: zero VITE_API_URL references, two '/api/onboarding/start' occurrences, tsc clean.
- Fix committed to main (HEAD touches OnboardingScreen.tsx only) and pushed; no unrelated files staged.
- Live: unauthenticated POST /api/onboarding/start returns 401/403/422; deployed bundle serves the corrected URL.
</verification>

<success_criteria>
Both onboarding SSE POSTs use the same-origin /api/onboarding/start path, the fix is on main
and deployed, and the live route no longer 405s. New-user onboarding chat is restored in production.
</success_criteria>

<output>
Create `.planning/quick/260702-v8z-fix-onboarding-chat-405-in-production-on/260702-v8z-SUMMARY.md` when done
</output>
